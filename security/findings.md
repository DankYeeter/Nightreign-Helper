# Sicherheitsbefunde -- Register

Vollstaendige Tabelle aller Sicherheitsbefunde, ein Befund = eine Zeile.
Chronologischer Verlauf, Director-Entscheidungen und Nachtraege stehen in
`security/verlauf.md` (abgetrennt in T-180, 12.09.2026).

Status je Befund beginnt mit genau einem Wort:
`offen | teilweise | behoben | geschlossen | zurueckgestellt | waived | unklar`
(optional gefolgt von ` -- <Erlaeuterung>`).

Quelle: T-003, `security-reviewer`, Zyklus 1, sowie alle in
`security/verlauf.md` genannten Auftraege. Repo-Stand bei der Erstpruefung:
`3da8428` (v1.7.1).

## Tabelle

| ID | Titel | Prioritaet | Status | Letzte Pruefung |
|---|---|---|---|---|
| SEC-001 | Endlosschleife beim Lesen UTF-16-terminierter Namen (`savefile.py`, `binary.py`, `tpf.py`, `fmg.py`) | Hoch | behoben — Retest bestanden; keine sechste Suche vorhanden | 2026-09-02 |
| SEC-002 | Unbegrenzte Zaehler aus der Datei steuern Schleifen und Allokationen (`savefile.py`, `bnd4.py` inkl. `read_split_header`, `fmg.py`, `dvdbnd.py`, `tae.py`) | Mittel | behoben — Retest bestanden; kein Integer-Ueberlauf moeglich, alle Pruefungen vor der Allokation. Klasse offen -> SEC-017 | 2026-09-02 |
| SEC-003 | *(gesperrt, im Audit entkraeftet)* | - | geschlossen | 2026-09-01 |
| SEC-004 | Save-Strings ungefiltert in Qt-Rich-Text, `QLabel` auf `AutoText` (`app.py`, `inventory.py`) | Hoch | behoben — Retest bestanden an den drei genannten Stellen. UNC-/SMB-Vorbehalt belegt -> SEC-019 | 2026-09-02 |
| SEC-005 | Nativer Texturdecoder erhaelt ungeprueftes Format und zu kurze Nutzlast (`dds.py`) | Hoch | behoben — Retest bestanden; Whitelist vollstaendig, Blockgroessen korrekt, Ausgabeallokation mitbegrenzt | 2026-09-02 |
| SEC-006 | Oodle-DLL aus dem Spielverzeichnis per `ctypes`, Zielgroesse ungeprueft (`oodle.py`) | Mittel | behoben — Retest bestanden; Herkunftspruefung akzeptiertes Restrisiko. Offen: relative Schranke statt fester 2 GiB (kein Blocker) | 2026-09-02 |
| SEC-007 | `powershell` ohne absoluten Pfad, CWD-/PATH-Hijack (`shortcut.py`) | Mittel | behoben — Retest bestanden; Rest ueber `%SystemRoot%` -> SEC-020 | 2026-09-02 |
| SEC-008 | Dateinamen aus `manifest.json` ungeprueft zu Pfaden (`iconpack.py`) | Niedrig | behoben — Retest bestanden; sieben Windows-Umgehungswege einzeln geprueft. Die Schreibseite war nie offen - der Regressionstest 4 prueft dort nichts | 2026-09-02 |
| SEC-009 | Lieferkette des veroeffentlichten EXE (`release.yml`) | Hoch | behoben — 07.09.2026 (T-105, `d92bab9`), vom Director gegengeprueft - Einzelheiten unten. Der urspruengliche Entscheid lautete: geteilt (Director 2026-09-02): Action-Pin auf Commit-SHA + SHA-256-Pruefsumme sperren das Release; Signatur und `--require-hashes` sind tragbares Restrisiko | 2026-09-02 |
| SEC-010 | `ElementTree` ohne Schutz vor Entitaeten-Expansion (`icons.py`) | Niedrig | behoben — Retest bestanden; auch externe Parameter-Entitaeten und externe DTD abgedeckt, kein SSRF | 2026-09-02 |
| SEC-011 | `pip-audit` fehlt; Schwachstellenstand der Abhaengigkeiten ungeprueft | Niedrig | geschlossen — (Nutzer 05.09.2026) - anderer Grund als die uebrigen vier: kein Angriff ueber Spieldateien, sondern ein fehlendes Abhaengigkeits-Audit. Gestrichen wegen der kleinen Abhaengigkeitsflaeche (PySide6 plus zstandard); haengt an derselben Frage wie `ruff` (F-A) und kommt mit ihr zurueck, falls der Nutzer Werkzeuge aufnimmt | 2026-09-02 |
| SEC-012 | Markup-Injektion in `bosstab.py::_stance_rank` (nicht `PART_NAMES`, siehe Korrektur) | Niedrig | behoben — Retest bestanden; wird als Instanz von SEC-019 gefuehrt | 2026-09-02 |
| SEC-013 | `owned_label.setToolTip(self.owned.folder)` ungeescapt (`app.py`) | Niedrig | behoben — Retest bestanden; Escape in beiden Richtungen gegen das echte Widget geprueft | 2026-09-02 |
| SEC-014 | `bossdata._utf16` schneidet nicht terminierte Namen still ab | Niedrig | behoben — Retest bestanden; Funktion geloescht | 2026-09-02 |
| SEC-015 | `bosstab.py detail_expedition` auf AutoText mit `everdark_label` | Niedrig | offen — Instanz von SEC-019, Prioritaet folgt der Klasse | 2026-09-02 |
| SEC-016 | `dcx.py:38-41` reicht `uncompressed_size` ungeprueft als `max_output_size` an `zstandard` **T-202 (12.09.2026):** Code unveraendert bei `b33461d` (`dcx.py:38-40`). **Die Freigabebedingung vom 05.09.2026 beschreibt das Programm nicht mehr** -- A15 liefert eine Route ueber einen frei gezeigten Ordner, der weder "boesartige Installation" noch "uebernommenes Konto" ist. Der `security-reviewer` hat den Befund **nicht erneut vorgelegt** (Nutzerentscheid respektiert), sondern die gewechselte Randbedingung gemeldet. Gefuehrt als SEC-037. | Niedrig | geschlossen — (Nutzer 05.09.2026) - setzt eine boesartige Spielinstallation oder ein bereits uebernommenes Benutzerkonto voraus; nicht erneut vorlegen | 2026-09-02 |
| SEC-017 | `tpf.read` prueft `file_count` gegen nichts; quadratische Speicheraufnahme, 256 KiB Eingabe -> 3,2 GiB Spitzenhaufen gemessen (`tpf.py:27/37/53`) **T-202 (12.09.2026):** wie SEC-016. Der Beleg ist ein gemessener Spitzenhaufen, also eine In-Process-Messung -- **von QA-237 nicht beruehrt**. | Niedrig | geschlossen — (Nutzer 05.09.2026) - setzt eine boesartige Spielinstallation oder ein bereits uebernommenes Benutzerkonto voraus; nicht erneut vorlegen | 2026-09-02 |
| SEC-018 | `dcx.py:41-42` DFLT-Zweig ohne jeden Deckel, `zlib.decompress` ohne `bufsize`; x1029 gemessen. Gehoert in denselben Fix wie SEC-016 **T-202 (12.09.2026):** wie SEC-016; `dcx.py:42` weiterhin `zlib.decompress(payload)` ohne Deckel und ohne `bufsize`, unveraendert bei `b33461d`. | Niedrig | geschlossen — (Nutzer 05.09.2026) - setzt eine boesartige Spielinstallation oder ein bereits uebernommenes Benutzerkonto voraus; nicht erneut vorlegen | 2026-09-02 |
| SEC-019 | **SEC-004 als Klasse offen:** 90 von 95 `QLabel` stehen auf AutoText, 35 von 36 Tooltips sind ungeescapt. Gemessen belegt, dass ein `<img src="file://host/...">` aus Spieltext Qt dazu bringt, eine Ressource von einem fremden Host zu oeffnen - bei UNC-Ziel eine SMB-Verbindung samt NTLMv2-Antwort des Windows-Kontos. Bricht zugleich die oeffentliche Zusage "kein Netzwerkzugriff" | **Mittel** | offen — Prioritaet am 2026-09-02 vom Nutzer auf Mittel gesenkt (Entscheid: die eigene Spielinstallation gilt als vertrauenswuerdig). Sperrt das Release nicht mehr. Der gemessene Beleg bleibt gueltig, und die Zusage "kein Netzwerkzugriff" muss trotzdem umformuliert werden | 2026-09-02 |
| SEC-020 | PowerShell-Pfad wird aus `%SystemRoot%` abgeleitet; Umgebungs-Hijack ueber `HKCU\Environment` bleibt (`shortcut.py:68`) | Niedrig | geschlossen — (Nutzer 05.09.2026) - setzt eine boesartige Spielinstallation oder ein bereits uebernommenes Benutzerkonto voraus; nicht erneut vorlegen | 2026-09-02 |
| SEC-021 | Bestand der Slotkarte (`RelicSlot._sync_mode`, `curse_tooltip`) interpoliert Relikt-, Effekt- und Fluchnamen ungefiltert in Rich Text bzw. Tooltip | Niedrig | offen -- urspruenglich am 07.09.2026 als P3/Major mit einer Save-Grenze gemeldet; am selben Tag (T-096) korrigiert und herabgestuft: Instanz von SEC-019, die Grenze ist Spielinstallation -> Anzeige, nicht Save -> Anzeige (der Reliktname kommt aus `relic_meta`, nicht aus dem Save). Haelt nur, solange `inventory.py` das so behaelt | 2026-09-07 |
| SEC-022 | Reliktdatensaetze aus einem Save sind unbegrenzt -- 131 069 Datensaetze je MiB gemessen, hochgerechnet ~21 Minuten unabbrechbare Vorsortierung bei einer 19-MiB-Datei | Hoch | behoben -- `MIN_BYTES_PER_RELIC_RECORD = 64` in `read_owned_relics` (laut ausfallend), zweiter Deckel in `inventory.relics_for`, `should_cancel` in die Vorsortierung gezogen (Commit `f79f770`, Statuskorrektur 2026-09-07) | 2026-09-07 |
| SEC-023 | Save-Pfad mit Steam-Konto-Id kann bei einer Race Condition auf der Fensterflaeche landen -- `inventory.load:199` sortiert per `p.stat()` ausserhalb des `try` von `_scan_save` **T-202 (12.09.2026):** die Pfadhaelfte **haelt fuer die Bauform dieses Befundtexts** (eine gefangene `OSError`, an einer Senke zitiert) -- Waechter mit Positivkontrolle, Schrumpf-Liste und Veraltungspruefung, 80 Tests gruen. **Sie traegt nicht als allgemeine Zusicherung, dass kein Pfad die Flaeche erreicht:** `exc.filename` ungedeckt (SEC-040), Alias und Ablegen-dann-zitieren ungedeckt, `raise`-Stellen eigener Klassen ungedeckt (`oodle.py:58` traegt den Spielpfad woertlich -- **kein Leck**, A2/A3 zeigen ihn absichtlich als PATH-Zeile, AK-129). **Nicht als "kein Pfad erreicht die Flaeche" weiterzitieren.** | Niedrig | behoben -- Retest T-185 (12.09.2026): `inventory._changed_at` (`f645c01`) faengt die `OSError` des Sortierschluessels, `_scan_save:514-521` bildet den Rest auf `strerror` ab. Positivkontrolle in-process: ohne das `try` traegt `FileNotFoundError` den vollen Pfad, und der Waechtertest reisst dreifach. **Randbedingung: geschlossen an der Quelle, nicht an der Senke** -- `app.py:4221` schreibt weiter ungeprueft `str(exc)` auf die Flaeche; eine neue pfadtragende Ausnahme auf diesem Weg macht den Befund wieder scharf, und nichts schlaegt dabei an. **SEC-035 ist dieselbe Zeile** | 2026-09-12 |
| SEC-024 | `find_loadout_table` friert den Start ein, vom SEC-022-Deckel nicht gedeckt -- Faktor 25 gemessen, rund 24 s eingefrorenes Fenster bei 19 MB (Befundtext urspruenglich in `qa/findings.md` gefuehrt) | Hoch | behoben -- Commit `912a39a`, `nrdata/savefile.py:312/345/353-361`: `MIN_BYTES_PER_LOADOUT_TABLE`, `allowed_starts`, lauter Abbruch am Marker (Statuskorrektur 2026-09-07) | 2026-09-07 |
| SEC-025 | `tests.yml` hat dieselbe unpinned-Tag-Schwaeche wie SEC-009 -- baut aber nicht das ausgelieferte Artefakt, sperrt A2 nicht | Niedrig | offen -- gehoert in den Fix-Stapel von Zyklus 17; per Code bestaetigt (12.09.2026) weiterhin offen, `.github/workflows/tests.yml` nutzt `actions/checkout@v4`/`actions/setup-python@v5`, keine Commit-SHA | 2026-09-07 |
| SEC-026 | A15 entzieht der Annahme von SEC-006 ihre Begruendung: `ctypes.CDLL` laedt eine native Bibliothek aus **frei gewaehltem** Ordner (`nrdata/oodle.py:44`, Annahme AK-112, Ladung bei **jedem** Datenneuaufbau) **T-202 (12.09.2026), die Falle praezisiert:** "wer haertet, macht SEC-016/017/018 wieder scharf" gilt **nicht gleichmaessig**. Haertung ueber **Herkunft** (nur Registry und `libraryfolders.vdf`) stellt die Vertrauensannahme wieder her und haelt die drei geschlossen; Haertung ueber **Zustimmung** (ein Klick auf einen Ordner unbekannter Herkunft) tut das nicht -- ein Klick ist keine Herkunftspruefung. Unabhaengig davon ist die Bedingung **schon bei `b33461d` abgelaufen**, vor jeder Haertung. | Mittel (Wirkung Kritisch) | offen — Vorlage an den Nutzer, kein Bauauftrag | 2026-09-08 |
| SEC-027 | Panel-Text A1 verspricht `It is only read`, waehrend aus demselben Ordner eine Bibliothek ausgefuehrt wird (`UI_SPEC.md:2870`, zu bauen in V2) | **Hoch** | behoben -- Wortlaut in T-145, Retest bestanden (`security/verlauf.md:553`); Registerzeile vom Director am 13.09.2026 nachgezogen, `grep -rn "It is only read" nrplanner nrdata` leer | 2026-09-13 |
| SEC-028 | AK-112 nimmt `regulation.bin` ohne Obergrenze an; danach vollstaendiges `read_bytes()` bei jedem Start (`firstrun.py:31`, `datasource.py:106`, `regulation.py:16`) | Mittel | offen — gehoert in V1 | 2026-09-08 |
| SEC-029 | `Find my save…` mit Filter `All files (*)` fuehrt jede Datei in ein ungedeckeltes `read_bytes()` **vor** dem ersten SEC-022-Deckel (`inventory.py:194`) | Mittel | offen — gehoert in V3/V4 | 2026-09-08 |
| SEC-030 | `search_from` (V1, ungebaut) schliesst Reparse-Punkte nicht aus; das 400/2-s-Budget begrenzt Zeit, nicht Ziel | Niedrig | offen — Hypothese, Funktion existiert nicht | 2026-09-08 |
| SEC-031 | `find_game_dir()` umgeht beides: die 64-MiB-Decke und die Nutzerbestaetigung -- ein praepariertes Laufwerk C-H laedt seine DLL ohne Fenster und ohne Klick | Mittel (Schwere Hoch) | behoben -- Retest T-185 (12.09.2026), **aber nicht durch die Stelle, die vorher als Beleg galt**: `gamefiles.py:69` (`looks_like_the_game`) ist Hygiene und haelt keinen Angreifer auf, der den Ordner selbst fuellt. Geschlossen wird der Pfad allein durch die zweite Haelfte -- die festen Kandidaten C bis H sind aus `find_game_dir` entfernt (`bd6bd90`), jeder Kandidat kommt aus Registry und `libraryfolders.vdf`. **Randbedingung:** gilt fuer den Angreifer des Befundtexts (Laufwerk ohne Kontozugang), **nicht** als Aussage ueber die Echtheit des gefundenen Ordners -- das bleibt SEC-026. Daneben offen: SEC-036 | 2026-09-12 |
| SEC-032 | `SEARCH_DEPTH = 3` dehnt eine Zustimmung auf drei Ebenen aus, C2 fragt bei einem Abstieg von mehr als einer Ebene nicht nach | Mittel | behoben -- Retest T-185 (12.09.2026), im Umfang des Director-Entscheids vom 09.09.2026: `LEVELS_TAKEN_ON_TRUST = 1` (`firstrun.py:190`), ab zwei Ebenen C3 mit ausdruecklichem Klick (`b0e7ee4`, AK-246/AK-247). Tiefe 0/1/2/3 und der Aufstieg sind einzeln ueber die gezeigte Panel-Folge belegt, nicht ueber ein Flag. `SEARCH_DEPTH` bleibt bewusst 3 (AK-249). **Randbedingung: die eine Ebene `INSIDE` bleibt klickfrei** -- so entschieden, Klasse SEC-026. Wer SEC-032 zitiert, zitiert diese Ebene mit | 2026-09-12 |
| SEC-033 | `savefile._members` deckelt die Mitgliedertabelle, nicht `offset`/`size` je Mitglied -- gemessen bis 400 MiB, rechnerisch bis 42,7 GiB, von der 256-MiB-Decke nicht gebunden | Mittel | behoben -- die vorgeschlagene `offset+size<=len(blob)`-Schranke band den gemessenen Fall selbst nicht (0/0-Sonderfall); gebaut ist stattdessen eine Summenschranke ueber alle Mitglieder (disjunkte BND4-Mitglieder, 99,9955 % Nachweis ueber zwei echte Saves) | 2026-09-09 |
| SEC-034 | Die SEC-022-Dichtegrenze ist relativ und erlaubt mit der neuen absoluten Decke 4 194 304 Datensaetze -- gemessen bei 8 MiB: 131 072 angenommen, 50,6 MiB, 16,37 s | Niedrig | behoben -- `MOST_RELIC_RECORDS_A_SLOT_MAY_HOLD = 16384` (= `1 048 608 // 64`, Faktor 53 ueber den 309 echten Datensaetzen); gemessen: vorher 131 072 in 20,121 s, jetzt verweigert nach 16 385 in 2,121 s | 2026-09-09 |
| SEC-035 | `inventory.scan` sortiert per `stat()` ausserhalb jedes `try` -- eine `OSError` traegt den vollen Pfad samt Steam-Konto-Kennung (AK-126) in den Fenstertext; Ausloeser ist ein Rennen, nicht reproduziert | Niedrig | behoben -- **Duplikat der Quellhaelfte von SEC-023**, nicht ein eigener Defekt: `scan:379` bei `e5c2b7a` ist woertlich dieselbe Anweisung wie `load:199` bei `a33f678`, dieselbe Senke, dasselbe Leck (AK-126). Ein Commit (`f645c01`) schliesst beide. Waechter `tests/test_hostile_savefile.py:836-873` bildet den Angriffspfad ab und ist ohne den Fix dreifach rot. Zwei Tage lang doppelt gefuehrt, ohne dass es auffiel | 2026-09-12 |
| SEC-036 | `_steam_roots` traegt die feste Wurzel `C:\Steam`, und `C:\` erlaubt jedem authentifizierten Konto das Anlegen von Unterordnern (`icacls C:\`: `Authenticated Users:(AD)` plus `(OI)(CI)(IO)(M)`). Wer dort die Ordnerkette bis `Game` mit den drei Dateien fuellt, die `looks_like_the_game` verlangt, erreicht denselben klick- und fensterlosen Weg zu `ctypes.CDLL` wie SEC-031 -- nur ohne angestecktes Laufwerk, es genuegt **ein zweites lokales Konto**. Trifft Opfer ohne ueber Steam auffindbares Nightreign und ohne gespeicherten bestaetigten Ordner. `nrdata/gamefiles.py:32-35`, `firstrun.py:1025`, `oodle.py:44`. Behebungsrichtung: feste Wurzel entfernen (Steam schreibt seine Wurzel in die Registry) **oder** einen nicht aus der Registry stammenden Ordner wie einen vom Nutzer gezeigten behandeln und C3 vorlegen; zusaetzlich muss der Waechter `test_game_dir_recognition.py:212` die **echte** Kandidatenliste messen statt `_steam_roots` zu stubben, sonst bleibt jede kuenftige feste Wurzel unsichtbar **Notiz des Directors, 12.09.2026** (gemeldet vom `developer` in T-198, von mir am Quelltext nachgeprueft): `nrdata/gamefiles.py:31-34` haengt **beide** festen Wurzeln unbedingt an, und `nrplanner/firstrun.py:153` traegt mit `STEAM_COMMON` dieselbe C:-Wurzel ein zweites Mal. Sie ist heute funktional redundant. **Korrigiert durch T-202 (12.09.2026) -- meine Notiz war zu stark.** Gemessen: `icacls "C:\Program Files (x86)"` gibt `BUILTIN\Users` nur `(RX)`; SEC-036s Angreifer (ein zweites unprivilegiertes Konto) kann `STEAM_COMMON` **nicht** fuellen, und die Wirkung reicht nur bis zum Startordner des Dialogs. `STEAM_COMMON` ist eine **Haertung**, nicht die offene Haelfte dieses Befunds. **Der tragende Grund ist ein anderer als der, den ich und der `developer` genannt haben:** `where_to_start_looking:485` fragt `steam_common_folders()` **vor** `STEAM_COMMON`, und das laeuft ueber dasselbe `_steam_roots()` samt `C:\Steam` -- **ein Fix an `_steam_roots` schliesst beides**, die klicklose `find_game_dir`-Route und den Dialogstart. Randbedingung: eine Maschine, 12.09.2026, und `C:\Program Files (x86)\Steam` existiert dort nicht -- gemessen ist die geerbte ACL des Elternordners. Laeuft das Programm als Administrator oder lockert Steam die ACL, kippt die Aussage. Siehe SEC-038: der Waechter dieses Befunds bindet die feste Wurzel nicht. **T-202 (12.09.2026):** Praemisse am System bestaetigt -- `icacls C:\` zeigt `NT AUTHORITY\Authenticated Users:(AD)`, jedes authentifizierte Konto darf `C:\Steam` anlegen. | Mittel (Schwere Hoch) | offen -- neu aus T-185, Einstufung durch den Director: eigene ID statt Aufgehen in SEC-026, weil die Voraussetzung eine andere ist (zweites Konto statt DLL-Vertrauen) und die Entfernung der Wurzel nichts kostet | 2026-09-12 |
| SEC-037 | **Die Freigabebedingung von SEC-016/017/018 ist abgelaufen.** A15 liefert eine Route, in der der Nutzer einen **beliebigen** Ordner zeigt -- ein entpacktes Archiv aus dem Netz ist weder eine boesartige Installation noch ein uebernommenes Konto, sondern ein dritter Fall, den der Freigabesatz nicht kennt. `looks_like_the_game` (`gamefiles.py:153-185`) prueft nur, dass drei Dateien da sind, und alle drei fuellt der Angreifer selbst; danach laeuft `dcx.decompress` mit `max_output_size` aus einem Vier-Byte-Feld **der Datei** (bis 4 GiB), der `DFLT`-Zweig ohne jeden Deckel (`dcx.py:38-40`, `:42`, `tpf.py:25-53`, Ausloeser `firstrun.py:1025`). **Wirkung:** Speichererschoepfung des Fensterprozesses, kein Codeausfuehrungsgewinn. **Keine Codeaenderung, eine Entscheidung des Nutzers:** Freigabetext auf den heutigen Wortlaut nachziehen, oder die Deckel nachziehen -- beides klein | Mittel | offen -- beim Nutzer, haengt an SEC-026 | 2026-09-12 |
| SEC-038 | **Der Waechter zu SEC-036 kann an der festen Wurzel nicht rot werden.** `tests/test_game_dir_recognition.py:143-156` stubbt `_steam_roots` per `monkeypatch.setattr`, obwohl SEC-036s eigene Behebungsnotiz verlangt, dass er die **echte** Kandidatenliste misst. Gemessen mit einem eigenen pytest-Plugin: der echte Koerper lief ueber 110 gruene Tests **26-mal** und gab jedes Mal **beide** feste Wurzeln zurueck; **5 von 5** `_steam_roots`-Fundstellen in `tests/` sind Stubs, **0** binden die Rueckgabeliste, **0** Testdateien nennen `Program Files` oder `C:\Steam`. Unsichtbar sind die Wurzeln nur, weil keine der beiden auf dieser Maschine existiert und `find_game_dir:78` sie per `if not root.exists()` wegwirft -- **gefiltert durch Zufall der Maschine, nicht durch Bau**. **Wirkung:** der SEC-036-Fix kann eingebaut, entfernt oder um eine neue feste Wurzel ergaenzt werden, ohne dass ein Test es merkt. **Vor dem Fix zu bauen**, sonst hat der Fix keine rote Phase | Mittel | offen | 2026-09-12 |
| SEC-039 | **`shortcut.create():138-140` gibt PowerShells `stderr` woertlich auf die Flaeche** -- zwei Zeilen unter der Ausnahmehaelfte, die QA-211 geschlossen hat, im selben Rueckgabewert derselben Funktion. **Kein Waechter sieht es, weil es keine Ausnahme ist** und der AST-Scan `except`-Bloecke liest. Gemessen: `stderr` traegt den vollen `.lnk`-Pfad samt Windows-Benutzernamen, und PowerShell bricht die Zeile bei 128 Zeichen um -- `detail[0]` liefert einen **mitten im Pfad abgeschnittenen** Satz (A7-Bruch). Die Sprachhaelfte ist Hypothese, diese Maschine laeuft `en-GB` (A8-Risiko). **Behebung:** wie die Ausnahmehaelfte -- den Ausgang des Kindes auf einen Satz dieses Repositorys abbilden; ein `returncode` ist eine Zahl und spricht keine Sprache, `stderr` gehoert auf die Konsole | Niedrig | offen | 2026-09-12 |
| SEC-040 | **Haertung: der SEC-023-Waechter deckt den reinen Pfadtraeger `exc.filename` nicht.** `WORDING_ATTRIBUTES` (`tests/test_exception_text_is_english.py:77`) enthaelt `strerror` -- laut der **eigenen Docstring derselben Datei** (Z. 11-12) die Haelfte, die **keinen** Pfad traegt -- und **nicht** `filename`, das dieselbe Docstring (Z. 53-55) ausdruecklich als Pfadtraeger benennt. Der Waechter deckt also den **Sprach**traeger und laesst den **reinen Pfad**traeger offen, bei einem Befund, dessen Gegenstand der Pfad ist. Heute **0** Fundstellen von `.filename`/`.filename2` in `nrplanner/` und `nrdata/`, deshalb Haertung und kein Leck. **Behebung:** `filename` und `filename2` aufnehmen. Die zwei weiteren gemessenen Luecken (Alias, Ablegen-dann-zitieren) braeuchten Datenflussverfolgung statt Musterabgleich -- **der `security-reviewer` empfiehlt sie ausdruecklich nicht**, solange die Bauform im Bestand nicht vorkommt, sondern als bekannte Decke im Docstring | Niedrig | offen | 2026-09-12 |
| SEC-041 | **Haertung: T-193 hat `binary.magic` ueber die Zitatgrenze geschoben.** `nrdata/binary.py:91-94` gibt 4 Byte Dateiinhalt woertlich auf die Flaeche; vorher warf die Stelle ein blankes `ValueError`, das `errortext.in_english` **abbildet**. Beide Fassungen in-process gemessen: vorher der allgemeine Satz, bei `b33461d` die Angreiferbytes woertlich. **Begrenzt auf 4 Byte** -- `magic()` liest `len(expected)`, und beide Aufrufstellen (`bnd4.py:91`, `:141`) uebergeben 4 Byte; eine holende Marke braucht mindestens 11 Zeichen, **keine SEC-019-Eskalation**. Der Deckel ist aber eine Eigenschaft der **zwei Aufrufstellen**, nicht des zitierenden Codes: eine dritte Stelle mit laengerer Magic verschiebt ihn, und nichts sagt es. **Behebung:** die Bytes aus der Absage fallen lassen oder im `raise` begrenzen | Niedrig | offen | 2026-09-12 |
| SEC-042 | **Haertung: `firstrun.STEAM_COMMON` ist nach einem SEC-036-Fix die letzte feste Wurzel im Modul** (`:153`, benutzt `:486`) -- aber **nicht** der Angreifer von SEC-036, und das ist gemessen, nicht geschlossen: `icacls "C:\Program Files (x86)"` gibt `BUILTIN\Users` nur `(RX)`, und die Wirkung reicht nur bis zum Startordner des Dialogs -- kein `looks_like_the_game`, kein `read_bytes`, kein `CDLL`, ohne Klick nichts. Mit dem SEC-036-Fix **mitnehmen als Aufraeumen**, nicht als Teil der Schliessung | Niedrig | offen | 2026-09-12 |
| SEC-037 | **Statuswechsel (T-219, 13.09.2026):** Deckel gebaut -- `MAX_UNCOMPRESSED_SIZE` (2 GiB) liegt jetzt in `nrdata/dcx.py` und deckelt ZSTD vor der Allokation und DFLT ueber `zlib.decompressobj(max_length)`; `tpf.read` weist Mitglieder ueber dem Dateiende ab; alle Ablehnungen `NotWhatItClaims`. Waechter `tests/test_hostile_gamedata.py` (+5, Rotlauf 4 failed / Gruenlauf 25 passed), Commit `670d879`, Suite 1794 passed / 9 skipped (developer gemessen, Director `git show --stat` geprueft) | Mittel | behoben -- Retest in der Pruefphase | 2026-09-13 |
| SEC-038 | **Statuswechsel (T-217, 13.09.2026):** Waechter `tests/test_steam_library_start_folder.py::test_the_real_root_list_holds_nothing_the_registry_did_not_name` misst die echte `_steam_roots()`-Liste; Rotlauf vor dem Fix `Extra items: C:/Steam, C:/Program Files (x86)/Steam`, danach 9 passed. Praemisse korrigiert: es waren 4 Stubs plus 1 Docstring-Erwaehnung, nicht 5 von 5 Stubs. Commit `09f5cdb` | Mittel | behoben -- Retest in der Pruefphase | 2026-09-13 |
| SEC-036 | **Statuswechsel (T-217, 13.09.2026):** beide festen Wurzeln aus `_steam_roots` entfernt (`nrdata/gamefiles.py`), nur noch Registry-Wurzeln plus `libraryfolders.vdf`. `firstrun.STEAM_COMMON` (SEC-042) bleibt als Dialogstart. Commit `09f5cdb` | Mittel (Schwere Hoch) | behoben -- Retest in der Pruefphase | 2026-09-13 |
| SEC-026 | **Statuswechsel (T-217, 13.09.2026), Nutzerentscheidung "Haertung ueber Herkunft":** `gamefiles.in_a_steam_library` (aufgeloester Pfad unter `<root>/steamapps/common` aus Registry + `libraryfolders.vdf`) ist letzte Bedingung von `looks_like_the_game`; alle drei Routen (`find_game_dir:84`, gezeigter Ordner `_search_within_budget:273/289`, gemerkter Pfad `gamepath.resolve_game:134`) laufen dort durch, die drei `oodle.load`-Aufrufer bekommen `game_dir` nur ueber `resolve_game`/`Settled.game`. Ablehnungssatz E1 dritte Zeile: `That folder is not inside a Steam library, so Nightreign Helper will not run the game's files from there. Pick the folder Steam installed the game in: in Steam that is Manage, then Browse local files.` Folge: ohne Steam in der Registry wird jeder Ordner abgelehnt (Bericht Befund 2). Suite 1797 passed / 9 skipped (developer gemessen). Spec AK-230/A28 und E1-Headline (Bericht Befund 3) noch nachzuziehen | Mittel (Wirkung Kritisch) | behoben -- Spec-Nachtrag und Retest in der Pruefphase offen | 2026-09-13 |
| SEC-039 | **Statuswechsel (T-218c, 13.09.2026):** Rueckgabe `the shortcut could not be written`, PowerShell-Ausgabe auf `sys.stderr`; Test mit gestubbtem `subprocess.run` (deutscher Text + Pfad) in `tests/test_exception_text_is_english.py`; zweite Maske 0 weitere `subprocess`-Senken. Commit `47ffcaa` | Niedrig | behoben -- Retest in der Pruefphase | 2026-09-13 |
| SEC-026 | **Retest T-229 (13.09.2026):** Herkunft an allen 4 Pruefstellen (`gamefiles.py:85/304/320`, `gamepath.py:134`), genau 1 `CDLL`-Stelle, Junction aus `common` hinaus abgelehnt, `\?\`-Praefix und relativer Registry-Wert fail-closed | Mittel (Wirkung Kritisch) | geschlossen -- Retest bestanden | 2026-09-13 |
| SEC-036 | **Retest T-229 (13.09.2026):** nur Registry-Wurzeln, echte Liste zur Laufzeit `['d:\steam']` | Mittel (Schwere Hoch) | geschlossen -- Retest bestanden | 2026-09-13 |
| SEC-037 | **Retest T-229 (13.09.2026):** ZSTD vor Allokation, DFLT 2,22 MiB Spitze bei 1-MiB-Probedeckel, KRAK vor DLL-Handle. Hinweis: SEC-017-Pfad (Mitglieder auf den ganzen Inhalt, 51 MiB Spitze bei 32 KiB) bleibt unter der Nutzerfreigabe vom 05.09. | Mittel | geschlossen -- Retest bestanden | 2026-09-13 |
| SEC-038 | **Retest T-229 (13.09.2026):** Waechter misst die echte Liste, Positivkontrolle (`C:\Steam` in-process angehaengt) rot | Mittel | geschlossen -- Retest bestanden | 2026-09-13 |
| SEC-039 | **Retest T-229 (13.09.2026):** `stderr` bleibt im Prozess; im Artefakt (`console=False`) verworfen, im Quelllauf unerreichbar. A7-Notiz: die PowerShell-Diagnose liest niemand | Niedrig | geschlossen -- Retest bestanden | 2026-09-13 |
| SEC-042 | **T-229 (13.09.2026):** Einstufung bleibt; `icacls "C:\Program Files (x86)"` erneut `BUILTIN\Users:(RX)`; kein `looks_like_the_game`, kein `CDLL` | Niedrig | offen -- Haertung bei Gelegenheit | 2026-09-13 |
| SEC-043 | **Haertung (T-229 F1): `nrdata/tpf.py:51-54` setzt den pufferlangen Mitgliedsnamen `{name!r}` in den zitierten `NotWhatItClaims`-Satz, `dds.py:69` vier Dateibytes** (SEC-041-Klasse). Der Satz erreicht `QMessageBox.critical` (AutoText) woertlich; heute nicht rich-text-wirksam, weil der Sink `"Could not read your game:\n\n"` voranstellt und Qts Erkennung am ersten `\n` stoppt (QLabel-Probe 623 = 623; ohne Praefix 429 != 623). Schutz ist Eigenschaft des einen Sinks, nicht des Satzes; die naechste Senke ohne Praefix macht daraus SEC-019 mit UNC-Fetch. Behebung: `{name!r}` durch Mitgliedsindex ersetzen, `dds.py:69` Bytes streichen. Zweite Maske: 7 `!r}`-Treffer, 3 davon Dateibytes (tpf, dds, binary) | Niedrig | offen | 2026-09-13 |
| SEC-043 | **Statuswechsel (T-230d, 13.09.2026):** `tpf.py` nennt den Mitgliedsindex statt `{name!r}`, `dds.py`/`binary.py` ohne Dateibytes; Waechter `test_no_refusal_quotes_what_a_game_file_wrote` mit Positivkontrolle; zweite Maske 7 → 5 Treffer, Dateibytes 0. Commit `dfe226d` | Niedrig | behoben -- Retest offen | 2026-09-13 |
| SEC-041 | **Statuswechsel (T-230d, 13.09.2026):** `binary.py:90` die vier Bytes gestrichen, derselbe Waechter | Niedrig | behoben -- Retest offen | 2026-09-13 |
| SEC-043 | **Retest T-234 (13.09.2026):** bestaetigt, Mutation `tpf-refusal-quotes-the-member-name` getoetet, beide Waechter | Niedrig | geschlossen -- Retest bestanden | 2026-09-13 |
| SEC-041 | **Retest T-234 (13.09.2026):** bestaetigt mit SEC-043 | Niedrig | geschlossen -- Retest bestanden | 2026-09-13 |
| SEC-029 | **Retest T-239b (14.09.2026):** Deckel `LARGEST_SAVE_TO_READ` 256 MiB auf beiden Wegen seit `aec3d05` (09.09.), in `savereader.read_the_save` jetzt direkt statt Wrapper; Positivtests `test_save_path_memory.py`. Registerzeile vom 08.09. war ueberholt | Mittel | behoben -- Register nachgezogen | 2026-09-14 |
| SEC-044 | **Beobachtung T-239b (14.09.2026), kein Befund:** `_SaveReadWorker.work` schreibt `traceback.print_exc()` auf stderr, dort kann der Save-Pfad (Steam-Id) stehen; nur lokal, nur bei Konsolenstart, vorbestehend | Niedrig | offen -- zurueckgestellt, kein Angriffspfad | 2026-09-14 |
| SEC-045 | **Haertung (T-251b, 14.09.2026):** `effectfilters.EffectFilters.__init__` liest `advisor/excluded`/`advisor/required` ohne Disjunktheitspruefung, `mark()` schreibt in zwei Registry-Vorgaengen; Ueberlappung (Prozessende dazwischen) wirft `ValueError` aus `SlotProblem.__post_init__` in `asking_from` (`advisorbar.py:97-99`) bei jedem `recompute` -- Werkzeug haengt bis zur Reparatur. Kein Angreifer ueber HKCU hinaus. Behebung: beim Lesen disjunkt machen (eine Richtung) oder ein `QSettings`-Objekt mit einem `sync()`; Waechter mit Ueberlappung | Niedrig | offen -- Fixauftrag T-252 | 2026-09-14 |
| SEC-019 | **Notiz T-251b (14.09.2026):** neue Textsenken des Markierens (`MarkedLine`, AK-279-Listen, Fussnote, Bar-Tooltip) alle PlainText oder escaped; `relicpicker.py` `line.label.setToolTip(curse_detail)` ist der verschobene Alt-Tooltip, Zaehlung unveraendert | Mittel | offen | 2026-09-14 |
| SEC-045 | **T-252 (15.09.2026, developer): behoben `e18d782`** -- Mengen beim Laden disjunkt (`required` gewinnt), ein Schreibvorgang; Waechter mit Ueberlappung | Niedrig | behoben -- Retest offen | 2026-09-15 |
| SEC-045 | **Retest T-253 (15.09.2026):** bestaetigt (rot-vorher + live) | Niedrig | geschlossen -- Retest bestanden | 2026-09-15 |
| SEC-046 | **Haertung (T-263b, 15.09.2026):** `firstrun._a_promised_icon_is_gone` `stat()`et Manifest-Dateinamen ohne den SEC-008-Schutz `_inside_pack`; absoluter/UNC-Eintrag verlaesst das Pack (SMB-Aufloesung je Start, erzwungener Rebuild); nur mit Schreibrecht als derselbe Nutzer. Behebung: resolve + `is_relative_to` vor `stat()` | Niedrig | offen -- Fixauftrag nach T-263 | 2026-09-15 |
| SEC-047 | **Haertung (T-263b, 15.09.2026):** `iconbuild.manifest_files` wirft `KeyError`/`TypeError` bei Variante ohne `file` oder Nicht-String; `firstrun.what_is_needed` faengt nur `ValueError`/`UnicodeDecodeError` -- Start bricht ab. Behebung: `.get("file")`, nur `str` yielden | Niedrig | offen -- Fixauftrag nach T-263 | 2026-09-15 |
| SEC-019 | **Notiz T-263b (15.09.2026):** +2 Instanzen aus Spieldaten: `app.vessel_tooltip` (Vesselname) und `statsheet.py:910-912` (`entry.name` in `<div>`); Klasse und Prioritaet unveraendert | Mittel | offen | 2026-09-15 |
| SEC-046 | **T-264 (15.09.2026, developer): behoben `2640602`** -- `iconpack.inside_pack(base, name)` als Modulfunktion, von `firstrun._a_promised_icon_is_gone` genutzt; Textvorfilter (Laufwerk/Wurzel/`..`) vor `resolve()` gegen Netzanfrage; Eintrag ausserhalb → gone (Rebuild schreibt eigenes Manifest); 3+3 Mutationen rot | Niedrig | behoben -- Retest offen | 2026-09-15 |
| SEC-047 | **T-264 (15.09.2026, developer): behoben `8e1ea80`** -- `manifest_files` nur `str` via `.get("file")`, `what_is_needed` faengt `AttributeError/KeyError/TypeError` → "icons"; Mutationen 1+3 rot | Niedrig | behoben -- Retest offen | 2026-09-15 |
| SEC-046 | **Retest T-265 (15.09.2026):** bestaetigt -- UNC/Laufwerk/Traversal → gone, 0 `stat()` ausserhalb des Packs, kein Netz-Stall | Niedrig | geschlossen -- Retest bestanden | 2026-09-15 |
| SEC-047 | **Retest T-265 (15.09.2026):** bestaetigt -- fehlerhafte Variante wird uebersprungen (kein Absturz); `['icons']` nur bei falscher Form des ganzen Manifests | Niedrig | geschlossen -- Retest bestanden | 2026-09-15 |
| SEC-048 | **T-283b (16.09.2026, security-reviewer):** `release.yml` Testschritt fuehrt pytest/xdist plus 6 ungepinnte transitive Pakete mit dem von `actions/checkout` persistierten `contents: write`-Token aus (`release.yml:39` ohne `persist-credentials: false`, `:86` `pip install -r requirements-dev.txt`); Behebung `persist-credentials: false` am Checkout und Pins in `requirements-dev.txt`. Gesamturteil PASS, 0/0/0/1, Releasevotum freigeben | Niedrig | offen -- Haertung, nicht releasesperrend | 2026-09-16 |
| SEC-048 | **T-284a (16.09.2026, developer): behoben `af85fbd`** -- `persist-credentials: false` am Checkout, sechs transitive Testpakete in `requirements-dev.txt` gepinnt | Niedrig | behoben -- Retest im Release-Lauf (Job-Log) | 2026-09-16 |
| SEC-048 | **Retest T-290c (17.09.2026, security-reviewer, statisch):** `release.yml:39` `persist-credentials: false`, `requirements-dev.txt` 6 Pins, kein Git-Credential-Verbrauch nach Checkout; Job-Log nicht gelesen. Kurzlauf 1.13.2 PASS 0/0/0/0 | Niedrig | geschlossen -- Retest statisch bestanden | 2026-09-17 |
| SEC-019 | **Notiz T-292c (17.09.2026, security-reviewer):** AD-039-Kopfzeilen und Allow-Tooltips sind kuenftige Instanzen der Klasse; Vorgabe an T-292d: Item-Text oder PlainText, Tooltips escaped (Sicherheitsblock Zeile 4) | Mittel | offen | 2026-09-17 |
| SEC-045 | **Notiz T-292c (17.09.2026, security-reviewer):** Randbedingung (HKCU, derselbe Nutzer) gilt unveraendert fuer `advisor/allowed`/`advisor/avoided_families`; Vorgabe: totale Zerlegung, Reparatur ueber drei Paare, `resolved_excluded` minus `required` und `allowed` | Niedrig | geschlossen -- Klasse beobachtet | 2026-09-17 |
| SEC-019 | **Retest T-293c (17.09.2026, security-reviewer, statisch):** `effectfilterdialog.py` 6 Senken (185, 188, 193, 239, 250, 317), alle Konstanten; Familienname nur Item-Text; Klasse unveraendert | Mittel | offen | 2026-09-17 |
| SEC-045 | **Retest T-293c (17.09.2026, security-reviewer, statisch):** drei Paare disjunkt beim Laden, `resolved_excluded` minus `allowed`/`required`, Waechter `test_effectfilters.py:188` gruen gelesen; Kurzlauf 1.14.0 PASS 0/0/0/0 | Niedrig | geschlossen -- Retest statisch bestanden | 2026-09-17 |
| SEC-019 | **T-296a (19.09.2026, technical-writer): behoben** -- Satz "makes no network connections at all" durch C-006-Fassung "it makes no network connection" ersetzt (`README.md:72`, `docs/anleitung/guide.md:476`); alte Formulierung nur noch in historischen Auftrags-/Standdateien | Mittel | behoben -- Retest security-reviewer im naechsten Release-Lauf | 2026-09-19 |
| SEC-049 | **Haertung (T-312b, 19.09.2026):** `bossdata._healthbar_names` (`:117-143`) deckelt `size` aus der EMEVD nur gegen die Blobgrenze -- je `(2000,6)`-Instruktion die ganze Datei als Ints, quadratisch. Gemessen 16/32/64/128 KiB = 0,05/0,21/0,86/3,50 s (x4 je Verdopplung), Spitze 1,0 MiB, 1 MiB extrapoliert ~224 s; Extraktions-Worker ohne Abbruch, keine Codeausfuehrung. Bauform von `_flag_entities`, jetzt auf 64 statt 10 Karten. Behebung: `size` gegen die Restlaenge des Argumentblocks. **Randbedingung: praeparierte Datei in der Spielinstallation -- die Freigabe vom 05.09.2026 (SEC-016/017/018) deckt sie, seit SEC-026 wieder getragen. Keine Vorlage, kein Blocker; scharf erst bei gelockerter Herkunftspruefung** | Niedrig | offen -- Haertung, innerhalb der Nutzerfreigabe | 2026-09-19 |
| SEC-050 | **Haertung (T-312b, 19.09.2026):** `bossdata.derive_places:720` packt jeden Part-Record als `<N i` plus `dict.fromkeys` aus -- gemessen x25 des Records (1 MiB -> 25,0 MiB, 4 MiB -> 100,0 MiB, linear). Ein MSB mit weit auseinander liegenden Section-Offsets macht einen Record so gross wie die Datei; `MemoryError` faellt in `extract.build`s `except` und kostet nur die Sub-Bosse. Behebung: Recordlaenge fuer diesen Zweck deckeln. **Randbedingung wie SEC-049** | Niedrig | offen -- Haertung, innerhalb der Nutzerfreigabe | 2026-09-19 |
| SEC-019 | **Retest T-312b (19.09.2026, statisch):** Wortlauthaelfte bestaetigt -- "makes no network connections at all" 0 Treffer in ausgeliefertem Text, aktuell `README.md:72` / `guide.md:502`. Neue Sub-Boss-Senken zaehlen nicht dazu: Kartenname PlainText (`bosstab.py:826`) und Tree-Item, Beutename `html.escape`, `depthstab` verliert seine Namensspalte. **Nur die Wortlauthaelfte ist geschlossen; die Klasse bleibt offen** | Mittel | offen -- Klasse; Wortlaut-Retest bestanden | 2026-09-19 |

| SEC-019 | **Retest T-322c (19.09.2026, statisch):** neue Spieltext-Senken aus T-321 geprueft — Schulname des Datensatzes erreicht Combo-Eintrag (`QStyledItemDelegate`, plain), `advisorblock._footer_text` ueber `_plain` (PlainText) und `relicpicker.findings:1180`/`caveats:1192` (PlainText); `GoalScore.display` traegt nur konstante `weapons.DAMAGE_LABELS`, `DAMAGE_TYPE_TOOLTIP` ist konstant. Keine neue Markup-Senke; **die Klasse bleibt offen** | Mittel | offen -- Klasse; Senken-Retest bestanden | 2026-09-19 |
| SEC-019 | **Retest T-325c (20.09.2026, statisch, security-reviewer):** A26-Senken geprueft — Schulname nur als Combo-Eintrag (plain) und ueber `chosen_label` in PlainText-Labels des Pickers (`relicpicker.py:710,1149,1171`), Zauber-/Katalysatorname in `unknowns` ueber `advisorblock._plain` und `relicpicker.findings:1191`, Zaubername in `GoalScore.display` erreicht die einzige RichText-Senke `advisorblock.line_markup:154` mit `html.escape`; beide neuen Tooltips konstant; 0 Zauber-/Schulnamen mit `<>&` im Abzug v16. Vorgabenblock T-324v 10/10 erfuellt, Kurzlauf PASS 0/0/0/0. **Die Klasse bleibt offen; Senken-Retest bestanden** | Mittel | offen -- Klasse; Senken-Retest bestanden | 2026-09-20 |
| SEC-019 | **Retest T-327e (21.09.2026, statisch, security-reviewer):** A27-Senken geprueft — Zaubername in der Zeile (`statsheet.py:664`), in der Tooltip-Kopfzeile (`:492`) und in der Fussnote `rating.reason` (`:498`) alle `html.escape`, der Tooltip ist RichText; `_rate_label` traegt nur Konstanten; neue Begruendungszeile aus `explain._SPELL_PATH_FIELD` ueber `advisorblock.line_markup` escaped bzw. `_plain`. Verschobenes `entry.name` (`:465/469`) bleibt unescaped, vorbestehend, kosmetisch: Abzug v16 hat 0 Zauber-/Waffen-/Effektnamen mit `<`/`>`, 15 Effektnamen mit literalem `&`. T-324v Punkt 6/9/10 erfuellt; Kurzlauf PASS 0/0/0/0. **Die Klasse bleibt offen; Senken-Retest bestanden** | Mittel | offen -- Klasse; Senken-Retest bestanden | 2026-09-21 |
| SEC-051 | **T-329l (22.09.2026, security-reviewer):** Rest-`nightreign_data.json.partial` aus `scripts/build_snapshot.py` (`extract.py:3228`) liegt nach hartem Abbruch im Repo-Baum, `.gitignore:14` deckt nur den genauen Namen (`check-ignore` rc=1) -- Spieldaten koennten per breitem `git add` ins oeffentliche Repo (NH-002). Behebung: Muster `nrplanner/data/nightreign_data.json*`. Nur Entwicklerskript; Programmpfad schreibt unter `LOCALAPPDATA` ausserhalb des Baums. Gesamturteil T-329l PASS | Niedrig | offen -- Haertung | 2026-09-22 |
| SEC-051 | *(Status 23.09.2026 01:30, Director)* `.gitignore` deckt `nrplanner/data/nightreign_data.json*` (`a5fef7a`), Waechtertest `git check-ignore` in `tests/test_release_spec_datas.py`; QA T-329s: Test prueft das Kriterium | Niedrig | behoben -- Retest Security im naechsten Release-Lauf | 2026-09-23 |
