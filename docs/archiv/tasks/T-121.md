# T-121 - Alles pushen, offene Punkte pruefen (archivist)

**Modus: `sync-out`.** Beide Repos.

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`, dazu die Projektzeilen in
`CLAUDE.md`.

## Lage, vom Director gemessen (08.09.2026)

| Repo | Branch | ungepusht | Remote voraus | Baum | Stashes |
|---|---|---|---|---|---|
| **Nightreign-Helper** | `docs/audit-and-advisor-design` | **0** | 0 | sauber | 0 |
| **claude-agent-team** | `main` | **4** | 0 | sauber | 0 |

Gemessen mit `git status --short`, `git rev-list --count @{upstream}..HEAD`
und `git rev-list --count HEAD..@{upstream}` nach `git fetch`.

**Im Projekt ist also nichts zu tun** ausser dem Nachweis. **Die vier Commits
liegen im Agenten-Repo** und sind der Grund fuer diesen Auftrag: der Umbau von
`commands/director.md` (Regel und Beleg getrennt), die Wiederherstellung von
drei dabei verlorenen Regeln, die Korrektur der `maxTurns`-Werte und drei vom
Nutzer entschiedene Widersprueche.

**Ein Konflikt ist nicht zu erwarten** — beide Repos sind dem Remote gegenueber
nur voraus, nicht auseinandergelaufen. **Tritt trotzdem einer auf**, gilt die
Grenze aus deiner Definition: Konflikte in **anhaengenden Dokumenten** loest du,
Konflikte in **Code** nicht — die meldest du und uebergibst.

**Bereits vom Director erledigt:** ein verwaister Worktree
(`agent-ae1f6543a6c23b08a`, zwei Commits) wurde entfernt, nachdem der Inhalt
gegen den Hauptbaum geprueft und als **identisch** befunden war; sein Branch
ist geloescht.

## Erlaubtes Remote

Projekt: ausschliesslich `https://github.com/DankYeeter/Nightreign-Helper.git`.
Weicht das tatsaechliche Remote ab, wird **nicht** gepusht, sondern gemeldet.
`main` ist geschuetzt, **PR #16** ist offen und zeigt auf den Arbeitsbranch —
**kein Merge.**

## Zusaetzlich zu pruefen und zu melden

- Gibt es **weitere** Stashes, Worktrees, verwaiste Branches oder unversionierte
  Dateien, die niemandem gehoeren? Nenne sie, **loesche nichts** ohne Auftrag.
- Fehlende `.gitignore`-Eintraege schlaegst du nur **vor**.

## Rueckgabeformat

Uebergabe-Kontrakt-Block plus Abschlussbericht. **Nenne je Repo gemessen**, wie
viele Commits nach dem Push noch ungepusht sind — beide muessen **0** sein,
sonst `STATUS: teilweise`. Der Director legt deinen Bericht ab.
