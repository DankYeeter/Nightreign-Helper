# T-084 - Vier offene Wortlaute und eine Zaehlgrenze vor S10b/S10c (ui-ux-designer, Spec-Modus)

**Ziel:** Fuenf benannte Luecken in `UI_SPEC.md` schliessen, bevor die
Oberflaeche des Beraters gebaut wird. Jede davon wuerde den `developer` sonst
zwingen, einen Nutzertext zu erfinden - und ein erfundener Nutzertext ist
genau der Fehler, gegen den A7 und A12 gebaut sind.

## Modus

**Spec-Modus.** Du schreibst Vorgaben und Akzeptanzkriterien nach
`UI_SPEC.md`. Du schreibst keinen Anwendungscode und aenderst keine Datei
ausser `UI_SPEC.md` und deinem Bericht.

## Kontext

Der Build-Berater hat seinen Rechenkern vollstaendig (S4 bis S9,
`nrplanner/advisor/`). Die Oberflaeche ist noch voellig unberuehrt. S10 ist in
drei Bauauftraege geteilt:

- **T-083 (S10a)** - die Advisor bar und der Zustandsautomat. **Laeuft gerade
  parallel zu dir**, fasst `nrplanner/advisorbar.py`, `nrplanner/app.py` und
  Tests an, **nicht** `UI_SPEC.md`. Eure Dateilisten sind disjunkt.
- **T-085 (S10b)** - der Vorschlagsblock in der Slotkarte, `Hold`, `Apply
  all`, `Undo apply`, der `Why`-Dialog. Noch nicht erteilt.
- **T-086 (S10c)** - der Relic Picker als Hauptweg (AD-018). Noch nicht
  erteilt.

**Punkt 1 unten blockiert T-086. Die Punkte 3, 4 und 5 blockieren T-085.**
Deshalb laeufst du jetzt und nicht im Review danach.

Verwandte IDs: OF-19 (`ARCHITECTURE.md:3393`), AK-63 (`UI_SPEC.md:1306`),
AD-010 (`budget_note`), AD-015, AD-023, D-2, D-11 und die Director-Korrektur
am Ende des T-080-Abschnitts (`UI_SPEC.md:4227-4264`).

**Quittung aus T-078 und T-080 (`ui-ux-designer`) und T-082 (`developer`):**
Die Sprache des Beraters ist in AK-133 bis AK-161 festgelegt - Form der
Zeilen, Fluchnamen, die Fuellungen (a) bis (e) der stummen Zeilen, der
gemessene Schlechtfall (14 gezeichnete Zeilen, 16 je Karte). Der Director hat
danach entschieden: jeder Fluch einzeln auf der Slotkarte, stumme Effektzeilen
dagegen **nur im `Why`-Dialog**; Fuellung (a) wird als `NOT WORKING` mit
Durchstreichung gezeichnet. Der `developer` hat in T-082 den Satz
`Optimize again.` fuer die Statuszeile 4.7 selbst formuliert, weil keiner
vorlag.

**Abnahmekriterium aus GOAL.md:** "**A7** Wo die Spieldateien eine Bewertung
nicht hergeben, sagt das Programm das, statt zu raten - die bestehende
Hausregel gilt auch fuer den Berater."

Und aus der Erweiterung vom 2026-09-05: "**A12** Jede Zahl und jede
Beschriftung auf diesen Tabs nennt ihre Einheit und ihren Geltungsbereich. Das
ist A7, ausgedehnt von der Rechnung auf die Anzeige: keine Zahl ohne
Bezugsgroesse, keine Zusicherung ohne die Angabe, was sie **nicht** deckt."

**Stand laut docs/state.md (2026-09-06, Director), die Zeilen, die dich
betreffen - woertlich:**

> - **Zwei Nutzertexte ohne Vorgabe, im S10-Review zu klaeren:** die
>   Statuszeile 4.7 sagt seit T-082 `Optimize again.` (Formulierung des
>   `developer`), und `AdvisorResult.budget_note` fordert AD-010, ohne dass je
>   ein Wortlaut geschrieben wurde - das Feld bleibt vorerst leer.

> - **Die Grenze zwischen den Fuellungen (c) und (d) der stummen Zeilen ist
>   offen.** Die in der Spec **angeschriebene** Regel ergibt 13 Faelle, der
>   `ui-ux-designer` hatte 58 gezaehlt; die Summe (106) stimmt exakt, nur die
>   Grenze liegt anders. **Im S10-Review zu klaeren** - bis dahin ist nichts
>   sichtbar, also kostet die Verschiebung nichts.

> - **Der Knopf heisst `Optimize`.** Die Zustandstabelle 4.1-4.14 sagt noch
>   `Suggest` und wird im S10-Auftrag mitkorrigiert.

> - **AD-015-Zeile wird mit der Zahlzeile desselben Fluchs verschmolzen** und
>   wandert aus `unknowns` in die Slotgruppe. AD-015 bekommt vor S10 eine
>   Korrekturnotiz.

**Korrektur des Directors, 2026-09-07:** Die dritte dieser Zeilen ist
teilweise veraltet - die Zustandstabelle `UI_SPEC.md:223-251` sagt bereits
durchgehend `Optimize` (Commit `bafc3e1`). Offen sind nur noch die
Fliesstextstellen; das ist Punkt 2 unten.

**Zwei Entscheidungen des Directors, die du nicht neu aufmachst:**
Der Knopf heisst `Optimize` (GOAL F4). Und es gibt **kein** `progress`-Signal
- der Fortschrittsbalken ist unbestimmt (`setRange(0, 0)`) und braucht keinen
Wert.

## Umfang

**Beruehrt Dateien:** `UI_SPEC.md` und `docs/berichte/T-084-ui-ux-designer.md`.
Sonst nichts. `ARCHITECTURE.md` gehoert dem `architect`; brauchst du dort eine
Korrekturnotiz, meldest du sie als Befund.

**Parallel:** ja - T-083 (`developer`) laeuft gleichzeitig und fasst
`UI_SPEC.md` nicht an.

Fuenf Punkte, nicht mehr. Faellt dir ein sechster auf, meldest du ihn.

### Punkt 1 - AK-63 und OF-19: die zweite Quelle (blockiert T-086)

`ARCHITECTURE.md:3393` haelt fest, dass Zeile 4 des Relic Pickers und Punkt 4
des `Why`-Dialogs sich aus **zwei** Quellen speisen muessen - der
Registry-Haelfte und der Ergebnis-Haelfte - und dass AK-63 heute nur eine
davon nennt. Die Folge, wenn es so bleibt: Zeile 4 des Pickers zeigt
**weniger** als die heutige Oberflaeche, und A7 wird ausgerechnet auf dem
Hauptweg des Beraters schlechter als vorher.

Schreib AK-63 so um, dass beide Quellen benannt sind, mit dem Wortlaut, der
gezeigt wird, und der Regel, wann welche Haelfte erscheint und was passiert,
wenn beide etwas zu sagen haben. Lies dazu `UI_SPEC.md:1229-1343` (der
QA-116-Nachtrag, aus dem AK-63 stammt) und die Stelle in `ARCHITECTURE.md`.

### Punkt 2 - `Suggest` im Fliesstext

Die Zustandstabelle ist bereits korrigiert. In `UI_SPEC.md` steht `Suggest`
noch an den Zeilen **463, 541, 886, 3161, 3693 und 3723-3726**. Sieh jede
Stelle einzeln an: manche sind Historie und beschreiben eine damalige
Ueberlegung - die bleiben stehen, gegebenenfalls mit einem Zusatz, dass der
Knopf heute `Optimize` heisst. Andere sind geltende Vorgabe und muessen
geaendert werden. **Der Bericht sagt je Stelle, was du getan hast und warum.**
Die offene Frage bei 3723-3726 ist damit beantwortet und wird als beantwortet
markiert, nicht geloescht.

### Punkt 3 - die Statuszeile 4.7 (blockiert T-085)

Heute steht in der Tabelle:
`Your build changed while this was working out. Optimize again.`

Die zweite Haelfte hat der `developer` formuliert, nicht du. Bestaetige den
Wortlaut oder ersetze ihn. Pruefe dabei zwei Dinge: ob der Satz in die Breite
der Statuszeile passt (die kuerzt per `QFontMetrics` auf `…`, der volle Text
steht im Tooltip - eine Aussage, die nur im Tooltip ankommt, ist keine), und
ob `Optimize again.` als Aufforderung richtig ist, wo der Nutzer gerade **gar
nichts falsch gemacht** hat.

### Punkt 4 - `AdvisorResult.budget_note` (blockiert T-085)

AD-010 fordert das Feld, ein Wortlaut wurde nie geschrieben, das Feld ist
leer. Entscheide beides: **welchen Satz es traegt und wo er erscheint** -
Statuszeile, `Why`-Dialog oder gar nicht. Ist deine Antwort "gar nicht", sag
das ausdruecklich und begruende es; dann wird das Feld entfernt statt leer
mitgeschleppt, und das ist ein Befund an den `architect`.

Zur Sache: die Zahl, auf die sich eine Budgetnotiz beziehen wuerde, gibt es
noch nicht - der `performance-tuner` setzt sie erst in S11. Ein Text, der eine
Zahl verspricht, die niemand gemessen hat, verstiesse gegen A12.

### Punkt 5 - die Grenze zwischen Fuellung (c) und (d) (blockiert T-085)

Die in `UI_SPEC.md` angeschriebene Regel ergibt **13** Faelle, du hattest in
T-080 **58** gezaehlt. Die Summe ueber alle Fuellungen (106) stimmt exakt
ueberein - es ist also keine Rechenfrage, sondern die Grenze zwischen zwei
Toepfen liegt an zwei verschiedenen Stellen.

Finde heraus, welche der beiden Grenzen du gemeint hast, schreib die Regel so
an, dass sie diese Grenze ergibt, und **nenne die Zahl, die deine Regel
erzeugt, mit dem Weg, auf dem du sie gezaehlt hast**. Ein Abgleich, der die
Zahl nicht nennt, verschiebt das Problem nur nach T-085.

**Rechnen darfst du.** Du hast `Bash` und `Read`; der Datenabzug liegt unter
`%LOCALAPPDATA%\NightreignHelper\nightreign_data.json` und ist **nur zu
lesen**. Zaehl am echten Bestand, nicht am Gedaechtnis.

## Vorgaben

- **Alle Nutzertexte Englisch** (GOAL A8). Deine Begruendungen im Bericht und
  in der Spec sind Deutsch, die Wortlaute selbst Englisch.
- **Kein neuer Zustand, kein neues Bedienelement.** Die vier Punkte schliessen
  Luecken in Vorhandenem. Faellt dir dabei eine fehlende Funktion auf, ist das
  ein Befund und ein Fall fuer den `product-strategist`, kein Nachtrag hier.
- **Jede neue oder geaenderte Vorgabe traegt ein pruefbares
  Akzeptanzkriterium.** Neue AK-Nummern ab **AK-162**, fortlaufend. Eine
  Vorgabe ohne AK ist eine Absichtserklaerung.
- **Was du aenderst, wird als Nachtrag angeschrieben**, im Stil der
  vorhandenen Nachtraege (`## Nachtrag zu … (ui-ux-designer, T-084) -
  2026-09-07`), mit Datum und Herkunft. Loesch keinen Verlauf: eine ersetzte
  Vorgabe wird als ersetzt markiert, nicht entfernt - die Begruendung von
  damals ist der Wert.
- **A12 gilt fuer deine eigenen Texte:** keine Zahl ohne Bezugsgroesse, keine
  Zusicherung ohne die Angabe, was sie nicht deckt.

## Testumgebung

Nur Lesen und Rechnen. Windows 10, Python 3.11 aus `.venv`. Der Datenabzug
unter `%LOCALAPPDATA%\NightreignHelper` wird **nicht** geschrieben, der Save
bleibt read-only. Kein Netzwerkzugriff.

Fuer Punkt 5: zaehl am Datenabzug und nenne den Befehl oder das Skript, mit
dem du gezaehlt hast, damit die Zahl nachvollziehbar ist.

## Abnahme

**Die Abnahme misst breiter als der Befund.** Fuer Punkt 5: nenne nicht nur
die Zahl deiner Grenze, sondern auch die der jeweils **anderen** Lesart, und
die Summe - stimmt die Summe nicht mehr, ist die Regel falsch angeschrieben,
nicht die Zaehlung.

**Zahlen tragen ihr Rezept.** Jede Zahl in deinem Nachtrag nennt, woran sie
gemessen wurde (Datei, Feld, Filter) und wie viele Faelle die Grundgesamtheit
hat. "58 von wie vielen" ist Teil der Zahl.

**Rot-vorher sinngemaess:** Fuer jedes neue AK sag in einem Satz, welche
Umsetzung es heute verletzen wuerde. Ein AK, das keine denkbare Umsetzung
ausschliesst, ist keins.

## Rahmen (steht woertlich in jedem Auftrag)

- **Nach jedem Teilschritt ein Commit mit gruener Suite.** Ein Abbruch darf
  hoechstens einen Teilschritt kosten, nie einen Umbau.
  *Fuer dich gilt:* du hast keine Commit-Rechte. Der Director committet deine
  Arbeit, sobald du fertig bist. Arbeite deshalb so, dass `UI_SPEC.md` nach
  **jedem** Punkt in sich stimmig ist - dein Zwischenstand kann jederzeit der
  Endstand sein.
- **Testzahlen gelten nur vom frischen Klon.** Eine Zahl, die als
  Freigabekriterium dient, muss auf einem fremden Rechner reproduzierbar
  sein. Nenne die Zahl aus diesem Lauf, nicht die aus dem Arbeitsbaum.
- **Laeuft parallel eine andere Rolle, arbeitest du in einem eigenen Klon**
  im Scratchpad - nicht im Arbeitsbaum.
  *Ausnahme fuer diesen Auftrag, vom Director verantwortet:* T-083 laeuft
  parallel, fasst aber `UI_SPEC.md` nicht an. Du arbeitest im Arbeitsbaum und
  **nur** in `UI_SPEC.md` und deinem Bericht.
- **Befund-IDs vergibt ausschliesslich der Director.** Melde Befunde mit
  Titel und Fundstelle, ohne Nummer. AK-Nummern vergibst du selbst, ab AK-162.
- **Zugschwelle.** Nach 150 Werkzeugaufrufen sperrt ein Hook alles ausser
  `git add/commit` und dem Bericht unter `docs/berichte/`. Dann: Stand
  sichern, Bericht mit `STATUS: teilweise` schreiben (fertig, offen,
  naechster Schritt), beenden. Der Director erteilt den Rest neu. Teile deine
  Arbeit so, dass du vor der Schwelle einen stimmigen Stand hast - Punkt 1
  zuerst, dann 5, 4, 3, 2.
- **Zahlen tragen ihr Rezept.** Jede Zahl, die als Schranke oder Kennwert
  dient, nennt Formel, Stichprobenfehler und Sicherheitsabstand; bei
  Messwerten Skript und Commit.
- **Abwesenheits-Behauptungen brauchen die Primaerquelle.** "Nicht
  vorhanden", "nicht geliefert", "unveraendert" wird nur nach direkter
  Pruefung des aktuellen Bestands gesagt (Dateisystem, git, Volltextsuche
  mit mindestens zwei unabhaengigen Begriffen) - nie aus einer Notiz oder
  einem Statuswort uebernommen.
- Projektspezifisch: Datenabzug unter `%LOCALAPPDATA%\NightreignHelper` -
  **nur lesen**. Save read-only. Kein Netzwerkzugriff. Alle Oberflaechentexte
  Englisch (A8). Du hast keine schreibenden Git-Rechte.

## Scope-Grenzen

- **Kein Review der Oberflaeche.** T-083 baut gerade; ein Review auf einem
  beweglichen Stand ist wertlos. Der Review-Modus kommt nach T-085 als eigener
  Auftrag.
- **`ARCHITECTURE.md` fasst du nicht an**, auch nicht die
  Korrekturnotizen zu AD-003.5 und AD-015. Melde sie als Befund; der Director
  entscheidet, wer sie schreibt.
- **Die dreizehn Streichvorschlaege je Tab** (`UI_SPEC.md` §8) bleiben liegen
  - die entscheidet der App Designer, nicht wir.
- **Die vier gesammelten Fragen an den App Designer** (F-B, F-C, F-F, F-G)
  fasst du nicht an. Sie liegen beim Nutzer.
- **Kein neuer Nutzertext ausserhalb der fuenf Punkte.** Faellt dir eine
  weitere Luecke auf, ist das ein Befund im Bericht.

## Rueckgabeformat

Schreibe deinen vollstaendigen Bericht **unveraendert** nach
`docs/berichte/T-084-ui-ux-designer.md`, bevor du ihn zurueckgibst.

Beginne den Bericht mit dem Uebergabe-Kontrakt-Block:

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEAENDERT:
ANNAHMEN:
NAECHSTER:
BLOCKIERT DURCH:
```

Danach je Punkt: was du entschieden hast, **warum**, welche Alternative du
verworfen hast und woran man die Entscheidung nachpruefen kann. Fuer Punkt 5
zusaetzlich die Zaehlung mit Befehl und Grundgesamtheit. Am Ende die Liste der
neuen AK-Nummern mit je einer Zeile, und was dir aufgefallen ist, das nicht in
den Auftrag gehoerte.
