# T-139 - Die Reste aus U10, und die Mutationen ins Register (developer)

**Ziel:** Drei Dinge, die aus den vorigen Auftraegen herausgefallen sind — zwei
davon, weil der Director sie nicht hineingeschrieben hat. Danach ist die
Picker-Kette vollstaendig und alle gefahrenen Mutationen sind wiederholbar.

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`.
Es gelten die Projektzeilen aus `CLAUDE.md`. Abweichung: **keine**.

**Scratchpad:** eigenes Unterverzeichnis `T-139/`.

## Umfang

**Stufe:** normal.

**Beruehrt Dateien:** `tests/test_picker_track_guards.py`,
`tests/picker_track.py`, `scripts/differential/mutate.py`. **Sonst nichts.**

## Kontext

**Quittung aus T-137 (`developer`, Commits `e2cfc10`, `10d6df2`, `ab522d7`):**
U9 und W8 stehen. Die Herleitung des `architect` zu `shutdown` **stimmt und ist
jetzt gefahren**: vor dem Fix meldet der neue Fall `['ready'] != []` — das
spaete `ready` nach dem Schliessen kam wirklich. Acht Mutationen, acht tot.
Suite **1357 passed, 9 skipped, 0 failed**.

**Er hat drei Luecken gemeldet, und zwei davon sind Fehler des Directors:**

> W8 bezeichnet in X-2 und in AK-218 Fassung 2 zwei verschiedene Waechter —
> gebaut ist der aus dem Auftrag, **der Struktur-Waechter ueber die vier
> unterbrechenden Stellen fehlt** · **zwei U10-Teile aus der
> Nachtrag-X-Tabelle sind nicht beauftragt und nicht gebaut** · die acht
> Mutationen sind **nicht** in `scripts/differential/mutate.py` registriert.

*Der Director hat den Auftrag T-137 aus der Zusammenfassung des `architect`
geschrieben statt aus der Tabelle in Nachtrag X. Das ist die Ursache der ersten
beiden Luecken, nicht ein Versaeumnis des `developer`.*

## Die drei Aufgaben

**1. Der Struktur-Waechter aus Nachtrag X-2.** Er prueft die **vier
unterbrechenden Stellen** (`worker.py`, `search.Cancelled`: Zeilen `309`,
`334`, `354`, `380` im Stand vor T-137 — **pruef die Zeilen nach**, U9 hat die
Datei bewegt). **Lies X-2 in `ARCHITECTURE.md` im Wortlaut**; T-137 hat den
Fall nur als Analyse in Abschnitt 6 seines Berichts, nicht als Test.

**Zur Benennung, Director-Entscheidung 08.09.2026:** In diesem Projekt gibt es
**zwei** unabhaengige W-Reihen — die Fassaden-Kette **W0 bis W6 aus AD-019**
(`ARCHITECTURE.md:1835, 2650, 2662`) und die Picker-Waechter **W1 bis W8 aus
AD-028**. Sie werden **nicht umbenannt** (die Reihen stehen in Tests, Berichten
und Entwuerfen), sondern **immer mit ihrer Herkunft zitiert**: `AD-019 W6`
gegen `AD-028 W6`. Halte dich im Code und im Bericht daran. Der Waechter aus
dieser Aufgabe heisst **`AD-028 W9`** — X-2s Fassung ist damit eindeutig, ohne
dass eine gebaute Nummer wandert.

**2. Die zwei uebrigen U10-Teile aus der Nachtrag-X-Tabelle**, woertlich dort
nachzulesen: **W1 um `candidates.pools` erweitern** (der Umgehungsweg, den
IX-5 als Vorschlag genannt hatte) und die **Randbedingung im
Tabellen-Docstring**. Steht in der Tabelle mehr, als hier genannt ist, **bau
es und sag es** — der Director hat sie beim Schreiben von T-137 nicht
vollstaendig gelesen.

**3. Die Mutationen ins Register.** Nicht registriert sind die aus **T-131**
(neun), **T-133** (zwoelf), **T-136** (elf gefahren, davon zwei ueberlebend und
als echte Verhaltensgleichheit begruendet) und **T-137** (acht). Sie stehen
woertlich in den jeweiligen Berichten unter `docs/berichte/`.

**Nachweis:** `mutate.py --apply` je nachgetragener Mutation und die Zahl der
roten Faelle im Standardlauf. **Traegt eine nicht mehr, ist das ein Befund** —
nicht passend machen. **Die zwei ueberlebenden aus T-136 werden mit ihrer
Begruendung eingetragen, nicht weggelassen**: ein Register, das nur die
toetenden kennt, verliert genau die Faelle, die jemand sonst noch einmal
untersucht.

**Wird Aufgabe 3 groesser als die anderen beiden zusammen, brich sie ab, trag
ein, was du geschafft hast, und melde den Rest.** Sie ist die am wenigsten
dringende der drei.

## Vorgaben

- **Kein Anwendungscode.** Findest du dabei einen Fehler: melden, nicht
  beheben.
- **Mutationsbeweis** fuer den neuen Waechter aus Aufgabe 1: Pflicht — ein
  Struktur-Waechter, der nichts bewacht, ist grün und faellt niemandem auf.
  *(Nutzerentscheidung 08.09.2026: voller Beweis nur dort, wo ein Bruch still
  bliebe. Hier trifft das zu.)*
- **Keine Zeitschranke in die Suite.**
- Ueberlebt eine Mutation: **melden**, nicht nachbessern.

## Testumgebung

Windows 10/11 x64, Branch `docs/audit-and-advisor-design`, Stand `ab522d7`.
Testbefehl nach `CLAUDE.md`. **Suitezahl gegen 1357 passed, 9 skipped,
0 failed** (T-137).

Alle drei Datenverzeichnisse umlenken und **jede Umlenkung nachweisen**, sonst
abbrechen und nur das melden. Der Spielstand ist read-only.

## Scope-Grenzen

- **Nicht anfassen:** `nrplanner/`, `nrdata/`, `UI_SPEC.md`,
  `ARCHITECTURE.md`, `GOAL.md`, `docs/state.md`, `qa/findings.md`,
  `docs/tasks/`.
- Nicht: das Nachmessen (der `performance-tuner` kommt als naechstes), AD-029
  Stufe B, die Pruefphase.

**Parallel:** derzeit nichts. **Diesmal stimmt das** — der Director committet
waehrend deines Laufs nicht. *(In T-137 stand dieselbe Zusage, und ein
Doku-Commit des Directors lag im selben Zeitfenster; der Lauf hat es gemeldet,
und das war richtig.)*

**Beruehrt Dateien:** die drei oben genannten.

## Rueckgabeformat

Uebergabe-Kontrakt-Block plus Abschlussbericht. **Der Auftrag gilt erst als
erledigt, wenn `docs/berichte/T-139-developer.md` auf der Platte liegt.**
Commit mit Pfad hinter `--`.
