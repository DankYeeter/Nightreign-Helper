# T-118 - S11: das Budget messen und setzen (performance-tuner)

**Erstlauf im Projekt.** Kein reiner Messlauf: ein Optimierungsplan mit
Priorisierung, und die Zahl, die A6 heute fehlt.

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`.

## Ziel

Drei Dinge, in dieser Reihenfolge:

1. **A6 bekommt seine Zahl.** `GOAL.md` A6 sagt woertlich, der Zielwert werde
   vom `performance-tuner` gesetzt — er wurde nie gesetzt, und deshalb ist A6
   das einzige Abnahmekriterium, das der A9-Lauf nicht entscheiden konnte.
2. **Zwei offene Architekturwerte festlegen**, die `ARCHITECTURE.md`
   ausdruecklich dir zuweist (Zeile 1620 ff. und 1632 ff.).
3. **Ein priorisierter Optimierungsplan** — was lohnt sich, was nicht, mit
   Zahlen statt Gefuehl.

## Kontext

**Quittung aus T-114 (`qa-engineer`, A9, abgeschlossen).** Urteil ueber sechs
Kriterien: A3, A4, A5, A7, A8 **PASS**, **A6 CONCERNS**. Seine Begruendung
woertlich sinngemaess: die erste Haelfte von A6 (die Oberflaeche blockiert
nicht) ist geprueft und haelt; die zweite (Antwortzeit im gemessenen Budget)
ist **mangels Zahl weder erfuellt noch verletzt**. Er hat am **verpackten**
Code 440 Beraterlaeufe ueber 10/10 Nightfarer und 74/74 Kelche gefahren, mit
echten 309 Relikten.

**Der bekannte Engpass (D-1, `docs/state.md`):** `model.compute` traegt
**94 % von 941,6 ms**. Diese Zahl stammt aus einem frueheren Zyklus und ist
**von dir nachzumessen, nicht zu uebernehmen** — sie ist mein Lesestand, nicht
dein Nachweis.

**Abnahmekriterium aus GOAL.md (woertlich):**
> **A6** Die Berechnung blockiert die Oberflaeche nicht: sie laeuft im
> Hintergrund, das Fenster bleibt bedienbar, und bei grossen Relikt-Bestaenden
> bleibt die Antwortzeit im gemessenen Budget (Zielwert wird vom
> `performance-tuner` gesetzt).

**Stand laut docs/state.md (woertlicher Auszug, 08.09.2026, Director):**
> **Entschieden 08.09., noch nicht beauftragt:** **S11**
> (`performance-tuner`, Erstlauf, opus) setzt A6 seine Zahl · die zwei
> fehlenden Waechter werden gebaut (QA-204 Englisch, QA-205 A3-Zusage), bevor
> ausgeliefert wird.

## Die zwei Werte, die `ARCHITECTURE.md` dir zuweist

**Zitiere sie im Bericht und begruende jede Zahl mit einer Messung:**

> (Vorschlag 100 ms gegen 250 ms) — 50 ms Rechnung hinter 250 ms Wartezeit
> fuehlt sich traeger an, als sie ist. Der `performance-tuner` setzt beide
> Werte in S11.

> Weil die Eintraege nun kleiner und zahlreicher sind, ist die LRU-Groesse aus
> AD-007 (Vorschlag 32) neu zu setzen — Aufgabe des `performance-tuner` in
> S11, Vorschlag 64.

`ARCHITECTURE.md` nennt ausserdem einen Messpunkt ausdruecklich fuer dich: der
Berater ist seit AD-018 **an der Interaktion beteiligt**, nicht daneben — jede
Verlangsamung von `model.compute()` ist damit sofort spuerbar. Das gehoert
gemessen, nicht nur erwaehnt.

## Messszenario — verbindlich, damit die Reihe vergleichbar bleibt

**Datenmenge:** der echte Spielstand des Nutzers, **309 Relikte**, 110
gespeicherte Builds. Das ist die realistische Obergrenze und zugleich die,
gegen die T-114 gemessen hat.
**Zielgeraet:** dieser Rechner, Windows 10.
**Kalt und warm:** beides getrennt ausweisen — kalter Cache (erster Lauf nach
Start) und warmer (Wiederholung). Der LRU-Wert haengt genau daran.
**Wiederholungen:** je Fall mindestens fuenf Laeufe, Median **und** Spanne.
Eine Einzelmessung ist eine Hypothese, keine Zahl — in diesem Zyklus ist genau
dieser Fehler schon einmal passiert (QA-198: 107 s gegen 5 min).

**Nimm den festen Testabzug**, statt den Datenabzug neu zu bauen:
`C:\Users\Daniel\AppData\Local\NightreignHelper-Testabzug` (841 Dateien,
19,8 MB, 1.8.0, `EXTRACT_VERSION` 11) in dein umgelenktes `LOCALAPPDATA`
**kopieren**. Das spart je Lauf 107 s bis 5 min. **Miss und nenne, wie viel es
tatsaechlich gespart hat** — die Ersparnis ist bisher erwartet, nicht gemessen
(E-1 in `docs/plan-restarbeiten.md`).

## Die Datenschutzsperre — Pflicht

309 Relikte, 110 gespeicherte Builds. **Lesen ja, schreiben nein.** Drei
Umlenkungen, alle drei nachzuweisen:
`NIGHTREIGN_SETTINGS_ORG` (`nrplanner/favourites.py:25`) auf einen eigenen
Wert · `LOCALAPPDATA` (`nrplanner/paths.py:20`) · `APPDATA`
(`nrplanner/shortcut.py:44-49`). Gelingt ein Nachweis nicht, **brich ab und
melde nur das**. In Zyklus 4 und 5 sind so drei Datenverluste entstanden
(QA-195).

## Umfang und Grenzen

**Beruehrt Dateien:** `docs/perf/baselines.md` (neu) und dein Bericht.
**In diesem Auftrag optimierst du nichts** — du misst, setzt die Werte und
priorisierst. Jede Codeaenderung ist ein Folgeauftrag an den `developer`, mit
deinen Zahlen als Vorher-Wert.

**Parallel: nein.** Du laeufst allein, es laeuft kein `developer` — Messwerte
auf beweglichem Stand sind wertlos. Ich committe in dieser Zeit nur unter
`docs/tasks/`, `docs/berichte/` und `docs/state.md`.

Temporaere Dateien und Messbaeume **ausschliesslich** unter `T-118/` im
Scratchpad — er ist zwischen gleichzeitig laufenden Rollen geteilt, und am
07.09. hat ein fremder Lauf dort einen Messbaum mitten im Messstapel geleert.

## Rueckgabeformat

Uebergabe-Kontrakt-Block (STATUS, AUFTRAG, GELESEN, GEAENDERT, ANNAHMEN,
NAECHSTER, BLOCKIERT DURCH) plus Abschlussbericht. **Der Auftrag gilt erst als
erledigt, wenn `docs/berichte/T-118-performance-tuner.md` auf der Platte
liegt** — du hast Schreibrecht, der Bericht ist Teil des Auftrags.

**Der Zielwert fuer A6 steht als ein Satz im Bericht**, so formuliert, dass er
woertlich nach `GOAL.md` uebernommen werden kann — mit der Messung, auf der er
beruht, und mit der Aussage, ob der heutige Stand ihn **einhaelt**. Setzen tue
ihn ich; herleiten musst du ihn.
