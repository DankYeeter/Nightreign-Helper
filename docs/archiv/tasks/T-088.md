# T-088 - Der Rest von T-087: Gegenproben, Suite, Commit, Bericht (developer)

**Ziel:** Die im Arbeitsbaum liegende, **uncommittete** Arbeit aus T-087
abschliessen. Der Inhalt steht; es fehlen die drei Gegenbauten, ein
Suite-Lauf, der Commit und der Bericht.

## Kontext

T-087 hat den Docstring von `explain.not_counted` nach AD-026 richtiggestellt
und zwei Regressionsfaelle geschrieben. Der Lauf ist **ohne Bericht und ohne
Commit stehengeblieben**, waehrend er auf einen Testlauf im Hintergrund
wartete. Sein Kontext ist nicht fortsetzbar, deshalb dieser neue Auftrag.
**Die Arbeit ist nicht verloren** - sie liegt im Arbeitsbaum.

**Quittung: was der Director im Baum vorgefunden und gelesen hat.**

`nrplanner/advisor/explain.py`, 9 Zeilen ein, 4 aus, ausschliesslich im
Docstring von `not_counted`: Der Abschnitt "Scope" sagt jetzt, drin sei jede
Bedingung, die der Spieler herstellen kann - einschliesslich einer Armatur,
die er gerade nicht fuehrt (AD-026); draussen seien fremde Nightfarer und der
klassengebundene Buff aus QA-104 (`magicSubCategoryChange`, 8 Effekte, keiner
konditional, sie landen in `Build.class_rates`). Der Text nennt ausdruecklich,
dass `qualitative` und `situational` denselben Effekt zugleich tragen koennen
und dass die alte Notiz genau das als Widerspruch gelesen hat. **Der Rumpf der
Funktion ist unveraendert.** Der Director hat den Diff gelesen und haelt ihn
fuer richtig.

`tests/test_advisor_explain.py`, 49 Zeilen neu, zwei Faelle ueber
`advisor.an_armament_type_gate`:
`test_an_unmet_weapon_type_gate_is_reported_as_uncounted` (Name in
`not_counted`, `len == 1`) und
`test_a_weapon_type_gate_met_by_the_grid_is_not_reported_as_uncounted` (Name
nicht drin, `len == 0`). Die Erwartungen stehen als Literal, nicht als
Ausdruck aus der bewachten Stelle. Beide unterscheiden sich nur im
`weapons_held` des Kontexts.

**Diese Arbeit uebernimmst du unveraendert.** Sie ist nicht neu zu schreiben.
Faellt dir daran etwas auf, meldest du es - aendern nur, wenn eine Gegenprobe
es erzwingt.

Verwandte IDs: AD-026, AD-010, QA-104, QA-106, D-3.

**Abnahmekriterium aus GOAL.md:** "**A7** Wo die Spieldateien eine Bewertung
nicht hergeben, sagt das Programm das, statt zu raten - die bestehende
Hausregel gilt auch fuer den Berater."

**Stand laut docs/state.md (2026-09-06, Director), woertlich:**

> - **D-3** `not_counted` behaelt AD-010 (konditionale Effekte). `UI_SPEC` 4.9
>   ist in T-078 in 4.9a und 4.9b geteilt.

**Nachtrag des Directors, 2026-09-07:** D-3 bleibt gueltig; AD-026 hat die
Begruendung nachgeliefert.

## Umfang

**Stufe:** klein - zwei Dateien, der Inhalt steht, die Tests sind geschrieben.
**Eskaliert auf opus**, weil der Vorlauf ohne Bericht abgebrochen ist.

**Beruehrt Dateien:**
- `nrplanner/advisor/explain.py` (liegt geaendert vor)
- `tests/test_advisor_explain.py` (liegt geaendert vor)
- `docs/berichte/T-088-developer.md` (neu)

**Parallel:** nein - es laeuft gerade kein anderer Agent. Arbeitsbaum, Branch
`docs/audit-and-advisor-design`, HEAD `f9bcad2`.

**Im Baum liegen ausserdem** `nrplanner/advisorbar.py`, `nrplanner/app.py` und
`tests/test_advisor_bar.py` als geaendert. Das ist ein veralteter
Statuseintrag - `git diff` zeigt fuer die drei nichts. **Fass sie nicht an und
nimm sie in keinen Commit.** Committe mit Pfadangabe hinter `--`.

### Schritt 1 - die drei Gegenbauten

Jeden einzeln herstellen, den Testlauf beobachten, danach zuruecknehmen. Der
`architect` hat sie in T-085 benannt:

1. `CONDITIONAL_FIELDS` um die drei Waffenfelder erleichtern ->
   `test_an_unmet_weapon_type_gate_is_reported_as_uncounted` **muss rot
   werden**.
2. In `not_counted` auf `_ARMAMENT_GATES` filtern -> derselbe Fall **muss rot
   werden**.
3. `satisfied_by_weapon` die Grid-Menge nicht mehr durchsuchen lassen ->
   `test_a_weapon_type_gate_met_by_the_grid_is_not_reported_as_uncounted`
   **muss rot werden**.

**Bleibt einer gruen, ist das ein Befund**, kein Grund weiterzumachen: dann
bewacht der Test nicht, was er bewachen soll. Melde es und halte an, statt den
Test nachzubessern - der Director entscheidet.

Fuer jeden Gegenbau nennst du im Bericht: die genaue Aenderung, welcher Test
rot wurde, und mit welcher Meldung.

### Schritt 2 - Suite, Commit, Bericht

Suite `-m "not slow"` laufen lassen, danach committen (Pfadangabe hinter
`--`), dann den Bericht schreiben.

**Die Suite laeuft rund neun Minuten.** Genau daran ist der Vorlauf
steckengeblieben. Warte sie ab, statt den Lauf in den Hintergrund zu schicken
und dich auf eine Benachrichtigung zu verlassen. Reicht die Zeit nicht, ist
`STATUS: teilweise` mit der genannten Zahl das richtige Ergebnis - **ein
Bericht ohne Zahl ist besser als kein Bericht.**

## Vorgaben

**Der `architect` hat woertlich verboten:** `CONDITIONAL_FIELDS`,
`WEAPON_TYPE_GATES`, `is_conditional`, `compute_qualitative`, `Situational`
oder irgendeine Zahl **dauerhaft** zu aendern. Die Gegenbauten sind
ausdruecklich erlaubt und werden **jeder einzeln wieder zurueckgenommen** -
pruef am Ende mit `git diff`, dass von ihnen nichts uebrig ist, und schreib das
Ergebnis dieser Pruefung in den Bericht.

`UI_SPEC.md`, `ARCHITECTURE.md` und die Statuszeile 4.9b fasst du nicht an.
**QA-185** und **QA-186** nimmst du nicht mit - eigene Auftraege.

## Testumgebung

Windows 10, Python 3.11 aus `.venv`, PySide6, `QT_QPA_PLATFORM=offscreen`
(setzt `tests/conftest.py`). Suite:
`.venv\Scripts\python.exe -m pytest -m "not slow"`.

Stand vor T-087: **1105 passed, 9 skipped, 5 deselected**, aus einem frischen
Export von `775173c`. Mit den zwei neuen Faellen sind **1107 passed** zu
erwarten - weicht die Zahl ab, ist das ein Befund.

**Bekannte Grenze, vom `architect` gemeldet:** beide Faelle brauchen den echten
Datensatz und ueberspringen sich auf einem fremden Runner (QA-106). Sag im
Bericht, ob deine Faelle das tun; bau keine Umgehung.

Datenabzug unter `%LOCALAPPDATA%\NightreignHelper` - nur lesen.

## Abnahme

**Die Abnahme misst breiter als der Befund.** Der Befund stammt aus der
Umgebung Wylder/Greatsword. Der `architect` hat mit Ironeye/Bow eine zweite
gemessen, der `ui-ux-designer` sie unabhaengig reproduziert - die Zahlen liegen
in `docs/berichte/T-085-architect.md` und `T-086-ui-ux-designer.md`. Sag, ob
deine beiden Faelle an einer bestimmten Figur haengen.

**Rot-vorher ist der Kern dieses Auftrags**, nicht ein Anhang: Schritt 1 ist
die Abnahme.

## Rahmen (steht woertlich in jedem Auftrag)

- **Nach jedem Teilschritt ein Commit mit gruener Suite.** Ein Abbruch darf
  hoechstens einen Teilschritt kosten, nie einen Umbau.
- **Testzahlen gelten nur vom frischen Klon.** Eine Zahl, die als
  Freigabekriterium dient, muss auf einem fremden Rechner reproduzierbar
  sein. Nenne die Zahl aus diesem Lauf, nicht die aus dem Arbeitsbaum.
- **Laeuft parallel eine andere Rolle, arbeitest du in einem eigenen Klon**
  im Scratchpad - nicht im Arbeitsbaum. *Hier laeuft niemand parallel*, und
  die Arbeit liegt im Arbeitsbaum; du arbeitest dort.
- **Befund-IDs vergibt ausschliesslich der Director.** Melde Befunde mit
  Titel und Fundstelle, ohne Nummer.
- **Zugschwelle.** Nach 150 Werkzeugaufrufen sperrt ein Hook alles ausser
  `git add/commit` und dem Bericht unter `docs/berichte/`. Dieser Auftrag
  braucht einen Bruchteil davon. Brauchst du mehr, ist etwas anderes los, als
  der Auftrag beschreibt - dann melde das.
- **Zahlen tragen ihr Rezept.** Jede Zahl, die als Schranke oder Kennwert
  dient, nennt Formel, Stichprobenfehler und Sicherheitsabstand; bei
  Messwerten Skript und Commit.
- **Abwesenheits-Behauptungen brauchen die Primaerquelle.** "Nicht
  vorhanden", "nicht geliefert", "unveraendert" wird nur nach direkter
  Pruefung des aktuellen Bestands gesagt (Dateisystem, git, Volltextsuche
  mit mindestens zwei unabhaengigen Begriffen) - nie aus einer Notiz oder
  einem Statuswort uebernommen.
- Projektspezifisch: Testbefehl
  `.venv\Scripts\python.exe -m pytest -m "not slow"`. Datenabzug nur lesen.
  Save read-only. Kein Netzwerkzugriff. Alle Oberflaechentexte Englisch (A8).
  Kein `push`, `pull`, `fetch`, `checkout`, `branch`, `reset`, `stash`.

## Scope-Grenzen

Alles unter "Vorgaben". Zusaetzlich: die **Advisor bar** aus T-083 und die
drei nur scheinbar geaenderten Dateien fasst du nicht an; die **Slotkarte**
ist ein spaeterer Auftrag.

## Rueckgabeformat

Schreibe deinen vollstaendigen Bericht **unveraendert** nach
`docs/berichte/T-088-developer.md`, bevor du ihn zurueckgibst.

Beginne den Bericht mit dem Uebergabe-Kontrakt-Block:

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEAENDERT:
ANNAHMEN:
NAECHSTER:
BLOCKIERT DURCH:
```

Danach: das Ergebnis **jedes** der drei Gegenbauten einzeln mit der Meldung
des rot gewordenen Tests, die Pruefung, dass kein Gegenbau uebrig ist, deine
Commit-Liste, Testergebnis vorher/nachher - und was dir aufgefallen ist, das
nicht in den Auftrag gehoerte.
