# T-327 — A27 Bau: Weapon art und Spell damage im Schadensblock; Fix QA-293/294

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen in der
`CLAUDE.md` (Umlenkung `DankYeeterT-327<x>`, Testabzug v16 kopieren,
NH-004). Stand `8d54281` (Release 1.17.0). Zeitbudget des Nutzers: eine
Stunde (21.09. 18:20) — Bau in zwei Worktrees parallel, danach Merge.

**Kriterium (GOAL.md A27, woertlich):** "Revenant mit Beast-Claw-Relikt: der
Block zeigt neben der Klauen-AR die Zeile 'Spell damage (Beast Claw) 578
(uncalibrated)' und ein Physical-Attack-Relikt bewegt beide Zeilen sichtbar;
Wylder zeigt 'Weapon art' und ein Skill-Buff bewegt nur diese Zeile."
Stand: nicht erfuellt (Block zeigt nur AR, `statsheet.py:504`).

## Vorgaben (bindend)
- Spec `UI_SPEC.md` §4.5, **AK-353..AK-364**; Entwurf AD-019/AD-038/AD-052/
  AD-053 (Fassade ist die einzige Quelle; kein zweiter Auswahlweg fuer
  Katalysator/Zauber — die Funktion aus `advisor/goals.py` `_start_catalyst`/
  `_spell_thrown` in die Fassade heben oder von dort importieren).
- Sicherheit: keine neue Vertrauensgrenze (nur Fassadenzahlen und Namen aus
  dem Datensatz); T-324v Punkt 9 gilt — Zaubername in Label/Tooltip nur
  PlainText/`html.escape` (SEC-019). Compliance: nichts Neues (A25/A26).
- Ponytail: kein neuer Zustand, keine Kopie der Ratenschleife.

## T-327a developer (normal) — A27 Block (Worktree)
Schritt 0: Worktree auf `8d54281` setzen (`git reset --hard 8d54281`).
Beruehrt: `nrplanner/damage.py` (Grundlinien-Antwort fuer `spell`, AK-359:
`bare`-Pendant wie `Rating.bare`/`now`), `nrplanner/statsheet.py`
(`_refresh_weapon_damage`: Zeilen *Weapon art* AK-354/355/356 und *Spell
damage (<Zauber>)* AK-357..361, Reihenfolge AK-363, Tooltip AK-361),
Tests `tests/test_damage_spell.py`, `tests/test_stat_sheet*.py`. Fensterlauf
(Revenant mit 7370900 im Build; Wylder mit 312300).

## T-327b developer (klein) — QA-293, QA-294 (Worktree)
Schritt 0 wie a. QA-293: Why-Zeile "1 of its 3 effects moved a number"
nennt unter Schulwahl den falschen Effekt — `advisor/candidates.py`/
`explain.py`: welcher Effekt "bewegte", muss aus dem Zauberpfad kommen
(Tauschzauber `start_magic_id` bewegt; ein Gattungs-/Schulbuff nur, wenn
`art_factor` ihn zaehlt). QA-294: Nullzelle unter Typwahl ohne Vorkommen im
Zauber ("Glintstone Pebble carries no fire damage") — Satz in `goals.py`
`_spell_cell` analog AK-344, Wortlaut eigener Text, PlainText. Beruehrt:
`advisor/candidates.py` oder `explain.py`, `advisor/goals.py`, Tests.

## T-327c — Merge, Pruefphase (qa-engineer am Quellstand, sonnet Retest-Stufe
nur fuer QA-293/294), Bau 1.18.0, Notes, Release — nach Nutzerfreigabe.

## T-327d developer (klein) — Version 1.18.0 (`nrplanner/__init__.py`)
## T-327e security-reviewer — Diff `8d54281..c43fe64` (statsheet Tooltip/Labels, explain, damage-Hebung)
## T-327f technical-writer — Guide: Schadensblock-Zeilen Weapon art / Spell damage (docs/anleitung/guide.md Build-planner-Absatz)
## T-327g release-manager (build) — nach d; h notes; Release (Nutzer 21.09. 19:25: kurze Kette)
