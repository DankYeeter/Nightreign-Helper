# T-287 — Retrospektive Zyklus 26 umsetzen: NH-004, NH-005, NH-006 (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen stehen
in der `CLAUDE.md` des Projekts; Abweichungen fuer diesen Auftrag: T-287b
arbeitet im Team-Repo (`~/.claude`, Junction-Ziel von `~/.claude/agents`),
nicht im Projektbaum.

**Ziel:** Die drei vom Nutzer am 16.09.2026 freigegebenen Vorschlaege aus
`docs/berichte/T-286-retrospective.md` (Z. 29-88) woertlich umsetzen.

**Stufe/Modus:** developer: klein (je Abschnitt)

**Abnahmekriterium:** kein GOAL-Kriterium; Prozessmassnahme aus
`docs/lessons.md` NH-004..NH-006.

**Stand (docs/state.md, 16.09.2026):** "Release v1.13.1 veroeffentlicht …
Naechster Schritt: `retrospective` Zyklus 26" — erledigt `4b2e931`.

## T-287a — Projekt (NH-004)

Vorschlag 1 (a)+(b) aus dem Bericht: Riegel in
`.claude/hooks/enforce-data-redirect.ps1` nach Z. 78 (Codeblock woertlich
aus `docs/lessons.md` Z. 1626-1645), Absatz in `CLAUDE.md` nach
"Plattformgrenze (QA-241 …)", `docs/release/ROLLOUT.md:309-312` auf
"siehe CLAUDE.md" kuerzen. Nachweis: einmal das `deny` zeigen (eine
laufende Kopie mit Umlenkung starten, zweiten Start ueber den Hook
provozieren, beide beenden, `Get-Process` = 0 im Bericht) und einmal, dass
`Get-FileHash`/`ls` auf die EXE weiter durchgehen. Beruehrt:
`.claude/hooks/enforce-data-redirect.ps1`, `CLAUDE.md`,
`docs/release/ROLLOUT.md`. Ein Commit im Projekt.

## T-287b — Team-Repo (NH-005, NH-006)

Vorschlag 2: `~/.claude/agents/power-user.md` Z. 186-189 ersetzen und
Z. 93-97 kuerzen, Wortlaut aus dem Bericht. Vorschlag 3: neuer Hook
`~/.claude/hooks/require-task-file.ps1` (`PreToolUse`, Matcher `Agent|Task`,
`deny` mit `cp`-Vorlagenbefehl, wenn `docs/tasks/` existiert und die
hoechste `T-###` im Prompt keine Datei hat; ohne Nummer passiert), Eintrag
in `~/.claude/settings.json` nach dem Muster der bestehenden Hooks; Test:
Skript mit einem Prompt-JSON fuer `T-999` (deny) und ohne Nummer (allow)
aufrufen, beide Ausgaben im Bericht. Dazu im Projekt
`docs/plan-restarbeiten.md:205-207` zur Einzeile "NH-001: Waechter
`require-task-file.ps1`". Beruehrt: `~/.claude/agents/power-user.md`,
`~/.claude/hooks/require-task-file.ps1`, `~/.claude/settings.json`,
`docs/plan-restarbeiten.md`. Commits: einer im Team-Repo (Pfad des Repos
per `git -C ~/.claude rev-parse --show-toplevel` bestimmen; wenn `~/.claude`
selbst kein Repo ist, im Junction-Ziel von `~/.claude/agents` committen und
die Hook-/Settings-Aenderung als uncommitted melden), einer im Projekt.

## Umfang

**Parallel:** ja (a und b, disjunkte Dateien und Repos) · **Scratchpad:**
`<Scratchpad>/T-287/<a|b>/`

## Scope-Grenzen

Kein Anwendungscode. Kein vierter Grund in `commands/director.md`
(verworfen). Keine anderen Hooks anfassen. Kein Push.

## Praemissen — jede Bestandsbehauptung mit Quelle

- Berichtstext Z. 29-88 · `docs/berichte/T-286-retrospective.md` · 16.09.
- Codeblock lessons.md Z. 1626-1645 · Bericht Vorschlag 1 · ungeprueft, Zeilen selbst suchen.
- `power-user.md` Zeilen 93-97, 186-189 · Bericht Vorschlag 2 · ungeprueft, Wortlaut selbst suchen.

## Rueckgabe

Kontraktblock plus hoechstens zwanzig Zeilen.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
