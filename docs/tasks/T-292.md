# T-292 — 1.14.0 Bau: A22 Attribute (AD-038) und A23 Familien (AD-039)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen stehen
in der `CLAUDE.md` des Projekts (Umlenkung je Lauf `DankYeeterT-292x`,
NH-004, Nutzerdaten nur lesen, kein Netz); Abweichungen: keine. Stand
`c64ec45`. Nutzerentscheide 17.09. 20:48: OF-42 **Startwaffe des
Nightfarers**, OF-43 dritte Richtung **behalten**.

**Ziel:** A22 und A23 aus `GOAL.md` (Nachtrag 17.09.) nach `ARCHITECTURE.md`
Themenbereich K (AD-038, AD-039, ab Z. 6477) umsetzen; Release 1.14.0.

**Abnahmekriterium (GOAL.md, woertlich):** A22 "Flueche senken, Buffs heben
die Zahl … die Why-Zeile nennt den Betrag"; A23 "je Familie eine Kopfzeile
mit Avoid … Mitglieder ein drittes Kaestchen Allow … Favourite bleibt
'muss rein'".

**Stand (docs/state.md, 17.09.2026):** 1.13.2 gebaut, QA-Fensterretest
offen (Nutzerkopie laeuft).

## T-292a — ui-ux-designer (Spec, OF-44)

`UI_SPEC.md`: AK-315 Betragswortlaut der Why-Zeile fuer Attribut-Effekte
(Form AK-290, `counted against it`, Buff und Fluch, Beispiel Duchess
Dex -3 → Attack -2); AK-316 Filterfenster mit Familien-Kopfzeilen (Avoid am
Kopf, Allow-Kaestchen an Mitgliedern nur unter vermiedener Familie,
Zaehlerklausel Ids/Familien, Suche ueber Kopf und Mitglieder, Sortierung
gegen AK-308, Persistenz-Verhalten beim Neustart), AK-317 Legende/
Tooltips fuer Allow. Wortlaut AK-300. Grundlage AD-039 (`family_key`,
`allowed`, `avoided_families`, `resolved_excluded`). Beruehrt: `UI_SPEC.md`.
Kein Commit.

## T-292b — developer (Stufe normal) — AD-038 Schritt 1: Bezugswaffe

Startarmatur des Nightfarers wird `reference` in `advisorbar.asking_from`
(AD-038, `hero["starting_weapon"]`, Stufe 1, ohne Rollen); der gebaute
Waffenzweig in `advisor/goals.py` `_max_damage` wird damit erreichbar.
Nachweis: Duchess Dex -3 → 72 → 70 (ARCHITECTURE AD-038 Messung), alle
zehn Startarmaturen = A20-Zellen; `scripts/measure_advisor_search.py`
wieder aktuell — A6-Messung (Optimize-Median vorher/nachher, 1358 ms
Referenz). Beruehrt: `nrplanner/advisorbar.py`, `nrplanner/advisor/goals.py`,
`scripts/measure_advisor_search.py`, Tests. Nicht: Betragszeile (Schritt 2,
wartet auf AK-315).

## T-292c — security-reviewer (Vorgaben-Vorlauf, kurz)

Welche Themen sind bei AD-039 zu beachten: neue QSettings-Schluessel
`advisor/allowed`, `advisor/avoided_families` (`\n`-getrennt, Familiennamen
aus Spieldaten mit Kommas/Sonderzeichen), `family_key` aus Effektnamen,
Anzeige der Familiennamen im Fenster. Fuenf Zeilen fuer den Block im
Auftrag T-292d. Nur Antwort.

## T-292d — developer (Stufe normal) — AD-039 Schritte 3+4 (nach a, c)

Familien: `effecttext.family_key`, `EffectFilters` + `allowed`/
`avoided_families`/`resolved_excluded`, Verdrahtung in `app.py`,
`advisorbar.py` (Zaehler), `advisorblock.py`; Fenster nach AK-316/317 in
`effectfilterdialog.py`. Mehr als fuenf Anwendungsdateien → in zwei Commits
(3, 4) mit Zwischenmeldung, nicht teilen. Sicherheitsblock aus T-292c.

## T-292e — developer (Stufe klein) — AD-038 Schritt 2 (nach a, b)

Betrag in der Why-Zeile: `explain._curses_the_goal_cannot_feel` →
`_felt_by_the_goal` (zweite Bewertung ohne den Effekt, Buff und Fluch),
Wortlaut AK-315. Beruehrt: `nrplanner/advisor/explain.py`, Tests.

## Umfang

**Parallel:** a ∥ b ∥ c; danach d ∥ e (disjunkte Dateien: d ohne
`explain.py`/`goals.py`, e nur `explain.py`) · **Scratchpad:**
`<Scratchpad>/T-292/<x>/`

## Scope-Grenzen

Kein Versionssprung. Keine Aenderung an `advisor/search.py`. Keine
Familienlogik unter `advisor/`. Kein Entfernen von `MAX_ATTRIBUTES`.

## Praemissen — jede Bestandsbehauptung mit Quelle

- AD-038/AD-039 Text und Messungen · `ARCHITECTURE.md` Themenbereich K · 17.09., ungeprueft durch den Director — Zahlen selbst nachmessen.
- 340 Ids → 222 Familien am Nutzerbestand · AD-039 · ungeprueft.

## Rueckgabe

Kontraktblock plus hoechstens zwanzig Zeilen je Abschnitt.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
