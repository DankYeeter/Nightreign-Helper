# T-324 — A26 Bau: Extraktor, Fassade, zwei Felder, Ziel, Spec, Leiste

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen in der
`CLAUDE.md` (Umlenkung Pflicht, `NIGHTREIGN_SETTINGS_ORG` `DankYeeterT-324<x>`;
Testabzug v15 kopieren — nach A26-1 ist er ungueltig, A26-2 ersetzt ihn;
NH-004 ein Fensterlauf je Zeitpunkt). Stand `b6e7f37`.

**Kriterium (GOAL.md A26 + Nachtrag 17:40, woertlich):** Nachweis-Absatz A26
und "Revenant mit 7370900 (Beast Claw) unter Incantations x All zeigt die
Beast-Claw-Zahl, physische Buffs zaehlen, Holy nicht". Stand: nicht erfuellt.

## Vorgaben (bindend)
- Entwurf `ARCHITECTURE.md` Themenbereich N, **AD-050..AD-054**, Verbotsliste
  "Was der developer nicht tun soll"; Forschung R-009, R-010 (Leseproben in N).
- Nutzerentscheide 20.09. 18:00: OF-54 ER-Formel als Praemisse, Zahl
  "uncalibrated"; OF-57 staerkster Einzeltreffer; OF-58 Spell Power ist die
  Grundlinie des Tauschzaubers, darauf die Passiven; ohne Tauschrelikt 0,00
  mit Satz, keine Ersatzzahl.
- Sicherheit/Compliance: Vorlauf T-324v (Block wird nachgetragen).

## T-324a developer (normal) — A26-1 Extraktor (AD-050)
`nrdata/extract.py`: `equip_Wep_Left_1` als `starting_weapon_left`;
`spells[].damage` ueber Magic.refId/refCategory → Bullet.atkId_Bullet →
AtkParam_Pc (Def-Alias `AtkParam.xml`; 464 B/211 Felder), je Schadensart
staerkster Einzeltreffer; `effects[].start_magic_id` aus SpEffectParam
Offset 1020 (s32, Def verlaengern; 10 Zeilen != -1); `EXTRACT_VERSION` 16.
Waechtertests neu (`tests/test_extraction.py` oder neue Datei): 111/160
Zauber mit Schaden, Beast Claw 6820 phys 362, Pebble 4000 mag 152, 10
Tauschrelikte → Magic-Id, drei linke Starthaende. Keine Waffenkunst-Daten.

## T-324b developer (klein) — A26-2 Testabzug v16
Nach a: Testabzug neu bauen (Rezept `docs/plan-restarbeiten.md`), Zahlen
messen, `CLAUDE.md` Abzugszeile und `docs/plan-restarbeiten.md` nachziehen.

## T-324c developer (normal) — A26-3 Fassade (AD-053)
`nrplanner/damage.py` `spell(...)`: Grundwert x SP/100 x r[T] x a(Gattung)
x a(Schule, nur wenn der Zauber der Schule angehoert); geteilte Raten-
schleife; Klassenraten/Zweihand/Konversion/Statusmalus erreichen keinen
Zauber; `tests/test_damage_art.py` erweitern. Parallel zu d (disjunkt).

## T-324d developer (normal) — A26-4 zwei Felder (AD-051)
`nrplanner/advisor/types.py`, `run.py`, `goals.py`: `hit_with` x
`damage_type` ersetzen `damage_art`, beide im Cache-Schluessel, alter
Einstellungswert verworfen; `tests/test_advisor_run.py`.

## T-324e developer (normal) — A26-5 Ziel (AD-052, Befunde)
Nach c und d. `goals.py`: Bezugsobjekt je `hit_with`, Tauschrelikt als
Kandidat bewegt den Grundwert, leere Zellen mit Satz; Kombination
Weapon art x Typ genau einmal; `tests/test_advisor_goals.py`.

## T-324f ui-ux-designer (Spec) — A26-6 (OF-55, OF-56), parallel zu a
Zwei Felder "Hit with" x "Damage type" statt der einen Box (AK-327..336
nachziehen), Beschriftung der unkalibrierten Zauberzahl, Leiste bei
leeren Zellen, Why-Zeilen. `UI_SPEC.md` + Register, AK ab AK-337.

## T-324g developer (normal) — A26-7 Leiste, nach e und f
`nrplanner/advisorbar.py`, `tests/test_advisor_bar.py`; Fensterlauf.

## T-324v — Vorlauf Sicherheit (10) und Compliance (GRUEN, 6), 20.09. 18:00
Sicherheit: (1) SpEffectParam-Def nur verlaengern, wenn Zeile 1024 B und
`row_size == 976`; `offset+4 <= row_size`, sonst laut abbrechen (Muster
AntiqueStandParam `extract.py:1705-1711`) — ein zu langer Def laesst die
ganze Effekttabelle still auf 0 Felder fallen. (2) AtkParam_Pc-Alias nach
`param.read` pruefen (Felder je Zeile > 0), Patch mit anderer Zeilenlaenge
scheitert laut, nie Schaden 0. (3) Kette Magic → Bullet → AtkParam nur ueber
`.get`/None-Pfade; fehlende Zeile = Zauber ohne `damage`. (4) Besuchsmenge +
Tiefendeckel gegen Zyklen; ein Id-Index je Tabelle statt `by_id` (linear,
SEC-049/050). (5) `atk*` nur endlich und >= 0; NaN/Inf verwerfen (json.dump
schriebe NaN); `damage` fuehrt nur Typen > 0. (6) Keine Division durch Spell
Power/Grundwert; Quoten mit Nenner 0 abfangen (Revenant-Standardzauber 0,00).
(7) `hit_with`/`damage_type` als unveraenderliche `str`-Felder der
AdvisorRequest, automatisch im `cache_key`. (8) Beide Einstellungsschluessel
beim Laden gegen Box-Eintraege pruefen (`findData >= 0`), alter `damage_art`
verworfen; nie in Formatstring/Pfad. (9) Neue Spieltext-Senken (Zaubername,
Schulname, Why, unknowns, Tooltip, Tauschrelikt-Befund) nur PlainText/
`html.escape` (SEC-019). (10) Kein Netz, kein Schreiben ins Spiel; Testabzug
wird mit EXTRACT_VERSION 16 ungueltig.
Compliance: Fixtures nur Namen/Ids/Zahlen, keine Zauberbeschreibung
(A-001); Fixture bleibt bei einer Handvoll Zauber, nicht 111 (A-007, §87b);
A-002-Waechter gegen Spielprosa beim Anlegen mit erledigen (eine Zeile);
Def-Verlaengerung im Code oder projekteigener Datei **ausserhalb**
`vendor/Paramdex/` — keine veraenderte Paramdex-XML (A-029/A-030, NOTICE);
Alias ist Lookup, keine Kopie.
