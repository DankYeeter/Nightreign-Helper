# T-156 - Das Mutationsregister fertig schreiben (developer)

**Ziel:** Die 85 noch nicht registrierten Mutationen kommen ins Register. Die
Vorarbeit ist getan — **bau sie nicht neu.**

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`.
Es gelten die Projektzeilen aus `CLAUDE.md`. Abweichung: **keine**.

**Scratchpad:** `T-156/` fuer Eigenes. **Lies aber zuerst `T-155/`** — dort
liegt die fertige Infrastruktur.

## Umfang

**Stufe:** klein — die Arbeit ist mechanisch und vorbereitet.
**Beruehrt Dateien:** `scripts/differential/mutate.py`. **Sonst nichts.**
*(`Beruehrt Dateien` bindet ab heute als Whitelist. Brauchst du eine Datei
ausserhalb: **melden und anhalten**, nicht ergaenzen. Director-Entscheidung
09.09.2026 nach T-153.)*

## Kontext

**Quittung aus T-155 (`developer`, `STATUS: teilweise`, Commit `dd2dea9`):**
Aufgabe 2 ist fertig — fuenf Skripte bauten ein Fenster ohne Bestand und haben
jetzt einen ausdruecklichen Wait. Er hat das nicht behauptet, sondern belegt
(`demo_owned.py`: `owned=None` sofort nach Konstruktion, `309 relics` erst
nach dem Warten) und ehrlich dazugesagt, dass die Race auf dieser Maschine
**nicht deterministisch reproduziert** — strukturell vorhanden, im Messwert
unveraendert.

**Aufgabe 1 ist vorbereitet, aber nicht geschrieben.** Sein Lauf endete an der
**Zugschwelle** (150 Werkzeugaufrufe). `scripts/differential/mutate.py` ist
**unveraendert**.

**Seine Zaehlung — benutz sie, sie ist belegt:**

> **95 Mutationen** in den sechs Berichten gezaehlt. Davon sind **8 (T-137)
> bereits registriert** und **2 (T-151 FASTa/FASTb) Kontrollen bereits
> vorhandener Eintraege** — bleiben **85 wirklich neue**. Fuer 35 davon
> (T-142+T-147) lief eine Nachweiskampagne; **10 von 13 T-142-Mutationen
> bestaetigt toetend**, bevor die Zugschwelle griff.

**Die Infrastruktur liegt fertig im Scratchpad `T-155/`:** `catalog.py` (alle
95 Eintraege), `means.py`, `write_registry.py`, `run_campaign.py`.
**Lies sie und benutz sie. Bau nichts davon neu** — der Director hat den
Hintergrundprozess der abgebrochenen Kampagne beendet, die Dateien sind
unversehrt.

**Stand des Registers (T-152, gezaehlt):** **230 Eintraege**, Anker-Waechter
gruen fuer alle 230.

## Wie du dich gegen die Zugschwelle absicherst

**Committe nach jedem Auftragsblock**, nicht am Ende. Reihenfolge nach
Herkunft: T-142 → T-147 → T-149 → T-150 → T-151. **Reisst der Lauf ab, ist
alles bis zum letzten Commit gerettet** — genau daran ist T-155 gescheitert,
und das ist kein Vorwurf an ihn, sondern die Lehre daraus.

**Erreichst du die Schwelle**, meldest du `STATUS: teilweise` mit einer
**Liste, welche Bloecke drin sind und welche fehlen**. Der Rest wird ein
neuer Auftrag. **Kein Hintergrundlauf, auf dessen Ende du wartest** —
`~/.claude/agents/_rahmen.md:49`, und in dieser Nacht schon zweimal gerissen.

## Vorgaben

- **Nachweis je Eintrag:** `mutate.py --apply` und die Zahl der roten Faelle.
  T-155 hat 10 von 13 aus T-142 bereits bestaetigt — **uebernimm seine
  Ergebnisse, statt sie neu zu fahren**, und sag, welche du uebernommen und
  welche du selbst gefahren hast.
- **Traegt eine Mutation nicht mehr: Befund. Melden, nicht passend machen.**
- **Ueberlebende werden mit ihrer Begruendung eingetragen, nicht weggelassen.**
  T-136, T-147 und T-149 haben welche gemeldet (Verhaltensgleichheit,
  "zwei Guertel, keiner allein"). Ohne Eintrag untersucht sie beim naechsten
  Mal jemand von vorn.
- **Der Anker-Waechter muss am Ende gruen sein** — er prueft, dass jeder
  Eintrag seine Stelle im echten Quelltext genau einmal findet.
- **Keine Zeitschranke in die Suite.**

## Abnahmekriterium aus GOAL.md (woertlich)

> **A1** Ein schriftlicher Audit-Bericht liegt vor, der Korrektheit,
> Sicherheit, Architektur und Bedienbarkeit abdeckt, mit priorisierten
> Befunden. Jeder Befund hat Status (offen / behoben / zurueckgestellt).

**Stand laut docs/state.md** (woertlicher Auszug, autonomer Lauf):

> 1. Zyklus 18 schliessen — U8 (AD-029 Stufe B), die zwei fehlenden
>    U7-Messungen, Pruefphase.

## Testumgebung

Windows 10/11 x64, Branch `docs/audit-and-advisor-design`. Testbefehl nach
`CLAUDE.md`. **Suitezahl gegen 1595 passed, 9 skipped, 0 failed** (T-153).
Deine Eintraege erzeugen neue Faelle, ohne dass du `tests/` anfasst — das
Register wird gelesen.

Alle drei Datenverzeichnisse umlenken und nachweisen, sonst abbrechen und nur
das melden. Spielstand read-only.

## Scope-Grenzen

**Nicht anfassen:** `nrplanner/`, `nrdata/`, `tests/`, `scripts/` ausser
`differential/mutate.py`, `UI_SPEC.md`, `ARCHITECTURE.md`, `GOAL.md`,
`docs/state.md`, `qa/findings.md`, `docs/tasks/`.

**Parallel laeuft T-157** (`developer`) — **in `nrplanner/inventory.py`,
`nrplanner/app.py` und `tests/`**. Disjunkt zu dir.
**Beruehrt Dateien:** `scripts/differential/mutate.py`.

## Rueckgabeformat

Uebergabe-Kontrakt-Block plus Abschlussbericht. **Der Auftrag gilt erst als
erledigt, wenn `docs/berichte/T-156-developer.md` auf der Platte liegt.**
Commit mit Pfad hinter `--`.
