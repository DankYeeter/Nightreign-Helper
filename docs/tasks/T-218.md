# T-218 — Vier Flaechenbefunde: QA-210, QA-232, SEC-039, QA-242 (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen
(Testbefehl, Datenverzeichnisse, verbotene Zugriffe, Zielsystem) stehen in
der `CLAUDE.md` des Projekts; Abweichungen fuer diesen Auftrag: **Schritt 0
unten (Worktree-Stand)**; Abschnitt b darf mehr als fuenf Dateien beruehren
(mechanisch, ein Muster, 21 `raise`-Zeilen in 8 Dateien).

**Ziel:** Drei A7-Brueche und ein Messfehler auf der Flaeche sind behoben:
der Picker sagt "kein Spielstand gelesen" statt "keine Zahlen in den
Spieldaten"; die 24 englischen Verweigerungen des Extraktionspfades erreichen
den Nutzer wieder woertlich; `shortcut.create` gibt kein PowerShell-`stderr`
auf die Flaeche; `RelicPicker.wanted_height()` und die tatsaechliche
Dialoghoehe stimmen ueberein, oder die Ursache der 90 px ist benannt.

**Stufe/Modus:** developer, normal

**Abnahmekriterium (GOAL.md, woertlich):** A7 — die Oberflaeche nennt die
Datenlage; A8 — "Oberflaeche des Programms: Englisch, ohne Ausnahme"
(`CLAUDE.md`); A2 — kritisch/hoch behoben.

**Stand (docs/state.md, 2026-09-12, woertlich):** "Eine Bauwelle: QA-242 (90
px, Ursache unbekannt — erster Anlauf, beim zweiten der `fehlerdiagnostiker`),
QA-210, QA-232, [...] SEC-039".

## Kontext

Vier Register-Zeilen, Wortlaut dort: `qa/findings.md:232` (QA-210), `:254`
(QA-232), `:265` (QA-242), `security/findings.md:57` (SEC-039). Quittungen:
T-124 (`docs/berichte/T-124-ui-ux-designer.md:154`, Ersatzwortlaut QA-210),
T-193 (`docs/berichte/T-193-developer.md:244-262`, B-2 mit der Liste der 10
Dateien und 24 Zeilen), T-207 (`docs/berichte/T-207-ui-ux-designer.md:63-77`,
Bildnachweise `design-review/2026-09-12/relicpicker-forced-wanted-height-1151.png`
und `-natural-height-1061.png`).

## Vorgaben

### T-218a — QA-210 (`relicpicker.py`, `advisorbar.py`)

- `advisorbar.asking_from` gibt fuer "kein Save" `None`; `SlotAdvice.ranking`
  reicht es durch; `_say_what_they_are_worth` (`relicpicker.py:1498`) bildet
  jedes `None` auf `NO_FIGURES_AT_ALL` (`:141`) ab. Erst klaeren: ist der
  Zustand "Karten auf dem Schirm, kein Save" erreichbar (`available_items()`
  leer?). Nicht erreichbar → Befund im Bericht, kein Umbau, QA-210 als
  "geschlossen — unerreichbar, Beleg" vorschlagen.
- Erreichbar → getrennter Satz, Wortlaut aus T-124: `No save was read, so
  there is nothing to rank these against — use Rescan save.` (Gedankenstrich
  bleibt, ist Teil des Spec-Satzes). Ein Test, der beide Saetze
  auseinanderhaelt.

### T-218b — QA-232 (`nrdata/*`, ohne `dcx.py` und `tpf.py`)

- `errortext.in_english` reicht nur Klassen aus `OUR_OWN_PACKAGES` durch
  (`errortext.py:93-112`). Die Klasse dafuer gibt es: `nrdata.binary.
  NotWhatItClaims(ValueError)` (`binary.py:8-28`, Docstring sagt, wofuer sie
  ist und wofuer nicht). Die `raise ValueError(...)`-Zeilen in `dds.py` (5),
  `dvdbnd.py` (5), `fmg.py` (3), `extract.py` (2), `oodle.py` (2),
  `paramdef.py` (2), `bnd4.py` (1), `param.py` (1) werden `NotWhatItClaims`,
  **wo sie eine Aussage ueber die Datei sind**; ein Programmfehler (falsches
  Argument) bleibt `ValueError` — je Zeile im Bericht benennen. `dcx.py` (2)
  und `tpf.py` (1) macht **T-219** parallel, nicht du.
- `firstrun.py:625-632` bleibt, wie es ist — der Weg ist dann richtig.
- Ein Test: eine dieser Verweigerungen kommt woertlich durch
  `in_english` (SEC-023-Waechter bleibt gruen).

### T-218c — SEC-039 (`nrplanner/shortcut.py:136-140`)

- `detail[0]` aus `stderr`/`stdout` faellt weg. `returncode != 0` oder Datei
  fehlt → ein Satz dieses Repositorys ("the shortcut could not be written"
  gibt es schon). `stderr` auf die Konsole (`print(..., file=sys.stderr)`
  oder Logging, was das Modul schon nutzt). Waechter: ein Test mit
  gestubbtem `subprocess.run`, dessen `stderr` einen Pfad und einen
  nicht-englischen Text traegt — beides darf nicht im Rueckgabewert stehen.

### T-218d — QA-242 (`relicpicker.py:1170-1192`, `_fit_to_three_rows`)

- **Erster Anlauf.** `wanted_height()` 1151 px, ungezwungene Dialoghoehe
  1061 px, Bildschirm 4096x1728, kein Klemmen (Director, 12.09.2026). Finde,
  woher die 90 px kommen — Kandidaten, die du **messen**, nicht raten sollst:
  Layout-Margins/Spacing doppelt gezaehlt, Scrollbar-Hoehe, Kopf-/Fussbereich
  vor `show()` noch ohne Groesse, `sizeHint` vs. `minimumSizeHint`,
  Fensterrahmen (`frameGeometry` vs. `geometry`). Messwerkzeug:
  `scripts/measure_picker_cards.py` und die Methode aus T-207.
- Ursache gefunden → Fix, sodass beide Zahlen im selben Lauf gleich sind
  (Toleranz 0), Test der beides misst. AK-196 (drei sichtbare Zeilen) muss
  weiter halten.
- Ursache nicht gefunden → `STATUS: teilweise` fuer diesen Abschnitt, im
  Bericht: was gemessen wurde, welche Kandidaten ausgeschlossen sind. **Kein
  zweiter Rateversuch** — der zweite Anlauf geht an den
  `fehlerdiagnostiker`.

## Testumgebung

Windows 11, `pytest <datei>` gezielt, am Ende `pytest -n auto`. Datenumlenkung
nach `CLAUDE.md`, `NIGHTREIGN_SETTINGS_ORG=DankYeeterT-218`, Testabzug in das
umgelenkte `LOCALAPPDATA` **kopieren**. Programmfenster fuer d per
`PrintWindow`, kein Bildschirmabzug.

## Umfang

**Beruehrt Dateien:** `nrplanner/relicpicker.py`, `nrplanner/advisorbar.py`,
`nrplanner/shortcut.py`, `nrdata/*.py` (nur `raise`-Zeilen der Liste in b, **nicht** `dcx.py`/`tpf.py`),
`nrplanner/firstrun.py` nur `:625-632` und nur, wenn noetig; Tests dazu. ·
**Parallel:** ja — T-217 (`nrdata/gamefiles.py`, `nrdata/oodle.py` ausser
den zwei `raise`-Zeilen, `tests/test_game_dir_recognition.py`), T-219
(`nrdata/dcx.py`, `nrdata/tpf.py` ganz), T-220 (`ARCHITECTURE.md`). ·
**Scratchpad:** `<scratchpad>/T-218/`

**Schritt 0 (Worktree):** `git log -1 --oneline` muss `41206be` zeigen, Branch
`docs/audit-and-advisor-design`. Zeigt er etwas anderes: sofort `STATUS:
blockiert`, `BLOCKIERT DURCH: Worktree-Stand <sha>`, nichts aendern.

Reihenfolge a → b → c → d, ein Commit je Abschnitt auf dem ausgecheckten
Branch, kein Push. Laeuft d ueber 60 Minuten: abbrechen, `teilweise`.

## Scope-Grenzen

- Kein Umbau an `_fit_to_three_rows` ueber den Fix der Ursache hinaus; keine
  Aenderung an Kartenhoehe oder Spaltenzahl (AK-196, AK-51).
- `scripts/setup_check.py` (B-3 in T-193) bleibt unangetastet.
- Keine neuen Texte ausser den genannten; kein Wort auf der Flaeche, das
  nicht in diesem Auftrag oder in T-124 steht.

## Praemissen — jede Bestandsbehauptung mit Quelle

- `_say_what_they_are_worth` in `relicpicker.py:1498`, `NO_FIGURES_AT_ALL`
  `:141`, `_fit_to_three_rows` `:1170` · `grep -n` · 13.09.2026
- `asking_from` in `advisorbar.py:317` · `grep -n` · 13.09.2026
- `shortcut.create` `:88`, `detail[0]` `:138-140` · `sed -n 136,141p` ·
  13.09.2026
- `firstrun.py:625-632` faengt `Exception` und ruft `errortext.in_english` ·
  `sed -n 625,632p` · 13.09.2026
- 24 Verweigerungen in 10 Dateien · `docs/berichte/T-193-developer.md:244-252`,
  gezaehlt 12.09.2026; `grep -c "raise ValueError" nrdata/*.py` gibt heute 25
  Treffer ueber alle Dateien · 13.09.2026 — **nachzaehlen**
- `NotWhatItClaims` in `nrdata/binary.py:8` · `grep -n "^class" nrdata/*.py` ·
  13.09.2026
- 1151/1061 px · `qa/findings.md:265`, T-207, 12.09.2026 — dein Lauf misst neu
- Suite 1789 passed, 9 skipped · `docs/state.md`, 12.09.2026

Widerspricht der Bestand einer Aussage dieses Auftrags, ist das ein Befund:
melden und auf dem Bestand arbeiten. Eine tragende Praemisse ohne Quelle darf
die Rolle zurueckweisen.

## Rueckgabe

Antwort: Kontraktblock plus hoechstens dreissig Zeilen: je Abschnitt Status,
Satz woertlich, Test, Zahlen (b: Zeilen gezaehlt; d: beide Hoehen). Datei
`docs/berichte/T-218-developer.md` nur bei Befunden oder `STATUS:
teilweise/blockiert` — d ohne Ursache ist ein solcher Fall.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
