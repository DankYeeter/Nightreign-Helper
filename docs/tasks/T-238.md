# T-238 — Ponytail-Audit Zyklus 22: sieben Schnitte plus ein Waechter (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen in der
`CLAUDE.md` des Projekts; Abweichungen fuer diesen Auftrag: keine.

**Ziel:** Die Funde des Ponytail-Audits auf `79de9ba` (delete/native/yagni)
sind umgesetzt; QA-128 Punkt 10 hat einen Waechter.

**Stufe/Modus:** normal (sechs Dateien, jeder Schnitt mechanisch)

**Abnahmekriterium:** keines aus GOAL.md — Beta-Gate-Regel (`director.md`,
Pruefung): Audit-Funde mit `delete`, `stdlib`, `native`, `yagni` werden ein
Auftrag; `shrink` nicht.

**Stand (docs/state.md, 14.09.2026, woertlich):** "Bauwelle abgeschlossen
`79de9ba`, Suite 1527 passed / 9 skipped (Director 07:12)."

## Vorgaben — je Fund eine Zeile, Audit 14.09. (Explore auf `79de9ba`)

1. delete `SaveReader.generation` (`@property`, kein Leser) — `savereader.py:239`.
2. delete `_refuse_a_file_no_save_can_be` (Ein-Zeilen-Wrapper) — in
   `read_the_save` inlinen als `inventory.refuse_a_size_no_save_can_have(
   save_path.stat().st_size)`; `tests/test_exception_text_is_english.py:396`
   patcht dann `inventory.refuse_a_size_no_save_can_have` — `savereader.py:92`.
3. delete `WHITE_SLOT = 4` in `app.py:41` — Leser `tests/conftest.py:467`,
   `tests/test_relic_restore.py:428/431` auf `model.WHITE_SLOT`.
4. native 12x `blockSignals(True)/try/finally blockSignals(False)` (5 in
   `relicslots.py` ab :368, 7 in `app.py` ab :1483) → `with QSignalBlocker(x):`
   (`PySide6.QtCore`).
5. yagni `_select_index` gibt immer `True` (`relicslots.py:874`) — `None`
   zurueckgeben, `select_copy`/`select_roll` geben `True` selbst.
6. delete unbenutzter Import `app as appmod` —
   `tests/test_exception_text_is_english.py:44`.
7. **Waechter** QA-128 Punkt 10: ein Test in `tests/test_world_events_display.py`,
   der rot wird, wenn der Satz zu `stamina recovery speed`
   (`eventstab.py:37 UNKNOWN_REFERENCE`, "files do not say what it is counted
   in") verschwindet — heute `grep "stamina recovery" tests/*.py` leer.

Nicht bauen (shrink, nur melden falls trivial mitgeht): `_heading`-Spaetimport
in `advisorbar.py:560`/`advisorblock.py:173` direkt aus `.statsheet`.

Kein Verhaltenswechsel, kein Text. Schnitt 4 ist Verhaltensgleichheit:
`QSignalBlocker` stellt den vorherigen Zustand wieder her, `blockSignals(False)`
setzt hart auf False — wo vorher verschachtelt geblockt wurde, pruefen.

## Testumgebung

`CLAUDE.md`; `pytest -n auto` am Ende, `python -m pyflakes nrplanner tests
scripts` leer.

## Umfang

**Beruehrt Dateien:** `nrplanner/savereader.py`, `nrplanner/app.py`,
`nrplanner/relicslots.py`, `tests/test_exception_text_is_english.py`,
`tests/conftest.py`, `tests/test_relic_restore.py`,
`tests/test_world_events_display.py` · **Parallel:** nein — niemand aendert
den Baum, Director committet nicht · **Scratchpad:** `<scratchpad>/T-238/`

Arbeit direkt auf `docs/audit-and-advisor-design`, ein Commit.

## Scope-Grenzen

Nichts ausserhalb der sieben Zeilen. Farbkonstanten in sechs Modulen sind
bekannte Debt (architect), nicht anfassen.

## Praemissen

- Suite auf `79de9ba`: 1527 passed / 9 skipped · Director · 14.09.2026 07:12
- Zeilennummern vom Audit auf `79de9ba` · Explore · 14.09.2026

## Rueckgabe

Kontraktblock + max. 20 Zeilen: Commit, Suitezahl, Zeilen-Delta (`git diff
--shortstat 79de9ba`). Berichtsdatei nur bei Befunden oder `teilweise`.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
