# T-331 — P-003 Lesegang Ruestungs-Negation (developer, nur lesend)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`; Projektfakten
`CLAUDE.md`. **Nutzerentscheid 23.09.2026 16:46 (Fragebogen):** "Read the
candidate table" — ein lesender Gang, keine Programmaenderung. Hook-Nebenfund
(`$istExeKommando`) gesammelt, nicht Teil dieses Auftrags.

## T-331a

**Stufe:** normal. Hauptbaum `docs/audit-and-advisor-design`, kein Worktree.

**Ziel:** Frage aus `docs/research/R-011.md` "Nicht beantwortet", Punkt 1
entscheiden: Tragen die Nightfarer-Startzeilen in `CharaInitParam`
(`nrdata/extract.py:2092-2102` liest die Tabelle schon) in
`equip_Helm/equip_Armer/equip_Gaunt/equip_Leg` Ruestungs-IDs, und haben diese
Zeilen in `EquipParamProtector` `*DamageCutRate`-Werte ungleich 1?

**Kriterium:** Tabelle je Nightfarer (Name, CharaInitParam-Zeile, vier
Ruestungs-IDs, acht `*DamageCutRate`-Werte je Stueck, Produkt je Schadensart)
oder begruendetes "nicht vorhanden". Zusatz, falls ohne Umweg lesbar: ein Feld
fuer *Defense* je Nightfarer (R-011 Punkt 2) — sonst nur "nicht gefunden".

**Vorgaben:**
- Nur lesen: Spielinstallation und `regulation.bin` read-only, ueber die
  vorhandenen Leser in `nrdata/` (`param.py`, `extract.py`). Kein Schreiben in
  Spielstand oder Installation.
- **Keine Code-Aenderung, kein Commit**, keine Spieldaten ins Repo. Hilfsskript
  nur im Scratchpad `…/scratchpad/T-331/`.
- Umlenkung laut `CLAUDE.md` (`NIGHTREIGN_SETTINGS_ORG=DankYeeterT-331`,
  `LOCALAPPDATA`/`APPDATA` auf Testverzeichnis), falls der Hook sie verlangt.
- Parallel: nichts. Director committet in der Zeit nicht.

**Rueckgabe:** `docs/berichte/T-331-developer.md` mit der Tabelle, den
Feldnamen und dem Lesegang in drei Zeilen; Kontraktblock in der Antwort.
