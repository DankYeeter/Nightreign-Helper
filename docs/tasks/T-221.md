# T-221 — Spec-Nachtrag nach der Bauwelle T-217/T-218 (ui-ux-designer, Spec)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen stehen
in der `CLAUDE.md` des Projekts; Abweichungen fuer diesen Auftrag: keine.
Kein Anwendungscode, keine Tests; das Programm darfst du mit Datenumlenkung
starten, musst es aber fuer 1-3 nicht.

**Ziel:** `UI_SPEC.md` sagt wieder, was das Programm nach `e465c17` tut, und
entscheidet zwei offene Designfragen (QA-242 A/B, QA-247).

**Stufe/Modus:** Spec

**Abnahmekriterium (GOAL.md, woertlich):** A7 — die Oberflaeche nennt die
Datenlage; A8 — englisch ohne Ausnahme; A2 — kritisch/hoch behoben.

**Stand (docs/state.md, 13.09.2026, woertlich):** "T-217 [...] Befunde:
E1-Headline passt nicht zum Herkunftssatz → Spec-Nachtrag" · "QA-242 Ursache
gemessen, A/B an `ui-ux-designer`; Nebenfund QA-247".

## Kontext

Quittungen: `docs/berichte/T-217-developer.md` (Ablehnungssatz, Befund 3),
`docs/berichte/T-218-developer.md` (a: QA-210 Satz, d: QA-242 Zeilen 118-188
mit Optionen A/B), `qa/findings.md` letzte vier Zeilen (QA-210, QA-232,
QA-242, QA-247), `security/findings.md` Zeile SEC-026 vom 13.09.

## Vorgaben — vier Punkte

1. **E1 / AK-230 / A28 (SEC-026, Nutzerentscheidung 13.09.: Herkunft):**
   `nrplanner/firstrun.py:334` E1 traegt jetzt als dritte Zeile, wenn der
   gezeigte Ordner ausserhalb jeder Steam-Bibliothek liegt: `That folder is
   not inside a Steam library, so Nightreign Helper will not run the game's
   files from there. Pick the folder Steam installed the game in: in Steam
   that is Manage, then Browse local files.` Die Headline `That folder does
   not hold a copy of the game.` steht darueber und ist bei einer echten
   Kopie ausserhalb Steams falsch (T-217 Befund 3). Spec nachziehen: Satz
   aufnehmen, Headline-Wortlaut fuer den Herkunftsfall festlegen (ein
   Vorschlag, englisch, A7). Nicht bauen — der Wortlaut geht als AK an den
   `developer`.
2. **§3.7 / AK-49 (QA-210):** `NO_FIGURES_AT_ALL` (`The game's data carries
   no figures …`) ist ersetzt durch `NO_SAVE_WAS_READ` = `No save was read, so
   there is nothing to rank these against — use Rescan save.`
   (`relicpicker.py`, T-124-Wortlaut). AK-49 und Z. 1854 nachziehen; der
   Fehlschlag-Satz bleibt getrennt (T-218a).
3. **QA-242, Entscheidung A oder B** (Bericht T-218 Z. 150-188): A =
   `wanted_height` ist die Zahl der Groessenwahl, einmal festgehalten; B =
   `_chrome_height` korrekt plus bewusste Reserve fuer Zeile 3b, dann A.
   Entscheide, trage es unter AK-216 als Nachtrag ein, mit dem Mass, das der
   `developer` bauen soll (bei B: die Reserve in px oder Zeilen). AK-51 und
   AK-196 muessen unter deiner Wahl weiter halten — sag, welches.
4. **QA-247:** Karten behalten nach einem Rescan ohne Fund den alten Bestand,
   die Kopfzeile sagt "No save was read". Absicht (letzter bekannter Bestand)
   oder A7-Bruch? Entscheide, AK dazu.

Nicht in diesem Auftrag: die `app.py:`-Zeilenzitate (T-220 B-5) — die
wandern erst mit AD-034.

## Testumgebung

Lesend; bei Programmstart Datenumlenkung nach `CLAUDE.md`,
`NIGHTREIGN_SETTINGS_ORG=DankYeeterT-221`.

## Umfang

**Beruehrt Dateien:** `UI_SPEC.md`, `UI_SPEC_REGISTER.md`. · **Parallel:**
ja — T-222 (`architect`, `ARCHITECTURE.md`), T-223 (`developer`, Worktree,
`scripts/differential/mutate.py`). · **Scratchpad:** `<scratchpad>/T-221/`

Ein Nachtrag A30 mit vier Punkten, AK-Nummern ab **AK-264** (Register
nachzaehlen). Kein Commit — der Director committet.

## Scope-Grenzen

Kein anderer Abschnitt der Spec; keine neuen Elemente ausser den vier
Punkten; kein Wort auf der Flaeche ausser Englisch.

## Praemissen — jede Bestandsbehauptung mit Quelle

- E1 Headline in `nrplanner/firstrun.py:334` · `grep -n "does not hold"` ·
  13.09.2026
- Ablehnungssatz woertlich · `docs/berichte/T-217-developer.md`, Abschnitt
  "Ablehnungssatz" · 13.09.2026
- `NO_SAVE_WAS_READ` in `nrplanner/relicpicker.py` · Commit `3798c57` ·
  13.09.2026
- QA-242-Messung 316/406/1061/1151 · `docs/berichte/T-218-developer.md:118-188`
  · 13.09.2026
- AK-49 `UI_SPEC.md:1538`, AK-216 `:1963`, AK-230 `:368-373` · `grep -n` ·
  13.09.2026
- Register: 264 AK-Zeilen · `grep -c "^| AK-" UI_SPEC_REGISTER.md` · 13.09.2026

Widerspricht der Bestand einer Aussage dieses Auftrags, ist das ein Befund:
melden und auf dem Bestand arbeiten. Eine tragende Praemisse ohne Quelle darf
die Rolle zurueckweisen.

## Rueckgabe

Antwort: Kontraktblock plus hoechstens dreissig Zeilen: je Punkt die
Entscheidung in einem Satz, neue AK-Nummern, der Headline-Vorschlag
woertlich. Datei `docs/berichte/T-221-ui-ux-designer.md` bei Befunden oder
`STATUS: teilweise/blockiert`.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
