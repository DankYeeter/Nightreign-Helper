# T-313 — QA-288 Beute-Umschalter, Neubau, Retest, Release 1.15.0 (developer, release-manager, qa-engineer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen in der
`CLAUDE.md` (Umlenkung je Rolle `DankYeeterT-313<a|b|c|d>`; Testabzug 841
Dateien, 21 131 645 B, Version 15, kopieren; NH-004 ein Fenster je
Zeitpunkt). Stand `756e6f7`. Nutzerentscheidung 19.09. 16:35: QA-288 fixen,
dann Release; OF-49 (1) bestaetigt.

## T-313a — developer (klein)

QA-288: der `QToolButton` "Show N more" (AK-324.3, `nrplanner/bosstab.py`)
reagiert auf `TogglePattern.Toggle()` nicht, nur auf echten Mausklick.
Ursache pruefen (Signal `clicked` vs. `toggled`, `setCheckable`), so
verdrahten, dass Toggle per UIA, Leertaste im Fokus und Mausklick denselben
Weg gehen. Test in `tests/test_nightlord_panel_display.py`: `toggle()` am
Knopf klappt auf/zu; Tab-Reihenfolge erreicht den Knopf. Kein Fensterlauf.
Commit `fix(bosstab): …`, nur `bosstab.py` + Test.

## T-313b — release-manager (build), Version bleibt 1.15.0

Wie T-312a; Bytes, SHA-256, Bericht `docs/berichte/T-313-release-manager.md`.
Rauchtest sofort beenden, `Get-Process` = 0.

## T-313c — qa-engineer (Retest, sonnet, nach b)

Nur QA-288 am neuen Artefakt (UIA-Toggle, Leertaste, Maus) plus ein
Stichprobenblick Fell Omen/Runebear unveraendert. Nummern ab QA-289.
Programm beenden, `Get-Process` = 0. Bericht `docs/berichte/T-313-qa-engineer.md`.

## T-313d — release-manager (notes) 1.15.0, nach c gruen

`CHANGELOG.md`, `docs/release/RELEASE_BODY.md` (Englisch): Unterbosse im
Nightlords-Tab (Feldbosse, Nachtbosse Tag 1/2 je Nachtfuerst, HP, Beute),
Red-variants-Tab ohne Beispielspalte (QA-286), Datenversion 15 (Cache wird
einmal neu gebaut, ~35 s plus Symbole). Kein Tag, kein Push — Freigabe
erteilt der director nach Vorlage.

## Umfang

**Parallel:** a allein, dann b, dann c, dann d · **Scratchpad:**
`<Scratchpad>/T-313/<rolle>/`

## Rueckgabe je Rolle

Kontraktblock plus hoechstens acht Zeilen.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
