# T-307 — A24 Forschung: Namen der zwei namenlosen Feldbosse c3252 und c4021 (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen in der
`CLAUDE.md` (Umlenkung Pflicht: `NIGHTREIGN_SETTINGS_ORG=DankYeeterT-307`,
eigenes `LOCALAPPDATA`/`APPDATA`; kein Fensterlauf). Stand `afa4bbe`.
Nutzerentscheidung 19.09. 14:50: kurzer Forschungsauftrag.

**Befund (T-305):** Karte `m46_54` Figur c3252 (HP 2148) und Karte
`m46_88` Figur c4021 (HP 2279) haben in NpcName weder einen
`90<chr><variant>`-Eintrag noch eine `nameId` an ihren NpcParam-Zeilen.
Nachbarn: c3250 Draconic Tree Sentinel, c3251 Tree Sentinel; c4020 in
Elden Ring der Black Knife Assassin.

**Frage:** Woher nimmt das Spiel den Namen am Lebensbalken dieser beiden?
Zu pruefen, in dieser Reihenfolge, jeweils mit Fundstelle: (1) alle
NpcParam-Zeilen der Figur (nicht nur die des ersten Parts) und deren
`nameId`; (2) die MSB-Part-Zeile (`PARTS_PARAM_ST`) der Karte — traegt sie
eine Varianten- oder Namens-Id? (3) andere FMG-Tabellen (NpcName-Varianten,
TalkMsg, MenuText, `EventTextForMap`/`EventTextForTalk`) nach Eintraegen
mit `3252`/`4021` im Schluessel; (4) SpEffect-/Buff-Zeilen oder
`NpcThinkParam` mit einem beschreibenden Namen; (5) wie es die zehn
benannten Sentinel-/Assassin-Verwandten machen (Vergleich der Kette).
Nichts aus Spielwissen ergaenzen.

**Ergebnis:** Bericht `docs/berichte/T-307-developer.md`: je Figur
"Name belegt: <Name>, Quelle" oder "nicht in den Dateien". Bei Fund die
Aenderung an `nrdata/bossdata.py`/`extract.py` als Vorschlag mit Diff-Skizze,
**nicht** umsetzen (T-306 laeuft im Baum).

**Stufe/Modus:** developer: klein, Skripte nur im Scratchpad, kein Commit.

## Umfang

**Beruehrt:** `docs/berichte/T-307-developer.md` · **Parallel:** ja (T-306
in `nrplanner/`) · **Scratchpad:** `<Scratchpad>/T-307/`

## Praemissen — jede Bestandsbehauptung mit Quelle

- Kein `90…`-Eintrag, keine `nameId` · T-305 Rueckgabe · an der
  Installation gemessen.
- HP 2148 / 2279 · T-299 Abschn. 2c · gemessen.

## Rueckgabe

Kontraktblock plus hoechstens acht Zeilen.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
