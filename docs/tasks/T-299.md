# T-299 — A24 Forschung: Register der Unterbosse am frischen Abzug (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen stehen
in der `CLAUDE.md` des Projekts. Stand `c03fd9d` plus ungecommitteter
Nachtrag A24 in `GOAL.md` (19.09. 12:36). Nutzerentscheidung 19.09. 12:28:
beide Stufen, Forschung zuerst.

**Ziel:** Belegen, ob und woher die Unterbosse (Nachtbosse Tag 1/2,
Feldbosse, Evergaol-Bosse) mit Kampfwerten, Beute und Nachtfuerst-Zuordnung
zu lesen sind. Kein Anwendungscode, kein Tab; Ergebnis ist ein Bericht mit
Param-, Zeilen- und Feldnamen, der den Architekten fuer Stufe 1 traegt.

**Stufe/Modus:** developer: mittel, ein Lauf, Skripte nur im Scratchpad.

## Fragen, in dieser Reihenfolge

1. **Member-Liste.** Welche Param-Tabellen enthaelt `regulation.bin`
   wirklich (nicht Paramdex)? Vollstaendige Liste in den Bericht, markiert:
   heute gelesen / ungelesen / ohne Paramdef.
2. **Roster Feld- und Evergaol-Bosse.** Kategorien 101, 104, 110, 135-138
   (Feld) und 160 (Evergaol) aus `ChaosMatchingMutationEnemyTableParam`:
   je Eintrag chr-Id, NpcParam-Zeile(n), Spielname (FMG). Loest
   `nrdata/bossdata.py` fuer diese chr-Ids HP, Stance, Schadens- und
   Statusfelder auf, ohne den EMEVD-Weg ueber `defeatEventFlag`? Wenn nein,
   was fehlt genau.
3. **Nachtbosse.** Ist Kategorie 120 der Nachtboss-Cast (Inhalt gegen die
   bekannten Nachtbosse halten: Fell Omen, Centipede Demon, Tree Sentinel,
   Draconic Tree Sentinel, Wormface, Ancient Hero of Zamor, Gaping Dragon,
   Nameless King, Smelter Demon u. a.)? Falls unvollstaendig: welche der
   ungelesenen Tabellen (`SmallBaseAndSpot*`, `PlayAreaCreate*`,
   `ScenarioPlacementParam`, `RandomAppearParam`, `NPCBotTableParam`)
   tragen chr-Ids oder NpcParam-Zeilen je Platz oder Tag?
4. **Zuordnung Boss → Nachtfuerst und Tag.** Lassen sich die
   `modifier`-Ids aus `LotResultMapPatternFlag` (heute unbenannt,
   `nrdata/extract.py:1408-1416`) ueber eine der Tabellen aus Frage 3 auf
   Bossplaetze abbilden? Belegt, teilweise, oder nicht ableitbar — mit
   Beispielzeile.
5. **Beute.** Greift `_event_drops` (`nrdata/extract.py:1144`) fuer die
   chr-Ids aus Frage 2 und 3 ohne Aenderung? Stichprobe: zwei Evergaol-,
   zwei Feld-, zwei Nachtbosse, jeweils die ItemLot-Kette.

## Umfang

**Beruehrt:** nichts im Repo ausser dem Bericht
`docs/berichte/T-299-developer.md` · **Parallel:** nein (frischer Abzug,
Umlenkung) · **Scratchpad:** `<Scratchpad>/T-299/` (Skripte, Rohtabellen,
frischer Snapshot).

## Scope-Grenzen

Kein Tab, kein Extraktor-Umbau, kein Commit von Spieldaten
(`nightreign_data.json` bleibt, wie sie ist — der frische Snapshot liegt
im Scratchpad). Datenumlenkung nach `CLAUDE.md` (`LOCALAPPDATA`,
`NIGHTREIGN_SETTINGS_ORG=DankYeeterT-299`); Testabzug kopieren, nicht
darauf zeigen. Spielstand und Spielinstallation nur lesen.

## Praemissen — jede Bestandsbehauptung mit Quelle

- NightBossMenuParam traegt nur Menuedaten, 18 Zeilen = 10 Nachtfuersten
  + 8 Everdark · `nrdata/extract.py:317-384`, Explore 19.09. · geprueft.
- Kategorie 160 = vier Arena-Bosse · `nrdata/extract.py:540-542` · geprueft.
- Kategorie 120 = Nachtbosse · `nrplanner/depthstab.py:87` "unconfirmed" ·
  ungeprueft, ist Frage 3.
- `modifier` unbenannt, `targetBoss` = Nachtfuerst · `nrdata/extract.py:983,
  1408-1416` · geprueft.
- Eingecheckter Snapshot aelter als Extraktor (kein `kinds`, `weights` statt
  `counts`) · Explore 19.09. · ungeprueft, am frischen Abzug bestaetigen.

## Rueckgabe

Kontraktblock plus hoechstens zwanzig Zeilen; Details im Bericht.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
