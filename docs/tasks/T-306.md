# T-306 — A24 Schritt 3: Oberflaeche Stufe 1 (AD-041, AK-319..326) (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen in der
`CLAUDE.md` (Umlenkung Pflicht: `NIGHTREIGN_SETTINGS_ORG=DankYeeterT-306`,
eigenes `LOCALAPPDATA`/`APPDATA`; Testabzug **kopieren**; **kein
Fensterlauf** — T-305 baut parallel den Testabzug neu, Tests laufen
offscreen). Stand `b27be19`. Oberflaeche Englisch (A8).

**Ziel:** Die Schrittzeile "3 — Stufe 1, Oberflaeche" aus `ARCHITECTURE.md`
Z. 7236 woertlich, Wortlaute und Verhalten ausschliesslich aus `UI_SPEC.md`
§7.4 (AK-319..324: Baum unter den Kacheln, gefiltert auf den gewaehlten
Nachtfuersten, Gruppen, Leerzustand, `key`-Vergleich, `ambiguous`/
`unresolved`-Saetze, HP-Zeile, Beute: fuenf offen, Rest per Toggle, seltenste
zuerst) und §7.6 (AK-325/326: `PLAYER_GROUPS` neu, Beispielspalte faellt).
Datenquelle: Snapshot-Block `subbosses` (Schema AD-040; `days` heute leer,
Tagesgruppen bleiben dann leer/ausgeblendet nach Spec), Beute aus
`world_events.drops` (keine Kopie). Tests wie in der Schrittzeile.

**Stufe/Modus:** developer: mittel, ein Lauf, Commit (Deutsch,
`feat(bosstab): …`) nur der Dateien unter "Beruehrt".

## Umfang

**Beruehrt:** `nrplanner/bosstab.py`, `nrplanner/depthstab.py`,
`tests/test_nightlord_selection.py`, `tests/test_nightlord_panel_display.py`,
`tests/test_red_variants_display.py` · **Parallel:** ja (T-305 in
`nrdata/`) · **Scratchpad:** `<Scratchpad>/T-306/`

## Scope-Grenzen

Kein `nrdata/`, kein neues Widget-Muster (Toggle nach AK-54/AK-292),
keine Wortlaute aus `ARCHITECTURE.md`. Hinweis: mit dem heutigen Testabzug
sind 11 von 29 Karten `single`, 16 `unresolved` — der Baum muss beides
zeigen; nach T-305 werden es 29 `single`, dein Test darf die Zahl nicht
festnageln. Volle Suite `pytest -n auto` vor dem Commit.

## Praemissen — jede Bestandsbehauptung mit Quelle

- Panel generisch, `grid_outer` VBox · Explore 19.09. / AD-041 · geprueft.
- Snapshot `subbosses` 29 Eintraege · T-303 Commit `3114100` · gemessen.
- Beute je chr in `world_events.drops`, 164 Figuren · T-299 Abschn. 5 ·
  geprueft.

## Rueckgabe

Kontraktblock plus hoechstens fuenfzehn Zeilen.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
