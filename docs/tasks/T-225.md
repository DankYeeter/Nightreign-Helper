# T-225 — Nach den Spec-Nachtraegen bauen: AK-264, AK-266, AK-267, AK-05/AK-194-Waechter, AK-268 (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen
(Testbefehl, Datenverzeichnisse, verbotene Zugriffe, Zielsystem) stehen in
der `CLAUDE.md` des Projekts; Abweichungen fuer diesen Auftrag: Schritt 0
unten (Worktree-Stand); vier Anwendungsdateien plus Tests.

**Ziel:** Die Suite ist wieder gruen, und vier Spec-Entscheidungen vom
13.09. sind gebaut: E1-Headline im Herkunftsfall, `wanted_height`
festgehalten, Picker faellt ohne Spielstand auf den Leerzustand zurueck,
AK-05/AK-194-Waechter messen am laufenden Fenster gegen die abgeleitete
Startbreite, Lesart-Box bei 4.8 deaktiviert (Assertion).

**Stufe/Modus:** developer, normal

**Abnahmekriterium (GOAL.md, woertlich):** A7 — die Oberflaeche nennt die
Datenlage; A6 — Oberflaeche blockiert nicht; A15 — "der gewaehlte Pfad wird
geprueft"; A2 — kritisch/hoch behoben.

**Stand (docs/state.md, 13.09.2026, woertlich):** "Suite im Worktree 1 failed
/ 1509 passed / 9 skipped — der Fehlschlag ist der AK-05-Waechter" ·
"Danach T-225 `developer`: AK-264, AK-266, AK-267, AK-05-Waechter".

## Kontext

Spec-Nachtraege A30 (T-221, `503fbed`) und A31 (T-226, `4017938`):
`UI_SPEC.md` AK-264 (Z. 199), AK-266 (Z. 2084), AK-267 (Z. 1958), AK-268
(Z. 3265), AK-05 geltender Wortlaut ab T-226 (Z. 1066-1080), AK-194 (Z. ~3380,
Schwelle > 0 px, Kurzform `Worst`/`Best` bleibt Rueckfall). Quittungen:
`docs/berichte/T-218-developer.md` (d, QA-242 Messung), T-224 Befunde 3-4.

## Vorgaben — fuenf Abschnitte, je ein Commit

### T-225a — AK-264 (`nrplanner/firstrun.py`, `tests/test_first_run_panel.py`)

E1 traegt im Herkunftsfall (dritte Zeile = Ablehnungssatz aus T-217) die
Headline `That folder is not part of a Steam installation.` statt `That
folder does not hold a copy of the game.`; alle anderen E1-Faelle
unveraendert. Wortlaut aus AK-264, nicht von hier.

### T-225b — AK-266 / QA-242 (`nrplanner/relicpicker.py`, `tests/test_relic_picker_geometry.py`)

Wahl A: `wanted_height()` gibt nach der Groessenwahl die beim ersten
Anstrich gemessene Zahl zurueck (festgehalten), `_chrome_height` bleibt.
Test: `wanted_height()` und `height()` im selben Lauf gleich (Toleranz 0),
AK-196 (drei Zeilen) weiter gruen.

### T-225c — AK-267 / QA-247 (`nrplanner/relicpicker.py` oder `app.py`, `tests/test_relic_picker_advisor.py`)

Nach `planner.owned = None` faellt der Picker auf den Zustand vor jedem je
gelesenen Spielstand zurueck (AK-212), keine alten Karten. Der Test aus
T-218a, der den Pfad belegt, wird zum Waechter (Erwartung umdrehen).

### T-225d — AK-05 / AK-194 (`tests/test_advisor_bar.py`, `tests/test_advisor_apply.py`; `advisorbar.py` nur, wenn der Kurzform-Rueckfall Code braucht)

Waechter messen am **laufenden Fenster** gegen `planner._opening_width()`
(A14, `app.py:2537`), kein `1320`, kein `offscreen`-Skip. AK-05: kein Text
ausser der Statuszeile abgeschnitten, Statuszeile darf schrumpfen, Tooltip
traegt den vollen Text. AK-194: Statuszeile > 0 px; faellt sie auf 0,
greift die Kurzform `Worst`/`Best` (bestehender Pfad aus T-224 — pruefen,
ob er existiert; sonst bauen, klein). **Bezugsbreite fuer AK-194 =
dieselbe abgeleitete Startbreite wie AK-05** (Entscheidung Director
13.09.2026, T-226 hatte sie offen gelassen). Rote Phase: der heutige
AK-05-Waechter ist rot (T-224), der neue muss auf HEAD gruen sein und mit
einer 400-px-Zielbox (monkeypatch `GOAL_BOX_WIDTH`) rot.

### T-225e — AK-268 (`tests/test_advisor_bar.py`)

Bestehenden 4.8-Test (Z. 332-341) um `reading_box.isEnabled() is False`
erweitern; Testname bleibt.

Mutationen: je toetende in `MUTATIONS` eintragen, fahren, Bericht (AD-033
Punkt 4). Messungen am Fenster per `PrintWindow`, kein Bildschirmabzug.

## Testumgebung

Windows 11, `pytest <datei>` gezielt, am Ende `pytest -n auto`. Datenumlenkung
nach `CLAUDE.md`, `NIGHTREIGN_SETTINGS_ORG=DankYeeterT-225`, Testabzug
**kopieren**. Spielstand read-only.

## Umfang

**Beruehrt Dateien:** `nrplanner/firstrun.py`, `nrplanner/relicpicker.py`,
`nrplanner/app.py` (nur c, falls noetig), `nrplanner/advisorbar.py` (nur d,
falls noetig); Tests wie je Abschnitt; `scripts/differential/mutate.py`. ·
**Parallel:** nein — niemand aendert waehrenddessen Code. · **Scratchpad:**
`<scratchpad>/T-225/`

**Schritt 0 (Worktree):** `git branch --contains 4017938` muss den
ausgecheckten Branch nennen. Sonst `STATUS: blockiert`, nichts aendern.

Reihenfolge a → b → c → d → e, ein Commit je Abschnitt, kein Push. Laenger
als 60 Minuten: `teilweise` mit Stand je Abschnitt.

## Scope-Grenzen

- `_chrome_height` nicht umbauen (Wahl A, AK-266).
- Keine Aenderung an `GOAL_BOX_WIDTH`, an Zieltexten (AK-257) oder an der
  Lesart-Logik (AD-035).
- `UI_SPEC.md`/`ARCHITECTURE.md` nicht anfassen; Widerspruch → melden, auf
  AK bauen.

## Praemissen — jede Bestandsbehauptung mit Quelle

- E1 Headline `firstrun.py:334` (Zeile kann sich durch T-217 verschoben
  haben) · `grep -n "does not hold"` · 13.09.2026
- `_opening_width()` `app.py:2537` · `grep -n` · 13.09.2026
- AK-05-Tests mit `resize(1320, ...)` in `tests/test_advisor_bar.py:686-733`,
  `tests/test_advisor_apply.py:536-551` · `grep -n 1320` · 13.09.2026
- Suite auf `9069d83`: 1 failed / 1509 passed / 9 skipped · T-224 · 13.09.2026
  — dein Lauf misst neu
- QA-242 Messung: `wanted_height` 1151, `height` 1061, Chrome 316/406 ·
  `docs/berichte/T-218-developer.md:118-188` · 13.09.2026

Widerspricht der Bestand einer Aussage dieses Auftrags, ist das ein Befund:
melden und auf dem Bestand arbeiten. Eine tragende Praemisse ohne Quelle darf
die Rolle zurueckweisen.

## Rueckgabe

Antwort: Kontraktblock plus hoechstens dreissig Zeilen: je Abschnitt
Status, Test, Rot/Gruen, Zahlen (b: beide Hoehen; d: Statuszeilenbreite am
laufenden Fenster), Mutationen, Suitezahlen, Commit-Shas. Datei
`docs/berichte/T-225-developer.md` bei Befunden oder `STATUS:
teilweise/blockiert`.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
