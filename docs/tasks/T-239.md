# T-239 — Pruefphase Zyklus 22 auf `a68cd3d` (qa-engineer · security-reviewer · ui-ux-designer · architect)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen
(Testbefehl, Datenumlenkung, verbotene Zugriffe) in der `CLAUDE.md` des
Projekts. **Code eingefroren auf `a68cd3d`; niemand aendert `nrplanner/`,
`tests/`, `scripts/`.** Rollen mit `Write`/`Edit` schreiben nur ihre eigenen
Dokumente (unten je Abschnitt). Kein Commit — der Director committet.

**Stand (docs/state.md, 14.09.2026, woertlich):** "Beta-Gate 07:30 auf
`a68cd3d`: [...] Suite **1528 passed / 9 skipped** (Director)."

**Nutzerfreigabe 14.09. 07:31:** "Ja, vier Rollen in einer Nachricht."

## Was seit der letzten Pruefphase (T-229/T-234 auf `7bc8b04`) geschah

`git log --oneline 7bc8b04..a68cd3d` — T-235 AD-034 (`app.py` 5145 → 3085;
neu `relicslots.py`, `savereader.py`, `statsheet.py`), T-237 (QA-229/QA-230
`5dd0cf7`, Picker 1114 px), T-238 (Audit-Schnitte, `QSignalBlocker`,
Waechter QA-128 Pkt 10). Berichte: `docs/berichte/T-236-developer.md`,
`docs/berichte/T-237-developer.md`; T-235/T-238 nur Kontraktbloecke in
`docs/state.md`.

---

## T-239a — `qa-engineer` (opus)

**Ziel:** Kriterien pruefen, nicht Code lesen. Urteil PASS/CONCERNS/FAIL.

1. **Regression nach AD-034:** Build-planner-Tab am laufenden Fenster
   (Datenumlenkung, Testabzug kopiert): Reliktkarte, Hold-Button, Picker,
   Wartetext beim Save-Lesen, `Find my save`, Werteblatt (Grundwerte,
   Aufschluesselung per Klick, Situationsschalter → `declared`), Optimize.
2. **A12/A13 je Tab einzeln** (sechs Tabs, 1536 px logisch, fusion,
   PrintWindow): Aussage je Tab, keine Sammelaussage. QA-128 Punkte 1-3
   (Waffenkacheln `Scaling STR 50`, `qa/findings.md:150`) sind ungeprueft.
3. **Retest** QA-229, QA-230 (T-237, Bilder im Scratchpad `T-237/shots/`).
4. **`MUTATIONS` nachfahren und loeschen** (OF-35): `favourite-star-back-
   beside-the-chip`, `relic-card-back-to-190-px` in
   `scripts/differential/mutate.py` — einzige erlaubte Aenderung an `scripts/`.
5. **Zaehlauftrag:** AK-188/AK-169-Zahlen (67/42/426/323) auf dem Spielstand
   mit **314** Kopien nachzaehlen (T-229 zaehlte 314; `GOAL.md` sagt 312 —
   beide Zahlen melden, nichts aendern).
6. A2-A8 gegen den Quellstand bestaetigen oder Befund.

**Schreibt:** `docs/berichte/T-239-qa-engineer.md`. Register nur anhaengend.
**Scratchpad:** `<scratchpad>/T-239/qa/`.

## T-239b — `security-reviewer` (opus)

**Ziel:** Pflichtlauf vor dem Build auf `a68cd3d` (Release-Kette).

Diff `7bc8b04..a68cd3d` — neue Module lesen Dateien (`savereader.py`:
Spielstand, Groessen-Deckel `inventory.refuse_a_size_no_save_can_have` jetzt
direkt statt ueber Wrapper) und bauen Texte (`statsheet.py`, `relicslots.py`
— SEC-019-Klasse AutoText/UNC). Pruefen: Deckel SEC-022/SEC-023 unveraendert,
kein neuer Pfad in die Flaeche, keine neue Abhaengigkeit
(`requirements*.txt`), offene SEC-019/028/029/015/021/025/030/040/042 kurz
gegen den neuen Stand. Antwort im Nachrichtentext (kein `Write`).

## T-239c — `ui-ux-designer` (Review, sonnet)

**Ziel:** `DESIGN_REVIEW.md` und `UI_SPEC.md` tragen den Stand.

1. DR-022/DR-023 auf **behoben** (T-234 bestaetigt), DR-013/014/015/016 auf
   behoben mit Commit (`d5b2035`, `0f270d8`, `82cb18a`, `a39d58f`; Belege
   `docs/berichte/T-236-developer.md`, `T-237-developer.md`).
2. **AK-262 neu vorlegen:** Chipstreifen jetzt 118 px (T-237); `BEST FOR
   ATTRIBUTES` (106 px) passt — Empfehlung mit Begruendung, Entscheidung
   trifft der Director.
3. UI_SPEC-Zitat Z. 3883 (Fundstelle nach AD-034 → `statsheet.py`).
4. Review Build-planner-Tab am laufenden Fenster nach dem Umzug (Picker
   1114 px, Karten 208 px): Widerspruch zu AK-46/AK-73/AK-212/AK-271?

**Schreibt:** `DESIGN_REVIEW.md`, `UI_SPEC.md`, `UI_SPEC_REGISTER.md`.

## T-239d — `architect` (Nachtrag, Entscheidungstiefe: Register)

**Ziel:** `ARCHITECTURE.md` stimmt mit dem gebauten AD-034 ueberein.

Abweichungen vom Vertrag (developer T-235c, in `docs/state.md`):
`draw(build, declared)` statt `show()` · `declared_changed = Signal(object)`
statt `Signal(dict)` (int-Schluessel, Emit schlug stumm fehl) · kein
`icons`-Parameter · `SituationalRow`/`_heading` mit umgezogen (Abhaengigkeits-
richtung) · stale-`declared` in `Planner.recompute` · app.py importiert
`SaveReader` + `_pick_a_save_file` · `_refuse_a_file_no_save_can_be` in T-238
geloescht (`ARCHITECTURE.md:5581/5584`) · AD-029/030/031: Fundstelle
`savereader.py` · `ARCHITECTURE.md:2459` nennt `baseline_for` (seit `7bb927c`
weg). Debt zur Entscheidung: Farbkonstanten in 6 Modulen (`theme.py`?),
`_custom_effects`/`_relic_count` mit Unterstrich modulueberschreitend,
`AttackRating` (QA-071), `Weighting` (OF-3), `reading_changed` vs. AD-035.
Nur Register/Nachtraege, keine neue Bauentscheidung ohne Auftrag.

**Schreibt:** `ARCHITECTURE.md`, `ARCHITECTURE_REGISTER.md`.

---

## Rueckgabe (alle)

Kontraktblock plus hoechstens dreissig Zeilen.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
