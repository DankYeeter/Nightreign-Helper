# T-202 — Pruefphase, Sicherheit: der eingefrorene Stand `b33461d`

**Rolle:** `security-reviewer` · **Modell:** opus

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`.
Es gelten die Projektzeilen aus `CLAUDE.md`, Abschnitt
"Projektzeilen fuer jeden Auftrag".

**Scratchpad:** `…/scratchpad/T-202/` und nichts daneben.

## Der Stand ist eingefroren

**`b33461d`.** Es aendert in dieser Zeit niemand Code. Der `qa-engineer`
prueft denselben Stand parallel (T-201), ein `developer` arbeitet
**ausserhalb** des Repositorys (T-200). Ich committe nur unter `docs/tasks/`
und `docs/berichte/`.

## Umfang

Letzte Sicherheitspruefung: **T-185** (an `git log` nachgesehen, 12.09.2026).
Seither, Bereich **`28dc45c..b33461d`**: 13 Dateien, +685/-136 in
`nrplanner/` und `nrdata/`, ueber 8 Commits. Besonders beruehrt:
`relicpicker.py` (+174), `advisor/goals.py`, `advisorbar.py`, `firstrun.py`,
`shortcut.py`, `inventory.py`.

## Drei Stellen, die ich ausdruecklich vorlege

### 1. SEC-036 und die feste Wurzel, die zweimal steht

**Notiz in `security/findings.md`, von mir am Quelltext nachgeprueft:**
`nrdata/gamefiles.py:31-34` haengt **beide** festen Wurzeln unbedingt an
(`C:\Program Files (x86)\Steam` und `C:\Steam`), und
`nrplanner/firstrun.py:153` traegt mit `STEAM_COMMON` dieselbe C:-Wurzel ein
zweites Mal.

**Die Fragen an dich:** wird SEC-036 durch Entfernen der festen Wurzeln
behoben, bleibt `STEAM_COMMON` die letzte feste Wurzel im Modul — ist der
Befund dann sachlich offen, obwohl er formal zu ist? Und ist `STEAM_COMMON`
fuer sich angreifbar, oder nur ein Dialog-Startpunkt, der einen Klick
verlangt? **Der `developer` hat das in T-198 als "sicherheitlich niedriger als
SEC-036" eingeordnet — das ist seine Einschaetzung, nicht meine, und nicht
geprueft.**

### 2. SEC-023 und die Senke, die halb bewacht ist

Aus `docs/state.md`, Abschnitt "Beschlossen, nicht beauftragt", woertlich:

> **Senken-Waechter zu SEC-023** (12.09.): die Pfadhaelfte ist drin, die
> Sprachhaelfte braucht eine eigene Bauform — die AST-Sammlung fasst
> Literale, `str(exc)` ist keins (Widerspruch des `developer` in T-190).

**Die Frage:** haelt die Pfadhaelfte allein, oder ist der Waechter in diesem
Zustand eine Zusicherung, die nicht traegt?

### 3. Eine Abhaengigkeit, die jetzt wirklich installiert ist

`texture2ddecoder==1.0.6` war in `requirements.txt` als **ausgelieferte**
Laufzeit-Abhaengigkeit deklariert, fehlte aber auf dieser Maschine; T-197 hat
sie installiert. **Keine neue Dependency**, aber der Extraktionspfad, den sie
bedient, laeuft damit erstmals unter Test. `tests/test_hostile_gamedata.py`
prueft feindliche Spieldaten — **sag, ob das Angriffsmodell dort noch
stimmt**, nachdem T-193 `binary.magic` auf `NotWhatItClaims` umgestellt hat
und T-197 zwei Erwartungen darauf verengt hat.

## Was du wissen musst

**QA-237, belegt am 12.09.2026:** Schreibvorgaenge unter dem echten
`%LOCALAPPDATA%` erreichen die Platte nicht — sie landen in einer
Ueberlagerung, die alle Claude-Sitzungen teilen. Der Nutzer hat das von seinem
echten System aus gegengeprueft: der Pfad existiert dort nicht.

**Das ist fuer dich mehr als eine Unbequemlichkeit:** jeder Beleg dieses
Projekts, dass ein Lauf *nicht* in Nutzerdaten geschrieben hat, ist damit von
innen nicht fuehrbar. **Sag, ob das eine Sicherheitsaussage dieses Projekts
entkraeftet** — insbesondere die zu SEC-016, SEC-017, SEC-018 und SEC-026, wo
es um Dateizugriff geht. **Gegenbeobachtung, die ich nicht wegerklaere:** am
07.09.2026 ist eine Verknuepfung im **echten** Start-Menue gelandet. Die
Ueberlagerung ist also nicht vollstaendig, und wo ihre Grenze liegt, weiss
niemand.

## Scope-Grenzen

- **Kein Code.** Fixes macht der `developer`.
- **SEC-026** (DLL-Seite haerten) liegt beim Nutzer und blockiert das
  Release, nicht den Bau. **Falle, die du kennen musst:** wer haertet, macht
  SEC-016/017/018 wieder scharf.
- Deine Tabelle fuer `security/findings.md` liefere im Bericht; **du legst die
  Datei nicht selbst an.**

## Parallel

`qa-engineer` (T-201) auf demselben Stand, `developer` (T-200) ausserhalb des
Repositorys.

## Beruehrt Dateien

Keine. **Du hast kein Schreibrecht im Projekt** — liefere den Bericht
vollstaendig in der Antwort, der Director legt ihn als
`docs/berichte/T-202-security-reviewer.md` ab.

## Rueckgabeformat

Uebergabe-Kontrakt-Block, dann Abschlussbericht gemaess Rollendefinition, mit
vierwertigem Urteil und priorisierten Befunden je mit **Angriffspfad** und
Behebungsrichtung.

**Widersprich, wenn der Auftrag nicht stimmt.**
