# T-241 — Release-Kette Zyklus 22: Artefakt 1.10.0 fuer den Ingame-Test (compliance-agent · technical-writer · release-manager · qa-engineer · power-user)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen in der
`CLAUDE.md` des Projekts. **Kein Merge, kein GitHub-Release, keine Signatur,
keine Weitergabe** (Nutzer 12.09., bestaetigt 14.09.). Ziel ist
`dist/NightreignHelper.exe` 1.10.0 fuer den eigenen Ingame-Test des Nutzers
am Abend des 14.09.

**Stand (docs/state.md, 14.09.2026):** "Pruefphase abgeschlossen 07:50" —
`qa-engineer` PASS, `security-reviewer` PASS 0/0/0/0, `ui-ux-designer`
ship-ready. Code-Stand nach T-240 (Chip `BEST FOR ATTRIBUTES`) und
Versionsbump; die Commit-Kennung nennt der Director im Dispatch.

**Abnahmekriterien (GOAL.md, woertlich):** A9 "Ein `qa-engineer`-Durchlauf
bestaetigt A3 bis A8 gegen ein gebautes Artefakt, nicht nur gegen den
Quellstand." · A11 "Ein nicht-technischer Spieler erreicht auf jedem Tab sein
Ziel ohne fremde Hilfe. Nachweis: `power-user`-Berichte, in denen kein 'ich
habe nicht verstanden, was das bedeutet' und kein 'ich habe geraten' mehr
steht." · A15 "[...] Nachweis: ein `power-user`-Lauf auf einem gebauten
Artefakt, bei dem die Automatik nachweislich ins Leere laeuft, und der Nutzer
trotzdem ohne Abbruch bis zu angezeigten Zahlen kommt."

---

## T-241a — `compliance-agent` (`auflagen`)

**Ziel:** `docs/legal/AUFLAGEN.md` sagt, welche Auflagen fuer ein **lokal
gebautes, nicht weitergegebenes** Artefakt gelten und welche erst bei
Weitergabe/Release greifen. Stand jeder Auflage gegen den Baum pruefen
(A-001 bis A-032; QA-243 meldet einen Widerspruch in `AUFLAGEN.md`, Register
`qa/findings.md:266`). Keine neue Klaerung; C-003 (Repo oeffentlich,
Weitergabe) bleibt beim Nutzer. Rote Auflage fuer den heutigen Zweck →
sofort melden. **Schreibt:** `docs/legal/AUFLAGEN.md`, ggf. `docs/legal/C-005.md`.

## T-241b — `technical-writer`

**Ziel:** `README.md` beschreibt das Programm, wie es heute ist. Jeder
Schritt geprueft (Programm aus dem Quellstand starten, Datenumlenkung
Pflicht, Testabzug kopieren).

Nachzuziehen, mit Quelle:
1. **Mindestbreite 1536 px logisch** (UI_SPEC AK-271, T-233); darunter
   duerfen Zielwahl und Lesart-Box abgeschnitten sein, Tooltip traegt den
   Status. Abschnitt "Requirements" oder "The window".
2. **Steam-Herkunft** (SEC-026, T-217): der gewaehlte Spielordner muss in
   einer Steam-Bibliothek liegen (Registry / `libraryfolders.vdf`); sonst
   `That folder is not part of a Steam installation.` (AK-264). Ohne Steam
   in der Registry wird jeder Ordner abgelehnt — als Grenze nennen.
3. **Worst/Best case** des Beraters (A16, AD-035, UI_SPEC AK-268): was die
   zwei Lesarten bedeuten, wann sie sich unterscheiden.
4. Dritter Chip `BEST FOR ATTRIBUTES` (AK-262, T-240), falls Chips im README
   vorkommen.
5. Angriffskraft = die Zahl des Spiels (`floor(0,6 x rate)`, QA-095, seit
   1.8/`99ed022`) — pruefen, ob "How values are derived" das schon sagt.
6. Fenster-Startbreite 1608 px (QA-248), Picker 1114 px — nur, wenn das
   README alte Zahlen nennt (`grep -n "px" README.md`).
Nicht: CHANGELOG (release-manager), Architektur, Design. Befund an den
Director, wo die Anleitung nur wegen des Produkts kompliziert wird.
**Schreibt:** `README.md`, `THIRD_PARTY.md` nur falls A-004 es verlangt.

## T-241c — `release-manager` (`build`)

Ablaufplan `docs/release/ROLLOUT.md:269-285`. Version **1.10.0** in
`nrplanner/__init__.py` ist gesetzt (developer, vor diesem Lauf). Bericht:
Commit, Dauer, Groesse, SHA-256, Pfad `dist/NightreignHelper.exe`, `upx`
ja/nein, `scripts/check_licences.py`-Ergebnis. Kein Code, kein Commit.

## T-241d — am Artefakt, in einer Nachricht

- `release-manager` (`clean-room`): Ablaufplan `ROLLOUT.md:287-314`, inkl.
  Update ueber die 1.9.0 vom 09.09. (`docs/berichte/T-174-release-manager.md`,
  SHA `a2180d5d…66ef3`): Nutzerdaten (Favoriten, gewaehlter Pfad,
  Datenabzug `EXTRACT_VERSION`) ueberleben.
- `qa-engineer` (A9): A3-A8 gegen `dist/NightreignHelper.exe`, nicht gegen
  den Quellstand; A6-Zeiten am Artefakt (Slot-Frage < 500 ms Median,
  Optimize < 6 s, Hauptthread < 50 ms) auf dem Spielstand mit 314 Kopien.
- `power-user` (A11, A15): Persona, Ziele, Zugang im Dispatch; ein Lauf, bei
  dem die Automatik nachweislich ins Leere laeuft (Spielordner an fremdem
  Ort), ohne Codekontext.

Alle Befunde → ein Fixauftrag → Rebuild → ein Retest. Sonst T-241e.

## T-241e — `release-manager` (`notes`)

`CHANGELOG.md` neu anlegen (englisch, Keep-a-Changelog-Form, 1.10.0 mit den
Nutzeraenderungen seit 1.9.0: A15 Pfaddialog + Steam-Herkunft, A16
Worst/Best, A17 Ranking ohne Bezugswaffe, Chips, Mindestbreite 1536 px,
Picker-Karten 208 px, Fensterstart 1608 px, AD-034 nur als "internal").
`docs/release/ROLLOUT.md` fortschreiben (Abschnitt 1.10.0, L1-L8 Stand).
Migration von Nutzerdaten 1.9.0 → 1.10.0 benennen (keine Schemaaenderung
erwartet — pruefen). Kein Release-Text fuer GitHub.

---

## Rueckgabe (alle)

Kontraktblock plus hoechstens dreissig Zeilen. Berichtsdateien:
`docs/berichte/T-241-<rolle>.md` bei Befunden oder `teilweise/blockiert`;
`release-manager` `build` und `clean-room` legen ihren Bericht **immer** ab
(Pruefsumme, Umgebung).

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
