# T-220 — P10: Entwurf fuer die Mutations-Registry und den `Planner`-Schnitt (architect)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen stehen
in der `CLAUDE.md` des Projekts; Abweichungen fuer diesen Auftrag: keine.
**Kein Anwendungscode**, keine Aenderung an `nrplanner/`, `nrdata/`,
`scripts/`, `tests/`.

**Ziel:** Zwei entschiedene, in `ARCHITECTURE.md` festgehaltene Entwuerfe
mit AD-Nummern: (1) was aus der Mutations-Registry wird (P10-2), (2) der
Modulschnitt fuer `class Planner` entlang der bestehenden Naht (P10-1),
geteilt in `developer`-Auftraege von hoechstens fuenf Dateien, jeder einzeln
lauffaehig. P10-2 vor oder mit P10-1, nie danach.

**Stufe/Modus:** architect, Entscheidungstiefe: **entscheiden**, nicht nur
Optionen — beide Richtungen von P10-2 abwaegen und eine waehlen; Alternativen
mit Begruendung im selben Abschnitt.

**Abnahmekriterium (GOAL.md, woertlich):** keines direkt — Vorgabe
"Sicherheit > Funktion korrekt > kein Technical Debt > Geschwindigkeit"
(`commands/director.md`, Grundsatz); Nutzerauftrag 12.09.2026: Ueberbau-Audit
Korb 3 "Umbauten (**P10** in `plan-restarbeiten.md`)".

**Stand (docs/state.md, 2026-09-12, woertlich):** "P10 braucht den
`architect`: Planner 3 318 Zeilen, Mutations-Registry 5 300, beides
bestaetigte Schuld. **P10-2 vor oder mit P10-1.**"

## Kontext

`docs/plan-restarbeiten.md:328-372` (P10, P10-1, P10-2), `:455-462`
(Altstelle `ARCHITECTURE.md:2459`, `baseline_for` existiert seit `7bb927c`
nicht mehr — gehoert dir, im selben Zug nachziehen), `:518-528` (die
Registry haengt auch an **Kommentartext**, T-206). `qa/findings.md:256`
(QA-234: erster verwaister Anker, die von P10-2 vorhergesagte Kopplung).
Register: `ARCHITECTURE_REGISTER.md` (Tabelle 1, AD; naechste freie Nummer
laut `ARCHITECTURE.md:42` **AD-032**, laut `docs/state.md` **AD-033** —
**zaehle am Register nach**, nimm die hoehere freie).

Der Differenzial-Track: `scripts/differential/` (`capture.py`, `compare.py`,
`mutate.py`, `plan.py`, `ratios.py`, `rasters/`), Waechter
`tests/test_differential_track.py` (647 Zeilen). Was er je gefangen hat,
steht in `qa/findings.md` und `docs/archiv/` — **suche es**, bevor du ueber
seinen Wert entscheidest; eine Registry, die nie etwas gefangen hat, ist ein
anderer Fall als eine, die QA-234 gefangen hat (das war der Waechter, nicht
die Mutation).

## Vorgaben

- **P10-2, zwei Richtungen aus dem Plan:** (a) Mutationen zur Laufzeit aus
  ihren Ankern erzeugen, (b) Registry auf die Mutationen einkuerzen, die je
  etwas gefangen haben. Dazu die Vorfrage: traegt der Track, was er kostet?
  Eine dritte Richtung (Track ganz stilllegen, Waechter behalten) ist
  zulaessig, wenn die Zahlen sie tragen — dann als Vorlage an den Nutzer
  markieren, nicht entscheiden.
- **P10-1:** `class Planner` `nrplanner/app.py:1836-5153`, 160 Methoden
  (`grep -c "^    def \|^    @"`, 13.09.2026, ungefaehr). Tabkoerper liegen
  schon in `bosstab.py`, `effectstab.py`, `arsenaltab.py`. Schnitt entlang
  dieser Naht: je Schritt ein Modul, Verdrahtung ueber die bestehenden
  Signale, kein neues Framework, keine Zwischenschicht "fuer spaeter"
  (Ponytail). Jeder Schritt hinterlaesst eine gruene Suite und ein
  startbares Programm; Schrittliste mit Dateien, Reihenfolge, und **welche
  Mutationsanker der Schritt bricht** (das ist die P10-2-Kopplung).
- Bestehende Entscheidungen, die der Schnitt beruehrt, benennen (AD-Nummer)
  und nachziehen, nicht stillschweigend ueberschreiben. `UI_SPEC.md` nicht
  anfassen — beruehrt ein Schritt ein AK, nenne es fuer den
  `ui-ux-designer`.
- Ablage: neuer H2-Abschnitt in `ARCHITECTURE.md` mit Datum und T-220,
  Registerzeilen in `ARCHITECTURE_REGISTER.md` (Tabelle 1, anhaengen). Die
  Altstelle `:2459` als Nachtrag korrigieren, nicht loeschen.

## Testumgebung

Lesend. `pytest tests/test_differential_track.py` darfst du fahren
(1,2 s, ohne `-n`), nichts anderes. Datenumlenkung nach `CLAUDE.md`, falls
du das Programm startest — besser nicht.

## Umfang

**Beruehrt Dateien:** `ARCHITECTURE.md`, `ARCHITECTURE_REGISTER.md`. ·
**Parallel:** ja — T-217/T-218/T-219 (`developer`, Code in eigenen
Worktrees; der Arbeitsbaum, in dem du schreibst, bleibt bei `41206be`). ·
**Scratchpad:** `<scratchpad>/T-220/`

Ein Schritt: beide Entwuerfe in einem Abschnitt. Kein Commit — der Director
committet deine Arbeit.

## Scope-Grenzen

- Keine Zeile Anwendungscode, kein Test, kein Skript.
- Kein Umbau anderer Abschnitte von `ARCHITECTURE.md` ausser der genannten
  Altstelle.
- Keine neue Abhaengigkeit; braeuchte ein Entwurf eine, ist das eine
  `researcher`-Frage, die du benennst, nicht beantwortest.

## Praemissen — jede Bestandsbehauptung mit Quelle

- `class Planner` ab `nrplanner/app.py:1836`, `main()` ab `:5154`, Datei 5216
  Zeilen · `grep -n "^class Planner\|^def main"`, `wc -l` · 13.09.2026
- `scripts/differential/mutate.py` 5464 Zeilen, `tests/test_differential_track.py`
  647 · `wc -l` · 13.09.2026
- Mutationsliterale `mutate.py:44-5368`, darunter ~100 Zeilen Logik ·
  `docs/plan-restarbeiten.md:357-362`, 12.09.2026 — nachmessen
- QA-234 verwaister Anker `advisorbar.py` nach `06be06e` · `qa/findings.md:256`
  · 12.09.2026 (Status dort lesen)
- Naechste AD-Nummer: `ARCHITECTURE.md:42` sagt AD-032, `docs/state.md` AD-033,
  Register Z. 64 fuehrt AD-032 als entschieden · 13.09.2026 — **nachzaehlen**

Widerspricht der Bestand einer Aussage dieses Auftrags, ist das ein Befund:
melden und auf dem Bestand arbeiten. Eine tragende Praemisse ohne Quelle darf
die Rolle zurueckweisen.

## Rueckgabe

Antwort: Kontraktblock plus hoechstens dreissig Zeilen: die Entscheidung zu
P10-2 in einem Satz mit der tragenden Zahl, die Schrittliste fuer P10-1
(Nummer, Dateien, gebrochene Anker), vergebene AD-Nummern. Datei
`docs/berichte/T-220-architect.md` bei Befunden (z. B. weitere Altstellen)
oder `STATUS: teilweise/blockiert`.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
