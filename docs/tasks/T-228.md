# T-228 — Ponytail-Audit Beta-Gate Zyklus 21: Loeschungen und ein native-Fund (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen
(Testbefehl, Datenverzeichnisse, verbotene Zugriffe, Zielsystem) stehen in
der `CLAUDE.md` des Projekts; Abweichungen fuer diesen Auftrag: Schritt 0
unten (Worktree-Stand); **mehr als fuenf Dateien** — 17 Funde, jeder eine
Loeschung ohne Ersatz, ein Muster (toter Code raus), deshalb ein Auftrag
(Regel `commands/director.md`: Audit-Befunde `delete`/`native`/`yagni` sind
**ein** Auftrag am Beta-Gate).

**Ziel:** Toter Code aus dem Audit vom 13.09.2026 ist geloescht, die Suite
bleibt gruen, kein Verhalten aendert sich. Zusaetzlich (bedingt)
`PressableFrame` auf `QAbstractButton`.

**Stufe/Modus:** developer, normal

**Abnahmekriterium (GOAL.md, woertlich):** keines direkt; Grundsatz
"kein Technical Debt" (`commands/director.md`), Nutzerentscheidung am
Beta-Gate 13.09.2026: "Audit-Auftrag, dann Pruefrollen".

**Stand (docs/state.md, 13.09.2026):** Bauphase abgeschlossen `550efc5`,
Suite 1519 passed / 9 skipped (Director gemessen).

## Kontext

Audit (Explore-Lauf, read-only, 13.09.2026, HEAD 550efc5), 22 Funde, davon
hier 17. **Nicht in diesem Auftrag** (Entscheidung Director): `AttackRating`
(QA-071, Nutzerentscheid), `Weighting` (OF-3 offen), `reading_changed`-Signal
(widerspricht AD-035), QSettings-Konstruktoren (die expliziten `ORG`/`APP`
tragen die Datenumlenkung `NIGHTREIGN_SETTINGS_ORG`; ein bares `QSettings()`
in Tests ohne `main()` wuerde an einen unbekannten Ort schreiben — QA-237),
drei `shrink`-Funde.

## Vorgaben — je Fund ein Grep-Beleg; **du zaehlst jeden nach**, bevor du loeschst

Loeschen, wenn dein Grep ueber `nrplanner nrdata scripts tests` (`--include=*.py`)
nur die Definition zeigt (ein Test als einziger Aufrufer: Test mitloeschen,
im Bericht nennen):

1. `VariantDialog` (`app.py:1369-1439`, 71 Z.; AD-034 nennt ihn B-1, Director:
   streichen) · `grep -rn VariantDialog`
2. `VesselStrip._backing`, `SPRITE`, `_tinted` (`app.py:430-433, 438, 445-470`,
   31 Z.; `show_slots` nutzt `slot_chip()`) · `grep -rn "_backing\|\.SPRITE\|_tinted"`
3. `RelicSlot._chance_suffix` (`app.py:985-1008`, 24 Z.)
4. `stacking.tier_note` (`stacking.py:191-205`, 15 Z.)
5. `firstrun.ensure_data` (`firstrun.py:981-987`, 7 Z.; einzige Nennung ist ein
   Docstring in `worker.py:7` — Docstring mit anpassen)
6. `uiscale.label_for` (`uiscale.py:59-63`, 5 Z.; nicht `model.label_for`)
7. `tae.Event.to_end` + `_FLT_MAX` (`tae.py:51, 62-65`, 5 Z.)
8. `Reader.i8`, `Reader.i16` (`binary.py:56-57, 62-63`, 4 Z.)
9. `search.matches` (`search.py:82-84`, 3 Z.)
10. `effecttext.is_described` (`effecttext.py:378-379`, 2 Z.)
11. Tote Konstanten (14 Z.): `EFFECTS_PER_RELIC`, `CUSTOM_RELIC` (+Kommentar)
    in `app.py:45, 83-84`; `deeptab.ATTACK_FIELDS` (+Kommentar, `:42-48`);
    `bhd5.BLOCK_OUT` (`:12`); `extract.DEEP_LABEL_ID` (`:238`),
    `extract.NPC_NAME_PREFIX` (`:914`); `gamefiles.STEAM_APP_ID` (`:10`)
12. pyflakes (`python -m pyflakes nrplanner nrdata`): `QCompleter` (`app.py:17`),
    `QIcon` (`arsenaltab.py:6`), `caption, describe` (`effectstab.py:17`),
    `control_size = 0` (`extract.py:634`), `_pc_row = param.read(...)`
    (`extract.py:2679`, voller Param-Parse ohne Leser — pruefen, ob der Parse
    einen Nebeneffekt hat, sonst weg). Danach muss pyflakes leer sein.
13. `advisor/__init__.py:29-58` Re-Export-Block + `__all__` (30 Z.): jeder
    Aufrufer importiert Module (`from .advisor import run|goals|...`) ·
    Beleg: `grep -rn "from nrplanner.advisor import\|from .advisor import"`
    zeigt nur Modulnamen
14. `datasource.load_data(prefer_live=True)` / `_load_data(prefer_live)`
    (`datasource.py:126-172`): kein Produktionsaufrufer gibt `False`; die zwei
    Tests `tests/test_exception_text_is_english.py:520,541` auf
    `monkeypatch` von `_regulation_matches` umschreiben, Parameter und beide
    `if prefer_live:`-Tore raus
15. Zeilenzitate oben stammen vom Audit auf `550efc5` — dein Grep zaehlt.

**16. `native`, bedingt:** `PressableFrame` (`nrplanner/pressable.py`, 160 Z.,
einzige Unterklasse `BossCard` `bosstab.py:297`) als `QAbstractButton`
(Button-Rolle, Press-Aktion, Space, Fokus, `QAccessibleButton` von Qt; behalten:
`paintEvent`, Return-Taste). **Nur** wenn alle bestehenden Tests zu Fokus,
Tastatur und Accessibility (`grep -rln "PressableFrame\|CardAccessible\|BossCard" tests/`)
**ohne Abschwaechung** gruen bleiben und `AK`-Vorgaben zur Karte (Spec
`bosstab`, §7.4) unveraendert halten. Haelt es nicht: nicht bauen, im Bericht
sagen, was fehlt. Eigener Commit.

**17.** Alles andere aus dem Audit ist nicht dein Auftrag.

Ein Commit fuer 1-15 (oder je Datei-Gruppe, hoechstens drei), einer fuer 16.
Mutationen: fuer reine Loeschungen keine (nichts zu toeten); fuer 16 eine,
wenn ein Test die Tastaturbedienung bewacht.

## Testumgebung

Windows 11, `pytest <datei>` gezielt, am Ende `pytest -n auto`. Datenumlenkung
nach `CLAUDE.md`, `NIGHTREIGN_SETTINGS_ORG=DankYeeterT-228`, Testabzug
**kopieren**. `python -m pyflakes` ist installiert? Sonst `pip install
pyflakes` nur in deiner Umgebung, keine Aenderung an `requirements*.txt`.

## Umfang

**Beruehrt Dateien:** `nrplanner/app.py`, `stacking.py`, `firstrun.py`,
`worker.py` (Docstring), `uiscale.py`, `search.py`, `effecttext.py`,
`deeptab.py`, `arsenaltab.py`, `effectstab.py`, `datasource.py`,
`advisor/__init__.py`, `pressable.py`, `bosstab.py`; `nrdata/tae.py`,
`binary.py`, `bhd5.py`, `extract.py`, `gamefiles.py`; Tests wie genannt. ·
**Parallel:** nein. · **Scratchpad:** `<scratchpad>/T-228/`

**Schritt 0 (Worktree):** `git branch --contains 550efc5` muss den
ausgecheckten Branch nennen. Sonst `STATUS: blockiert`, nichts aendern.

Kein Push. Laenger als 60 Minuten: `teilweise`, Stand je Fund.

## Scope-Grenzen

- Keine Aenderung an Verhalten, Texten, Spec, Architektur.
- Kein Fund ausserhalb der Liste; ein Fund, dessen Grep einen Aufrufer
  zeigt, bleibt stehen und wird im Bericht als "Audit-Irrtum" genannt.
- QSettings-Konstruktoren, `AttackRating`, `Weighting`, `reading_changed`
  nicht anfassen.

## Praemissen — jede Bestandsbehauptung mit Quelle

- Zeilenangaben und Grep-Belege je Fund · Audit-Lauf 13.09.2026 auf
  `550efc5`; Director hat 10 Stichproben nachgezaehlt (alle nur Definition),
  `PressableFrame` 6 Treffer (Definition, `bosstab.py`, Tests) · 13.09.2026
- Suite `550efc5`: 1519 passed / 9 skipped · Director `pytest -n auto` ·
  13.09.2026

Widerspricht der Bestand einer Aussage dieses Auftrags, ist das ein Befund:
melden und auf dem Bestand arbeiten. Eine tragende Praemisse ohne Quelle darf
die Rolle zurueckweisen.

## Rueckgabe

Antwort: Kontraktblock plus hoechstens dreissig Zeilen: je Fund
geloescht/stehen gelassen (mit Grund), Zeilen netto (`git diff --stat`),
pyflakes-Ergebnis, Suitezahlen, Commit-Shas, Entscheidung zu 16. Datei
`docs/berichte/T-228-developer.md` bei Audit-Irrtuemern oder `STATUS:
teilweise/blockiert`.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
