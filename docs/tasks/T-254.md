# T-254 — Zyklus 24: A20 Zweihand, Wortlaute, tote Skips (ui-ux-designer · developer x2)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`; Projektzeilen in der
`CLAUDE.md` des Projekts. Autonomer Lauf (Nutzer 15.09. 00:19): Annahmen
stehen in `docs/state.md` "Annahmen des autonomen Laufs" (AN-1, AN-2).

**Abnahmekriterium (GOAL.md A20, woertlich):** "Jede Angriffskraft-Anzeige
(Kachel, Werteblatt, Arsenal, Berater-Zeile) kennt den Zweihandwert neben
dem Einhandwert, wo die Waffe zweihaendig gefuehrt werden kann; Effekte mit
Bedingung *when Two-Handing* (`8300000-2`, `7006000-1`) rechnen auf den
Zweihandwert." · OF-41 (Nutzer 21:15): "welche Hand der Berater rankt, folgt
einem neuen Umschalter 1H/2H an der Waffenausruestung."

**Stand:** AD-037 (`ARCHITECTURE.md:6304ff`, Bau war gesperrt bis R-008),
R-008 (`docs/research/R-008.md`): flacher Faktor auf die ungerundete
Einhandzahl **T = 1,03**, **T_Raider = 1,144**, 6/6 Messzellen (GOAL A20).
Spec AK-286-288 (Darstellung `{1H} / {2H} 2H`, Ranking-Hand offen → jetzt
Umschalter). Suite `08ddba6`: 1602 passed / 10 skipped.

## T-254a — `ui-ux-designer` (Spec)

1. **Umschalter 1H/2H** an der Waffenausruestung (`weaponslots.py`-Kachel):
   Ort, Wortlaut, Zustand je Build gespeichert (AN-2, Voreinstellung 1H),
   Wirkung: Kachel/Werteblatt/Arsenal zeigen weiter beide Werte, der
   Berater rankt nach der gewaehlten Hand; `when Two-Handing`-Effekte
   zaehlen nur bei 2H. AK-292ff. AK-288 abschliessen.
2. **QA-270:** Wortlaut fuer 4.11 unter unerfuellbarer Pflicht statt `3 slots
   have nothing to choose from` (Nutzer: aendern).
3. **QA-269:** rohe Feldnamen in Why-Zeilen (`conditionHp +40`,
   `physicsAttackPower -30`): Regel, wie ein Feld ohne Spielername
   dargestellt wird (A7/A12) — Wortlaut oder Auslassung.
4. **A18 Satz 1:** Nutzer 00:19 "Effektname reicht vorerst" → Vermerk an
   AK-290, GOAL-Satz bleibt, erfuellt durch den Namen.
Fensterlauf mit Umlenkung erlaubt (du bist die einzige Instanz; developer b
und c starten kein Fenster). **Schreibt:** `UI_SPEC.md`, `UI_SPEC_REGISTER.md`.

## T-254b — `developer` normal: AD-037 Rechenschicht, Qt-frei

`weapons.rate(two_handed=)` mit `TWO_HANDED_RATE = 1.03`,
`RAIDER_TWO_HANDED_RATE = 1.144` als gemessene Kalibrierung (Quelle R-008,
Kommentar mit den sechs Zellen), `damage.Rating.two_handed: Rating | None`
aus `_rate` (None, wo die Waffe nicht zweihaendig fuehrbar ist —
Datenfeld pruefen, sonst melden), Scope 124 → `class_rates["two_handed"]`,
Effekte `8300000-2`/`7006000-1` rechnen auf den Zweihandwert. Tests: sechs
Messzellen (Duchess 72/74, Wylder 122/125, Raider 158/180, Guardian 107/110,
Raider Great Stars 188/216, Wylder Great Stars 147/151) als Waechter,
Mutation je Faktor. **Keine UI** (T-255 nach Spec). Dateien: `weapons.py`,
`damage.py`, `model.py` falls noetig, Tests, `mutate.py`.

## T-254c — `developer` klein: tote Skips

~42 `skip("no save to read")`-Stellen in `tests/` sind seit
`conftest.no_living_save` (`2a0ca0d`) tote Zweige (Slot ist immer da).
Entfernen; wo ein Test dadurch erstmals laeuft und faellt, den Fall melden,
nicht stumm anpassen. Nur `tests/`. Suite `-n auto` gruen.

**Parallel:** a (UI_SPEC), b (weapons/damage/model + Tests dort), c (nur
tests/ ohne die Dateien von b — b nennt seine Testdateien im Bericht; c
fasst `test_attack_power*`, `test_damage*`, `weapon_damage_cases.py` nicht
an). **Scratchpad:** `<scratchpad>/T-254/<rolle>/`.

## Rueckgabe (alle)

Kontraktblock + max. 20 Zeilen.
