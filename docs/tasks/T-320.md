# T-320 — A25 Entwurf und Spec: Schadensart im Berater waehlbar (architect, ui-ux-designer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen in der
`CLAUDE.md`. Stand `ec6da4f`. **Kein Programmlauf, kein Commit**, nur die
eigene Datei; die Sitzung T-312 (Release 1.15.0) laeuft parallel im Baum.

**Abnahmekriterium (GOAL.md A25, woertlich):** "Revenant mit Siegel als
Referenzwaffe und Auswahl 'Holy' bzw. 'Incantations' liefert eine andere
Reihung als 'Alle', und die Why-Zeile nennt die Art; Wylder mit beiden
Startwaffen-Relikten und Auswahl 'Skill attack' zaehlt die Konversion in
die Schadenszahl." Stand: nicht erfuellt (kein Ziel liest `final_per_type`,
`scoped:`-Raten, `starting_flat`; Explore 19.09. 15:38).

## Kontext (Quittung: R-009, Explore-Bericht 19.09.)
- Ziele: `nrplanner/advisor/goals.py:452-456` (drei), `_max_damage`
  236-319 rankt `Rating.final_headline`; `advisorbar.GOAL_ORDER`.
- Per-Art: `damage.Rating.final_per_type` (389-398), `breakdown_figures`.
- Scopes: `model.MOVE_SCOPED_EFFECT_IDS` 584-592, `attack_scope` 527,
  `build.rates["scoped:<name>"]`; Scope 112 Skills, 111 (Praemisse:
  aufgeladene Skills), 110 aufgeladene Zauber, 23 Bestial; Schulen aus
  `spell_families`. R-009 Tabelle.
- Startwaffe: `damage.converted` 550-595, `is_starting_armament`,
  `STARTING_AR_RATE_FOR`; Kandidaten mit Konversion werden heute nicht
  gewertet (`advisor/candidates.py:161-203`).
- Katalysator-Spell-Power: `Rating.catalyst_scaling` (Attribute nur).

## T-320a architect (Entscheidungstiefe: Modul + Datenfluss, AD-Nummern ab AD-045)
Entscheide: (1) Auswahl als Parameter des Ziels `max_damage` oder eigene
Ziele je Art; (2) Zuordnung Scope-Wert → Art (wo, wie belegt, was mit
unbekannten Scopes); (3) wie `scoped:`-Raten und Startwaffen-Konversion in
die Kandidatenwertung kommen, ohne AD-020 zu brechen; (4) Verhalten bei
Katalysator-Referenz mit "Sorceries"/"Incantations" (Rate auf Spell Power
anwenden ja/nein — heute bewusst nein, `damage.py:346-357`); (5)
Testschnitt. Schreibe `ARCHITECTURE.md` + Register. Praemissen: Nutzer
19.09. — Skill attack = nur Weapon Arts; keine neue Extraktion.

## T-320b ui-ux-designer (Spec, AK ab AK-327)
Auswahl der Schadensart in der Berater-Leiste neben "Maximise damage":
Platz, Bedienelement, Voreinstellung "All", Wortlaut englisch (A8), Why-
Zeile nennt die Art, Verhalten wenn die Referenzwaffe die Art nicht
traegt. Bestehende Leiste: `nrplanner/advisorbar.py`, `UI_SPEC.md`
Abschnitt Berater. Schreibe `UI_SPEC.md` + `UI_SPEC_REGISTER.md`.

## Rueckgabe
Kontraktblock; Entscheidungen je in einem Satz; offene Fragen an den
Nutzer gesammelt.
