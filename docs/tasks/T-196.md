# T-196 — QA-231: den festen Testabzug wieder aufbauen

**Rolle:** `developer` · **Stufe:** klein · **Modell:** sonnet

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`.
Es gelten die Projektzeilen aus `CLAUDE.md`, Abschnitt
"Projektzeilen fuer jeden Auftrag" (Zielsystem, Testbefehl,
Datenverzeichnisse, verbotene Zugriffe, Scratchpad). **Abweichung fuer diesen
Auftrag:** der Abschnitt "Fester Testabzug statt Neubau" beschreibt genau das,
was hier erst hergestellt wird — sein Sollwert ist deine Vorgabe, nicht deine
Voraussetzung.

**Scratchpad:** `…/scratchpad/T-196/` und nichts daneben.

## Ziel

Das Verzeichnis `C:\Users\Daniel\AppData\Local\NightreignHelper-Testabzug`
existiert, traegt die **aktuelle** `EXTRACT_VERSION` und laesst sich als
`LOCALAPPDATA`-Inhalt eines Fensterlaufs verwenden, ohne dass der Lauf den
Datenabzug neu baut.

## Kontext

Wortlaut aus `qa/findings.md`, Zeile 253 (QA-231, P1, Major, offen,
12.09.2026):

> **Der feste Testabzug aus `CLAUDE.md` existiert nicht.** Das Verzeichnis
> `NightreignHelper-Testabzug` unter `%LOCALAPPDATA%` fehlt -- `ls` schlaegt
> fehl (Director, 12.09.2026; der `ui-ux-designer` hatte es in T-192 mit zwei
> unabhaengigen Pruefungen zuerst gemeldet). `CLAUDE.md` beschrieb ihn als
> vorhanden -- 841 Dateien, 19,8 MB, `EXTRACT_VERSION` 11 -- und jeder Auftrag
> verweist darauf. **Wirkung:** jede Rolle, die ein Fenster braucht, zahlt den
> Neuaufbau (gemessen 107 s bis 5 min, QA-198), und der echte Cache traegt eine
> aeltere `extract_version`, auf der ein `Planner` abstuerzt. **Vor der
> Baurunde zu beheben**, weil `power-user` und `clean-room` beide
> Fensterlaeufe sind

Wortlaut aus `docs/state.md`, Abschnitt "Vor der Baurunde zu erledigen",
Punkt 1:

> **QA-231 — der feste Testabzug existiert nicht** (am Dateisystem geprueft).
> Jeder Fensterlauf zahlt 107 s bis 5 min Neuaufbau. `power-user` und
> `clean-room` sind beide Fensterlaeufe.

Wortlaut aus `GOAL.md`, A9:

> **A9** Ein `qa-engineer`-Durchlauf bestaetigt A3 bis A8 gegen ein gebautes
> Artefakt, nicht nur gegen den Quellstand.

Dieser Auftrag erfuellt A9 nicht. Er raeumt den Blocker weg, der vor der
Baurunde steht, auf der A9 geprueft wird.

## Vom Director geprueft, nicht angenommen

- **Die Spielinstallation ist vorhanden:**
  `D:\Steam\steamapps\common\ELDEN RING NIGHTREIGN`, 27 GB
  (`du -sh`, 12.09.2026). Steam-Wurzel laut Registry
  `HKCU:\Software\Valve\Steam` → `d:/steam`. Die Vorgabepfade
  unter `C:\Program Files (x86)\Steam` existieren **nicht** — wenn dir ein
  Skript oder ein Kommentar den C-Pfad nennt, ist der falsch.
- **Das Zielverzeichnis existiert nicht** (`ls`, 12.09.2026).

## Faehigkeitsprobe — erster Schritt, mit Abbruch

Bevor du irgendetwas baust: starte das Programm **einmal** mit allen drei
umgelenkten Variablen und stelle fest, ob es die Spieldaten findet und den
Abzug anlegt. **Kommt kein Abzug zustande, brich ab und melde nur das** —
mit der Fehlermeldung und dem Pfad, an dem es gesucht hat. Baue dann nichts
von Hand nach.

## Vorgaben

1. **Alle drei Variablen umlenken und jede Umlenkung nachweisen.** Die Tabelle
   steht in `CLAUDE.md`. Der Nachweis gehoert in den Bericht:
   je Variable der gesetzte Wert und ein Beleg, dass dorthin geschrieben wurde
   (und nicht ins echte Verzeichnis). **Gelingt ein Nachweis nicht, brichst du
   ab und meldest nur das.** Der Nutzer hat 309 Relikte und rund 110
   gespeicherte Builds im Programm: lesen ja, schreiben nie.
2. Den fertigen Abzug **kopieren** nach
   `C:\Users\Daniel\AppData\Local\NightreignHelper-Testabzug`. Nicht
   verschieben, nicht verlinken, nicht das umgelenkte Verzeichnis dorthin
   zeigen lassen.
3. **Den neuen Sollwert messen und nennen:** Dateizahl, Groesse, die
   `EXTRACT_VERSION`, mit der er gebaut wurde, und die Programmversion. Die
   Zahlen in `CLAUDE.md` (841 Dateien, 19,8 MB, `EXTRACT_VERSION` 11) sind der
   **alte** Stand. Weicht deine Messung ab, ist das ein Befund und kein Fehler:
   nenne beide Zahlen.
4. **Messen, was es spart:** ein Fensterstart mit dem Abzug gegen einen ohne.
   Zwei Zahlen, je mit Kommando. Ist die Messung nicht sauber zu bekommen,
   sag das statt zu schaetzen.

## Scope-Grenzen

- **Kein Anwendungscode.** Dieser Auftrag aendert keine Datei unter
  `nrplanner/`, `nrdata/` oder `tests/`.
- **`CLAUDE.md` fasst du nicht an** — die Zahlen traegt der Director nach.
- Nicht ins Repository kopieren. Der Abzug stammt aus der Spielinstallation;
  NH-002 und A-003 verbieten das, und er liegt deshalb ausserhalb des
  Projektbaums.
- QA-233 ist ein **anderer** Auftrag (T-197) und laeuft parallel. Fass
  `tests/` nicht an.

## Parallel

T-197 (`developer`, QA-233) laeuft gleichzeitig.

## Beruehrt Dateien

`docs/berichte/T-196-developer.md` — und sonst keine Datei im Repository.

## Rueckgabeformat

Uebergabe-Kontrakt-Block, dann Abschlussbericht gemaess Rollendefinition,
vollstaendig nach `docs/berichte/T-196-developer.md`.

**Widersprich, wenn der Auftrag nicht stimmt.**
