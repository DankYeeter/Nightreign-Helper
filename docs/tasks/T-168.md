# T-168 - Das Artefakt bauen, zweiter Anlauf (release-manager, Modus `build`)

**Modus: `build`.** Nur bauen. **Kein Release, keine Veroeffentlichung, kein
Tag, kein Push.**

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`.
Es gelten die Projektzeilen aus `CLAUDE.md`.

**Scratchpad:** eigenes Unterverzeichnis `T-168/`.

## Die Sperre ist weg — so, wie es vorgesehen ist

**Dein Vorgaenger T-167 hat den Bau verweigert**, weil `A-010` und `A-020` in
`docs/legal/AUFLAGEN.md` auf **ROT/offen** standen. **Das war richtig**, und er
hat ausdruecklich nicht selbst entschieden, sie seien "eigentlich erledigt".

**Beide sind jetzt vom Nutzer entschieden** (09.09.2026, im Register
eingetragen, Commit `b9996a5`):

- **A-020 — `erfuellt`, abgenommen durch den Nutzer.** Die Arbeit war getan
  und belegt: T-117 hat alle zwoelf Releases nachgeruestet, T-114 hat es je Tag
  mit `gh release view` gegengeprueft (12 von 12). Das Register sagte selbst:
  *"ausgefuehrt, Nachweis erbracht — die Abnahme setzt der Nutzer."*
- **A-010 — zurueckgestellt, sperrt nicht mehr.** Sie stammt aus C-002, das
  der Nutzer am 02.09.2026 ausdruecklich ignoriert hat (*"sperrt aber nichts
  mehr"*, `GOAL.md`). Die Entscheidung war nie ins Register uebertragen worden
  — ein Versaeumnis des Directors.

**Pruef das selbst nach**, statt es mir zu glauben: `docs/legal/AUFLAGEN.md`,
die zwei Tabellenzeilen und der Abschnitt "Abnahmen durch den Nutzer,
2026-09-09".

**Was weiterhin offen ist und offen bleibt: A-025** (GRAU — ob die Verbreitung
der EXE fortgesetzt, beendet oder anwaltlich geklaert wird). **Sie sperrt das
Release, nicht den Bau** — Bauen und Pruefen ist keine Weitergabe, und der
Nutzer hat genau das angeordnet: *"arbeite aber weiter damit lokal die fertige
Version ist. alles bezueglich release und sicherheit und co klaeren wir
danach."*

## Ziel

Ein reproduzierbares Artefakt aus dem heutigen Stand — PyInstaller-Einzeldatei,
wie die 1.8.0 aus Zyklus 16. **Der Nachweis am gebauten Artefakt ist der
einzige Weg zu A9, A11 und A15**, und alle drei sind offen.

## Was seit dem letzten Bau passiert ist

**Zyklus 18, elf Bauauftraege.** Das vorhandene `dist/NightreignHelper.exe`
(59 010 777 B, SHA-256 `42B21AA2…F221`, 1.8.0) stammt aus **Zyklus 16** und
kennt nichts davon:

- **A15 vollstaendig neu** — Auswahldialog fuer Spielordner **und**
  Spielstand, Pfadgedaechtnis in zwei QSettings-Schluesseln, Rueckfrage bei
  abweichendem Fund, acht Panels.
- **Spielstand-Lesen im Hintergrund** (AD-029 Stufe B): **6147,6 → 657,2 ms**.
- **Der Berater rechnet im Worker**: Picker-Hauptthread **318,1 → 7,2 ms**.
- **Ein langsamer Rueckfallweg** samt Satz an den Spieler.
- **Fuenf Sicherheitsbefunde behoben** (SEC-027 bis SEC-031, SEC-033 bis
  SEC-035).
- **`README.md` neu** (T-166) — erstmals mit dem ehrlichen Satz, dass eine
  Bibliothek aus dem Spielordner **ausgefuehrt** wird (Auflage aus SEC-006,
  offen seit 02.09.).

**Suite: 1699 passed, 9 skipped, 0 failed** (T-164); der Director hat
zwischendurch selbst gemessen (1691/9/0 bei `ef6761b`).

## Abnahmekriterien aus GOAL.md (woertlich)

> **A9** Ein `qa-engineer`-Durchlauf bestaetigt A3 bis A8 gegen ein gebautes
> Artefakt, nicht nur gegen den Quellstand.

> **A15** […] Nachweis: ein `power-user`-Lauf auf einem **gebauten Artefakt**,
> bei dem die Automatik nachweislich ins Leere laeuft, und der Nutzer trotzdem
> ohne Abbruch bis zu angezeigten Zahlen kommt.

**Stand laut docs/state.md** (woertlicher Auszug, 09.09.2026):

> `compliance-agent` (`auflagen`) → `technical-writer` → `release-manager`
> (`build`) → (`clean-room`) → `power-user` → (`notes`).

## Vorgaben

- **Version entscheiden und begruenden.** 1.8.0 liegt als Zyklus-16-Artefakt
  vor; dieser Stand ist inhaltlich ein anderer. **`CHANGELOG.md` nicht
  anfassen** — das ist `notes`, nach dem `power-user`.
- **Reproduzierbarkeit belegen, nicht behaupten:** Groesse, SHA-256, Commit.
  Zyklus 16 hat **62 von 62 Modulen bytecode-identisch zum Quellstand**
  nachgewiesen — **kannst du den Nachweis wieder fuehren, fuehr ihn.**
- **`requirements.txt` nicht anfassen** (die Trennung nach
  `requirements-build.txt` ist P9 und braucht denselben Lauf, der sie prueft).
- **Bricht der Bau: Fixauftrag an den `developer`**, du fixt nichts selbst.

## Testumgebung

Windows 10/11 x64, Branch `docs/audit-and-advisor-design`. **Alle drei
Datenverzeichnisse umlenken und jede Umlenkung nachweisen** — gelingt ein
Nachweis nicht, brich ab und melde nur das. **Der Spielstand ist read-only**;
309 Relikte und rund 110 Builds des Nutzers, drei Datenverluste in frueheren
Zyklen auf genau diesem Weg. **Testabzug aus `CLAUDE.md` kopieren, nicht neu
bauen** (99,2 % gespart, gemessen).

*Steigt `EXTRACT_VERSION` in diesem Stand ueber 11, ist der Abzug ungueltig —
dann **ersetzt du die Vorlage und vermerkst es** (E-1 in
`docs/plan-restarbeiten.md`).*

## Scope-Grenzen

- Nicht: `clean-room`, `notes`, das Release, der `power-user`.
- **Nicht anfassen:** `nrplanner/`, `nrdata/`, `tests/`, `UI_SPEC.md`,
  `ARCHITECTURE.md`, `CHANGELOG.md`, `GOAL.md`, `docs/state.md`,
  `qa/findings.md`, `security/findings.md`, `docs/legal/`, `docs/tasks/`.

**Parallel laeuft nichts.**

## Rueckgabeformat

Uebergabe-Kontrakt-Block plus Abschlussbericht. **Der Auftrag gilt erst als
erledigt, wenn `docs/berichte/T-168-release-manager.md` auf der Platte liegt.**

**Nenne in der Zusammenfassung ganz oben: Pfad, Groesse, SHA-256, Commit und
Version.** Der `clean-room`-Lauf und der `power-user` bekommen genau diese
Angaben.
