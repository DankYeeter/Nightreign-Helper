# T-223 — AD-033 Punkt 2: `MUTATIONS` einmal auf leer (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen stehen
in der `CLAUDE.md` des Projekts; Abweichungen fuer diesen Auftrag: Schritt 0
unten (Worktree-Stand).

**Ziel:** `scripts/differential/mutate.py` haelt keine Mutationsliterale
mehr; Logik, Kopfkommentar und Anker-Waechter bleiben im Wortlaut.

**Stufe/Modus:** developer, klein

**Abnahmekriterium (GOAL.md, woertlich):** keines direkt; Grundsatz "kein
Technical Debt" und Nutzerauftrag 12.09.2026 Ueberbau-Audit Korb 3 (P10).

**Stand (docs/state.md, 13.09.2026, woertlich):** "AD-033 Registry als
Quittung je Zyklus, `MUTATIONS` einmal leer [...] Reihenfolge P10: erst T-217
zusammenfuehren (aendert Anker in `mutate.py`), dann AD-033 Punkt 2".

## Kontext

`ARCHITECTURE.md:5364-5440` (AD-033, Entscheidung D, Punkte 1-5). T-217 ist
zusammengefuehrt (`e465c17`), alle Literale stehen gruen gegen HEAD
(Suite 1805 passed / 9 skipped, Director 13.09.).

## Vorgaben

- AD-033 Punkt 1 und 2 woertlich: `Mutation`, `MUTATIONS`, `newline_of`,
  `apply`, `guard_the_own_tree`, `main`, Kopfkommentar unveraendert;
  `MUTATIONS` wird die leere Liste. **Keine** Archivdatei.
- Commit-Nachricht nennt den Stand, an dem die Literale zuletzt gruen gegen
  den Quelltext standen (dein HEAD vor der Aenderung, `git rev-parse HEAD`)
  und den Archivbefehl `git show <sha>:scripts/differential/mutate.py`.
- Punkt 3: `tests/test_differential_track.py` bleibt im Wortlaut. Laeuft die
  Parametrisierung ueber `MUTATIONS` bei leerer Liste auf `skip` statt
  `pass`, ist das der erwartete Zustand — Zahl im Bericht. Laeuft sie auf
  `error`, kleinste Aenderung am Test, im Bericht nennen.
- Zaehlen, nicht schaetzen: Zeilen vorher/nachher (`wc -l`), Anzahl
  entfernter Eintraege.

## Testumgebung

`pytest tests/test_differential_track.py` (T-220 hat 59,9 s gemessen, nicht
1,2 s), dann `pytest -n auto`. Datenumlenkung nach `CLAUDE.md`,
`NIGHTREIGN_SETTINGS_ORG=DankYeeterT-223`, Testabzug kopieren.

## Umfang

**Beruehrt Dateien:** `scripts/differential/mutate.py`,
`tests/test_differential_track.py` nur bei `error`. · **Parallel:** ja —
T-221 (`UI_SPEC.md`), T-222 (`ARCHITECTURE.md`). · **Scratchpad:**
`<scratchpad>/T-223/`

**Schritt 0 (Worktree):** `git branch --contains 59c6cd1` muss den
ausgecheckten Branch nennen. Sonst `STATUS: blockiert`, nichts aendern.

Ein Commit auf dem ausgecheckten Branch, kein Push.

## Scope-Grenzen

Keine Aenderung an `capture.py`, `compare.py`, `plan.py`, `ratios.py`,
`rasters/`; kein Anwendungscode.

## Praemissen — jede Bestandsbehauptung mit Quelle

- `mutate.py` 5464 Zeilen, Literale Z. 44-5368 (5 325 Zeilen), 96 Zeilen
  Logik · T-220 B-6, `sed -n 44,5368p | wc -l` · 13.09.2026
- 289 Eintraege · `ARCHITECTURE.md:5366` (T-220 gezaehlt) · 13.09.2026 —
  nachzaehlen
- Waechter 344 passed in 59,9 s · T-220 B-3 · 13.09.2026

Widerspricht der Bestand einer Aussage dieses Auftrags, ist das ein Befund:
melden und auf dem Bestand arbeiten. Eine tragende Praemisse ohne Quelle darf
die Rolle zurueckweisen.

## Rueckgabe

Antwort: Kontraktblock plus hoechstens zwanzig Zeilen: Zeilen vorher/nachher,
Eintraege entfernt, Waechterlauf (passed/skipped), Suite, Commit-Sha.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
