# T-174 - Bauen mit 1.9.0 (release-manager, Modus `build`)

**Modus: `build`.** Nur bauen. **Kein Release, kein Tag, kein Push.**

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`.
Es gelten die Projektzeilen aus `CLAUDE.md`.

**Scratchpad:** eigenes Unterverzeichnis `T-174/`.

## Was seit deinem letzten Bau passiert ist

**Du hast in T-168 schon einmal gebaut** (59 063 047 B, SHA-256
`357F8D92…`, Commit `f0eb0fd`). Dieses Artefakt ist **ueberholt** — seither:

- **Deine eigene Meldung ist umgesetzt:** die Version steht auf **1.9.0**
  (T-169, `84a22ef`), samt Waechter, dass `nrplanner/__init__.py` die einzige
  Stelle bleibt. **Deine Begruendung war die Grundlage** — neue Funktion, kein
  Hotfix.
- **Ein Fehler wurde gefunden, der nie funktioniert hat** (D-001): der
  Datensatz hatte **zwei Gestalten** — `extract.build()` lieferte
  Zahl-Schluessel, jeder JSON-Umlauf machte Text daraus. Behoben in T-171
  (`a1172fb`), geprueft in T-172.
- **QA-220 geschlossen** durch einen Waechter, der **auch in der CI laeuft**
  (T-173).

**Suite: 1704 passed, 9 skipped, 0 failed — in beiden Datenbedingungen**
(mit Testabzug 173,9 s, ohne erreichbaren Datenabzug 275,5 s). *Die
Datenbedingung gehoert ab jetzt zu jeder Suitezahl dieses Repos — ohne sie ist
sie wertlos, siehe `docs/debug/D-001.md`.*

**Die JSON-Datei auf der Platte hat sich durch D-001 um kein Zeichen
geaendert** (gleicher sha256, `EXTRACT_VERSION` bleibt **11**) — **der feste
Testabzug bleibt gueltig.**

## Was zu tun ist

**Bau erneut**, jetzt mit **1.9.0**. Alles Weitere wie in T-168, das ist
belegt gelaufen:

- **Reproduzierbarkeit belegen, nicht behaupten:** Groesse, SHA-256, Commit.
- **Der Bytecode-Nachweis, den du in T-168 gefuehrt hast** (63 von 63 Module
  identisch zum Quellstand, mit funktionierender Positivkontrolle) — **fuehr
  ihn wieder.**
- **Keine Spieldaten im Bundle** — die Archivliste hast du in T-168 geprueft.
- **`requirements.txt` nicht anfassen**, **`CHANGELOG.md` nicht anfassen**
  (das ist `notes`, nach dem `power-user`).
- **Bricht der Bau: Fixauftrag an den `developer`**, du fixt nichts selbst.

**Zwei Dinge aus deinem letzten Bericht, die jetzt zaehlen:**

1. Du hast gemeldet, `dist/NightreignHelper-notices.zip` sei **veraltet**
   (07.09.). **Es bleibt vorerst, wie es ist** — es gehoert zum naechsten
   echten Release, und das ist gesperrt (A-025). **Sag im Bericht, ob dein
   Bau daran etwas aendert.**
2. Zwei Baeuge waren in T-168 **nicht bit-identisch**, erste Abweichung bei
   Byte 273 (PE-Zeitstempel), dieselbe bekannte Ursache wie in frueheren
   Zyklen. **Das ist kein neuer Befund** — nenn es, erklaer es nicht neu.

## Abnahmekriterien aus GOAL.md (woertlich)

> **A9** Ein `qa-engineer`-Durchlauf bestaetigt A3 bis A8 gegen ein gebautes
> Artefakt, nicht nur gegen den Quellstand.

> **A15** […] Nachweis: ein `power-user`-Lauf auf einem **gebauten Artefakt**,
> bei dem die Automatik nachweislich ins Leere laeuft, und der Nutzer trotzdem
> ohne Abbruch bis zu angezeigten Zahlen kommt.

**Stand laut docs/state.md** (woertlicher Auszug, 09.09.2026):

> `compliance-agent` (`auflagen`) → `technical-writer` → `release-manager`
> (`build`) → (`clean-room`) → `power-user` → (`notes`).

## Die Sperre, unveraendert

**A-010 und A-020 sind vom Nutzer entschieden** (09.09.2026, Commit
`b9996a5`) — A-020 **erfuellt und abgenommen**, A-010 **zurueckgestellt**.
**A-025 bleibt offen und sperrt das Release, nicht den Bau.** Der Nutzer hat
das angeordnet: *"arbeite aber weiter damit lokal die fertige Version ist.
alles bezueglich release und sicherheit und co klaeren wir danach."*

## Testumgebung

Windows 10/11 x64, Branch `docs/audit-and-advisor-design`. **Alle drei
Datenverzeichnisse umlenken und jede Umlenkung nachweisen** — gelingt ein
Nachweis nicht, brich ab und melde nur das. **Der Spielstand ist read-only**;
309 Relikte, rund 110 Builds, drei Datenverluste in frueheren Zyklen auf genau
diesem Weg. **Testabzug kopieren, nicht neu bauen.**

*Hinweis aus QA-221: `tests/conftest.py` ueberschreibt
`NIGHTREIGN_SETTINGS_ORG` unter `pytest` unbedingt. Fuer deinen **Programm**-
start gilt dein eigener Wert — dort wirkt er.*

## Scope-Grenzen

- Nicht: `clean-room` (eigener Auftrag), `notes`, das Release, der
  `power-user`.
- **Nicht anfassen:** `nrplanner/`, `nrdata/`, `tests/`, `scripts/`,
  `UI_SPEC.md`, `ARCHITECTURE.md`, `CHANGELOG.md`, `GOAL.md`,
  `docs/state.md`, `qa/findings.md`, `security/findings.md`, `docs/legal/`,
  `docs/tasks/`.

**Parallel laeuft nichts.**

## Rueckgabeformat

Uebergabe-Kontrakt-Block plus Abschlussbericht. **Der Auftrag gilt erst als
erledigt, wenn `docs/berichte/T-174-release-manager.md` auf der Platte liegt.**

**Nenne in der Zusammenfassung ganz oben: Pfad, Groesse, SHA-256, Commit und
Version.** Der `clean-room`-Lauf und der `power-user` bekommen genau diese
Angaben — und der `clean-room` prueft das **Update ueber eine aeltere
Version**, wofuer 1.9.0 gegen die 1.8.0 aus Zyklus 16 antritt.
