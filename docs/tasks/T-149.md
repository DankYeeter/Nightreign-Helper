# T-149 - V2: der Auswahldialog und seine Panels (developer)

**Ziel:** A15 wird sichtbar. Findet die Automatik den Spielordner nicht,
erscheint **statt der Fehlermeldung** der Ablauf aus `UI_SPEC.md` — Panel,
Systemdialog, Pruefung, Bestaetigung im Fall C3, und der Pfad wird behalten.

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`.
Es gelten die Projektzeilen aus `CLAUDE.md`. Abweichung: **keine**.

**Scratchpad:** eigenes Unterverzeichnis `T-149/`.

## Umfang

**Stufe:** normal.

**Beruehrt Dateien:** `nrplanner/firstrun.py`, `nrplanner/app.py` und die
zugehoerigen Dateien unter `tests/`. **Nicht** `nrdata/gamefiles.py` und
**nicht** `nrplanner/gamepath.py` — die sind aus V1 fertig und werden benutzt,
nicht geaendert.

## Kontext

**Quittung aus T-147 (`developer`, V1, Commits `83e1b90`, `87d6334`,
`7f2f5c6`):** Der Aufloesungspunkt steht. `nrplanner/gamepath.py` ist neu —
zwei feste Schluessel, Pfad im **Wert**, `type=str`, beschaedigter Eintrag gilt
wie abwesend, **kein `remove`**, und ein Automatikfund wird **nicht**
zurueckgeschrieben. `nrdata/gamefiles.py` hat `looks_like_the_game`
(ein Praedikat fuer Annahme **und** spaetere Gueltigkeit), `is_named_nightreign`
und `search_from` (3 runter, 2 rauf, 400 Verzeichnisse, 2 s, keine
Reparse-Punkte, Ergebnis ueber `Path.resolve()`). Suite **1490 passed,
9 skipped, 0 failed**; 22 Mutationen, 20 tot, 2 als "zwei Guertel" begruendet.

**Eine Ausnahme wartet auf dich:** `app.py:4863` liegt laut T-147 **noch auf
der Automatik** und steht als benannte, sich selbst raeumende Ausnahme im
Waechter. **Du raeumst sie.**

**Abnahmekriterium aus GOAL.md** (woertlich):

> **A15** Der Erststart fuehrt jeden Nutzer bis zu lesbaren Daten, ohne fremde
> Hilfe. Findet die Automatik den Spielordner oder den Spielstand, merkt der
> Nutzer nichts — der Ablauf bleibt wie heute. Findet sie ihn nicht, erscheint
> **statt der Fehlermeldung** ein Auswahldialog; der gewaehlte Pfad wird
> geprueft, behalten und beim naechsten Start wieder benutzt. Nachweis: ein
> `power-user`-Lauf auf einem **gebauten Artefakt**, bei dem die Automatik
> nachweislich ins Leere laeuft, und der Nutzer trotzdem ohne Abbruch bis zu
> angezeigten Zahlen kommt.

**Stand laut docs/state.md** (woertlicher Auszug, autonomer Lauf):

> 2. **A15** Erststart — spezifiziert AK-106 bis AK-132, **null Zeilen
>    gebaut** (geprueft 08.09.: kein `QFileDialog`/`getExistingDirectory` in
>    `nrplanner/`).

*Die Absenz-Aussage gilt weiter fuer die Oberflaeche: V1 hat die Qt-freie
Seite gebaut, den Dialog gibt es nach wie vor nicht.*

## Deine Vorgabe ist `UI_SPEC.md`, und sie ist juenger als der Entwurf

**Lies den A15-Abschnitt ganz** (AK-106 bis AK-132) **und die vier Nachtraege
dazu** — sie aendern ihn an mehreren Stellen:

- **T-145** — Text **A1** sagt jetzt, dass eine Bibliothek aus dem Ordner
  **ausgefuehrt** wird; der Satz `It is only read.` ist **weg** (SEC-027).
  W1 traegt sinngemaess dasselbe. **AK-230 bis AK-239.**
- **T-146** — die Rueckfrage **C3** greift **nur beim Verlassen des gewaehlten
  Baumes** (Aufstieg, Zweigwechsel, Verzeichnisverknuepfung). **Der Abstieg —
  der Regelfall — loest keine Rueckfrage aus.** Die Pruefung gehoert an den
  **aufgeloesten** Pfad, nicht an die Zeichenkette. **AK-240 bis AK-242**,
  dazu nachgezogen: AK-232, AK-234, AK-237, §3.3, §3.4.
- **T-141/T-148** — der Wartezustand beim Lesen (AK-220 bis AK-229) ist aus
  T-142 **gebaut**; **AK-243 und AK-244 sind neu und noch nicht gebaut** —
  sie gehoeren dir, weil sie `app.py` und `advisorbar.py` betreffen: AK-243
  benennt **beide** im Wartezustand gesperrten Elemente mit ihren
  **verschiedenen** Freigabebedingungen, AK-244 den fuenften Satz, den
  `reload_chalices` ueber `load_equipped` ausloest.

## Vorgaben

- **Die Texte stehen woertlich in der Spec.** Erfinde keinen. Englisch (A8).
- **Kein neuer persistenter Zustand ausser den zwei Schluesseln aus AD-030** —
  und die sind in V1 gebaut, du benutzt sie.
- **`paths/game` wird nur aus einer Nutzerbestaetigung geschrieben, nie aus
  einem Automatikfund.** Das ist die sicherheitstragende Invariante M3 aus
  T-144: **faellt sie, faellt die einzige Schranke aus SEC-026.** Wenn dein
  Code sie beruehrt, gehoert die toetende Mutation dazu.
- **Waechter mit vollem Mutationsbeweis** fuer alles, dessen Bruch **still**
  bliebe. Wo ein Bruch sofort auffaellt, genuegt der Waechter ohne Beweis.
  *(Nutzerentscheidung 08.09.2026.)* Ueberlebt eine Mutation: **melden**,
  nicht nachbessern.
- **Keine Zeitschranke in die Suite.**
- Die Regressionstests **R7 bis R12** aus Nachtrag XI gehoeren hierher; T-147
  hat ihren Qt-freien Teil bereits gebaut und sagt das im Bericht.

## Testumgebung

Windows 10/11 x64, Branch `docs/audit-and-advisor-design`. Testbefehl nach
`CLAUDE.md`. **Suitezahl gegen 1490 passed, 9 skipped, 0 failed** (T-147).

**Alle drei Datenverzeichnisse umlenken und jede Umlenkung nachweisen** —
gelingt ein Nachweis nicht, abbrechen und nur das melden. **Das ist hier
besonders scharf:** dieser Auftrag schreibt QSettings-Schluessel, und im
Schluesselraum des Nutzers sind schon drei Datenverluste entstanden. Der
Spielstand ist read-only. Testabzug **kopieren**, nicht neu bauen.
Bildnachweise nur aus dem Programmfenster (NH-002).

## Scope-Grenzen

- Nicht: der langsame Rueckfallweg (V4), SEC-029 (V3/V4), das Mutationsregister.
- **Nicht anfassen:** `nrdata/`, `nrplanner/gamepath.py`, `UI_SPEC.md`,
  `ARCHITECTURE.md`, `security/findings.md`, `GOAL.md`, `docs/state.md`,
  `qa/findings.md`, `docs/tasks/`.

**Parallel laeuft nichts, und der Director committet waehrend deines Laufs
nicht.** *(In T-147 stand derselbe Satz und er stimmte nicht — der Director
hatte in derselben Nachricht einen zweiten Auftrag gestartet. Der Lauf hat es
gemeldet, und das war richtig. Diesmal gilt er.)*

**Beruehrt Dateien:** `nrplanner/firstrun.py`, `nrplanner/app.py`, `tests/`.

## Rueckgabeformat

Uebergabe-Kontrakt-Block plus Abschlussbericht. **Der Auftrag gilt erst als
erledigt, wenn `docs/berichte/T-149-developer.md` auf der Platte liegt.**
Commit mit Pfad hinter `--`.
