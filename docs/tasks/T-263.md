# T-263 — Pruefphase Zyklus 24 auf `478101c` (qa-engineer · security-reviewer · ui-ux-designer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`; Projektzeilen in der
`CLAUDE.md` des Projekts. **Code eingefroren auf `478101c`**; Register nur
anhaengend; kein Commit. Eine Fensterinstanz zugleich: `ui-ux-designer`
zuerst, `qa-engineer` Qt-frei bis `docs/berichte/T-263-ui-ux-designer.md`
existiert (oder 45 min). Autonomer Lauf: Annahmen AN-1..AN-8 in
`docs/state.md`; Rollen entscheiden Offenes selbst und nennen es.

**Seit T-251 (`528ff78`) gebaut:** A20 Zweihand (T-254b/T-255b/T-255c),
AK-294/295 Wortlaute (T-255a), Wurzelfix `conditionHp` (T-255d), 16 tote
Skips (T-254c), Bauwelle 2 (T-257a/b/c: QA-172/186/099a/175/036/173,
Flacker QA-249/238), DR-029 Arsenal-Zaehler (T-259), Kartencache (T-260),
`_rate_pair` (T-261), acht Audit-Schnitte (T-262). Suite 1681 passed / 9
skipped (Director, 03:52).

## T-263a — `qa-engineer` (opus)

1. **A20** (GOAL.md): sechs Zellen am Fenster (Kachel `{1H} / {2H} 2H AR`,
   Werteblatt, Arsenal), Umschalter 1H/2H je Build gespeichert ueber
   Neustart, Berater rankt nach Hand (AK-293: `when Two-Handing` nur bei
   2H), Schalterwechsel waehrend Optimize → 4.7-Satz.
2. **A18/A19-Regression** kurz (Markierung, Persistenz, Pflicht-Satz).
3. **Retest** QA-172, QA-186, QA-099a, QA-175, QA-036, QA-173 (AK-296),
   QA-269, QA-270 (inkl. Mischfall), DR-029, QA-258 (Zahlen), QA-249/238
   (3 Volllaeufe gruen — Hook-Grenze: zwei Volllaeufe + ein Teillauf).
4. `MUTATIONS` nachfahren und loeschen (OF-35). A2-A8 bestaetigen.
**Schreibt:** `docs/berichte/T-263-qa-engineer.md`, `qa/findings.md`.

## T-263b — `security-reviewer`

Diff `528ff78..478101c -- nrplanner nrdata scripts`: `chalices.py` Hand-Feld
im Store (Parser fremder Werte), `iconbuild.write_manifest` temp+`os.replace`
und `manifest_files()` (Pfade aus Manifest → Dateisystem: Traversal?),
`firstrun._a_promised_icon_is_gone`, `app._move_onto_the_screen`, neue
Texte (`vessel_tooltip`, Gate-Wortlaute, `field_label`) als Rich-Text?
SEC-019-Klasse. Antwort im Nachrichtentext.

## T-263c — `ui-ux-designer` (Review)

Nachholen aus T-258: alle sieben Tabs bei **1536 px** und die vier dort
nicht geprueften Content-Tabs bei 1608 px, PrintWindow je Tab (Umlenkung
`DankYeeterT-263`); DR-028 als Messartefakt schliessen (T-259-Beleg), DR-029
Retest, DR-030 geschlossen (AN-8); AK-292 Schalter live, AK-286 Kachel,
AK-296 Tooltip, AK-294/295 Texte. `DESIGN_REVIEW.md`, Spec-Nachtraege nur
bei Widerspruch. **Bericht `docs/berichte/T-263-ui-ux-designer.md` sofort
nach dem Fensterlauf ablegen.**

## Rueckgabe (alle)

Kontraktblock + max. 25 Zeilen.
