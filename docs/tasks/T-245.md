# T-245 — Picker-Suche: Operatoren nur in GROSS, Fluchtext mitsuchen, Hinweis am Feld (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen in der
`CLAUDE.md` des Projekts; Abweichungen: keine.

**Ziel:** `not`, `and`, `or`, `-wort` in Kleinschreibung sind Suchtext;
nur `NOT`, `AND`, `OR` (und `"..."`) sind Operatoren. Fluchnamen sind Teil
des durchsuchten Texts. Das Suchfeld sagt seine Syntax.

**Stufe/Modus:** klein

**Nutzerentscheidungen 14.09.2026 19:55 (woertlich die gewaehlten Optionen):**
QA-263 "Nur GROSS als Operator — `NOT`, `AND`, `OR` in Grossbuchstaben sind
Operatoren; `not`, `-afflicted` werden als Text gesucht. README nennt das."
· QA-264 "Ja, mitsuchen — Fluchname im Haystack; `poise NOT curse` aus der
README funktioniert weiter ueber den Fluchnamen."

**Stand (T-244, `docs/berichte/T-244-qa-engineer.md`):** Suchpfad
`search.parse(text)` → Teilstring, lower, gegen
`" ".join(slot.effect_names(item))` (`relicslots.py:556`); `NOT_WORDS`,
`startswith("-")` in `nrplanner/search.py`; `but not for self` → 0 Treffer
statt 4, `-afflicted` → 206 statt 4; Fluchtext bei 76/315 Kopien nicht
durchsucht.

## Vorgaben

1. `nrplanner/search.py`: Operatorerkennung case-sensitiv auf `AND`, `OR`,
   `NOT`; `&`, `+`, `!` und fuehrendes `-` pruefen — Director-Entscheid:
   `-wort` und `!wort` **bleiben** NOT (kein Wort beginnt so), `&`/`+`
   bleiben AND. Kleingeschriebene Woerter sind Text. Ein Test je Fall aus
   T-244 (`but not for self` → 4, `-afflicted` → 4 bzw. 6 Deep, `NOT` gross
   weiter NOT).
2. Haystack in `relicslots.py:556` (oder wo `effect_names` fuer die Suche
   gejoint wird) um die Fluchnamen der Kopie ergaenzen; Test: `All
   Resistances Down` (nur Fluch) → 3 Deep-Treffer. Pruefen, ob der Weapons-
   Tab oder andere Nutzer von `search.parse` mitziehen sollen — nur melden.
3. Suchfeld-Platzhalter (A8, Director-Wortlaut, Spec-Nachtrag folgt):
   `Search effects and curses — AND, OR, NOT, "exact phrase"` — nur, wenn
   das Feld heute keinen Platzhalter hat; sonst melden. QA-265.
4. `README.md` §Searching: Operatoren gross schreiben, `poise NOT curse`
   erklaeren (Fluchname), Kleinschreibung = Text.

## Umfang

**Beruehrt Dateien:** `nrplanner/search.py`, `nrplanner/relicslots.py`,
`nrplanner/relicpicker.py` (nur Platzhalter), Tests, `README.md` ·
**Parallel:** nein · **Scratchpad:** `<scratchpad>/T-245/`. Ein Commit.

## Praemissen

- Suite auf `70bbe3d`: 1532 passed / 9 skipped · Director · 19:36
- T-244-Zahlen gegen Spielstand-Kopie 315 Kopien / 314 Rolls · qa-engineer · 19:48

## Rueckgabe

Kontraktblock + max. 15 Zeilen: Commit, Testzahlen, Platzhalter ja/nein.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
