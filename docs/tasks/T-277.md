# T-277 — A21: Effektfilter-Fenster Favourite/Avoid, Punkte an den Effektzeilen entfallen

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`; Projektzeilen in der
`CLAUDE.md` des Projekts.

**Abnahmekriterium (GOAL.md A21, 15.09.2026, Nutzer woertlich):** "liefer ein
Filter Symbol und darin dann Favourite und Avoid marker. eine liste nur von
effekten die ich habe. nicht gruppiert pro relikt oder so. curses ebenfalls"
— und 12:30: "nimm die alternative. komplett raus." (Punkte an den
Effektzeilen im Picker und im Why-Dialog entfallen, das Fenster ist der
einzige Weg).

**Vorgabe:** `UI_SPEC.md` §3.6, AK-300 bis AK-311 (Commit `33f8ecb`).
Datenweg bleibt AD-036: `nrplanner/effectfilters.py` (`stored`, `store`),
`SlotProblem.excluded/required`, AK-289-Zweig in `the_build_changed`.
Kein Umbau der Rechnung.

## T-277a — `ui-ux-designer` (erledigt, `33f8ecb`)

## T-277b — `developer` (Worktree): Fenster + Knopf

Neu `nrplanner/effectfilterdialog.py` (AK-303..309, AK-311): eine Zeile je
Effekt-/Fluch-Id ueber alle besessenen Kopien, zwei disjunkte `QCheckBox`
(Favourite/Avoid), Suchfeld mit AK-275-Syntax (Parser aus `relicpicker.py`
wiederverwenden, nicht kopieren), Zaehler AK-307, drei Leerzustaende AK-309,
alphabetisch AK-308. `nrplanner/advisorbar.py`: `Filters`-Knopf (AK-302) in
allen 14 Zustaenden aktiv, Zaehler-Tooltip-Klauseln auf Favourite/Avoid
(AK-300). Speichern ueber `effectfilters.store`, danach derselbe Weg wie
bisher beim Markieren (AK-289). Tests `tests/test_effectfilterdialog.py`
neu, `tests/test_advisor_bar.py` anpassen. Hoechstens fuenf Dateien.

## T-277c — `developer` (Worktree): Punkte raus, Umbenennung

`nrplanner/relicpicker.py`: `MarkedLine`/`MarkButton` und die Ids/Zustaende
der `RelicCard` entfernen (AK-301) — Karte zeigt Effektzeilen wieder als
Text; `nrplanner/advisorblock.py`: `WhyDialog` zeigt Favourite/Avoid nur
lesend (kein `QToolButton`), Legende AK-301 woertlich aus der Spec;
`nrplanner/advisor/types.py`, `nrplanner/effectfilters.py`: Wortlaute
Must include/Don't include → Favourite/Avoid (AK-300; Schluessel
`advisor/excluded`/`advisor/required` **unveraendert**, gespeicherte
Markierungen bleiben gueltig). Tests `test_relic_picker_advisor.py`,
`test_advisor_block.py`, `test_advisor_explain.py`, `test_effectfilters.py`
anpassen; `MUTATIONS`-Eintraege, die auf Entferntes zeigen, loeschen.
Projektweite Suche nach den alten Wortlauten in `nrplanner/` und `tests/`
ins Protokoll (README macht der `technical-writer`). Hoechstens fuenf
Code-Dateien plus Tests.

**Parallel:** b und c disjunkt (b: neues Modul + `advisorbar.py`; c:
`relicpicker.py`, `advisorblock.py`, `advisor/types.py`, `effectfilters.py`).
Wer den anderen braucht: `blockiert` melden, nicht hinuebergreifen.
Fensterlauf nur mit Datenumlenkung (`NIGHTREIGN_SETTINGS_ORG=DankYeeterT-277x`,
Testabzug **kopieren**); die Instanzsperre ist maschinenweit (QA-256) —
Fensterlaufe nur in b, c bleibt headless (`QT_QPA_PLATFORM=offscreen`).
Gezielte Tests ohne `-n`; volle Suite misst der Director nach dem Merge.
**Scratchpad:** `<scratchpad>/T-277/<rolle>/`.

## Rueckgabe (alle)

Kontraktblock + max. 30 Zeilen.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
