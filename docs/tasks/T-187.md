# T-187 — sync-out nach Zyklus 19 (archivist)

**Modus: `sync-out`.**

**Ziel:** Den abgeschlossenen Zyklus 19 auf das Remote bringen. Projekt-Repo
und Agenten-Repo.

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`.
Es gelten die Projektzeilen aus `CLAUDE.md`, Abschnitt "Projektzeilen fuer
jeden Auftrag".

## Kontext

Zyklus 19 (Dokumentenstruktur) ist abgenommen. **Es laeuft kein anderer
Agent** — das ist eine Zusicherung des Directors, keine Vermutung: T-180 bis
T-186 sind alle zurueck und ihre Ergebnisse committet.

Stand beim Erteilen, gemessen 12.09.2026: **15 Commits ungepusht**
(`git rev-list --count @{upstream}..HEAD`), Arbeitsbaum sauber
(`git status --porcelain` leer). Branch `docs/audit-and-advisor-design`.

**Wichtig zur Repo-Kategorie:** `CLAUDE.md` nennt als einziges erlaubtes Remote
`https://github.com/DankYeeter/Nightreign-Helper.git`. Pruef `git remote -v`
dagegen, bevor du pushst. Weicht es ab, **nicht pushen, sondern melden**.

**`main` ist geschuetzt.** Du pushst auf `docs/audit-and-advisor-design`, nicht
auf `main`. PR #16 ist offen und bleibt offen — **der Merge gehoert dem
Nutzer**, nicht dir und nicht dem Director.

**Abnahmekriterium aus GOAL.md:** dieser Auftrag dient keinem Kriterium
unmittelbar.

**Stand laut docs/state.md** (12.09.2026, Director): *"2026-09-12, **Zyklus 19
abgeschlossen** (Dokumentenstruktur). Branch `docs/audit-and-advisor-design`,
`main` geschuetzt, PR #16 offen — **Merge gehoert dem Nutzer.**"*

## Umfang

**Beruehrt Dateien:** keine im Projekt. Du committest nichts Fremdes — alles
Anstehende ist bereits committet.

**Parallel:** nein. Du laeufst allein, so wie immer.

## Vorgaben

- `fetch`, dann `pull --ff-only` bzw. `--rebase` auf sauberem Baum, dann
  `push`. Kein `--force`, kein `reset --hard`.
- **Das Agenten-Repo unter `%USERPROFILE%\claude-agent-team` existiert auf
  dieser Maschine nicht** — `ls` am 12.09.2026 meldet "No such file or
  directory", obwohl der SessionStart-Hook "agent-team (main): synchron"
  ausgegeben hat. **Das ist ein Widerspruch, den ich nicht aufloesen konnte.**
  Find heraus, was der Hook tatsaechlich geprueft hat, und melde es. Ist dort
  nichts zu synchronisieren, sag das — aber sag es belegt.
- Faellt der Push auf eine Branch-Protection (GH013), ist das **keine
  Stoerung**: melde `teilweise` mit `BLOCKIERT DURCH: Branch-Protection` und
  uebergib an den Director.
- Fehlende `.gitignore`-Eintraege schlaegst du **vor**, du traegst sie nicht
  ein.

## Testumgebung

Kein Programmstart, kein Bau. Scratchpad: **nur** `…/scratchpad/T-187/`.

## Scope-Grenzen

- **Kein Merge**, kein Tag, kein Release.
- Kein `checkout`, kein `branch` — das ist dir verboten, und zwar damit bei
  ungespeicherten Aenderungen nichts verlorengeht.
- Konflikte in Anwendungscode loest du nicht; du meldest sie.

## Widersprich, wenn der Auftrag nicht stimmt — Pflicht

**Widerspricht eine Aussage in diesem Auftrag dem, was du im Code, in den
Dateien oder in deiner Messung vorfindest, ist das ein Befund.** Melde ihn,
auch wenn er nicht zu deiner Aufgabe gehoert, und arbeite auf dem, was du
vorgefunden hast — nicht auf dem, was hier steht.

Das gilt fuer Zahlen, Befundstatus, Dateilisten, Behauptungen ueber andere
Rollen und fuer "existiert nicht". **Der Director ist die einzige Rolle ohne
Pruefer** — seine Auftragstexte sind das, was am seltensten gegengelesen wird.

## Rueckgabeformat

**Der Bericht steht in der Datei, nicht in der Antwort.** Schreib ihn
vollstaendig nach `docs/berichte/T-187-archivist.md` und gib in deiner Antwort
**nur** zurueck:

1. den Uebergabe-Kontrakt-Block (unten, vollstaendig),
2. dein Ergebnis in **hoechstens dreissig Zeilen** — Urteil, Zahlen,
   Befunde, offene Entscheidungen,
3. den Pfad der Berichtsdatei.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEAENDERT:
ANNAHMEN:
NAECHSTER:
BLOCKIERT DURCH:
```
