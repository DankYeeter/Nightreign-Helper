# T-300 — A24 Entwurf: Unterbosse im Nightlords-Tab, beide Stufen, plus QA-286 (architect)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen stehen
in der `CLAUDE.md` des Projekts. Stand `c03fd9d` plus ungecommittet:
`GOAL.md` Nachtrag A24, `docs/berichte/T-299-developer.md`, QA-286.
Nutzerentscheidung 19.09. 13:10: beide Stufen und QA-286 in einem Entwurf.

**Ziel:** Ein Entwurf in `ARCHITECTURE.md` (neue AD-Nummer ab AD-040,
Register nachziehen), der dem `developer` den Bau von A24 in einzeln
lauffaehigen Schritten vorgibt. Grundlage ist ausschliesslich der belegte
Befund aus T-299 (Abschnitt 7 nennt die Leseroute); nichts daraus neu
herleiten, Luecken als Frage benennen.

**Stufe/Modus:** architect: Entwurf, kein Anwendungscode.

## Zu entscheiden

1. **Extraktor.** Wo die neue Leseroute liegt (`LotResultPlayAreaParam`,
   `LotResultSmallBaseAndSpot`, MSB `PARTS_PARAM_ST` je Karte, Einstieg in
   `bossdata` ueber Karten-Id statt `defeatEventFlag`), wie der
   Snapshot-Block heisst und ob `EXTRACT_VERSION` steigt (dann gilt
   `CLAUDE.md`: Testabzug ersetzen, `docs/plan-restarbeiten.md`).
   QA-286 wird auf derselben Route behoben: `kinds` liest Orte, nicht
   Figuren; die Deep-of-Night-Beschriftungen folgen dem Befund.
2. **Erstlauf-Kosten.** T-299 hat 199 MSB in rund 4 Minuten gelesen
   (einmal gemessen); Stufe 1 braucht ~120 Karten, Stufe 2 weitere 35.
   Vorgabe: Messauftrag an den `performance-tuner` **vor** dem Bau als
   eigener Schritt formulieren (was gemessen wird, Grenzwert fuer den
   Erstlauf), nicht selbst schaetzen.
3. **Bosswahl je Karte.** In 4 von 35 Nachtboss-Karten und in den
   Evergaol-Karten stehen mehrere boss-grosse Figuren. Regel festlegen
   oder als Pruefpunkt mit den Kandidaten `bossdata._tuned`,
   Gruppenboss-Regel, `variationId`/`mapIndex` (ungeprueft) an den
   `developer` geben; Anzeige bei Unklarheit nach A7 ("nicht belegt"
   statt Auswahl).
4. **Tab.** Wie die Unterbosse in `nrplanner/bosstab.py` landen (zweite
   Liste vor dem Stretch in `grid_outer`, Detailpanel generisch, siehe
   Explore 19.09.): Gruppierung nach Nachtfuerst und Tag, Feld/Evergaol
   getrennt, HP und Beute neu im Panel. UI-Wortlaut an den
   `ui-ux-designer` (Spec-Modus) als Folgeschritt benennen, nicht selbst
   festlegen.
5. **Schrittfolge.** Stufe 1 und Stufe 2 als getrennt abnehmbare
   Schritte, QA-286 im ersten; Tests je Schritt (Wächtertest, dass
   `NightreignHelper.spec` weiter zwei Quellen zaehlt, bleibt).

## Umfang

**Beruehrt:** `ARCHITECTURE.md`, `ARCHITECTURE_REGISTER.md`,
`docs/berichte/T-300-architect.md` bei Befunden · **Parallel:** nein ·
**Scratchpad:** `<Scratchpad>/T-300/`

## Scope-Grenzen

Kein Anwendungscode, keine Messung, kein neuer Param-Fund (dafuer war
T-299). Kein Netzwerk, keine Spieldaten ins Repo.

## Praemissen — jede Bestandsbehauptung mit Quelle

- Leseroute und Tabellen · `docs/berichte/T-299-developer.md` Abschn. 3,
  4, 7 · geprueft (Messung 520/520, 116/116).
- `_event_drops` greift unveraendert · Bericht Abschn. 5 · geprueft
  (sechs Stichproben).
- Testabzug und Cache `extract_version` 12 mit `kinds`/`counts` · Bericht
  Abschn. 6 · geprueft.
- 4 Minuten je 199 MSB · Bericht Abschn. 8 · einmal gemessen, nicht
  wiederholt.
- `bosstab.py` Aufbau · Explore 19.09. (Zeilen 587-655, 1134-1166) ·
  geprueft.

## Rueckgabe

Kontraktblock plus hoechstens fuenfzehn Zeilen.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
