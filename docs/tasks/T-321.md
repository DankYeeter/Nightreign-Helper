# T-321 — A25 Umsetzung: Schadensart im Berater (developer, drei Abschnitte nacheinander)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen in der
`CLAUDE.md` (Umlenkung Pflicht, `NIGHTREIGN_SETTINGS_ORG` `DankYeeterT-321`;
Testabzug kopieren; NH-004 ein Fensterlauf je Zeitpunkt). **Start erst,
wenn der Baum frei ist** (T-313 der anderen Sitzung: Release 1.15.0
abgeschlossen; `git status` leer, kein Agent laeuft). Stufe: normal.

**Abnahmekriterium (GOAL.md A25, woertlich):** "Revenant (Startwaffe Cursed
Claws, 71,63 von 88,65 AR Magic) mit Auswahl 'Magic' bzw. 'Incantations'
liefert eine andere Reihung als 'Alle', und die Why-Zeile nennt die Art;
Wylder mit beiden Startwaffen-Relikten und Auswahl 'Skill attack' zaehlt
die Konversion in die Schadenszahl." Stand: nicht erfuellt.

## Vorgaben (bindend)
- Entwurf: `ARCHITECTURE.md` Themenbereich M, **AD-045..AD-049** inkl. der
  neunpunktigen Verbotsliste; Schritte A25-1..A25-5 in dieser Reihenfolge.
- Spec: `UI_SPEC.md` §3.7, **AK-327..AK-334**.
- Nutzerentscheide 19.09. 16:25: OF-50 Nachweis Revenant/`Magic`; **OF-51
  ja** — unter einer Schulwahl zaehlt der allgemeine Sorcery-/
  Incantation-Buff multiplikativ mit (AD-046 "vorlaeufig nein" ist damit
  aufgehoben; Umsetzung: Art-Faktor = Produkt aus Schul-Skalar und
  Gattungs-Skalar); OF-52 Annahme nein, AD-048 bleibt; Auswahl faellt bei
  Neustart auf `All` zurueck (keine Persistenz).
- Praemisse (Nutzer): Scope 112/111 = Weapon Arts; Nightfarer-
  Faehigkeiten (`characterSkillAttackRate`) bleiben unbeachtet.
- Sicherheit (Vorlauf 19.09.): Scope nur `int` und nicht `bool`, Etikett
  nur nicht-leerer `str`, unbenannte Scopes nie anbieten; Raten mit
  `math.isfinite` und Obergrenze pruefen (NaN zerstoert die Rangfolge
  still, T-077); `damage_art` immer `str` in Id-Form (`family:23`,
  `art:skill`), nie Etikett, immer im `cache_key`, und in
  `run._refuse_a_request_that_asks_about_another_run` aufgezaehlt;
  Etiketten sind Spieltext: jede neue Senke (Auswahl, Tooltip, Why,
  `unknowns`) `html.escape`/PlainText beim Bauen (SEC-019).
- Compliance (gruen): Tests tragen Ids/Schluessel, keine Effekt-Captions
  als Fixture-Prosa (A-001); keine Handtabelle Scope→Name ueber die vier
  bestehenden Fallbacks hinaus.

## T-321a — model + damage (A25-1, A25-2)
Beruehrt: `nrplanner/model.py`, `nrplanner/damage.py`,
`tests/test_move_scoped_effects.py`, `tests/test_damage_art.py` (neu).
M1 Bestand bitgleich (`art=None`), M2 Zuordnungswaechter, M3 neu.

## T-321b — Anfrage, Kontext, Ziel, Saetze (A25-3, A25-4)
Beruehrt: `nrplanner/advisor/types.py`, `run.py`, `goals.py`,
`candidates.py`, `tests/test_advisor_goals.py`. M4 Rangfolge +
Konversionsanker; die zwei falschen Konversionssaetze loeschen (AD-047).
Liest T-321a.

## T-321c — Leiste (A25-5)
Beruehrt: `nrplanner/advisorbar.py`, `tests/test_advisor_bar.py`,
Guide-Zeile `docs/anleitung/guide.md` nur wenn ein Satz reicht. AK-334:
Breitenfolge messen und im Bericht nennen. Fensterlauf per
`scripts/drive_window.ps1`. Liest T-321b.

## Rueckgabe
Kontraktblock; Suite-Zahl gemessen; Breite gemessen; Bericht
`docs/berichte/T-321-developer.md` nur bei Befund oder teilweise.

## Erledigt 19.09.2026
T-321a `424baa3` · b `717ec29` · c `6c1718b` · d `78a0888` (Option B). Suite 1827/9.
