# T-236 — A12/A13 auf den Datentabs: Einheiten und nichts abgeschnitten (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen
(Testbefehl, Datenverzeichnisse, verbotene Zugriffe, Zielsystem) stehen in
der `CLAUDE.md` des Projekts; Abweichungen fuer diesen Auftrag: keine.

**Ziel:** Die fuenf Datentabs (Effects, Nightlords, World events, Depths,
Deep of Night) nennen zu jeder Zahl Einheit und Bezug und schneiden am
Standardmass nichts ab. Befunde QA-128, QA-140, DR-013, DR-014, DR-015.

**Stufe/Modus:** normal

**Abnahmekriterium (GOAL.md, woertlich):** A12 "Jede Zahl und jede
Beschriftung auf diesen Tabs nennt ihre Einheit und ihren Geltungsbereich."
A13 "Die sechs Tabs sind unter sich gestalterisch konsistent und in sich
fehlerfrei: [...] nichts abgeschnitten, kein Wortumbruch mitten im Begriff,
keine waagerechte Bildlaufleiste am Standardmass. Nachweis am laufenden
Fenster mit Screenshots, nicht am Code."

**Stand (docs/archiv/state-bis-2026-09-12-zyklus19.md:65, woertlich):**
"A12/A13 | Einheiten, Gestaltung | 4 bzw. 3 von 6 Tabs".

## Kontext

Die Befunde stammen vom 05.09. (QA-128, QA-140, DR-013 bis DR-015); seither
sind 20+ Bauauftraege gelaufen, und die Registerzeilen bis `qa/findings.md:269`
wurden nie fortgeschrieben. **Ein Teil kann bereits behoben sein.** Darum:

**Pruefregel je Befund, zuerst:** am laufenden Fenster (PrintWindow, Standard-
mass 1536 px logisch, `fusion`) reproduzieren. Reproduziert → fixen. Nicht
reproduziert → als "bereits behoben" melden **mit Bild und Commit** (`git log
-S`), nichts aendern.

## Vorgaben

- Befundtexte: `qa/findings.md:150` (QA-128, 10 Belegstellen — A7-Fall
  `Reward multiplier x1.47` bleibt A7: Bezugsgroesse nennen oder sagen, dass
  sie unbekannt ist, nicht raten), `:162` (QA-140); `DESIGN_REVIEW.md:455`
  (DR-013), `:473` (DR-014), `:496` (DR-015). Dort stehen die AK-Nummern.
- A8: alle Texte Englisch. A7-Hausregel gilt.
- Ein Fix, der einen Befund schliesst, bekommt einen `MUTATIONS`-Eintrag in
  `scripts/differential/mutate.py` (AD-033), Suite muss ihn toeten.
- Ein Waechter je Befund: der kleinste Test, der rot wird, wenn der Fix
  zurueckgenommen wird. Screenshot-Nachweis fuer A13 ins Scratchpad.

## Testumgebung

`CLAUDE.md`. Fensterlaeufe nur mit Datenumlenkung; Testabzug kopieren.

## Umfang

**Beruehrt Dateien:** `nrplanner/effectstab.py`, `nrplanner/bosstab.py`,
`nrplanner/eventstab.py`, `nrplanner/depthstab.py`, `nrplanner/deeptab.py`,
`scripts/differential/mutate.py`, Tests · **Parallel:** ja — T-235 haelt
`app.py`, T-237 haelt `relicpicker.py`, `arsenaltab.py`, `advisorbar.py`,
`advisorblock.py`; **diese Dateien nicht anfassen.** Braucht ein Fix
`app.py`: melden, nicht bauen. · **Scratchpad:** `<scratchpad>/T-236/`

**Schritt 0:** Worktree auf `origin/HEAD` (`49b5a78` oder juenger), sonst
`blockiert`.

## Scope-Grenzen

Kein neues Layoutkonzept, keine Spec-Aenderung (Widerspruch zur `UI_SPEC.md`
melden). Build planner und Weapons-Tab gehoeren T-237. Andere offene
Befunde derselben Tabs (QA-129, QA-132, QA-133, QA-130, QA-131 ...) nur
melden, wenn sie beim Reproduzieren mit auffallen.

## Praemissen — jede Bestandsbehauptung mit Quelle

- Standardmass ist 1536 px logische Breite · `UI_SPEC.md` AK-271 (T-233) · 13.09.2026
- Fenster-Stil `fusion` · QA-146 (`qa/findings.md:168`) · Waechter messen `windowsvista` — **Fenster mit dem Programmstil messen**
- Suite auf `49b5a78`: 1531 passed / 9 skipped · Director 13.09.2026

Widerspricht der Bestand einer Aussage dieses Auftrags, ist das ein Befund:
melden und auf dem Bestand arbeiten.

## Rueckgabe

Antwort: Kontraktblock plus hoechstens dreissig Zeilen; je Befund eine Zeile
`behoben <hash>` | `bereits behoben <hash>` | `offen, weil`. Datei
`docs/berichte/T-236-developer.md` bei Befunden oder `teilweise/blockiert`.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
