# T-230 — Fixauftrag nach der Pruefphase T-229 (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen
(Testbefehl, Datenverzeichnisse, verbotene Zugriffe, Zielsystem) stehen in
der `CLAUDE.md` des Projekts; Abweichungen fuer diesen Auftrag: Schritt 0
unten (Worktree-Stand); **ein** Auftrag fuer alle Befunde der Pruefphase
(Regel: gebuendelt, danach ein Retest), deshalb mehr als fuenf Dateien —
sieben Abschnitte, je ein Commit.

**Ziel:** Alle Befunde aus T-229 (QA FAIL mit einem P2, UI-Review DR-022
kritisch, Security SEC-043 niedrig) sind behoben; Suite gruen und
reproduzierbar.

**Stufe/Modus:** developer, normal

**Abnahmekriterien (GOAL.md, woertlich):** A6 "Oberflaeche blockiert nicht"
· A7 Datenlage nennen · A8 Englisch ohne Ausnahme · A16 "jede Zahl sagt,
welche gerade gilt, und die sieben bedingten Flueche seines Spielstands
bewegen im schlechtesten Fall die Rangfolge nachweislich".

**Stand (docs/state.md, 13.09.2026):** Pruefphase T-229 auf `719c46d`
durch: Security PASS, UI "braucht Arbeit", QA FAIL (ein P2). Registry seit
`43edd95` leer.

## Kontext

Berichte: `docs/berichte/T-229-qa-engineer.md` (Befunde 1-6, Retests),
`T-229-ui-ux-designer.md` + `DESIGN_REVIEW.md` DR-022/DR-023 (Z. 60, 120),
`T-229-security-reviewer.md` F1. Registerzeilen vom 13.09.: `qa/findings.md`
QA-250, QA-251, QA-232 (teilweise), QA-249, QA-252; `security/findings.md`
SEC-043. Mess-Skript `<scratchpad>/T-229/qa/row_at_room.py` (falls noch da;
sonst `tests/advisor_row_at_the_window.py` um `room` erweitern — QA-Vorschlag:
"mit einem `room`-Parameter waere Befund 1 ein Dreizeiler im Waechter").

## Vorgaben — sieben Abschnitte

### T-230a — QA-250 (P2), Nutzerentscheidung 13.09. woertlich: "Boxen haben Vorrang, Statuszeile darf verschwinden"

`nrplanner/advisorbar.py`: unter der abgeleiteten Startbreite bekommen
Zielwahl und Lesart-Box ihre volle Breite (kein `cut`), die Statuszeile
schrumpft bis 0 px (Stretch-/Groessenpolitik, kein Umbau), ihr voller Text
steht im Tooltip **der Leiste** (nicht nur des 0-px-Labels). AK-05 haelt in
jedem `room`; AK-194 (> 0 px) gilt nur ab der abgeleiteten Breite. Waechter:
`tests/advisor_row_at_the_window.py` bekommt `room`; Faelle `room=1366`,
`room=1536` und Laufmaschine; rot-vorher bei 1366 (`cut == ['goal_box',
'reading_box']`). Wortlaut der AK schreibt der `ui-ux-designer` parallel
(T-231) — du baust nach diesem Absatz.

### T-230b — DR-022 (kritisch) + QA-251

`relicpicker.py:1006`: `stock_replaced` setzt `self.ranking` zurueck (und
was sonst am Bestand haengt), sodass der offene Picker nach Rescan ohne
Fund den Leerzustand AK-212 zeigt (leere Kopfzeile, `No save was read …`
AK-265, keine Custom-Kachel). `app.py` `_the_save_has_been_read`: am Ende
die Leiste neu ruhen lassen (`the_build_changed()` / `_show(_resting())`),
sodass 4.8/AK-268 steht (Zielwahl, Lesart, Optimize deaktiviert). Tests:
T-225c-Test um Kopfzeile erweitern; Leisten-Test um den Rescan-Uebergang.

### T-230c — QA-232 Rest

`nrdata/dcx.py:74` (`NotImplementedError`), `:79` (`ValueError` size
mismatch), `dds.py:97/99/103`, `bnd4.py:192` → `NotWhatItClaims`, wo es eine
Aussage ueber die Datei ist (alle sechs laut QA). Danach beide QA-Masken
(Bericht Befund 3) leer. Test: eine dieser sechs kommt woertlich durch
`in_english`.

### T-230d — SEC-043

`nrdata/tpf.py:51-54`: `{name!r}` durch den Mitgliedsindex ersetzen;
`dds.py:69`: die vier Dateibytes aus dem Satz streichen (wie SEC-041-Richtung).
Waechter: kein `NotWhatItClaims(f"...{<dateiinhalt>!r}...")` mehr — die
zweite Maske des `security-reviewer` (`!r}` in `raise NotWhatItClaims`,
3-Zeilen-Fenster) darf nur noch Konstanten und Paramdef-Namen treffen.

### T-230e — QA-252

`tests/test_advisor_goals.py:1114` `COPIES_THE_WORST_CASE_MOVES = 11` haengt
am lebenden Spielstand (heute 314 Kopien). Eingefrorener Inventar-Fixture
(Handles, Effekt-/Fluch-Ids der Kopien, **keine** Spielstanddatei, kein
Spielstand im Repo — nur Ids und Zahlen, keine Namen aus `nightreign_data`)
traegt das Literal; der Live-Lauf behaelt nur die qualitativen Zusagen
(Rangfolge bewegt, jede bewegte Kopie traegt einen Fluch). Auf fremden
Maschinen kein `skip` mehr fuer die Literalhaelfte.

### T-230f — QA-249

Die drei zeitabhaengigen Tests (`test_save_read_in_the_background.py:194`,
`test_save_path_memory.py::test_a_window_whose_picked_file_is_gone_says_nothing_about_it`,
`test_advisor_worker.py:260`) auf Zustaende statt Zeitspannen (Ereignis
statt `delay`); ist das bei einem nicht klein, stattdessen eigene
xdist-Gruppe (`@pytest.mark.xdist_group`) und im Bericht sagen, warum. Ziel:
drei `pytest -n auto`-Laeufe hintereinander gruen.

### T-230g — DR-023

`advisorbar.py:451` `_ElidingLabel`: `accessibleName` gesetzt, Tastaturroute
zum vollen Text (Fokus oder `accessibleDescription` = `whole_text()`), ohne
neue UI. Test: Accessible-Name und -Beschreibung tragen den vollen Text bei
67 px.

Mutationen: je Abschnitt eine toetende in `MUTATIONS` (AD-033 Punkt 4),
fahren, Bericht. **Nicht** in diesem Auftrag: QA-253 (Wortlaut, Spec),
DR-024 (nice-to-have), AK-160 (Spec).

## Testumgebung

Windows 11, `pytest <datei>` gezielt, am Ende **drei** Laeufe `pytest -n auto`
(QA-249). Datenumlenkung nach `CLAUDE.md`, `NIGHTREIGN_SETTINGS_ORG=DankYeeterT-230`,
Testabzug **kopieren**. Spielstand read-only.

## Umfang

**Beruehrt Dateien:** `nrplanner/advisorbar.py`, `relicpicker.py`, `app.py`;
`nrdata/dcx.py`, `dds.py`, `bnd4.py`, `tpf.py`; Tests wie je Abschnitt,
`tests/advisor_row_at_the_window.py`, `tests/conftest.py` (Fixture);
`scripts/differential/mutate.py`. · **Parallel:** ja — T-231
`ui-ux-designer` nur `UI_SPEC.md`/`UI_SPEC_REGISTER.md`. · **Scratchpad:**
`<scratchpad>/T-230/`

**Schritt 0 (Worktree):** `git branch --contains 43edd95` muss den
ausgecheckten Branch nennen. Sonst `STATUS: blockiert`, nichts aendern.

Reihenfolge a → b → c → d → e → f → g, ein Commit je Abschnitt, kein Push.
Zugschwelle: bei `teilweise` Stand je Abschnitt und Suitezahlen des letzten
Laufs.

## Scope-Grenzen

- Keine zweite Zeile in der Leiste, keine Aenderung an `GOAL_BOX_WIDTH`,
  Zieltexten, Lesart-Logik (AD-035), `_opening_width` (T-227 bleibt).
- Kein Spielstand, kein Relikt-Name im Repo (Fixture: Ids und Zahlen).
- `UI_SPEC.md`, `ARCHITECTURE.md`, `DESIGN_REVIEW.md` nicht anfassen.

## Praemissen — jede Bestandsbehauptung mit Quelle

- Messwerte 1366/1536/1600/1608 px · T-229a Befund 1, `row_at_room.py` ·
  13.09.2026
- `relicpicker.py:1006` `stock_replaced.connect(self._refresh)` · `grep -n` ·
  13.09.2026
- Sechs Verweigerungen (`dcx.py:74/79`, `dds.py:97/99/103`, `bnd4.py:192`) ·
  T-229a Befund 3 · 13.09.2026 — nachzaehlen
- `tpf.py:51-54` `{name!r}`, `dds.py:69` `{dds[:4]!r}` · T-229b F1 · 13.09.2026
- `COPIES_THE_WORST_CASE_MOVES = 11` `test_advisor_goals.py:1114` · `grep -n`
  · 13.09.2026; Spielstand 314 Kopien (T-229a, `inventory.load`)
- QA-249: 2 Laeufe je 1 failed, drei verschiedene Tests · T-229a Befund 4 ·
  13.09.2026
- Suite auf `719c46d` (Registry noch voll): 1519 passed / 9 skipped · Director
  · 13.09.2026; nach `43edd95` (Registry leer) erwartet 1509 / 10 — **dein
  Lauf misst**

Widerspricht der Bestand einer Aussage dieses Auftrags, ist das ein Befund:
melden und auf dem Bestand arbeiten. Eine tragende Praemisse ohne Quelle darf
die Rolle zurueckweisen.

## Rueckgabe

Antwort: Kontraktblock plus hoechstens dreissig Zeilen: je Abschnitt Status,
Test, Rot/Gruen, Zahlen (a: Statuszeile/`cut` bei 1366/1536/1608; f: drei
Suitelaeufe), Mutationen, Commit-Shas. Datei
`docs/berichte/T-230-developer.md` bei Befunden oder `STATUS:
teilweise/blockiert`.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
