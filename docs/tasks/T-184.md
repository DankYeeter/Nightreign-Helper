# T-184 — UI_SPEC konsolidieren, das Register steht schon (ui-ux-designer)

**Modus: Spec.** Kein Review. Nachtrag zu T-181.

**Ziel:** Lieferung 2 aus T-181 nachholen — `UI_SPEC.md` thematisch ordnen und
den chronologischen Verlauf abtrennen. Das Register ist fertig.

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`.
Es gelten die Projektzeilen aus `CLAUDE.md`, Abschnitt "Projektzeilen fuer
jeden Auftrag".

## Kontext

T-181 (`docs/tasks/T-181.md`, dein Auftrag — lies ihn, die Vorgaben gelten
unveraendert) brach am 12.09.2026 mit "Connection lost mid-response" ab,
unmittelbar nachdem du das Register geschrieben hattest. **Das Register ist
gerettet und committet** (`9098229`): `UI_SPEC_REGISTER.md`, 255 AK-Zeilen,
keine Dublette, drei Faelle `widerspruechlich`. `UI_SPEC.md` ist unveraendert
(9639 Zeilen).

**Korrektur einer eigenen Zahl des Directors:** T-181 nannte **256** AK-IDs.
Es sind **255**. Die 256. war `AK-2` — ein Bruchstueck aus dem Suchmuster
`grep -rn "AK-2[45][0-9]"`, das in Zeile 9461 von `UI_SPEC.md` als Text steht
und meiner eigenen Suche in die Falle ging. Dein Register ist also
**vollstaendig**, nicht um eine Zeile zu kurz.

**Abnahmekriterium aus GOAL.md:** dieser Auftrag dient keinem Kriterium
unmittelbar. Das naechste offene lautet woertlich: *"**A9** Ein
`qa-engineer`-Durchlauf bestaetigt A3 bis A8 gegen ein gebautes Artefakt, nicht
nur gegen den Quellstand."*

**Stand laut docs/state.md** (09.09.2026, Director): *"2026-09-09, **Zyklus 18
abgeschlossen, Baurunde offen**. Branch `docs/audit-and-advisor-design`, `main`
geschuetzt, PR #16 offen — **Merge gehoert dem Nutzer.**"*

## Umfang

**Beruehrt Dateien:** `UI_SPEC.md`, `docs/archiv/ui-spec-verlauf.md` (neu),
`docs/berichte/T-184-ui-ux-designer.md` (neu). `UI_SPEC_REGISTER.md` ist
fertig — nur anfassen, wenn dir beim Konsolidieren ein Fehler darin auffaellt,
und das dann im Bericht nennen.

**Parallel:** ja. Gleichzeitig laufen T-180 (`qa/`, `security/`) und T-183
(nur ein Bericht). `ARCHITECTURE.md` ist seit gestern konsolidiert und tabu.

## Vorgaben

**Der Lauf ist schon einmal an einem Verbindungsabbruch gestorben. Arbeite so,
dass ein Abbruch nichts kostet:**

**Schritt 1 — den Verlauf sichern, mit der Shell, nicht mit `Write`.**
`cp UI_SPEC.md docs/archiv/ui-spec-verlauf.md`, dann ein Inhaltsverzeichnis
(Ueberschrift + Zeilennummer je Abschnitt) an den Kopf. 9639 Zeilen durch ein
Schreibwerkzeug zu schicken, kostet unnoetig Zuege — und die Zugschwelle liegt
bei 150. Der `architect` ist in T-182 denselben Weg gegangen.

**Schritt 2 — `UI_SPEC.md` neu aufbauen, Bereich fuer Bereich, anhaengend.**
Nach Oberflaechenbereich gegliedert, nicht nach Datum. Je AK die **heute
geltende** Fassung im Wortlaut des juengsten Nachtrags. Nach jedem Bereich ist
die Datei in sich lesbar; bricht der Lauf ab, ist das Fertige brauchbar.
**Nenn im Bericht, welche Bereiche fertig sind und welche fehlen.**

Unveraendert aus T-181: **nichts wird inhaltlich gestrichen** — `UI_SPEC.md`
und die Verlaufsdatei zusammen duerfen nicht weniger hergeben als 9639 Zeilen.
**Keine neuen AK-Nummern, keine geaenderten Wortlaute, kein neuer Entwurf.**
Die drei `widerspruechlich`-Faelle loest du nicht auf — sie gehoeren dem App
Designer; uebernimm in `UI_SPEC.md` beide Fassungen nebeneinander mit dem
Vermerk, dass die Entscheidung aussteht.

## Testumgebung

Reine Dokumentenarbeit. Kein Programmstart, kein Bau, keine Suite. Die
Umlenkung der drei Datenverzeichnisse aus `CLAUDE.md` entfaellt.

Scratchpad: **nur** `…/scratchpad/T-184/`.

## Abnahme

Gemessene Zahlen im Bericht:

- `wc -l` von `UI_SPEC.md` + `docs/archiv/ui-spec-verlauf.md` ist **nicht
  kleiner** als 9639.
- `grep -cE '^## (Nachtrag|Director-Korrektur)' UI_SPEC.md` = **0**.
- Jede der 255 AK-IDs aus dem Register kommt in der neuen `UI_SPEC.md` vor
  **oder** ist im Register mit Fundstelle im Verlauf gefuehrt.
- Welche Bereiche fertig sind, welche nicht.

## Scope-Grenzen

- `DESIGN_REVIEW.md`, `ARCHITECTURE.md`, `qa/`, `security/` bleiben unberuehrt.
- Kein Anwendungscode, keine Tests.
- Du committest nicht. Der Director committet, wenn er gegengeprueft hat —
  auch dann, wenn du nur die Haelfte schaffst.

## Widersprich, wenn der Auftrag nicht stimmt — Pflicht

**Widerspricht eine Aussage in diesem Auftrag dem, was du im Code, in den
Dateien oder in deiner Messung vorfindest, ist das ein Befund.** Melde ihn,
auch wenn er nicht zu deiner Aufgabe gehoert, und arbeite auf dem, was du
vorgefunden hast — nicht auf dem, was hier steht.

Das gilt fuer Zahlen, Befundstatus, Dateilisten, Behauptungen ueber andere
Rollen und fuer "existiert nicht". **Der Director ist die einzige Rolle ohne
Pruefer** — seine Auftragstexte sind das, was am seltensten gegengelesen wird.

## Rueckgabeformat

**Der Bericht steht in der Datei, nicht in der Antwort.** Schreib ihn
vollstaendig nach `docs/berichte/T-184-ui-ux-designer.md` und gib in deiner Antwort
**nur** zurueck:

1. den Uebergabe-Kontrakt-Block (unten, vollstaendig),
2. dein Ergebnis in **hoechstens dreissig Zeilen** — Urteil, Zahlen,
   Befunde, offene Entscheidungen,
3. den Pfad der Berichtsdatei.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEAENDERT:
ANNAHMEN:
NAECHSTER:
BLOCKIERT DURCH:
```
