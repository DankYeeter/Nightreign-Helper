# T-167 - Das Artefakt bauen (release-manager, Modus `build`)

**Modus: `build`.** Nur bauen. **Kein Release, keine Veroeffentlichung, kein
Tag, kein Push** — der Nutzer hat das in dieser Nacht zweimal ausdruecklich
gesagt: *"merge bleibt bei mir"* und *"alles bezueglich release und sicherheit
und co klaeren wir danach"*.

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`.
Es gelten die Projektzeilen aus `CLAUDE.md` (Zielsystem, Testbefehl, die drei
Datenverzeichnis-Umlenkungen, fester Testabzug, verbotene Zugriffe).

**Scratchpad:** eigenes Unterverzeichnis `T-167/`.

## Ziel

Ein reproduzierbares Artefakt aus dem heutigen Stand — `dist/NightreignHelper.exe`,
PyInstaller-Einzeldatei, wie die Fassung 1.8.0 aus Zyklus 16. **Der Nachweis
am gebauten Artefakt ist der einzige Weg zu A9, A11 und A15**, und alle drei
sind offen.

## Was seit dem letzten Bau passiert ist

**Zyklus 18, elf Bauauftraege.** Das Artefakt aus Zyklus 16
(`dist/NightreignHelper.exe`, 59 010 777 B, 1.8.0, SHA-256 `42B21AA2...F221`)
kennt nichts davon:

- **A15 vollstaendig neu**: Auswahldialog fuer Spielordner **und** Spielstand,
  Pfadgedaechtnis in zwei QSettings-Schluesseln, Rueckfrage bei abweichendem
  Fund, Panels A1/A2/A3/E1/W1/C1/C2/C3.
- **Das Spielstand-Lesen laeuft im Hintergrund** (AD-029 Stufe B) —
  **6147,6 → 657,2 ms** gemessen. Das Fenster steht sofort da.
- **Der Berater rechnet im Worker** — Picker-Hauptthread **318,1 → 7,2 ms**.
- **Ein langsamer Rueckfallweg** fuer den Fall, dass ein Spielpatch die
  Id-Annahme bricht, samt Satz an den Spieler.
- **Fuenf Sicherheitsbefunde behoben** (SEC-027 bis SEC-031, SEC-033 bis
  SEC-035).
- **`README.md` neu** (T-166) — darin erstmals der ehrliche Satz, dass eine
  Bibliothek aus dem Spielordner **ausgefuehrt** wird.

**Suite: 1699 passed, 9 skipped, 0 failed** (T-164). Der Director hat
zwischendurch selbst gemessen: 1691/9/0 bei `ef6761b`.

## Abnahmekriterium aus GOAL.md (woertlich)

> **A9** Ein `qa-engineer`-Durchlauf bestaetigt A3 bis A8 gegen ein gebautes
> Artefakt, nicht nur gegen den Quellstand.

> **A15** […] Nachweis: ein `power-user`-Lauf auf einem **gebauten Artefakt**,
> bei dem die Automatik nachweislich ins Leere laeuft, und der Nutzer trotzdem
> ohne Abbruch bis zu angezeigten Zahlen kommt.

**Stand laut docs/state.md** (woertlicher Auszug, 09.09.2026):

> `compliance-agent` (`auflagen`) → `technical-writer` → `release-manager`
> (`build`) → (`clean-room`) → `power-user` → (`notes`).

*Der `compliance`-Lauf ist bewusst ausgelassen — der Nutzer hat Recht und
Sicherheit auf "danach" gelegt. Die Auflagen, die die Doku brauchte, sind
erfuellt (T-166).*

## Vorgaben

- **Version:** entscheide und **begruende** sie. 1.8.0 ist gebaut und liegt
  als Zyklus-16-Artefakt vor; dieser Stand ist inhaltlich ein anderer.
  **Aendere `CHANGELOG.md` in diesem Auftrag nicht** — das ist der Modus
  `notes` und kommt nach dem `power-user`-Lauf.
- **Reproduzierbarkeit belegen**, nicht behaupten: Groesse, SHA-256, und
  woraus gebaut (Commit). Zyklus 16 hat **62 von 62 Modulen bytecode-identisch
  zum Quellstand** nachgewiesen — **wenn du diesen Nachweis wieder fuehren
  kannst, fuehr ihn.**
- **`requirements.txt` nicht anfassen.** Die Trennung von
  `requirements-build.txt` steht in `docs/plan-restarbeiten.md` P9 und ist
  **nicht** dieser Auftrag; sie braucht denselben Lauf, der sie danach prueft.
- **Bricht der Bau, ist das ein Fixauftrag an den `developer`** — du fixt
  nichts selbst.
- **Keine Veroeffentlichung.** Kein `gh release`, kein Tag auf dem Remote,
  kein Push. Nur `dist/`.

## Testumgebung

Windows 10/11 x64, Branch `docs/audit-and-advisor-design`. Testbefehl nach
`CLAUDE.md`.

**Alle drei Datenverzeichnisse umlenken und jede Umlenkung nachweisen** —
gelingt ein Nachweis nicht, brich ab und melde nur das. **Der Spielstand ist
read-only**; der Nutzer hat 309 Relikte und rund 110 gespeicherte Builds
darin, und auf diesem Weg sind in frueheren Zyklen **drei Datenverluste**
entstanden. **Der feste Testabzug aus `CLAUDE.md` wird kopiert, nicht neu
gebaut** — das spart je Lauf rund fuenf Minuten (99,2 % gemessen).

*Achtung: der Testabzug stammt von Fassung 1.8.0, `EXTRACT_VERSION` 11. Steigt
sie in diesem Stand darueber, ist der Abzug ungueltig — dann **ersetzt du die
Vorlage und vermerkst es**, so wie E-1 in `docs/plan-restarbeiten.md` es
vorsieht.*

## Scope-Grenzen

- Nicht: `clean-room` (eigener Auftrag), `notes`, das Release, der
  `power-user`.
- **Nicht anfassen:** `nrplanner/`, `nrdata/`, `tests/`, `UI_SPEC.md`,
  `ARCHITECTURE.md`, `CHANGELOG.md`, `GOAL.md`, `docs/state.md`,
  `qa/findings.md`, `security/findings.md`, `docs/tasks/`.

**Parallel laeuft nichts.**

## Rueckgabeformat

Uebergabe-Kontrakt-Block plus Abschlussbericht. **Der Auftrag gilt erst als
erledigt, wenn `docs/berichte/T-167-release-manager.md` auf der Platte liegt.**

**Nenne in der Zusammenfassung ganz oben:** Pfad, Groesse, SHA-256, Commit und
Version des Artefakts. Der `clean-room`-Lauf und der `power-user` bekommen
genau diese Angaben.
