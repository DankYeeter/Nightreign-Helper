# T-328 — Restposten nach 1.18.0: Nightfarer merken, Register nachziehen

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen
(Testbefehl, Datenverzeichnisse, verbotene Zugriffe, Zielsystem) stehen in
der `CLAUDE.md` des Projekts; Abweichungen fuer diesen Auftrag: keine.

**Stand (docs/state.md, 21.09.2026):** "A27 abgeschlossen: Release 1.18.0
(21.09. 19:35, Tag `v1.18.0` auf `1ae6952`) ... Offen: ... Nightfarer nicht
gemerkt, AUFLAGEN-Register seit 1.13.1 nicht fortgeschrieben, ROLLOUT.md ohne
1.16-1.18." Nutzer 21.09. 21:25: Restposten buendeln, kein Release, sammeln.

**Director-Entscheid zu QA-295 (21.09. 21:30):** AK-355 nachgezogen — die
Differenz der Zeile `Weapon art` ist gefaerbter Text, kein Link (AK-361 nennt
nur zwei Link-Schluessel, ein dritter haette keinen Tooltipinhalt). Kein
Codeauftrag; Register-Zeile geschlossen.

---

## T-328a — Gewaehlter Nightfarer ueberlebt den Neustart (developer)

**Stufe: klein**

**Ziel:** Beim naechsten Start ist der Nightfarer gewaehlt, der beim letzten
Lauf zuletzt gewaehlt war. Unbekannter oder fehlender Wert: erster Nightfarer
wie bisher.

**Abnahmekriterium (Director-Vorgabe OF-55, kein GOAL-Nachtrag; Bauform
laut AD-030 "fester Schluessel, veraenderlicher Wert", wie `damage_type`
AK-347):**

> Ein fester QSettings-Schluessel `hero` im vorhandenen Speicher
> (`QSettings(favourites.ORG, favourites.APP)`) traegt die `id` des zuletzt
> gewaehlten Nightfarers. `select_hero` schreibt ihn; der Start liest ihn und
> waehlt den Nightfarer mit dieser `id`, sonst Index 0. Kein aus Nutzertext
> abgeleiteter Schluessel, kein `remove`.

### Kontext

- Wahl: `nrplanner/app.py:1296` `select_hero(index)`; Start fest
  `self.select_hero(0)` `app.py:652`; `self.heroes = data["heroes"]` `app.py:449`.
- Muster fester Schluessel mit Wert daneben: `HeroTile.set_variant` /
  `restore_variant` `app.py:421-430` (`variant/{hero_id}`), und
  `nrplanner/advisorbar.py:1002-1041` (`_remember_the_choice`, `_choice_made`,
  `DAMAGE_TYPE_KEY`).
- Der Kommentar `app.py:641-642` ("OF-15 is why nothing new goes into the
  settings store") meint aus Nutzertext abgeleitete Schluessel und Handles
  (ARCHITECTURE.md AD-030, Z. 4856-4896); ein fester Schluessel mit Wert
  faellt nicht darunter. Kommentar dort nicht umschreiben.

### Vorgaben

Vorlauf: kein Ausloeser (kein neuer Dateizugriff, keine Abhaengigkeit).
Sicherheitsrahmen: Speichern nur ueber die drei Umlenkvariablen der
Projekt-`CLAUDE.md`; der Test setzt `NIGHTREIGN_SETTINGS_ORG` wie
`tests/test_advisor_bar.py:48-57`.

### Testumgebung

Projekt-`CLAUDE.md`. `pytest tests/test_nightfarer_is_named.py` gezielt;
Klon-Volllauf `pytest -n auto` im Hintergrund, sobald der Commit steht,
Ausgabe `<Scratchpad>/T-328/klon.txt`; Bericht nennt Zahl und Uhrzeit.

### Umfang

**Beruehrt Dateien:** `nrplanner/app.py`, `tests/test_nightfarer_is_named.py`
(ein Test: Wahl von Index 3, neues Fenster, Index 3 gewaehlt; zweiter Fall:
Schluessel mit fremder `id` → Index 0) · **Parallel:** ja (compliance-agent
an `docs/legal/`) · **Scratchpad:** `<…>/scratchpad/T-328/`

### Scope-Grenzen

Nicht: Builds, Vessel, Artwork-Variante (bereits je Nightfarer gemerkt);
nicht `advisorbar.py`; nicht den Kommentar zu OF-15 umbauen. Widerspricht
der Bestand: melden.

### Praemissen

- `select_hero(0)` ist der einzige Startaufruf · `grep -n "select_hero(0)"
  nrplanner/app.py` · 21.09.2026 (Prüfauftrag: weitere Aufrufer nennen).
- `heroes[i]["id"]` ist als Settingswert bereits in Gebrauch
  (`variant/{hero_id}`) · `app.py:424` · 21.09.2026.

Commit auf `docs/audit-and-advisor-design`, Praefix `feat(app):`, Deutsch.

### Rueckgabe

Kontraktblock plus hoechstens dreissig Zeilen; Bericht nur bei Befund.

---

## T-328b — Auflagen-Register 1.14.0 bis 1.18.0 nachziehen (compliance-agent)

**Modus: pruefen**

**Ziel:** `docs/legal/AUFLAGEN.md` endet heute mit dem Abschnitt zu 1.13.1
(Z. 529, Stand `1f51485`, 16.09.). Seither fuenf Releases (1.14.0 `ffac292`,
1.15.0 `c536daa`, 1.16.0 `a33e92b`, 1.17.0 `28b7f1e`, 1.18.0 `1ae6952`). Pruefe
am Diff `git diff --stat v1.13.1..v1.18.0 -- requirements*.txt
NightreignHelper.spec nrdata/ .github/workflows/`, ob eine Auflage neu
entsteht oder eine bestehende (A-001..A-038) ihren Status wechselt, und
schreibe **einen** Abschnitt "Stand 1.18.0" ans Ende des Registers — nur
Aenderungen, keine Wiederholung erfuellter Auflagen.

Bekannt: PyInstaller ist seit T-296 (19.09.) in `requirements-dev.txt`,
Pillow bleibt Laufzeitabhaengigkeit (Quittung `docs/state.md` 19.09.);
A-033/A-035 (C-006, Frist 07.11.2026) beim Nutzer offen.

**Beruehrt Dateien:** `docs/legal/AUFLAGEN.md` (Anhang), ggf.
`docs/legal/C-007.md` neu · **Parallel:** ja (developer an `nrplanner/`,
`tests/`) · kein Commit (Director committet).

**Rueckgabe:** Kontraktblock, Ampel je neuer oder geaenderter Auflage.

---

## T-328c — ROLLOUT.md 1.15.0 bis 1.18.0 (release-manager)

**Modus: notes** — eingeschraenkt: **keine** Versionsaenderung, **kein**
CHANGELOG, **kein** Bau. Nur `docs/release/ROLLOUT.md`.

**Ziel:** Vier Abschnitte in der Form des Abschnitts `## 1.14.0 —
2026-09-19, T-295d, Modus notes` (Z. 591-620, mit `### Migration` und
`### Ergebnis`), Quellen: `CHANGELOG.md`, `git log v1.14.0..v1.18.0
--oneline`, `docs/state.md` Tabelle "Veroeffentlicht" (Run-IDs, Assets),
`docs/tasks/T-312/T-313/T-325/T-327`. Migration: EXTRACT_VERSION 13→16
(Cache-Neubau beim Erststart, clean-room T-325 37 s), Settings-Schluessel
`damage_type`/`hit_with` neu (AK-347), keine Build-Migration.

**Beruehrt Dateien:** `docs/release/ROLLOUT.md` · **Parallel:** nein (laeuft
nach T-328a) · kein Commit.

**Rueckgabe:** Kontraktblock; Zahlen, die du nicht aus einer Quelle belegen
kannst, als "ungeprueft" kennzeichnen.
