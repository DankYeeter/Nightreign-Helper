# T-159 - Pruefphase: haelt A15? (qa-engineer)

**Erstlauf, kein Retest.** Der ganze A15-Bau dieser Nacht steht zur Pruefung.

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`.
Es gelten die Projektzeilen aus `CLAUDE.md`. Abweichung: **keine**.

**Scratchpad:** eigenes Unterverzeichnis `T-159/`. Mutationen und Messskripte
in deinen **eigenen Klon**.

## Der Stand ist eingefroren

Branch `docs/audit-and-advisor-design`, Stand `e5c2b7a`. **Suite vom Director
selbst nachgemessen: 1674 passed, 9 skipped, 0 failed** (181 s, Arbeitsbaum
sauber). Waehrend deines Laufs aendert **kein** Agent den Code.

## Was in dieser Nacht gebaut wurde

Elf Bauauftraege, alle committet. In der Reihenfolge, in der sie aufeinander
stehen:

| | |
|---|---|
| **T-142** | Spielstand-Lesen im Hintergrund (AD-029 Stufe B), Wartezustand |
| **T-147** | ein Aufloesungspunkt fuer den Spielordner, `gamepath.py` neu, SEC-028, SEC-030 |
| **T-149** | der Auswahldialog und seine sechs Panels |
| **T-150** | `Find my save…`, `paths/save`, SEC-029 erste Haelfte |
| **T-151** | der langsame Rueckfallweg (AD-031) |
| **T-153** | der Rueckfallsatz wird gesagt (A7), Dialog-Startort (AK-110) |
| **T-157** | SEC-029 zu Ende, A7-Bruch bei unlesbarem Save, zwei Waechter |

Die Vorgaben stehen in `UI_SPEC.md` (**AK-106 bis AK-132** und die Nachtraege
**AK-220 bis AK-245**) und in `ARCHITECTURE.md` (**Nachtrag XI**, AD-030,
AD-031, Regressionstests **R1 bis R18**).

## Die Frage dieses Auftrags

**Nicht "laufen die Tests durch"** — das hat der Director gemessen. Sondern:
**erfuellt das Gebaute A15, oder erfuellt es die Tests, die dazu geschrieben
wurden?**

Die Regel, an der dieser Auftrag haengt (`commands/director.md`):

> **Tests des `developer` sind kein Nachweis fuer sich.** Verlange vom
> `qa-engineer` ausdruecklich die Aussage, ob die vorhandenen Tests das
> Kriterium ueberhaupt pruefen — oder nur, dass der Code tut, was er tut.

## Sieben Stellen, die die Rollen selbst als ungeprueft gemeldet haben

**Sie sind nicht die Grenze deines Auftrags, sondern sein Anfang.**

1. **AK-129, AK-130, AK-132, AK-239 — Skalierung.** Kein Lauf hat sie am
   **echten Fenster bei 100/125/150 %** geprueft. T-149 hat dabei zwei eigene
   Messfehler gefunden (`adjustSize()` liess fuenf von sechs Panels auf 230 px
   mit **abgeschnittenem Text** stehen) und sagt ausdruecklich, seine Messung
   sei **kein** Nachweis dafuer.
2. **Der Systemdialog selbst** (`QFileDialog`) steht **unter keinem Test** —
   von T-149 und T-153 unabhaengig gemeldet. Er wird in der ganzen Suite nie
   geoeffnet.
3. **R10 von Hand:** gemerkte Datei umbenennen, neu starten. Erwartet:
   **still** auf die Automatik zurueckfallen, **kein Text, kein Dialog, und
   der gemerkte Wert bleibt stehen.**
4. **Zwei echte Steam-Konten gegeneinander** — der Nutzer hat zwei
   Kontoordner mit je einem Spielstand (234 und 309 Records).
5. **AK-110 mit mehreren Steam-Bibliotheken:** der Dialog soll in der
   **naechstgelegenen** starten, nicht im Standardpfad. T-153 hat nur gegen
   eine selbstgebaute `libraryfolders.vdf` getestet, nie gegen eine echte
   Installation.
6. **Ein lesbarer Spielstand ohne Relikte** — T-150 konnte ihn nur synthetisch
   abbilden.
7. **AK-228 am echten Fall** ist vermutlich **nicht** reproduzierbar (er
   braucht ein kuenftiges Spiel-Update). Sag, was stattdessen als Nachweis
   taugt.

## Abnahmekriterien aus GOAL.md (woertlich)

> **A15** Der Erststart fuehrt jeden Nutzer bis zu lesbaren Daten, ohne fremde
> Hilfe. Findet die Automatik den Spielordner oder den Spielstand, merkt der
> Nutzer nichts — der Ablauf bleibt wie heute. Findet sie ihn nicht, erscheint
> **statt der Fehlermeldung** ein Auswahldialog; der gewaehlte Pfad wird
> geprueft, behalten und beim naechsten Start wieder benutzt. Nachweis: ein
> `power-user`-Lauf auf einem **gebauten Artefakt**, bei dem die Automatik
> nachweislich ins Leere laeuft, und der Nutzer trotzdem ohne Abbruch bis zu
> angezeigten Zahlen kommt.

> **A7** Wo die Spieldateien eine Bewertung nicht hergeben, sagt das Programm
> das, statt zu raten.

> **A6** Die Berechnung blockiert die Oberflaeche nicht: sie laeuft im
> Hintergrund, das Fenster bleibt bedienbar, und bei grossen Relikt-Bestaenden
> bleibt die Antwortzeit im gemessenen Budget — eine **Slot-Frage des Beraters
> ist im Median unter 500 ms** beantwortet, ein **Gesamtlauf (`Optimize`) unter
> 6 s**, und **keine Beraterrechnung haelt den Hauptthread laenger als 50 ms
> an**.

> **A8** Alle Texte in der Oberflaeche sind Englisch.

*A15s letzter Halbsatz — der Nachweis am **gebauten Artefakt** — ist
**nicht** dein Auftrag. Der kommt in der Baurunde. **Du pruefst den
Quellstand**, damit die Baurunde nicht an etwas scheitert, das hier schon
sichtbar war.*

**Stand laut docs/state.md** (woertlicher Auszug, autonomer Lauf):

> 1. Zyklus 18 schliessen — U8 (AD-029 Stufe B), die zwei fehlenden
>    U7-Messungen, **Pruefphase**.

## Vorgaben

- **Urteil vierwertig:** PASS, CONCERNS, FAIL. **WAIVED vergibst du nicht.**
- **Je Kriterium einzeln**, nicht als Sammelaussage — A14 verlangt das fuer die
  Tabs, und hier gilt dasselbe.
- **Eigene Zahl nennen**, gegen **1674 passed, 9 skipped, 0 failed**.
- **Deine Mutationen laufen in deinem eigenen Klon.** Im Arbeitsbaum aenderst
  du nichts.
- **Neue Befunde ab QA-215.** Sicherheitsfunde gehen an den
  `security-reviewer`, der parallel laeuft — nenne sie trotzdem.
- **Startest du das Programm: alle drei Datenverzeichnisse umlenken und jede
  Umlenkung nachweisen.** Gelingt ein Nachweis nicht, brich ab und melde nur
  das. **Der Spielstand ist read-only** — 309 Relikte und rund 110 Builds des
  Nutzers haengen daran, und auf diesem Weg sind drei Datenverluste
  entstanden. Testabzug aus `CLAUDE.md` **kopieren**, nicht neu bauen.
  Bildnachweise nur aus dem Programmfenster (NH-002).

## Scope-Grenzen

- Nicht: den Berater neu pruefen (T-114 hat ihn am Artefakt bestaetigt), die
  sechs Tabs, das Release.
- Im Arbeitsbaum kein `checkout`, `reset`, `clean`, `stash`; kein `push`.

**Parallel laeuft T-160** (`security-reviewer`) auf demselben eingefrorenen
Stand — **lesend, er aendert nichts.**
**Beruehrt Dateien:** nur `docs/berichte/T-159-qa-engineer.md`.

## Rueckgabeformat

Uebergabe-Kontrakt-Block plus Abschlussbericht. **Der Auftrag gilt erst als
erledigt, wenn `docs/berichte/T-159-qa-engineer.md` auf der Platte liegt.**
