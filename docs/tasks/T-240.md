# T-240 — AK-262: dritter Chip heisst `BEST FOR ATTRIBUTES` (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen in der
`CLAUDE.md` des Projekts; Abweichungen: keine.

**Ziel:** Der Chip der dritten Zielrichtung auf den Reliktkarten lautet
`BEST FOR ATTRIBUTES`; Test und Codekommentar tragen die Entscheidung.

**Stufe/Modus:** klein

**Abnahmekriterium (UI_SPEC.md AK-262, Nachtrag 14.09.2026, woertlich):**
"Entschieden (Nutzer, 14.09.2026 07:52): der Chip der dritten Richtung heisst
**`BEST FOR ATTRIBUTES`**."

**Stand (docs/state.md, 14.09.2026):** "Pruefphase abgeschlossen 07:50, Stand
`a68cd3d` unveraendert im Code."

## Vorgaben

- `nrplanner/relicpicker.py:112` `DIRECTION_NOUNS["max_attributes"]` →
  `attributes`; Kommentar `:107` auf die Entscheidung umschreiben. Pruefen,
  wo `DIRECTION_NOUNS` sonst gelesen wird (`:273` "Nothing you own raises
  {noun} …") — Wortlaut dort mit dem neuen Nomen pruefen, A8 Englisch.
- `tests/test_relic_picker_advisor.py:1293` erwartet `BEST FOR ATTRIBUTES`;
  Waechter `test_the_chip_is_whole_on_a_favourite_card` (T-237) muss weiter
  gruen sein — er misst den laengeren Text.
- Kein Layoutwechsel: Streifen bleibt 118 px.

## Umfang

**Beruehrt Dateien:** `nrplanner/relicpicker.py`,
`tests/test_relic_picker_advisor.py` (+ ggf. ein zweiter Test, der `STATS`
prueft) · **Parallel:** nein · **Scratchpad:** `<scratchpad>/T-240/`

Direkt auf `docs/audit-and-advisor-design`, ein Commit.

## Praemissen

- `grep -rn "FOR STATS" nrplanner tests scripts` = 3 Treffer (relicpicker.py:107, test_relic_picker_advisor.py:1287/1293) · Director · 14.09.2026 07:53
- Suite auf `a68cd3d`: 1528 passed / 9 skipped · Director · 07:29

## Rueckgabe

Kontraktblock + max. 10 Zeilen: Commit, `pytest tests/test_relic_picker_advisor.py tests/test_relic_picker_geometry.py` Zahl.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
