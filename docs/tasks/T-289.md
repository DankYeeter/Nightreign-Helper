# T-289 — Favorit auf gehaltenem Relikt: Satz auch im Vorschlagsblock; Hook-Maske (ui-ux-designer, developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen stehen
in der `CLAUDE.md` des Projekts; Abweichungen fuer diesen Auftrag: keine.
Stand `fb4898a`.

**Ziel:** Der Spieler sieht ohne Klick auf `Why`, dass ein favorisierter
Effekt bereits durch ein gehaltenes Relikt erfuellt ist (Nutzer 17.09.
19:12: "auch direkt unter dem Vorschlag"). Danach Bau 1.13.2.

**Abnahmekriterium (GOAL.md, woertlich):** A19 "Pflicht-Effekt"; A5 "Jeder
Vorschlag nennt eine nachvollziehbare Begruendung"; A8 Englisch.

**Stand (docs/state.md / T-288, 17.09.2026):** Fix `bc4443e` —
`explain.required_met_by_a_hold` liefert je favorisiertem Effekt auf einem
gehaltenen Relikt einen Satz in `result.unknowns`; der landet ueber
`advisorblock._footer_text` **nur im Why-Dialog** (Modul-Docstring
`advisorblock.py:1-12`: der Block zeigt Zahlen und Flueche, der Dialog die
prozeduralen Saetze). Wortlaut derzeit: `{effect}, which you favourited, is
carried by {relic} held in Slot {n}.`

## T-289a — ui-ux-designer (Spec-Modus, kurz)

Neue AK-314 in `UI_SPEC.md` Bereich 6: (1) endgueltiger Wortlaut des Satzes
(AK-300-Woerter, Slot-Nummerierung wie `explain.curses`); (2) wo er im
`SuggestionBlock` steht (Vorschlag: eine gedaempfte Zeile unter den
Effektzeilen, vor `Use`/`Why`, gleiche Typo wie die Fluchzeilen ohne
Strich), ob er im Why-Dialog zusaetzlich bleibt (Vorschlag: ja, AK-274 "ein
Inhalt"); (3) Verhalten, wenn mehrere Favoriten gehalten sind (eine Zeile
je Effekt oder eine Zeile "2 favourited effects are already on held
relics"); (4) Akzeptanzkriterien mit Pruefweg, toetende Mutation. Keine
Aenderung an AK-281/AK-291. Beruehrt: `UI_SPEC.md`. Kein Commit.

## T-289b — developer (Stufe klein, nach T-289a)

1. AK-314 umsetzen: Zeile im `SuggestionBlock` (`nrplanner/advisorblock.py`),
   Wortlaut-Konstante in `explain.py` anpassen, Test in
   `tests/test_advisor_run.py`/`tests/test_advisorblock*.py`.
2. **Hook-Maske NH-004:** `.claude/hooks/enforce-data-redirect.ps1`
   (`$istExeKommando`, T-287a) schlaegt auf Lesebefehle an, die `run.py`
   im Text tragen (`grep … nrplanner/advisor/run.py`, 17.09. zweimal
   beobachtet: developer T-288 und Director). Maske auf echte Startformen
   engen (EXE oder `python`/`pythonw` + `run.py` als Kommando am
   Zeilenanfang oder nach `&`/`;`, nicht als Argument von `grep`/`sed`/
   `cat`/`git`). Nachweis: `grep -n x nrplanner/advisor/run.py` geht durch,
   ein Start `python run.py` bei laufender Kopie wird weiter abgewiesen
   (Kopie danach beenden, `Get-Process` = 0).
Beruehrt: `advisorblock.py`, `explain.py`, ein bis zwei Testdateien,
`enforce-data-redirect.ps1`. Zwei Commits (a: fix(advisor), b:
chore(hooks)). Klon-Volllauf (L-031).

## Umfang

**Parallel:** nein (b liest a) · **Scratchpad:** `<Scratchpad>/T-289/`

## Scope-Grenzen

Kein Versionssprung (release-manager). Keine Aenderung an
`effectfilterdialog.py`, keine an AK-281/AK-291-Saetzen.

## Praemissen — jede Bestandsbehauptung mit Quelle

- `_footer_text` haengt `result.unknowns` an · `advisorblock.py:586` · 17.09.
- Docstring Block vs. Dialog · `advisorblock.py:1-12` · 17.09.
- Hook-Fehlalarm auf `run.py` in Leseaufrufen · T-288-Bericht, Director 19:13 · 17.09.

## Rueckgabe

Kontraktblock plus hoechstens zwanzig Zeilen.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
