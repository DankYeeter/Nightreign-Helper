# T-332 — Zyklus 31: Abschluss vor dem Ingame-Test

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen
(Testbefehl, Datenverzeichnisse, verbotene Zugriffe, Zielsystem) stehen in
der `CLAUDE.md` des Projekts; Abweichungen: keine.

**Nutzer 23.09. 19:35:** Aufraeumen, Ponytail-Audit, Fixes, QA, Exe bauen;
Veroeffentlichung erst nach seinem Ingame-Test. Autonom bis die Exe steht.

**Stand (docs/state.md, 23.09.):** "Community-Vermerke entfernen (Entscheid 1
oben) ... Hook `$istExeKommando`: Textteile vor dem Abgleich entfernen (T-330a
Nebenfund)."

## T-332a — Community-Vermerke entfernen (developer)

**Ziel:** Deep of Night, Red variants und World Events zeigen ihre festen
Community-Angaben als normale Angaben: kein Vermerk "community-reported", keine
blaue Sonderfarbe. Inhalt (Texte, Zahlen) bleibt unveraendert.

**Stufe:** normal.

**Abnahmekriterium (GOAL.md Nicht-Ziele, geaendert 23.09. mit
Nutzerfreigabe):** "Kein Netzwerkzugriff, keine Telemetrie, keine zur Laufzeit
geladenen Wiki-Daten. Feste Community-Angaben im Code nur fuer Deep of Night,
Red variants und World Events; sie erscheinen wie Spieldaten, ohne eigenen
Vermerk (Nutzer 23.09.)."

**Vorgaben (Nutzer 23.09. 19:20, woertlich aus state.md):** "Community-reported:
nicht streichen, als Fakt fuehren, Vermerk weg — Deep of Night
(`deeptab.py:419`, Kommentar `:43`), Red variants (`depthstab.py:117`
"COMMUNITY-REPORTED: ..."), World Events (blaue Zeilen `eventstab.py:101`,
Panel `:335`), Farbe `COMMUNITY`/Kommentar `bosstab.py:206` pruefen."

Klasse: `grep -rn -i "community" nrplanner/` (Stand 19:33: `bosstab.py:206`,
`deeptab.py:43,419`, `depthstab.py:35,117,124`, `eventlore.py:1,11,84,122,159`,
`eventstab.py:5,21,46-49,101,135,219,261,291,332,335,339-349`,
`stacking.py:45`, `theme.py:23`). Jeder Treffer, der Vermerk oder Sonderfarbe
in der Oberflaeche erzeugt, faellt; Kommentare, die nur die Herkunft im Code
erklaeren, duerfen bleiben (Herkunft bleibt fuer Entwickler sichtbar). Text,
der in der Oberflaeche "Community write-ups disagree ..." sagt
(`eventlore.py:84,122`), ist Inhalt, kein Vermerk — nur umformulieren, falls
er ohne Vermerkzeile nicht mehr lesbar ist. `COMMUNITY` in `theme.py` entfaellt,
wenn kein Nutzer bleibt. Der `OBSERVED_COLOUR`-Hinweis in `bosstab.py` (AK-74)
ist eine andere Farbe mit eigener Bedeutung: bleibt, nur der Vergleich mit dem
Red-variants-Tab im Kommentar faellt.
Tests, die den Vermerk oder die Farbe erzwingen, werden angepasst, nicht
geloescht, wo sie sonst noch etwas pruefen. Suite laufen lassen.

Guide (`docs/anleitung/guide.md:479,521,704`) gehoert nicht dazu —
`technical-writer` (T-332f).

**Berührt Dateien:** `nrplanner/deeptab.py`, `depthstab.py`, `eventstab.py`,
`eventlore.py`, `bosstab.py`, `theme.py`, betroffene `tests/` ·
**Parallel:** ja, `isolation: worktree` (T-332b beruehrt nur `.claude/hooks/`
und `tests/test_redirect_hook_masks.py`) · **Scratchpad:** `<scratchpad>/T-332a/`

**Schritt 0 (Worktree):** `git fetch` und auf
`origin/docs/audit-and-advisor-design` bzw. den lokalen Branch
`docs/audit-and-advisor-design` zuruecksetzen, `git log -1` in den Bericht —
der Worktree startet auf `origin/HEAD`, nicht auf diesem Branch.

## T-332b — Hook `$istExeKommando` (developer)

**Stufe:** klein.

**Befund (T-330a Nebenfund, Nutzer 16:46 sammeln):**
`.claude/hooks/enforce-data-redirect.ps1:83` — `$istExeKommando` weist ein
Grep-Muster mit `|NightreignHelper.exe` ab, weil `|` innerhalb eines
Anfuehrungszeichen-Texts als Befehlsgrenze `[;&|(]` zaehlt.

**Kriterium:** `grep -E "foo|NightreignHelper.exe" datei` und
`Select-String -Pattern 'a|NightreignHelper\.exe'` laufen ohne Umlenkung durch;
weiterhin gesperrt ohne Umlenkung: `& "C:\x\NightreignHelper.exe"`,
`Start-Process -FilePath ".\dist\NightreignHelper.exe"`,
`dist\NightreignHelper.exe`, `cmd; NightreignHelper.exe`. Also: Grenzzeichen
in Anfuehrungszeichen zaehlen nicht, der in Anfuehrungszeichen stehende
Exe-Pfad an der Befehlsstelle schon. Faelle als Tests in
`tests/test_redirect_hook_masks.py` (vorhanden).

**Berührt Dateien:** `.claude/hooks/enforce-data-redirect.ps1`,
`tests/test_redirect_hook_masks.py` · **Parallel:** ja, Hauptbaum (braucht
PowerShell; Worktree-Sitzungen haben keine, state.md A27) · **Scratchpad:**
`<scratchpad>/T-332b/`

## Rückgabe (alle Abschnitte)

Antwort: Kontraktblock plus hoechstens dreissig Zeilen. Datei
`docs/berichte/T-332x-<rolle>.md` nur bei Befunden oder `STATUS:
teilweise/blockiert`.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```

## Ponytail-Audit 23.09. 20:23 (Director, Explore-Lauf) — Auftragsgrundlage

Befunde `delete`/`stdlib`/`native`/`yagni` werden zwei Laeufe (T-332c nrdata,
T-332d nrplanner), `shrink` nicht. **Nicht beauftragt (Director):** Fund 1
(35 ungelesene Snapshot-Felder, braucht `EXTRACT_VERSION` 16→17 = jeder Nutzer
extrahiert neu, Testabzug ungueltig, clean-room faellig) — Nutzen kleiner als
Kosten. Jede Null-Aufrufer-Angabe unten ist eine Prämisse aus einer
Symbolsuche (L-033): vor dem Loeschen selbst greppen (`nrplanner/ nrdata/
scripts/ tests/ run.py`); trifft sie nicht zu, Fund auslassen und melden.
Verhalten bleibt unveraendert; Suite gruen.

## T-332c — Audit-Funde nrdata (developer)

**Stufe:** normal. **Worktree** (`isolation: worktree`), Schritt 0 wie T-332a:
auf lokalen Branch `docs/audit-and-advisor-design` setzen, `git log -1` in den
Bericht, eigener Branch.

- delete: `Reader.f32`, `Reader.f64` ohne Aufrufer [nrdata/binary.py:73-77]
- stdlib: `reverse_bits` Schleife → `int(f"{value:08b}"[::-1], 2)` (Breite
  pruefen) [nrdata/binary.py:143-148]
- delete: `ParamTable.by_id` ohne Aufrufer [nrdata/param.py:37-41]
- yagni: 32-Bit-Offset-Zweig in `fmg.read` (NR-FMGs immer Version 2) — nur
  wenn ein Test mit echter FMG-Form das haelt [nrdata/fmg.py:20-29]
- native: Kanaltausch von Hand in `dds.decode` → Rohbytes und
  `Image.frombytes("RGBA", size, raw, "raw", "BGRA")` an den zwei Aufrufern
  [nrdata/dds.py:123-125; icons.py:136; iconbuild.py:288] — nur mit Test, der
  ein Pixel byte-gleich vor/nach vergleicht
- delete: `Texture.format`, `FileEntry.hash` ungelesen [nrdata/tpf.py:19,59;
  nrdata/dvdbnd.py:26]
- yagni: Parameter `key=SAVE_KEY` von `decrypt_member`/`read` nie uebergeben
  [nrdata/savefile.py:119,132] — Tests, die ihn uebergeben, zaehlen als
  Aufrufer: dann bleibt er
- delete: ungenutzter Parameter `archives` in `_event_names`
  [nrdata/bossdata.py:444,759]

**Berührt Dateien:** `nrdata/binary.py`, `param.py`, `fmg.py`, `dds.py`,
`icons.py`, `iconbuild.py`, `tpf.py`, `dvdbnd.py`, `savefile.py`,
`bossdata.py`, betroffene `tests/` · **Parallel:** ja (T-332d nur
`nrplanner/`) · **Scratchpad:** `<scratchpad>/T-332c/`

## T-332d — Audit-Funde nrplanner (developer)

**Stufe:** normal. Hauptbaum, Branch `docs/audit-and-advisor-design`, nur
eigene Dateien committen (Pfade hinter `--`).

- yagni: `LORE`-Schluessel `sources` (jeder Eintrag), `nightlords` (ausser
  auf `UNANNOUNCED`), `confirmed` von der Oberflaeche nie gelesen → Herkunft
  als Kommentar; Modul-Docstring beschreibt noch die von T-332a entfernte
  Sonderfarbe → berichtigen [nrplanner/eventlore.py:1-28,64-65,113-127,259]
- **gestrichen (Director 20:30, T-329r hat sie mit Grund behalten):** nur von Tests genutzte Zugriffe `EffectFilterWindow.boxes/
  allow_box/family_box`, `AdvisorBar.situation`, `BossCard.hovered`,
  `CardGrid.columns`, `RunningCopy.release`; Tests lesen die privaten Felder
  bzw. `_memory.detach()` [effectfilterdialog.py:300-311; advisorbar.py:883-887;
  bosstab.py:599-602; cardgrid.py:137-141; singleinstance.py:127-130]
- yagni: `AdvisorRequest.pool_order_only` von keinem Programmcode gelesen
  [nrplanner/advisor/types.py:276-284; relicpicker.py:451,523]
- delete: `deeptab._note`, `eventstab._note` gleich `tabheader.question`
  (Gleichheit pruefen) [deeptab.py:76-80; eventstab.py:38-42]
- yagni: `IconPack.locate`-Rueckfaelle `base/"icons"`, `base/"data"/"icons"`
  → nur `paths.icons_dir()` [nrplanner/iconpack.py:70-84]
- stdlib: `_factory_installed`-Flag → `@functools.cache` [bosstab.py:460-473]
- yagni: Parameter `spacing` von `room_for`/`CardGrid` nie uebergeben
  [cardgrid.py:46,70]
- stdlib: `sorted(fields)[0]` → `min(fields)` [model.py:1609]
- delete (Debt-Eintrag): Parameter `index` von `make_relic` unbenutzt seit
  AD-055, Aufrufer anpassen [tests/relics.py:74-77]

**Berührt Dateien:** `nrplanner/eventlore.py`, `effectfilterdialog.py`,
`advisorbar.py`, `bosstab.py`, `cardgrid.py`, `singleinstance.py`,
`advisor/types.py`, `relicpicker.py`, `deeptab.py`, `eventstab.py`,
`iconpack.py`, `model.py`, `tests/` · **Parallel:** ja (T-332c nur `nrdata/`)
· **Scratchpad:** `<scratchpad>/T-332d/`

## T-332e — Guide an T-332a angleichen (technical-writer)

**Ziel:** `docs/anleitung/guide.md` beschreibt die Tabs so, wie sie seit
T-332a (`0a4545b`) aussehen: kein Vermerk "community-reported", keine blaue
Sonderfarbe. Stellen (Stand 20:48): `:476-479` (Deep of Night "says so on
itself ... community-reported"), `:521-522` ("Anything shown in blue is
community-reported"), `:702-704` ("marked *confirmed in game* or
*community-reported* where they appear"). Die Aussage muss zum Programm
passen (Programmtext in `nrplanner/deeptab.py`, `depthstab.py`,
`eventstab.py` nachlesen); nicht behaupten, der Wert komme aus den
Spieldateien. Projektweite Suche `grep -rn -i "community" README.md docs/anleitung/`
nach der Aenderung, Ergebnis in die Antwort. Englisch (CLAUDE.md).
**Berührt Dateien:** `docs/anleitung/guide.md` · **Parallel:** ja (nur Doku)

## T-332f — Pruefung am Quellstand (qa-engineer)

**Stand:** Merge `116f5a3` (T-332a Community, b Hook, c/d Audit), Suite vom
Director am selben Stand. Eingefroren: waehrend des Laufs aendert niemand
den Baum ausser `docs/anleitung/guide.md` (T-332e).
Fensterlauf aus dem Quellbaum mit `scripts/drive_window.ps1`, Umlenkung laut
`CLAUDE.md`, fester Testabzug. Pruefen:
1. Deep of Night, Red variants, World Events: kein "community"-Vermerk,
   keine blaue Sonderfarbe (auch nicht angekuendigte Ereignisse in der Liste,
   Titel gold); Texte vollstaendig lesbar (GOAL.md Nicht-Ziele, Fassung
   23.09.: "sie erscheinen wie Spieldaten, ohne eigenen Vermerk").
2. Regressionen T-332c/d: Symbole beim Erststart (Kanaltausch `dds`/`icons`,
   `iconpack.locate` entfernt → `paths.icons_dir()`), Relikt-Auswahl
   Rangfolge (`pool_order_only` entfernt), Boss-Karten mit Tastatur, Welt-
   Events-Hinweise.
3. Seit 1.18.0 ungeprueft am Fenster: Neustart waehlt den gemerkten
   Nightfarer (T-328, `a0ae6b7`).
4. Kurzer Rundgang Kernablauf (Save laden, Nightfarer, Optimize, Why, Build
   speichern) auf Regression.
Pflichtaussage (autonomer Lauf): je Punkt, ob die vorhandenen Tests das
Kriterium pruefen oder nur, dass der Code tut, was er tut.
Urteil PASS | CONCERNS | FAIL, Befunde mit P1-P4, Bericht
`docs/berichte/T-332f-qa-engineer.md` bei Befunden.

## T-332g — Sicherheit (security-reviewer)

**Ausloeser (CLAUDE.md-Tabelle, `git diff --stat v1.18.0..HEAD`):**
`nrdata/` (`savefile.py`, `param.py`, `tpf.py`, `extract.py`, `binary.py`,
`dds.py`, `fmg.py`, `bossdata.py`, `dvdbnd.py`, `icons.py`, `iconbuild.py`),
`nrplanner/paths.py`, `gamepath.py`, `.claude/hooks/enforce-data-redirect.ps1`
(T-332b). Retest SEC-051 (`a5fef7a`, Waechter
`tests/test_release_spec_datas.py`). Diff seit `v1.18.0`, Schwerpunkt
Parser-Eingaben aus Spieldateien/Spielstand (FMG ohne 32-Bit-Zweig, DDS-Rohbytes).
Nur lesend. Urteil und Befunde (Schwere) in der Antwort; Bericht
`docs/berichte/T-332g-security-reviewer.md` bei Befunden.

Ergebnis Pruefung (21:22): QA T-332f **CONCERNS** (alle vier Punkte am Fenster
erfuellt, QA-297 P4), Security T-332g **PASS** (SEC-052 Niedrig), Guide
T-332e `6e11404`. Berichte `docs/berichte/T-332f-qa-engineer.md`.

## T-332h — Fixbuendel nach der Pruefung (developer)

**Stufe:** normal. Hauptbaum (PowerShell noetig), Branch
`docs/audit-and-advisor-design`.

1. **SEC-052 (Niedrig, security-reviewer, woertlich):** "T-332b-Blendung in
   `.claude/hooks/enforce-data-redirect.ps1:94-106` laesst verschachtelte
   Exe-Starts (`powershell -Command "cd dist; .\NightreignHelper.exe"`,
   `cmd /c "cd dist & …exe"`, `powershell -Command "& 'C:\x\…exe' --x"`,
   `bash -c "cd dist && ./…exe"`) ohne Umlenkung und ohne Instanzsperre
   durch; vorher gesperrt. Behebungsrichtung: Grenzzeichen nur ausblenden,
   wenn der Text in Anfuehrungszeichen an ein Suchwerkzeug geht (`grep`,
   `rg`, `Select-String`). Nach `-Command`, `-c` oder `/c` den Befehl
   ungeblendet abgleichen." Die vier Faelle als `A_START`, die T-332b-Faelle
   bleiben gruen, in `tests/test_redirect_hook_masks.py`. Wenn das einfacher
   und gleich sicher ist: Blendung ganz zuruecknehmen und nur Suchbefehle
   (`grep`/`rg`/`Select-String` an der Befehlsstelle) von `$istExeKommando`
   ausnehmen — Kriterium ist das Verhalten, nicht der Weg. Im Zweifel sperren.
2. **QA-297 (P4, woertlich):** "Kanaltausch im Symbolbau
   (`nrdata/iconbuild.py:288`) von keinem Test gehalten; Mutation ueberlebt
   volle Suite." Pixeltest wie `test_a_red_texture_comes_out_of_the_atlas_red`
   (T-332c) fuer den `iconbuild`-Pfad; Gegenprobe (ohne `"BGRA"` rot).
3. Veraltete Kommentare seit T-332a: `nrplanner/deeptab.py:3-4` ("labelled
   where it appears"), `nrplanner/eventstab.py:110` (eigenes Kennzeichen).
   Klasse: `grep -rn -i "labelled\|community-reported\|marked apart" nrplanner/`.

Suite danach voll (`pytest -n auto`), Zahl in die Antwort.
**Berührt Dateien:** `.claude/hooks/enforce-data-redirect.ps1`,
`tests/test_redirect_hook_masks.py`, ein Test fuer `iconbuild`,
`nrplanner/deeptab.py`, `nrplanner/eventstab.py` · **Parallel:** nein

Ergebnis T-332h (21:31): `269355b` SEC-052, `0b2d9fa` QA-297 (beide
Aufrufer ueber `icons.texture_image`), `35ab941` Kommentare; Suite laut
developer 1941 passed, 9 skipped.

## T-332i — Retest am Quellstand (qa-engineer, Retest)

Stand `35ab941`. Ein Lauf, nur die Fixes aus T-332h:
1. Erststart mit leerem umgelenktem `LOCALAPPDATA` (kein Testabzug):
   Extraktion byte-gleich mit dem festen Testabzug wie in T-332f;
   Charakterbilder (Nightfarer-Wahl, Varianten) und Relikt-Symbole ohne
   Blaustich (Pixelprobe an einem bekannt roten/gelben Bild).
2. SEC-052: `pytest tests/test_redirect_hook_masks.py` und zwei Faelle live
   gegen den Hook (`powershell -Command "cd dist; .\NightreignHelper.exe"` →
   deny ohne Umlenkung; `grep -E "foo|NightreignHelper.exe" README.md` →
   erlaubt). Der Exe-Fall darf nicht wirklich starten: Umlenkung weglassen,
   damit der Hook sperrt; greift er nicht, ist das der Befund.
3. Volle Suite einmal (`pytest -n auto`), Zahl und Uhrzeit.
Pflichtaussage: ob die Tests das Kriterium pruefen. Urteil PASS | CONCERNS | FAIL.

Ergebnis T-332i (21:44): **PASS**, Suite 1941 passed, 9 skipped (21:42);
QA-297, SEC-052 geschlossen.

## T-332j — Release bauen, nicht veroeffentlichen (release-manager, build+notes)

Stand: HEAD nach diesem Commit (Code `35ab941`). Letzter Tag `v1.18.0`
(`1ae6952`). Inhalt seit 1.18.0 laut `git log v1.18.0..HEAD`: Nightfarer
gemerkt (T-328), Community-Angaben ohne Vermerk (T-332a), Bereinigung
(T-329q/r, T-332c/d), Speicherpfad-/Save-Hinweise (T-329o/p), Farbrollen
AK-367..371, atomares Schreiben des Snapshots. Versionsnummer, CHANGELOG,
`docs/release/RELEASE_BODY.md`, `docs/release/ROLLOUT.md`-Abschnitt und Bau
der Einzeldatei gehoeren dir. **Kein Tag, kein Push, keine Veroeffentlichung**
— der Nutzer testet die Exe zuerst ingame (Nutzer 19:35). Artefakt-Pfad,
Groesse (Bytes) und SHA-256 in die Antwort. Ausloeser laut Tabelle:
clean-room ja (`nrplanner/firstrun.py` geaendert) — als eigener Lauf T-332k
danach, nicht in diesem.
