# T-332 — Zyklus 31: Abschluss vor dem Ingame-Test

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen
(Testbefehl, Datenverzeichnisse, verbotene Zugriffe, Zielsystem) stehen in
der `CLAUDE.md` des Projekts; Abweichungen: keine.

**Nutzer 23.09. 19:35:** Aufraeumen, Ponytail-Audit, Fixes, QA, Exe bauen;
Veroeffentlichung erst nach seinem Ingame-Test. Autonom bis die Exe steht.

**Stand (docs/state.md, 23.09.):** "Community-Vermerke entfernen (Entscheid 1
oben) ... Hook `$istExeKommando`: Textteile vor dem Abgleich entfernen (T-330a
Nebenfund)."

## T-332a — Community-Vermerke entfernen (developer)

**Ziel:** Deep of Night, Red variants und World Events zeigen ihre festen
Community-Angaben als normale Angaben: kein Vermerk "community-reported", keine
blaue Sonderfarbe. Inhalt (Texte, Zahlen) bleibt unveraendert.

**Stufe:** normal.

**Abnahmekriterium (GOAL.md Nicht-Ziele, geaendert 23.09. mit
Nutzerfreigabe):** "Kein Netzwerkzugriff, keine Telemetrie, keine zur Laufzeit
geladenen Wiki-Daten. Feste Community-Angaben im Code nur fuer Deep of Night,
Red variants und World Events; sie erscheinen wie Spieldaten, ohne eigenen
Vermerk (Nutzer 23.09.)."

**Vorgaben (Nutzer 23.09. 19:20, woertlich aus state.md):** "Community-reported:
nicht streichen, als Fakt fuehren, Vermerk weg — Deep of Night
(`deeptab.py:419`, Kommentar `:43`), Red variants (`depthstab.py:117`
"COMMUNITY-REPORTED: ..."), World Events (blaue Zeilen `eventstab.py:101`,
Panel `:335`), Farbe `COMMUNITY`/Kommentar `bosstab.py:206` pruefen."

Klasse: `grep -rn -i "community" nrplanner/` (Stand 19:33: `bosstab.py:206`,
`deeptab.py:43,419`, `depthstab.py:35,117,124`, `eventlore.py:1,11,84,122,159`,
`eventstab.py:5,21,46-49,101,135,219,261,291,332,335,339-349`,
`stacking.py:45`, `theme.py:23`). Jeder Treffer, der Vermerk oder Sonderfarbe
in der Oberflaeche erzeugt, faellt; Kommentare, die nur die Herkunft im Code
erklaeren, duerfen bleiben (Herkunft bleibt fuer Entwickler sichtbar). Text,
der in der Oberflaeche "Community write-ups disagree ..." sagt
(`eventlore.py:84,122`), ist Inhalt, kein Vermerk — nur umformulieren, falls
er ohne Vermerkzeile nicht mehr lesbar ist. `COMMUNITY` in `theme.py` entfaellt,
wenn kein Nutzer bleibt. Der `OBSERVED_COLOUR`-Hinweis in `bosstab.py` (AK-74)
ist eine andere Farbe mit eigener Bedeutung: bleibt, nur der Vergleich mit dem
Red-variants-Tab im Kommentar faellt.
Tests, die den Vermerk oder die Farbe erzwingen, werden angepasst, nicht
geloescht, wo sie sonst noch etwas pruefen. Suite laufen lassen.

Guide (`docs/anleitung/guide.md:479,521,704`) gehoert nicht dazu —
`technical-writer` (T-332f).

**Berührt Dateien:** `nrplanner/deeptab.py`, `depthstab.py`, `eventstab.py`,
`eventlore.py`, `bosstab.py`, `theme.py`, betroffene `tests/` ·
**Parallel:** ja, `isolation: worktree` (T-332b beruehrt nur `.claude/hooks/`
und `tests/test_redirect_hook_masks.py`) · **Scratchpad:** `<scratchpad>/T-332a/`

**Schritt 0 (Worktree):** `git fetch` und auf
`origin/docs/audit-and-advisor-design` bzw. den lokalen Branch
`docs/audit-and-advisor-design` zuruecksetzen, `git log -1` in den Bericht —
der Worktree startet auf `origin/HEAD`, nicht auf diesem Branch.

## T-332b — Hook `$istExeKommando` (developer)

**Stufe:** klein.

**Befund (T-330a Nebenfund, Nutzer 16:46 sammeln):**
`.claude/hooks/enforce-data-redirect.ps1:83` — `$istExeKommando` weist ein
Grep-Muster mit `|NightreignHelper.exe` ab, weil `|` innerhalb eines
Anfuehrungszeichen-Texts als Befehlsgrenze `[;&|(]` zaehlt.

**Kriterium:** `grep -E "foo|NightreignHelper.exe" datei` und
`Select-String -Pattern 'a|NightreignHelper\.exe'` laufen ohne Umlenkung durch;
weiterhin gesperrt ohne Umlenkung: `& "C:\x\NightreignHelper.exe"`,
`Start-Process -FilePath ".\dist\NightreignHelper.exe"`,
`dist\NightreignHelper.exe`, `cmd; NightreignHelper.exe`. Also: Grenzzeichen
in Anfuehrungszeichen zaehlen nicht, der in Anfuehrungszeichen stehende
Exe-Pfad an der Befehlsstelle schon. Faelle als Tests in
`tests/test_redirect_hook_masks.py` (vorhanden).

**Berührt Dateien:** `.claude/hooks/enforce-data-redirect.ps1`,
`tests/test_redirect_hook_masks.py` · **Parallel:** ja, Hauptbaum (braucht
PowerShell; Worktree-Sitzungen haben keine, state.md A27) · **Scratchpad:**
`<scratchpad>/T-332b/`

## Rückgabe (alle Abschnitte)

Antwort: Kontraktblock plus hoechstens dreissig Zeilen. Datei
`docs/berichte/T-332x-<rolle>.md` nur bei Befunden oder `STATUS:
teilweise/blockiert`.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
