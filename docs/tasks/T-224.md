# T-224 — A16 bauen: Worst case / Best case im Berater (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen
(Testbefehl, Datenverzeichnisse, verbotene Zugriffe, Zielsystem) stehen in
der `CLAUDE.md` des Projekts; Abweichungen fuer diesen Auftrag: Schritt 0
unten (Worktree-Stand); fuenf Anwendungsdateien plus zwei Testdateien
(OF-34: Testdateien zaehlen nicht gegen die Fuenf).

**Ziel:** Der Spieler waehlt in der Beraterleiste `Worst case` (voreingestellt)
oder `Best case`; die Wahl belegt die Bedingungsfelder vor, rechnet nicht
selbst, und vier Orte sagen, welche Lesart gilt.

**Stufe/Modus:** developer, normal

**Abnahmekriterium (GOAL.md, woertlich, A16):** "Der Spieler kann im Berater
zwischen beiden Lesarten wechseln, jede Zahl sagt, welche gerade gilt, und
die sieben bedingten Flueche seines Spielstands bewegen im schlechtesten Fall
die Rangfolge nachweislich." Nicht-Ziel: "Das Statblatt im Build planner
bleibt unberuehrt."

**Stand (docs/state.md, 13.09.2026, woertlich):** "A16 | best/worst case |
**nicht gebaut**"; AD-035 entschieden (T-222, `e593a44`).

## Kontext

Entwurf: `ARCHITECTURE.md` Themenbereich H ab Z. 5693, **AD-035** (Z. 5700
ff.) — lies ihn ganz, er ist die Vorgabe: Lesart = `Planner.worst_case: bool
= True`, gesetzt von der `AdvisorBar` per Signal; in `asking_from` eine
Merge-Zeile `{**model.reading_defaults(planner.worst_case),
**planner.declared}` (Handeingabe gewinnt); `reading_defaults` aus zwei
Tabellen, die `model.configure(data)` fuellt (Fluch = `is_curse`, bedingt =
`is_conditional(eff, None)` und nicht `NO_SWITCH`); Zaehlwert 1; Umschalten =
`the_build_changed()`, kein Lauf. Spec: `UI_SPEC.md` AK-182 (Z. 3114) bis
AK-188 (Z. 3186). Der Abschnitt "Was der `developer` nicht tut" in AD-035
gilt woertlich.

Vorlauf Sicherheit/Compliance: entfaellt — keine neue Vertrauensgrenze,
keine Abhaengigkeit, nur Spieldaten, die schon gelesen werden (Director,
13.09.2026).

## Vorgaben

- Genau die Dateiliste aus AD-035: `nrplanner/model.py`, `nrplanner/advisorbar.py`,
  `nrplanner/app.py`, `nrplanner/advisorblock.py`, `nrplanner/relicpicker.py`;
  Tests `tests/test_advisor_goals.py`, `tests/test_advisor_bar.py`.
- Die vier benannten Tests aus AD-035 (Abschnitt "Der benannte Test"),
  Namen woertlich: `test_the_worst_case_moves_the_ranking_where_a_conditional_curse_sits`
  (GOAL-Abnahme, Skip ohne Save), `test_the_readings_share_not_counted_and_invent_nothing`
  (AK-187), `test_reading_defaults_name_the_seven_conditional_curses_and_nothing_else`
  (Qt-frei), `test_a_declared_condition_outlives_both_readings` (AK-186).
  Rote Phase je Test vor dem Bau.
- Literale im Test (Anzahl bewegter Kopien, 7 Fluch-Ids) **am ersten Lauf
  nachzaehlen** und mit Befehl im Bericht nennen: der Spielstand des Nutzers
  hat heute **312** Kopien, `GOAL.md` rechnet mit 309 (T-222 B-1). Der
  Spielstand ist read-only.
- AK-183: Umschalten verwirft einen lebenden Vorschlag, bricht einen
  laufenden Lauf ab, startet nichts.
- AK-184: nie zwei Zahlen fuer dieselbe Kopie unter verschiedenen Lesarten.
- AK-185: die vier Orte, die die Lesart nennen — wortgleich aus der Spec.
- Mutationen: jede, die ein Test als toetend nennt, in `MUTATIONS`
  eintragen (AD-033 Punkt 4), fahren, im Bericht Name + Ergebnis. Die
  Registry ist seit `64dd006` leer; du bist der erste Eintrag.
- Ponytail: kein neuer Typ, kein Feld auf `AdvisorRequest`/`AdvisorResult`,
  kein Persistieren der Lesart.

## Testumgebung

Windows 11, `pytest <datei>` gezielt, am Ende `pytest -n auto`. Datenumlenkung
nach `CLAUDE.md`, `NIGHTREIGN_SETTINGS_ORG=DankYeeterT-224`, Testabzug in das
umgelenkte `LOCALAPPDATA` **kopieren**. Spielstand lesen ja, schreiben nie.

## Umfang

**Beruehrt Dateien:** die fuenf plus zwei oben. · **Parallel:** ja — T-221
(`ui-ux-designer`, nur `UI_SPEC.md`/`UI_SPEC_REGISTER.md`). ·
**Scratchpad:** `<scratchpad>/T-224/`

**Schritt 0 (Worktree):** `git branch --contains e593a44` muss den
ausgecheckten Branch nennen. Sonst `STATUS: blockiert`, nichts aendern.

Ein Schritt; Commits auf dem ausgecheckten Branch, kein Push. Laenger als
60 Minuten: `teilweise` mit Stand.

## Scope-Grenzen

- Statblatt (`_build_right`, `recompute`-Zeichenhaelfte) unberuehrt.
- Keine Aenderung an `explain`-Saetzen (AK-188), keine an `goals.py`.
- Keine Aenderung an `UI_SPEC.md`/`ARCHITECTURE.md`; Widerspruch zwischen
  AD-035 und AK-182ff. → melden, auf AK bauen (Spec ist Abnahme).
- QA-242/QA-247 (Picker-Hoehe, Kartenbestand) sind nicht dein Auftrag.

## Praemissen — jede Bestandsbehauptung mit Quelle

- `declared` Tupel `(effect id, count)` · `nrplanner/advisor/types.py:283,436-437`
  · 13.09.2026
- `asking_from` `advisorbar.py:317`, `planner.declared` `:401` · `grep -n` ·
  13.09.2026
- `is_curse` 24 Ids, davon 7 `is_conditional`; `curse` 326; `is_debuff` 78 ·
  T-222 am Testabzug gemessen · 13.09.2026 — nachzaehlen
- 312 Kopien im Spielstand · T-222 B-1 · 13.09.2026
- Suite auf HEAD `64dd006`: 1501 passed / 10 skipped (Worktree T-223) ·
  13.09.2026 — dein Lauf misst neu

Widerspricht der Bestand einer Aussage dieses Auftrags, ist das ein Befund:
melden und auf dem Bestand arbeiten. Eine tragende Praemisse ohne Quelle darf
die Rolle zurueckweisen.

## Rueckgabe

Antwort: Kontraktblock plus hoechstens dreissig Zeilen: die vier Tests mit
Rot-/Gruenlauf, gezaehlte Literale mit Befehl (bewegte Kopien, Fluch-Ids),
Mutationen mit Ergebnis, Suitezahlen, Commit-Shas. Datei
`docs/berichte/T-224-developer.md` bei Befunden oder `STATUS:
teilweise/blockiert`.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
