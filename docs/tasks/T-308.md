# T-308 — A24 Schritt 4: Stufe 2, Nachtbosse Tag 1/2 (AD-040.3, .5) (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen in der
`CLAUDE.md` (Umlenkung Pflicht: `NIGHTREIGN_SETTINGS_ORG=DankYeeterT-308`,
eigenes `LOCALAPPDATA`/`APPDATA`; Testabzug kopieren; kein Fensterlauf).
Stand `7f12d5f` (T-306 committet; T-305 `25f8958`).

**Ziel:** Die Schrittzeile "4 — Stufe 2, Nachtbosse" aus `ARCHITECTURE.md`
Z. 7237 woertlich: `LotResultPlayAreaParam` (`bossId1` = Tag 1, `bossId2` =
Tag 2, 35 Bosskarten, T-299 Abschn. 3b) als zweiter Einstieg in denselben
Block `subbosses` (`days`, `nightlords` ueber den `patternId`-Join),
`bossdata` unveraendert — **mit der Randbedingung AD-042 Punkt 4**: die
Ortsregel (hoechste HP) gilt auf den Nachtkarten nicht; dort bleibt die
Regel aus AD-040 (`INFERRED_MIN_HP`), gezaehlte Fehltreffer `m48_90`,
`m49_20` erscheinen als `ambiguous`/`unresolved`, nicht mit falschem Namen.
Tab: Tagesgruppen sind gebaut (T-306), werden durch Daten sichtbar.
`EXTRACT_VERSION` 14, Testabzug neu, `CLAUDE.md` Z. 75-78 (Zahlen, Herkunft
T-308, Grenze "ueber 14") und `docs/plan-restarbeiten.md`. Tests und Mutation
wie in der Schrittzeile; zusaetzlich `test_nightlord_panel_display.py`:
`also Day 1/2`-Zusatz fuer eine Karte mit `days=[1,2]`, falls es eine gibt
(sonst Testfall benennen und weglassen).

**Stufe/Modus:** developer: mittel, ein Lauf, Commit (Deutsch,
`feat(extract): …`) nur der Dateien unter "Beruehrt".

## Umfang

**Beruehrt:** `nrdata/extract.py`, `nrplanner/bosstab.py`,
`tests/test_extraction.py`, `tests/test_nightlord_panel_display.py`,
`scripts/differential/mutate.py`, `tests/golden/weapon_damage.json`
(Stempel), `CLAUDE.md` Z. 75-78, `docs/plan-restarbeiten.md`, Testabzug
ausserhalb des Repos · **Parallel:** ja (T-307 schreibt nur einen Bericht)
· **Scratchpad:** `<Scratchpad>/T-308/`

## Scope-Grenzen

`nrdata/bossdata.py` unveraendert. Kein `depthstab.py`. Kein Netzwerk,
keine Spieldaten ins Repo. Volle Suite `pytest -n auto` vor dem Commit;
Zaehlung single/group/ambiguous/unresolved ueber die 35 Nachtkarten in die
Rueckgabe, Snapshot-Groesse vorher/nachher (Pruefpunkt d), `build`-Dauer.

## Praemissen — jede Bestandsbehauptung mit Quelle

- 35 Bosskarten, 520 Muster, Join ueber `patternId` · T-299 Abschn. 3b, 4 ·
  gemessen.
- Zusatzkosten 35 Karten 6,6 ms · `docs/perf/baselines.md` S13 · gemessen.
- Fehltreffer `m48_90`, `m49_20` unter der Ortsregel · AD-042 Punkt 4 ·
  gezaehlt vom architect.
- Testabzug heute 841 Dateien, 20 953 085 B · `CLAUDE.md` Z. 75 · T-305.

## Rueckgabe

Kontraktblock plus hoechstens fuenfzehn Zeilen.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
