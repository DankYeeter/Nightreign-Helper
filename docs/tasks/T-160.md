# T-160 - Pruefphase: halten die fuenf Befunde? (security-reviewer)

**Ziel:** Du hast in T-144 fuenf Befunde gemeldet, bevor A15 gebaut wurde.
A15 ist jetzt gebaut. **Prueft, was daraus geworden ist** — und ob der Bau
neue Flaechen geoeffnet hat.

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`.
Es gelten die Projektzeilen aus `CLAUDE.md`. Abweichung: **keine**.

**Scratchpad:** eigenes Unterverzeichnis `T-160/`.

## Der Stand ist eingefroren

Branch `docs/audit-and-advisor-design`, Stand `e5c2b7a`. **Suite vom Director
selbst nachgemessen: 1674 passed, 9 skipped, 0 failed.** Waehrend deines Laufs
aendert **kein** Agent den Code.

## Deine fuenf Befunde und was damit geschah

*Das ist die Buchfuehrung des Directors, **keine Messung** — pruef jede
Zeile.*

| Befund | zugeordnet an | was gebaut wurde, laut Bericht |
|---|---|---|
| **SEC-026** (DLL aus frei gewaehltem Ordner, Annahme von SEC-006 widerlegt) | **niemanden** — Vorlage an den Nutzer | nichts. Der Nutzer hat noch nicht geantwortet. **Blockiert das Release, nicht den Bau.** |
| **SEC-027** (Panel sagt `It is only read`) | T-145 → T-149 | Der Satz ist **weg**. Neu: `Nothing in that folder is changed, moved or deleted. To read the game's files, Nightreign Helper runs a small program out of that folder, so pick a copy of the game you trust — normally the one you play.` |
| **SEC-028** (`regulation.bin` ohne Obergrenze) | T-147 (V1) | **64 MiB in `looks_like_the_game`**, gegen gemessene 1 974 720 Byte. |
| **SEC-029** (`read_bytes()` ohne `stat`) | T-150, dann T-157 | Erst nur der gewaehlte Weg (256 MiB), dann von T-157 nach `inventory.py` gezogen — **beide Wege**, eine Zahl, ein Satz. |
| **SEC-030** (Reparse-Punkte) | T-146 (Spec) + T-147 (V1) | Offenlegungsteil ueber die Rueckfrage C3; Traversierungsteil in `search_from`. |

**Dazu eine Entscheidung, die du kennen musst** (Director, 08.09.2026):
**SEC-026 bleibt Mittel.** Du hattest die Gegenposition offengelegt — wer die
**Wirkung** fuer massgeblich haelt, kommt auf Kritisch und damit auf FAIL. Ich
bin deiner Begruendung gefolgt: kein Rechtegewinn, kein anonymer Ausloeser,
Mechanismus unvermeidbar und bereits angenommen. **Kein WAIVED.** Widersprichst
du dieser Einstufung heute, sag es — sie ist ein Urteil, kein Messwert.

**Und eine, die du nicht vorhergesehen hast:** Die Rueckfrage **C3 greift nur,
wenn der aufgeloeste Ordner den gewaehlten Baum verlaesst** (T-146). Der
Abstieg — laut `UI_SPEC` §4.2 der Regelfall — loest **keine** Rueckfrage aus.
Der Director hat das entschieden, nachdem der `ui-ux-designer` gezeigt hat,
dass C2 der Normalfall ist. **Traegt deine S1-Empfehlung das noch?**

## Was zu beantworten ist

1. **Halten die vier gebauten Befunde?** Je Befund: geschlossen, teilweise,
   oder offen — **am Code, nicht am Bericht.**
2. **Hat der Bau neue Flaechen geoeffnet?** Elf Bauauftraege in einer Nacht,
   darunter zwei neue Dateien (`nrplanner/gamepath.py`, ein
   Systemordnerdialog), ein neuer Worker fuer das Spielstand-Lesen, zwei neue
   QSettings-Schluessel und ein wiederbelebter langsamer Lesepfad.
3. **Die Invariante M3**, die du selbst als sicherheitstragend benannt hast:
   `paths/game` wird **nur** aus einer Nutzerbestaetigung geschrieben, nie aus
   einem Automatikfund; die Gueltigkeitspruefung beim Start ist **dieselbe
   Funktion** wie die Annahmepruefung. **Haelt beides?** T-149 sagt, `_confirm`
   sei die einzige schreibende Stelle. **Pruef es.**
4. **Zwei Stellen, die die Bauenden selbst als ungedeckt gemeldet haben:**
   der `QFileDialog`-Aufruf steht unter keinem Test, und `find_game_dir()`
   geht **nicht** durch `looks_like_the_game` — die Groessendecke greift auf
   dem Automatikweg also nicht (T-147 hat es gemeldet und nicht eigenmaechtig
   geaendert, weil es AK-106/107 beruehrt).

**Abnahmekriterium aus GOAL.md** (woertlich):

> **A2** Alle Befunde der Prioritaet "kritisch" und "hoch" sind behoben oder
> vom Nutzer ausdruecklich zurueckgestellt.

**Stand laut docs/state.md** (woertlicher Auszug):

> **Zuordnung:** SEC-027 → T-145 (Wortlaut), dann V2 · SEC-028 und SEC-030 →
> V1 · SEC-029 → V3/V4 · A-024/A-027 und der ehrliche README-Satz aus SEC-006
> → `technical-writer` nach V3. **Alle fuenf sind zugeordnet, keiner
> zurueckgestellt.**

## Vorgaben

- **Priorisierte Befunde mit Angriffspfad und Behebungsrichtung.** Neue Ids ab
  **SEC-031**.
- **Verhaeltnismaessigkeit bleibt Teil des Urteils** — ein privates
  Einzelplatzwerkzeug, kein Dienst. Eine Vorkehrung, die den Erststart wieder
  zur Sackgasse macht, hat A15 gebrochen statt gesichert. Das war dein eigener
  Satz, und er gilt weiter.
- **Du aenderst keinen Code.**
- **Sag ausdruecklich, ob die Baurunde starten darf** oder ob etwas davor
  gehoert.

## Testumgebung

Windows 10/11 x64, Stand `e5c2b7a`. Lesend. Startest du das Programm, lenkst
du **alle drei** Datenverzeichnisse um und weist jede Umlenkung nach; sonst
brich ab und melde nur das. Der Spielstand ist read-only.

## Scope-Grenzen

- Nicht: der Berater, die sechs Tabs, das Release selbst.
- **Nicht anfassen:** irgendeine Datei. Du liest und berichtest.

**Parallel laeuft T-159** (`qa-engineer`) auf demselben eingefrorenen Stand.
Er faehrt Mutationen **in seinem eigenen Klon**, nicht im Arbeitsbaum.

## Rueckgabeformat

Uebergabe-Kontrakt-Block plus Abschlussbericht **vollstaendig in deiner
Antwort** — du hast kein `Write`, der Director legt ihn unter
`docs/berichte/T-160-security-reviewer.md` ab.
