# T-310 — A24 Schritt 5: Balkenname (AD-043), Ortsregel auf allen Karten, Float-Toleranz (AD-044) (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen in der
`CLAUDE.md` (Umlenkung Pflicht: `NIGHTREIGN_SETTINGS_ORG=DankYeeterT-310`,
eigenes `LOCALAPPDATA`/`APPDATA`; Testabzug kopieren; kein Fensterlauf).
Stand `dd6b4e6` plus ungecommittet AD-043/AD-044 in `ARCHITECTURE.md`
(T-309, 19.09. 15:19).

**Ziel:** Die Schrittzeile "Schritt 5" aus `ARCHITECTURE.md` (Umsetzung ab
Z. 7867, AD-043 ab Z. 7592, AD-044 ab Z. 7719) woertlich: `bossdata.py` —
float32-Toleranz in `_tuned`, `arena_rule` raus, 90015000-Leser neben
`_flag_entities`, Entity je Part, `name_id`; `extract.py` — `derive_places`
einmal ueber beide Kartenmengen, Name = Ereignisname vor `chr_names`,
`EXTRACT_VERSION` 15, Testabzug neu, `CLAUDE.md` Z. 75-78 und
`docs/plan-restarbeiten.md`. Erwartung: 64 `single`, 59 mit Namen, genau 21
geaenderte Zeilen (Pruefpunkt g). Drei Mutationen nach AD-033 aus dem
Dokument. Verbotsliste des Schritts einhalten.

**Stufe/Modus:** developer: mittel, ein Lauf, Commit (Deutsch,
`feat(extract): …`) nur der Dateien unter "Beruehrt".

## Umfang

**Beruehrt:** `nrdata/bossdata.py`, `nrdata/extract.py`,
`tests/test_extraction.py`, `scripts/differential/mutate.py`,
`tests/golden/weapon_damage.json` (Stempel), `CLAUDE.md` Z. 75-78,
`docs/plan-restarbeiten.md`, Testabzug ausserhalb des Repos ·
**Parallel:** ja (T-311 technical-writer in `docs/anleitung/`) ·
**Scratchpad:** `<Scratchpad>/T-310/`

## Scope-Grenzen

Kein `nrplanner/`. `derive` und die vier EMEVD-Funktionen unveraendert.
Volle Suite `pytest -n auto` vor dem Commit; Zaehlung, Namensliste der 21
Aenderungen, Snapshot-Groesse, `build`-Dauer in die Rueckgabe.

## Praemissen — jede Bestandsbehauptung mit Quelle

- 29/29 Ortskarten mit Aufruf, 0/35 Nachtkarten; 23 gleich, 2 Gewinn, 1
  Wechsel (4666 → Black Blade Kindred) · T-309 · gemessen.
- Nachtkarten unter Ortsregel + Toleranz: 35 single, 30 benannt · T-309 ·
  gemessen.
- Testabzug heute 841 Dateien, 21 012 433 B, Version 14 · `CLAUDE.md` Z. 75.

## Rueckgabe

Kontraktblock plus hoechstens fuenfzehn Zeilen.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
