# T-329 — Zyklus 29: Restposten (Lizenz, Extraktion, QA-004/016/255/294)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektfakten:
`CLAUDE.md` (Projektsprache, Umlenkung, Verbote). Nutzerentscheid 22.09.2026
21:58: alle Punkte in diesem Zyklus, **kein Release (sammeln)**, Ingame-Test
des Nutzers zuletzt (QA-222, QA-282, OF-54 dort).

**Phase Bau, Runde 1** — a, b, c laufen parallel (disjunkte Ablagen, keine
Code-Aenderung). Runde 2 (developer) folgt nach Rueckkehr aller drei.

---

## T-329a — EULA-Restrisiko Extraktion 12→16 und Ereignisskripte (compliance-agent)

**Modus:** klaeren. **Stufe:** normal.

**Ziel:** Entscheidungsvorlage, ob die Extraktionsaenderungen seit dem
Nutzerentscheid 09.09. (A-025 FORTSETZEN) eine "groessere Aenderung an der
Extraktion" im Sinne von `docs/legal/AUFLAGEN.md` Z. 13-16 sind.

**Kriterium (woertlich, AUFLAGEN.md Z. 13-16):** "EULA-Restrisiko bleibt
offen. ... der Punkt steht bewusst unentschieden und ist bei jeder groesseren
Aenderung an der Extraktion erneut vorzulegen."

**Stand und Luecke:** Deine Einschaetzung Z. 606-611 ("13→16 keine solche —
Mechanismus unveraendert, nur mehr Tabellen") nennt selbst als **nicht
geprueft**: den Git-Diff und ob das Lesen der Ereignisskripte
(`nrdata/bossdata.py`, `/event/<map>.emevd.dcx`, Z. 8/724) erst seit 1.15.0
besteht. Genau das ist zu klaeren: ist das Lesen einer **neuen Dateiart**
(EMEVD-Ereignisskripte statt Param-Tabellen) derselbe Mechanismus? Lies
`nrdata/bossdata.py` und `nrdata/extract.py` selbst (du hast Read/Grep).
Belegte Tatsache vom Director (22.09.): `git log --oneline v1.14.0..v1.15.0 --
nrdata/bossdata.py` — ungeprueft, lies die Datei statt dem Log zu glauben;
T-307 (19.09.) fuehrte die EMEVD-Lesung laut `docs/state.md` ein.

**Ausserdem eintragen (Nutzerantwort 22.09.2026 21:58, woertlich):** "Nein,
keine [Beanstandung], A-033 abgenommen." → A-033 foermliche Abnahme durch den
Nutzer 22.09.2026; A-035 Bestaetigung bis 22.09.2026, 60-Tage-Ende 07.11.2026
bleibt als letzte Ja/Nein-Frage.

**Rueckgabe:** Ampel + ein Satz Empfehlung (vorlegen ja/nein), Eintrag in
`docs/legal/AUFLAGEN.md` (anhaengen, keine Zeile umschreiben). Kein
Anwaltsauftrag, keine Code-Aenderung. Berichtsdatei nur bei GRAU/ROT.

**Beruehrt:** `docs/legal/AUFLAGEN.md`, ggf. `docs/legal/C-007.md`. **Parallel:** ja.

---

## T-329b — Spec: Pool-Meldung ohne Grund (QA-294) und "N uebersprungen" (QA-004) (ui-ux-designer)

**Modus:** Spec. **Stufe:** normal.

**Befund QA-294 (woertlich, `qa/findings.md` Z. 487/489):** "Nullzelle ohne
Grund -- Recluse Sorceries x Fire: '0 of 3 slots have nothing to choose from'
ohne Satz, dass Glintstone Pebble keinen Feuerschaden traegt und kein
Tauschrelikt mit Feuerzauber im Bestand liegt (AK-344 deckt nur
Katalysatorfaelle)." Director 21.09.: "Symptom liegt in der Pool-Meldung
('slots have nothing to choose from'), nicht in der Zielkarte; ein Satz in
`_spell_cell` widerspraeche AK-342/343." Fundstellen:
`nrplanner/advisorbar.py:224`, `nrplanner/advisor/run.py:413`,
`nrplanner/advisor/types.py:770`.

**Befund QA-004 (woertlich, Z. 23/369):** "Mehrere Steam-Konten: das
'vollste' Save gewinnt still" — Stand T-256: "Automatik waehlt weiter still
(inventory.py:426-441); Abmilderung Ordner-Tooltip (61633c5), Find my save
mit Konto-Satz (3f08e0a); Zustand 'N uebersprungen' fehlt."

**Ziel:** je Befund neue AK ab **AK-356** in `UI_SPEC.md` mit Wortlaut der
Meldung (Englisch, A8), Ort, Ausloesebedingung, und was sie **nicht**
zusichert (A12). Widerspruchsfrei zu AK-342/343/344. Kleinste Loesung: ein
Satz an der vorhandenen Stelle, kein neues Bedienelement.

**Rueckgabe:** AK-Nummern + je ein Satz, welche Datei:Zeile der developer
aendert. **Beruehrt:** `UI_SPEC.md`, `UI_SPEC_REGISTER.md`. **Parallel:** ja.
Kein Fensterlauf (Codelesung genuegt).

---

## T-329c — Eine Identitaetsregel fuer Relikt-Exemplare (QA-016) (architect)

**Entscheidungstiefe:** klein (eine AD). **Stufe:** normal.

**Befund QA-016 (woertlich, Z. 35/371):** "Praemisse der AD-013-Abweichung
am echten Save widerlegt; Offset-Zweig tot." Stand T-256: "zwei
Identitaetsregeln bestehen fort (copy_key mit Offset im Picker, Handle-only
im Berater, AD-013 Pkt 4 unveraendert); Wirkung nur ohne lesbare
Loadout-Tabelle, A7-Zeile vorhanden."

**Ziel:** eine AD (ab **AD-050**) in `ARCHITECTURE.md`: welche Regel gilt
ueberall, AD-013 Pkt 4 angepasst, Liste der Datei:Zeile, die der developer
aendert (hoechstens fuenf Dateien), und der Test, der die Einheitlichkeit
haelt. Tot-Zweig loeschen, wenn er tot ist (Nachweis im Code, nicht aus dem
Befund).

**Rueckgabe:** AD-Nummer, Dateiliste, ob `nrdata/` beruehrt wird (dann
Security-Vorlauf). **Beruehrt:** `ARCHITECTURE.md`, `ARCHITECTURE_REGISTER.md`.
**Parallel:** ja.

---

## Ergebnis Runde 1 (22.09.2026 22:05-22:09)

- a: GRUEN, nicht erneut vorlegen; A-038 Schwellenregel, A-033 abgenommen,
  C-007 neu. EMEVD-Lesung besteht seit spaetestens 02.09. (vor A-025).
- b: AK-365 (QA-294), AK-366 (QA-004) in `UI_SPEC.md` Nachtrag T-329b;
  Nutzerentscheid 22:10: `Find my save...` bleibt sichtbar, wenn AK-366
  greift (Nachtrag dort).
- c: AD-055 in `ARCHITECTURE.md` Themenbereich O (`f1444ed`, Architekt hat
  ohne Recht committet — Retrospektive). Sechs Dateien freigegeben (Director).
- QA-255: kein Code. `CLAUDE.md` nennt die Fallback-Suche und die
  `USERPROFILE`-Umlenkung (gemessen 22.09.: `Path.home()` folgt `USERPROFILE`).
- Vorlauf: **kein Ausloeser** — `nrdata/` wird in d-f nicht beruehrt
  (AD-055 ausdruecklich), keine neue Abhaengigkeit.

## Runde 2 — gemeinsame Vorgaben fuer d, e, f

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`; Projekt-`CLAUDE.md`.
Tests: `pytest <datei>` gezielt ohne `-n`, am Ende einmal `pytest -n auto`
(Stand 21.09.: 1894 passed, 9 skipped). Kein Fensterlauf. Kein Push.
Oberflaechentexte Englisch (A8). Commit-Message Deutsch, Praefix englisch.

---

## T-329d — AK-365 Pool-Meldung + Lizenzwaechter (developer)

**Stufe:** normal. **Worktree:** ja. **Schritt 0:** im Worktree
`git reset --hard <SHA>` auf den Director-Commit, den der Dispatch nennt, und
`git log --oneline -1` in den Bericht — der Worktree startet sonst auf
`origin/HEAD` mit altem Code.

**Vorgabe 1 — AK-365 woertlich:** lies `UI_SPEC.md` Abschnitt
"AK-365 — QA-294/QA-290" (Nachtrag T-329b, ab Z. 7535) und setze ihn
**woertlich** um; die Wortlaute dort sind verbindlich:
- Statuszeile Einzahl `"1 slot has nothing to choose from: nothing you own reaches {choice} here"`,
  Mehrzahl `"{count} slots have nothing to choose from: nothing you own reaches {choice} here"`, kein Punkt am Ende.
- `unknowns`: `"Nothing you own reaches {choice} here: your starting equipment does not carry it, and nothing in your inventory swaps in a spell or weapon art that does."`
- `{choice}` = `chosen_label(hit_with, damage_type)` (`advisor/goals.py:279`)
  ueber `_lower_case_first`.
- Ausloeser: der bestehende QA-290-Zweig `nrplanner/advisor/run.py:403-423`;
  Flag auf `AdvisorResult` neben `blocked_by_a_requirement`
  (`advisor/types.py:772`); `advisorbar.py:228-234` neue Klauselfunktion,
  `:339-344` dritte Wahl. Nur unter `SUGGESTED_WITH_AN_EMPTY_SLOT`.
- **Nicht aendern:** `_spell_cell`, AK-342/AK-343-Zeilen.
- Regressionstest: der QA-294-Fall (Recluse × Sorceries × Fire, kein
  Feuertraeger) zeigt beide Saetze; ein Fall mit Traeger zeigt sie nicht.

**Vorgabe 2 — Lizenzwaechter:** `scripts/check_licences.py:32-33` liest nur
`requirements.txt`. PyInstaller (`requirements-dev.txt` Z. 3,
`pyinstaller==6.21.0`) wird mit dem Bootloader ausgeliefert und fiel beim
Audit (T-296) aus dem Waechter. Der Waechter muss es wieder pruefen —
kleinste Loesung (z. B. beide Dateien lesen oder PyInstaller ausdruecklich).
Bestehender Test dazu anpassen, falls vorhanden.

**Beruehrt:** `nrplanner/advisor/run.py`, `nrplanner/advisor/types.py`,
`nrplanner/advisorbar.py`, `scripts/check_licences.py`, Tests.
**Parallel:** ja, zu T-329e (disjunkt). **Scratchpad:** `T-329d/`.

---

## T-329e — AD-055 eine Identitaetsregel (developer)

**Stufe:** normal. **Worktree:** nein (Hauptbaum). **Parallel:** ja, zu d.

**Vorgabe (AD-055, `ARCHITECTURE.md` Themenbereich O, Liste des Architekten
woertlich, als ein Commit):**
1. `nrplanner/inventory.py:42-47`: Feld `offset` samt Kommentar loeschen.
   `:73-74`: Offset-Zweig in `copy_key` loeschen, Docstring (Z. 50-70) ohne
   die falsche Praemisse neu fassen. `:466`: `offset=entry.offset,` loeschen.
2. `nrplanner/relicslots.py:696-706`: `_holdable` filtert
   `r.handle is not None`. Im `_same_copy`-Docstring (Z. 162-165) faellt
   "or the record's own place".
3. `tests/relics.py:74-89`: `FIRST_OFFSET`, `OFFSET_STRIDE` und `offset=`
   loeschen; Parameter `index` bleibt (24 Aufrufe in 5 Dateien) mit
   `ponytail:`-Kommentar.
4. `tests/test_hostile_savefile.py:569`: `, offset=index * 80` loeschen.
5. `tests/test_relic_ownership.py`: Test Z. 80-98 ersetzen durch
   `test_a_copy_without_a_handle_is_offered_nowhere` (Inventar mit Handles 9
   und None; Picker bietet das Exemplar ohne Handle in keinem Slot an;
   `candidates.pool` fuehrt nur Handle 9 und genau eine A7-Zeile).
   Z. 118-133: fuer das Exemplar ohne Handle `copy_key(...) is None`.

**Nicht:** kein Filter in `Inventory.relics_for`; keine Aenderung an
`candidates.py`; kein Satz im Picker; nichts unter `nrdata/`.
**Wiederholter Text:** Suche `loadout table cannot be read|ohne lesbare`
projektweit, Ergebnis in den Bericht (Architekt fand 5; die zwei in
`ARCHITECTURE.md` bleiben als Verlauf).

**Beruehrt:** die sechs Dateien oben. **Scratchpad:** `T-329e/`.

---

## T-329f — AK-366 "N other saves" + Knopf sichtbar (developer)

**Stufe:** normal. **Nach T-329e** (gleiche Datei `inventory.py`), Hauptbaum.

**Vorgabe — AK-366 woertlich** (`UI_SPEC.md` Abschnitt "AK-366", Nachtrag
T-329b): Fand `scan()` ohne vom Nutzer gewaehlten Pfad (`save_path is None`)
mehr als einen **lesbaren** Spielstand (keinen, der `SaveNotReadable` warf),
traegt die Notizzeile (`app.py:2465`, direkt nach
`"{relic_count} relics in {source}"`, vor der Loadouts-Klausel):
- Einzahl `" — 1 other save was found; this is the one with more relics"`
- Mehrzahl `f" — {n} other saves were found; this is the one with the most relics"`
- kein Punkt am Ende. Nicht bei nur einem Save, nicht nach `Find my save...`.
- Stellen: `inventory.py:381-431` zaehlt, neues Feld auf `Inventory`/`SaveScan`
  (`:92-117`, `:294-313`), `app.py:2465`.

**Vorgabe — Nutzerentscheid 22.09.2026 22:10 (Nachtrag unter AK-366):**
`Find my save...` bleibt nach erfolgreichem Auto-Load sichtbar, wenn der
Halbsatz erscheint; mit einem Spielstand weiter ausgeblendet (`app.py:2488`).

**Tests:** zwei lesbare + ein unlesbarer Save → "1 other save"; ein Save →
kein Halbsatz, Knopf verborgen; `Find my save...`-Pfad → kein Halbsatz.

**Beruehrt:** `nrplanner/inventory.py`, `nrplanner/app.py`, Tests.
**Scratchpad:** `T-329f/`.

---

## Ergebnis Runde 2 (22.09.2026 22:35)

d `d1f78b1`+`6f5f3ce` (Worktree, gemerged `01ef093`), e `e065ab9`, f
`976dfcf`. Suite auf `01ef093`: **1904 passed, 9 skipped, 141 s** (Director).
Offen fuers Gate: Wortform "fire Sorceries" (AK-365 vs. DR-038), Gleichstand
"more relics" (AK-366), erster Import ueberdeckt die Notizzeile (`app.py:2721`).

## Runde 3 — Nutzerentscheid 22.09.2026 22:55: "Absolut alles. Ingame Verifizierung der Zahlen als ALLER letztes."

Reihenfolge: **g ‖ j** → **h** (nach g, gemeinsame Dateien) → **k QA-Fensterlauf**
→ Nutzer ingame. Gemeinsame Vorgaben aus Runde 2 gelten.
**Vorlauf:** Ausloeser `nrdata/extract.py` (Datei schreiben) — Director-Vorgabe
statt Lauf, weil die Senke dieselbe bleibt: Zwischendatei im **selben
Verzeichnis** wie `out`, `os.replace`, bei Ausnahme Zwischendatei loeschen,
kein neuer Pfad ausserhalb von `out.parent`. Security prueft im naechsten
Release-Lauf.

---

## T-329g — Wortform AK-365, Gleichstand AK-366, Cache atomar, Symbolcache (developer)

**Stufe:** normal. Hauptbaum. **Parallel:** ja, zu j.

1. **AK-365 Wortform** (`UI_SPEC.md`, "Nutzerentscheid 22.09.2026 22:45",
   woertlich): "`{choice}` folgt der DR-038-Form aus `advisor/goals.py:688-689`
   (`_max_damage`), wenn Schadenstyp **und** Waffenkunst gewaehlt sind: 'fire
   damage with Sorceries', nicht 'fire Sorceries'. Ist nur eines gewaehlt,
   bleibt der bisherige Wortlaut. Eine Formulierungsstelle fuer beide Senken."
   Stelle: `no_carrier_for` in `nrplanner/advisor/run.py` (T-329d `d1f78b1`).
   Test: beide gewaehlt → DR-038-Form in Statuszeile und `unknowns`.
2. **AK-366 Gleichstand** (woertlich): "traegt kein anderer lesbarer
   Spielstand mehr Relikte als der gewaehlte, aber mindestens einer gleich
   viele, endet der Halbsatz mit 'this is the most recent of those with the
   most relics' statt 'this is the one with (more|the most) relics'."
   Stellen: `inventory.scan()` (`976dfcf`), `app.py` Notizzeile. Test: zwei
   Saves gleich viele Relikte.
3. **Cache atomar:** `nrdata/extract.py:3225` schreibt `out` direkt
   (`out.write_text(...)`); ein Abbruch mittendrin hinterlaesst eine
   halbe Datei. Nach Vorlauf-Vorgabe oben umstellen. Pruefe und nenne im
   Bericht, wie der Leser (`paths`/Laden des Snapshots) eine kaputte Datei
   heute behandelt. Test: Schreibfehler simuliert → altes `out` unveraendert,
   keine Zwischendatei uebrig.
4. **Symbolcache:** `nrplanner/iconpack.py:111-120` `_pixmap` cacht ein
   fehlendes/unlesbares Symbol nicht (`return None` vor dem Cache) — jeder
   Aufruf liest die Platte erneut. `None` mitcachen, wenn das ohne
   Verhaltensaenderung geht (Wiederholungslogik `_read_with_retries`
   beachten: gilt ein Fehlschlag als endgueltig?). Wenn nicht: begruenden,
   nicht aendern.

**Beruehrt:** `advisor/run.py`, `inventory.py`, `app.py`, `nrdata/extract.py`,
`iconpack.py`, Tests. **Scratchpad:** `T-329g/`.

---

## T-329j — Duplikate in `tests/` und `scripts/` (developer)

**Stufe:** normal. **Worktree:** ja, Schritt 0 `git reset --hard <SHA aus
Dispatch>`. **Parallel:** ja, zu g — **nur** `tests/` (ohne die Testdateien,
die g anlegt/aendert: `test_advisor_run.py`, `test_advisor_bar.py`,
`test_save_path_memory.py`, `test_extraction.py`, Symbol-Tests) und `scripts/`.

**Ziel (Ponytail-Audit 19.09., `shrink`, `docs/tasks/T-296.md` Z. 80-82):**
"Harness-Duplikate in `scripts/`, Fixture-Duplikate in `tests/`". Finde
Hilfsfunktionen/Fixtures, die in mehreren Dateien gleich stehen, und fuehre
sie an **einer** vorhandenen Stelle zusammen (`tests/conftest.py`,
`tests/relics.py`, `tests/advisor_cases.py`; in `scripts/` die gemeinsame
Datei, die `drive_window.ps1` schon nutzt oder eine neue). Nur echte
Duplikate (gleiches Verhalten); keine neue Abstraktion. **Nicht:**
`scripts/drive_window.ps1` umbauen (NH-009), `scripts/check_licences.py`.
Suite am Ende gleich viele Tests (Zahl vorher/nachher in den Bericht).
Mehr als fuenf Dateien erlaubt (OF-43: Alternative ist Duplikat-Logik).

**Scratchpad:** `T-329j/`.

---

## T-329h — `_settings()`-Kopien und Farbkonstanten der Oberflaeche (developer)

**Stufe:** normal. Hauptbaum, **nach g**. Zwei Commits.

1. **`_settings()`**: sechs gleichnamige Kopien (`advisorbar.py:141`,
   `chalices.py:42`, `effectfilters.py:58`, `favourites.py:30`,
   `gamepath.py:47`, `uiscale.py:45`, gezaehlt 22.09.). Pruefe, ob sie
   gleich sind (`NIGHTREIGN_SETTINGS_ORG`-Umlenkung in `favourites.py`
   muss fuer alle gelten, CLAUDE.md). Gleiche zusammenfuehren an einer
   Stelle (vorhandenes Modul bevorzugt); abweichende benennen, nicht
   anfassen.
2. **Farbkonstanten**: Hex-Farben als Modulkonstanten bzw. `QColor("#...")`
   in 15 Dateien unter `nrplanner/` (Grep 22.09.). Gleiche Farbrolle mit
   gleichem Wert → eine Stelle (`nrplanner/theme.py` neu, nur Konstanten).
   Verschiedene Werte fuer dieselbe Rolle: **nicht** angleichen, in den
   Bericht (das ist eine Designfrage, A13). Kein sichtbarer Unterschied.
   Mehr als fuenf Dateien erlaubt (OF-43).

**Beruehrt:** die Module oben, `theme.py`, Tests. **Scratchpad:** `T-329h/`.

---

## T-329k — Fensterlauf am Quellstand (qa-engineer, nach h)

Entsteht nach h mit dem Stand im Dispatch.

## Ingame-Test (Nutzer, zuletzt; Programm aus dem Quellbaum `python run.py`)

1. Nightfarer waehlen, Programm schliessen, neu starten: derselbe ist gewaehlt.
2. Recluse, Damage type Fire, Optimize: Leiste sagt "... nothing you own
   reaches fire ... here", Why nennt den ganzen Satz. Mit Magic: kein Satz.
3. Falls zwei Steam-Konten: Notizzeile "1 other save was found ...",
   `Find my save...` sichtbar.
4. QA-222: Fenster nicht vorn, einmal auf Optimize/Tab/Liste klicken —
   wirkt der erste Klick?
5. OF-54: eine Zauberzahl im Berater gegen den Schaden ingame (grob, ein
   Treffer) — passt die Groessenordnung?
