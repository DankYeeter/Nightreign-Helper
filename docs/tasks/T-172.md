# T-172 - Retest: haelt der Fix aus D-001? (qa-engineer)

**Retest.** Ein bekannter Fall wird nachgeprueft — der Regressionstest steht
fest und ist derselbe, den der `developer` gebaut hat.

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`.
Es gelten die Projektzeilen aus `CLAUDE.md`.

**Scratchpad:** eigenes Unterverzeichnis `T-172/`. Mutationen in deinem
**eigenen Klon**.

## Der Fall

**`docs/debug/D-001.md` — lies ihn zuerst.** Kurz: `extract.build()` lieferte
`hero["levels"]` mit **Zahl**-Schluesseln, jeder JSON-Umlauf machte **Text**
daraus. Der Datensatz hatte zwei Gestalten. **Es hat nie funktioniert** —
`model.py:915` griff seit dem ersten Commit nach beiden.

**Der Fehler war nicht sporadisch**, sondern streng determiniert durch die
erreichbare Datenquelle. Vier Messungen widersprachen sich nur deshalb, weil
**keine ihre Bedingung genannt hat**.

## Quittung aus T-171 (`developer`, Commits `a1172fb`, `a350fcb`)

Drei Fundstellen plus zwei Mitzieher: `extract.py` `levels[str(lvl)]` ·
`bossdata.py:415/:487` beide `parts`-Schluessel auf `str` · `model.py:915`
Doppelgriff weg · `build_snapshot.py` auf `["1"]`/`["15"]`. **Die zehn roten
Faelle wurden nicht angefasst und sind von selbst gruen.**

| Bedingung | vorher | nachher |
|---|---|---|
| **leeres** umgelenktes `LOCALAPPDATA` (**die Abnahme**) | 1692 passed, 1 failed, 9 errors | **1703 / 9 / 0** |
| Testabzug im umgelenkten `LOCALAPPDATA` | 1702 passed | **1703 / 9 / 0** |

**Vom Director unabhaengig nachgemessen** in der Bedingung ohne Abzug:
**1703 passed, 9 skipped, 0 failed**, 335 s, Umlenkung nachgewiesen
(`cache_dir` im Scratchpad, Abzug nachweislich abwesend).

Die JSON-Datei auf der Platte aendert sich **um kein Zeichen** — beide
Serialisierungen 8 007 011 Zeichen, sha256 `bf08618a…4d7cc4`.
**`EXTRACT_VERSION` bleibt 11**, Testabzug und Spieler-Cache bleiben gueltig.

## Was du pruefst

1. **Der Regressionstest aus D-001, wortgleich:**
   `tests/test_extraction.py::test_the_built_dataset_survives_a_json_round_trip_unchanged`.
   *"Kein `dict` im gebauten Baum hat einen Schluessel, der keine Zeichenkette
   ist; die Meldung nennt den Pfad."* Plus
   `json.loads(json.dumps(data)) == data` fuer die Wertseite.
   **Toetet er wirklich?** Der `developer` hat beide Haelften einzeln
   zurueckgenommen: `levels` allein → rot mit 10, `parts` allein → rot mit 18,
   **10 + 18 = 28** wie in der Diagnose. **Fahr das nach, in deinem Klon.**
2. **Die Abnahmebedingung: der Lauf OHNE erreichbaren Datenabzug.**
   Der `developer` warnt ausdruecklich: **`-m "not slow"` laesst den neuen Test
   aus und belegt nichts.**
3. **Seine zwei Gegenproben:** das Stat-Blatt fuer Level 1 und 15 muss in
   **beiden** Bedingungen identisch sein · ein `nightreign_data.json` von
   **vor** dem Commit muss weiter laden.
4. **Die Dauerauflage aus D-001** — und die ist der eigentliche Grund, warum du
   und nicht nur der `developer` das ansieht:

   > Die Suite muss **mindestens einmal je Zyklus ohne erreichbaren Datenabzug**
   > laufen, sonst ist der Pfad `extract.build()` → Berechnung im gruenen Lauf
   > nicht abgedeckt.

   **Sag, ob und wie das kuenftig sichergestellt ist.** Heute haengt es daran,
   dass jemand daran denkt — genau so ist der Fehler achtzehn Zyklen lang
   unentdeckt geblieben. *Kostenhinweis des `developer`: der abzugsfreie Lauf
   kostet rund **108 s mehr**, weil jeder xdist-Worker selbst extrahiert.*

## Vorgaben

- **Urteil vierwertig:** PASS, CONCERNS, FAIL. **WAIVED vergibst du nicht.**
- **Eigene Zahl nennen — mit Datenbedingung.** Ohne sie ist sie wertlos; das
  ist die Lehre dieses Falls.
- **Deine Mutationen laufen in deinem eigenen Klon.** Im Arbeitsbaum aenderst
  du nichts.
- Neue Befunde ab **QA-220**.

## Abnahmekriterium aus GOAL.md (woertlich)

> **A3** Der Build-Berater liefert fuer jeden Nightfarer und jedes bekannte
> Kelch-Layout mindestens zwei benannte Zielrichtungen (Schaden maximieren,
> erlittenen Schaden minimieren) und schlaegt je Slot ein konkretes Relikt
> aus dem Besitz des Spielers vor.

**Stand laut docs/state.md** (woertlicher Auszug, 09.09.2026):

> **Suite: 1703 passed, 9 skipped, 0 failed — und zwar in BEIDEN
> Datenbedingungen** […] **Eine Suitezahl ohne genannte Datenbedingung ist in
> diesem Repo wertlos.**

## Testumgebung

Windows 10/11 x64, Branch `docs/audit-and-advisor-design`, Stand `a350fcb`.
Testbefehl nach `CLAUDE.md`.

**Alle drei Datenverzeichnisse umlenken und jede Umlenkung nachweisen** —
gelingt ein Nachweis nicht, brich ab und melde nur das. **Der Spielstand ist
read-only**; 309 Relikte, rund 110 Builds.

*Hinweis des `developer`, damit du es nicht fuer einen Fehler haeltst: das
umgelenkte `APPDATA` nimmt der Suite den Spielstand **nicht** weg —
`savefile.save_roots()` sucht `~/AppData/Roaming/Nightreign` unbedingt.
Umgelenkt ist der Verknuepfungspfad, gelesen wird der echte Save, read-only.*

## Scope-Grenzen

- Nicht: der Bau, der `clean-room`-Lauf, der `power-user` — **die warten auf
  dein Urteil.**
- Im Arbeitsbaum kein `checkout`, `reset`, `clean`, `stash`; kein `push`.

**Parallel laeuft nichts.**

## Rueckgabeformat

Uebergabe-Kontrakt-Block plus Abschlussbericht. **Der Auftrag gilt erst als
erledigt, wenn `docs/berichte/T-172-qa-engineer.md` auf der Platte liegt.**
