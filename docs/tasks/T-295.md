# T-295 — Allow immer klickbar, Bau, Retest, Release 1.14.0 (developer, release-manager, qa-engineer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen stehen
in der `CLAUDE.md` des Projekts (Umlenkung, NH-004, Nutzerdaten nur lesen);
Abweichungen: keine. Stand `a3f8667` + Spec-Nachtrag AK-317 (19.09.).

**Ziel:** Release 1.14.0. Nutzer hat 1.14.0 (Bau `a3f8667`) am 18./19.09.
selbst getestet: "passt". Ein Nutzerbefund: `Allow` war grau, solange die
Familie nicht vermieden war — jetzt immer klickbar (AK-317 Nachtrag).

**Abnahmekriterium (GOAL.md, woertlich):** A23 "Mitglieder ein drittes
Kaestchen Allow: das Mitglied darf in Vorschlaege, muss aber nicht"; A9.

**Stand (docs/state.md):** T-294a Fixes `f5e91ca`, `150d209`, `0bb3086`;
Bau `a3f8667` (59.141.218 B, SHA-256 8CF4B694…D3E7); Retest T-294c beim
Sessionende 17.09. abgebrochen, kein Bericht.

## T-295a — developer (Stufe klein)

`nrplanner/effectfilterdialog.py`: Allow-Kaestchen nie `setEnabled(False)`;
Haken persistiert unabhaengig vom Familienzustand (Modell `allowed` kann
das schon, pruefen `effectfilters.py`); Tooltip-Konstante nach AK-317
Nachtrag (ein Text statt aktiv/inaktiv). Test: Allow klickbar ohne
Familien-Avoid, Haken bleibt nach Familien-Avoid an/aus, `resolved_excluded`
unveraendert. Beruehrt: `effectfilterdialog.py`, `tests/test_effectfilterdialog.py`.
Ein Commit. Kein Fensterstart.

## T-295b — release-manager (build), Version bleibt 1.14.0

Wie T-294b; Sicherung des Vorstands; Bericht T-295.

## T-295c — qa-engineer (Retest, sonnet)

Am neuen Artefakt, Umfang klein: DR-032 (Fenster 608×642 mit drei
Zaehlerklauseln), QA-285 (Duchess Intelligence nackt/Dexterity Betrag;
Recluse Mind ohne Betrag), QA-284 (gehaltener Slot in Statuszeile), Allow
ohne Familien-Avoid klickbar und nach Avoid wirksam. Nummern ab QA-286.
Programm am Ende beenden. Bericht `docs/berichte/T-295-qa-engineer.md`.

## T-295d — release-manager (notes) 1.14.0

CHANGELOG [1.14.0], RELEASE_BODY "What's new" 1.14.0 (A22 Attribute,
A23 Familien/Allow, Fixes), ROLLOUT-Abschnitt (neue Settings-Schluessel
`advisor/allowed`, `advisor/avoided_families`; keine Migration).

## Umfang

**Parallel:** a → b → c; d parallel zu c (kein Fensterstart) ·
**Scratchpad:** `<Scratchpad>/T-295/<rolle>/`

## Scope-Grenzen

Nur der Allow-Punkt im Code. Kein Tag vor QA-Urteil.

## Praemissen — jede Bestandsbehauptung mit Quelle

- Allow ist heute `setEnabled` nach Familienzustand · developer T-292d "Allow-Kaestchen … inaktiv" (Tooltip-Varianten AK-317) · ungeprueft, developer sucht.
- Nutzertest 1.14.0 "passt" · Nutzer 19.09. 08:30.

## Rueckgabe

Kontraktblock plus hoechstens fuenfzehn Zeilen.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
