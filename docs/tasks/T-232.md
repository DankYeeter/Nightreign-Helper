# T-232 — Retest-Fix: AK-270, Waechterfall 1366 px, zwei RuntimeWarnings (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen in der
`CLAUDE.md` des Projekts; Abweichungen fuer diesen Auftrag: Schritt 0 unten.

**Ziel:** Drei kleine Reste vor dem Retest: der Abbruchsatz beim
Lesart-Wechsel nennt die Lesart (AK-270); der Leisten-Waechter kennt die
Nutzer-Untergrenze 1536 px; die zwei `RuntimeWarning` aus
`relicpicker.done()` sind weg.

**Stufe/Modus:** developer, klein

**Abnahmekriterium (GOAL.md, woertlich):** A7 Datenlage nennen; A6.

**Stand (docs/state.md, 13.09.2026):** T-230 gemerged, Suite 1525/9 dreimal;
Nutzerentscheidung: AK-05 gilt ab 1536 px.

## Vorgaben

1. **AK-270** (`UI_SPEC.md:3627-3640`): Lesart-Wechsel waehrend eines Laufs
   sagt `The reading changed while this was working out — use Optimize
   again.`; Zielwechsel behaelt `Your build changed …` (`advisorbar.py:237`).
   Test, der beide Faelle unterscheidet; rot vorher.
2. **Waechter 1366:** `tests/advisor_row_at_the_window.py:41`
   `NARROW_DESKTOPS = (1536,)` — Fall `1366` aufnehmen mit der Erwartung aus
   der Nutzerentscheidung: `goal_box`/`reading_box` **duerfen** dort
   abgeschnitten sein, Tooltip traegt den Statustext weiter; ein Kommentar
   nennt die Untergrenze 1536 und AK-05 (Spec-Satz liefert T-233 parallel).
   Kein Code an der Leiste.
3. **RuntimeWarnings:** `relicpicker.py` `done()`
   `stock_replaced.disconnect(self._the_stock_was_replaced)` warnt zweimal
   je Suite — Ursache messen (doppeltes `disconnect`? nie verbunden?), so
   beheben, dass `pytest -W error::RuntimeWarning tests/test_relic_picker_advisor.py`
   gruen ist.

Mutation je Punkt 1 und 3 (AD-033 Punkt 4).

## Testumgebung

`pytest <datei>` gezielt, dann `pytest -n auto` einmal. Datenumlenkung nach
`CLAUDE.md`, `NIGHTREIGN_SETTINGS_ORG=DankYeeterT-232`, Testabzug kopieren.

## Umfang

**Beruehrt Dateien:** `nrplanner/advisorbar.py`, `nrplanner/relicpicker.py`,
`tests/advisor_row_at_the_window.py`, `tests/test_advisor_bar.py`,
`tests/test_relic_picker_advisor.py`, `scripts/differential/mutate.py`. ·
**Parallel:** ja — T-233 `ui-ux-designer` nur `UI_SPEC.md`,
`UI_SPEC_REGISTER.md`, `DESIGN_REVIEW.md`. · **Scratchpad:** `<scratchpad>/T-232/`

**Schritt 0 (Worktree):** `git branch --contains a23a581` muss den
ausgecheckten Branch nennen. Sonst `STATUS: blockiert`.

Ein Commit je Punkt, kein Push.

## Scope-Grenzen

Nichts an `_opening_width`, Groessenpolitik der Leiste, Texten ausser AK-270.

## Praemissen — jede Bestandsbehauptung mit Quelle

- `Your build changed` in `advisorbar.py:237` · `grep -n` · 13.09.2026
- `NARROW_DESKTOPS = (1536,)` `tests/advisor_row_at_the_window.py:41` ·
  `grep -n` · 13.09.2026
- 2 RuntimeWarnings je Suite · T-230-Bericht, Director-Laeufe (2 warnings) ·
  13.09.2026
- Suite `a73b3b1`: 1525 passed / 9 skipped · Director 2x · 13.09.2026

## Rueckgabe

Kontraktblock plus hoechstens fuenfzehn Zeilen: Satz woertlich, Waechterfall,
Ursache der Warnung, Suitezahlen (0 warnings), Commit-Shas.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
