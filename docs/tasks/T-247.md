# T-247 — Spec: Why-Platz, Lesart-Benennung, Suchhinweis (ui-ux-designer, Spec)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`; Projektzeilen in der
`CLAUDE.md` des Projekts. Kein Anwendungscode, kein Commit.

**Ziel:** Drei Befunde aus der Ingame-Session des Nutzers (14.09.2026) haben
verbindliche Akzeptanzkriterien mit Wortlaut, damit ein `developer`-Auftrag
sie baut.

**Stufe/Modus:** Spec

**Nutzeraussagen, woertlich (14.09.2026, 17:58-18:01):**
- QA-261: "best case war viel 'at low hp' und so weiter. [...] aber wenn ich
  low hp bin, ist es doch worst case? ich haette 'at full hp' oder 'improved
  physical damage negation' als best case eingestuft."
- QA-260: "why war ganz oben, habe es uebersehen. dachte es ist da 'pro
  relikt'."
- QA-265 (T-244): Suchfeld `Filter by effect…` ohne Syntax; seit T-245 sind
  nur `AND`/`OR`/`NOT` gross Operatoren, `"..."` Phrase, `-wort`/`!wort`
  NOT; Fluchnamen werden mitgesucht.

## Aufgaben

1. **QA-261, AK-268-Nachtrag:** Die Lesarten heissen heute Worst case /
   Best case und bedeuten "bedingte Effekte zaehlen nicht / zaehlen alle".
   Der Spieler liest "Best case" als Spielsituation. Vorschlag mit
   Begruendung: Benennung (z. B. `Guaranteed` / `If every condition
   triggers`, oder Erklaerungszeile unter dem Umschalter), A8 Englisch, A12
   (Geltungsbereich nennen). Zwei Varianten, Empfehlung zuerst — die
   Wortwahl entscheidet der Nutzer, nicht du.
2. **QA-260:** `Why` steht ueber allen Vorschlaegen; Erwartung "pro Relikt".
   Vorschlag: `Why`-Zugang je Vorschlagskarte (Knopf/Link) oder sichtbarere
   Platzierung; gegen AK-46/AK-73 (Kartenbreite 208 px, Chipstreifen 118 px)
   pruefen — passt ein Knopf, ohne QA-229/230 zu brechen?
3. **QA-265:** Tooltip- oder Platzhaltertext fuers Picker-Suchfeld, der die
   Syntax nennt; Weapons-Feld (`arsenaltab.py:372`) als Vorbild, beide
   gleich formulieren (A13 Konsistenz).

Am laufenden Fenster pruefen (Datenumlenkung Pflicht, Testabzug kopieren,
PrintWindow). **Parallel arbeitet ein `developer` an `weapons.py`/
`damage.py`/`model.py` (T-246) — kein Fensterlauf gleichzeitig noetig?
Doch: nur eine Instanz zugleich (QA-256); der developer startet kein
Fenster, du darfst.**

## Umfang

**Beruehrt Dateien:** `UI_SPEC.md`, `UI_SPEC_REGISTER.md` (naechste freie
AK-273) · **Parallel:** ja, T-246 · **Scratchpad:** `<scratchpad>/T-247/`

## Rueckgabe

Kontraktblock + max. 20 Zeilen: je Befund AK-Nummer, Wortlaut-Varianten,
Empfehlung, offene Nutzerfrage.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
