# T-298 — Retrospektive Zyklus 27 umsetzen: NH-007, NH-008, NH-009 (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen stehen
in der `CLAUDE.md` des Projekts. Stand `02995be`. Nutzerfreigabe 19.09.
09:45 fuer alle drei Vorschlaege aus `docs/berichte/T-297-retrospective.md`
(Z. 23-75) — dort steht der Wortlaut, hier nur der Rahmen.

**Ziel:** Drei Projekt-Hooks/Skripte, kein Anwendungscode.

**Stufe/Modus:** developer: klein je Abschnitt (ein Lauf, drei Commits).

## T-298a — NH-007: eine Maske im Umlenkungs-Hook

`.claude/hooks/enforce-data-redirect.ps1`: `$istExeStart` streichen,
`$istExeKommando` an beiden Gates (Umlenkung und Instanzsperre). Nachweis:
die sieben Fehlalarm-Beispiele aus dem Bericht (Lesebefehle mit `run.py`,
`tasklist`, `certutil`, `Get-FileHash`) gehen durch; ein echter Start ohne
Umlenkung und ein Start bei laufender Kopie werden abgewiesen (Kopie
selbst starten und beenden, `Get-Process` = 0). Schliesst QA-283.

## T-298b — NH-008: Riegel am Dispatch einer Fensterrolle

Neuer Projekt-Hook `.claude/hooks/no-window-dispatch.ps1` (PreToolUse,
Matcher `Agent|Task`, Eintrag in `.claude/settings.json` nach Muster der
bestehenden): `deny`, wenn der Dispatch-Prompt `qa-engineer`, `power-user`
oder `clean-room` nennt **und** ein `NightreignHelper`-Prozess laeuft;
Meldung nennt PID/Startzeit. Test: Skript mit JSON-Stdin, drei Faelle
(Fensterrolle + laufend → deny; Fensterrolle ohne Prozess → allow; andere
Rolle + laufend → allow).

## T-298c — NH-009: Fenstertreiber ins Repo

`scripts/drive_window.ps1` aus dem Treiber der QA-Laeufe (Scratchpad
`T-293/qa-engineer/drv.ps1`, 144 Z.; fehlt er, aus
`docs/berichte/T-295-qa-engineer.md`/T-285 rekonstruieren): Fenster
finden, Vordergrund (Alt-Trick), Klick per `GetWindowRect`-Koordinaten,
Text per UIA lesen, Beenden. Kopfkommentar mit Aufruf; eine Zeile in
`CLAUDE.md` unter "Testbefehl". Kein Nutzerdatenzugriff im Skript ohne
Umlenkung (Kommentar).

## Umfang

**Beruehrt:** `.claude/hooks/enforce-data-redirect.ps1`,
`.claude/hooks/no-window-dispatch.ps1` (neu), `.claude/settings.json`,
`scripts/drive_window.ps1` (neu), `CLAUDE.md` · **Parallel:** nein ·
**Scratchpad:** `<Scratchpad>/T-298/`

## Scope-Grenzen

Kein Anwendungscode, keine Team-Repo-Dateien (`~/.claude`).

## Praemissen — jede Bestandsbehauptung mit Quelle

- Zwei Masken, sieben Fehlalarme · T-297-Bericht Z. 23-40 · 19.09.
- `drv.ps1` 144 Zeilen im Scratchpad · T-297-Bericht · ungeprueft, pruefen.

## Rueckgabe

Kontraktblock plus hoechstens fuenfzehn Zeilen.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
