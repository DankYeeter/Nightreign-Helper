# T-251 — Pruefphase A18/A19 auf `528ff78` (qa-engineer · security-reviewer · ui-ux-designer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`; Projektzeilen in der
`CLAUDE.md` des Projekts. **Code eingefroren auf `528ff78`**, niemand
aendert `nrplanner/`, `tests/`, `scripts/`; Register nur anhaengend; kein
Commit. Nur eine Fensterinstanz zugleich (QA-256): `ui-ux-designer` zuerst
am Fenster, `qa-engineer` beginnt Qt-frei und meldet sich per Bericht.

**Abnahmekriterien (GOAL.md, woertlich):** A18 "Ein markierter Effekt zaehlt
in keinem Vorschlag und in keiner Rangfolge des Beraters [...] das Relikt
selbst bleibt waehlbar [...] Die Markierung wird gespeichert und beim
naechsten Start wieder angewandt." · A19 "Der Berater liefert dann nur
Konstellationen, in denen mindestens ein gewaehltes Relikt jeden markierten
Effekt traegt [...] Gibt es im Besitz keine passende Kopie fuer die freien
Slots, sagt er das (A7)." · Nachweise A18/A19 laut GOAL.

**Stand:** T-248d Schritt 1-3 (`d6fda65`, `9423eee`, `e7a8eef`), T-250a/b
(`115dd4e`, `528ff78`); Suite 1597 passed / 9 skipped / 4 failed
(QA-266-Trio + QA-249). Spec AK-276-291, AD-036. Berichte
`docs/berichte/T-248d-developer.md`; Bilder `<scratchpad>/T-248/developer/shots/`.

## T-251a — `qa-engineer` (opus)

1. A18/A19-Nachweise woertlich (Fixture und eingefrorener Save, 314 Kopien):
   ausgeschlossener Effekt in keinem Vorschlag/keiner Rangfolge, Relikt
   bleibt waehlbar; Pflicht in jedem Vorschlag, unerfuellbar → AK-281/291-
   Satz; Rangfolge ohne Markierung = alte Best-case-Rangfolge (OF-37).
2. Persistenz ueber echten Neustart (umgelenkter Store), AK-289 waehrend
   Optimize, Zaehler-Tooltip, drei Zustaende auf Karte und Why-Dialog.
3. A5/A7/A8 auf den neuen Saetzen; A6 mit Pflicht (developer: +23 %).
4. **QA-266 aufklaeren:** drei Fenster-Tests fallen deterministisch seit ca.
   21:30 auch auf `b3a2c2a`, das um 21:08 gruen war (`test_advisor_bar::
   test_the_row_does_not_scroll_away_with_the_slots` — Scrollbar-Maximum 0
   bei 1320x560; `test_relic_restore::…no_other_slot_was_given`;
   `test_save_read_in_the_background::…neither_the_window_nor_the_focus`).
   Kein fremder Prozess mehr, Spiel laeuft seit 18:04 (auch um 21:08).
   Umgebung oder Zustand? `git bisect` auf den Commits dazwischen ist
   erlaubt (in einem Scratch-Klon).
5. `MUTATIONS` nachfahren und loeschen (OF-35).
**Schreibt:** `docs/berichte/T-251-qa-engineer.md`, `qa/findings.md`.

## T-251b — `security-reviewer`

Diff `dde2efc..528ff78`: neuer Store `effectfilters.py` (QSettings-Schluessel
`advisor/excluded`, `advisor/required`: Deserialisierung fremder Werte —
was passiert bei Nicht-Ints, riesigen Listen, fremdem Typ?), Ids aus dem
Store in `SlotProblem` (Vertrauensgrenze Registry → Rechnung), neue Texte
in Rich-Text-Labels (SEC-019-Klasse: Effektnamen aus der Spielinstallation
in `MarkedLine`, Tooltips, Legende). Antwort im Nachrichtentext.

## T-251c — `ui-ux-designer` (Review)

AK-276-291 am laufenden Fenster (Umlenkung, PrintWindow): drei Zustaende,
Legende, Zaehler, AK-289-Satz, Leistenbreite (596/338/1608 laut developer),
Why-Gruppen +4 px bei 21 Zeilen — abnehmen oder Befund; developer-Fragen:
Enter drueckt den QToolButton nicht (nur Leertaste; AK-278 "oder"),
Tooltip-Wortwahl "Click to include it again" fuehrt zu Must include;
`_marked_line` als Codename in UI_SPEC Z. 4135/4148. DR-Register und
`DESIGN_REVIEW.md` nachziehen; AK-Aenderungen nur als Nachtrag.
**Schreibt:** `DESIGN_REVIEW.md`, `UI_SPEC.md`, `UI_SPEC_REGISTER.md`.

## Rueckgabe (alle)

Kontraktblock + max. 30 Zeilen.
