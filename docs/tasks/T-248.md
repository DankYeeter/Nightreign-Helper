# T-248 — A18: Lesarten weg, Ausschlussfilter fuer Effekte (architect · ui-ux-designer · developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`; Projektzeilen in der
`CLAUDE.md` des Projekts.

**Abnahmekriterium (GOAL.md A18, 14.09.2026, woertlich):** "Der Berater hat
keinen Umschalter Worst case / Best case mehr. Bedingte Effekte gehen in die
Rechnung ein wie unbedingte, sofern der Spieler sie nicht ausgeschlossen hat
[...] Der Spieler kann einzelne Effekte [...] als *don't include* markieren.
Ein markierter Effekt zaehlt in keinem Vorschlag und in keiner Rangfolge des
Beraters [...] das Relikt selbst bleibt waehlbar [...] Die Markierung wird
gespeichert und beim naechsten Start wieder angewandt."

**Stand (docs/state.md, 14.09.2026):** A16 gebaut in T-224 (AD-035, zwei
Lesarten `reading` im `AdvisorRequest`, Umschalter in `advisorbar.py`,
AK-268); QA-261: Nutzer liest "Best case" als Spielsituation.

---

## T-248a — `architect` (Entscheidungstiefe: Schnitt + Datenmodell)

AD-036: Ersatz von AD-035. Fragen: Ausschlussmenge (Effekt-Ids) im
`AdvisorRequest` statt `reading`; wo die Rechnung bedingte Effekte heute
nach Lesart filtert (`advisor/goals.py`, `evaluate.py`, `candidates.py`,
`search.py`) und was davon faellt; Persistenz der Markierung (`favourites.py`
QSettings-Store, `__schema` — Migration?); Verhaeltnis zu `declared`
(Situationsschalter im Werteblatt: bleibt, ist etwas anderes); `MUTATIONS`-
Anker, die fallen. Zeilen- und Dateizahl je Schritt, hoechstens fuenf
Dateien je developer-Auftrag (OF-34). **Schreibt:** `ARCHITECTURE.md`,
`ARCHITECTURE_REGISTER.md`.

## T-248b — `ui-ux-designer` (Spec)

AK-268 und AK-273 entfallen (Vermerk). Neu: wo der Spieler Effekte
ausschliesst (Vorschlag: im Relic picker je Effektzeile und in der
`Why`-Erklaerung ein `Don't include`; Liste der Ausschluesse in der
Berater-Leiste oder Tooltip mit Zaehler `3 effects excluded`), Wortlaut A8,
A12 (was der Ausschluss deckt und was nicht: nur Berater, nicht Werteblatt),
Rueckweg (Ausschluss aufheben), Leistenbreite ohne Umschalter (AK-194/AK-05
neu messen, 1536 px). Am laufenden Fenster (Datenumlenkung Pflicht,
PrintWindow). **Schreibt:** `UI_SPEC.md`, `UI_SPEC_REGISTER.md` (ab AK-276).

## T-248c — `developer` klein, **jetzt parallel**: AK-274 + AK-275

Unabhaengig von A18: `Why`-Knopf in jeder `SuggestionBlock`-Karte neben
`Use` (AK-274, `UI_SPEC.md` §4.3, gleicher Dialoginhalt wie die Leiste) und
Platzhalter des Picker-Hauptsuchfelds `Filter by effect — supports AND, OR,
NOT and "quoted phrases"` (AK-275, wortgleich `arsenaltab.py:290`).
Dateien: `nrplanner/advisorblock.py`, `nrplanner/relicpicker.py:1087`,
Tests. Kein Fensterlauf (der `ui-ux-designer` hat die Instanz). Ein Commit,
`pytest tests/test_advisor_block.py tests/test_relic_picker_advisor.py`
ohne -n.

## T-248d — `developer` (nach a und b, naechste Sitzung)

Bau nach AD-036 und AK-276ff; Vorgaben aus den beiden Berichten.

---

**Parallel:** a, b, c gleichzeitig — disjunkte Dateien (ARCHITECTURE /
UI_SPEC / advisorblock+relicpicker). **Scratchpad:** `<scratchpad>/T-248/<rolle>/`.

## Rueckgabe (alle)

Kontraktblock + max. 30 Zeilen.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
