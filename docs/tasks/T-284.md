# T-284 — Release-Haertung und Release-Text 1.13.1 (developer, technical-writer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen stehen
in der `CLAUDE.md` des Projekts; Abweichungen fuer diesen Auftrag: keine.
Stand `a4e8b97`. Pruefphase T-283 abgeschlossen: QA PASS (0 Befunde),
Security PASS (SEC-048 niedrig), Compliance GELB (A-037 empfohlen).

**Ziel:** Das per Tag ausgeloeste GitHub-Release traegt von Anfang an die
Pflichtbeschreibung (A-023/A-024) und laeuft ohne persistiertes Schreib-
Token; die Freunde des Nutzers finden auf der Release-Seite, was sie tun
muessen.

**Abnahmekriterium (GOAL.md, woertlich):** A8 "Alle Texte in der
Oberflaeche sind Englisch"; Rahmen: Release-Texte Englisch (CLAUDE.md).

**Stand (docs/state.md, 16.09.2026):** "Release per Tag `v1.13.1` auf dem
Branch, `release.yml` baut; PR #16 mergt der Nutzer selbst".

## T-284a — developer (Stufe klein)

1. **SEC-048** (`security/findings.md`, 16.09.): `actions/checkout` in
   `.github/workflows/release.yml:39` bekommt `with: persist-credentials:
   false`; `requirements-dev.txt` pinnt die transitiven Testpakete
   (colorama, iniconfig, packaging, pluggy, pygments, execnet — Versionen aus
   `pip show` der lokalen Umgebung, im Bericht nennen).
2. **A-037** (`docs/legal/C-006.md`): `softprops/action-gh-release`
   (`release.yml:153`) bekommt `body_path: docs/release/RELEASE_BODY.md`.
   Die Datei schreibt parallel der technical-writer (T-284b) — du legst sie
   nicht an; existiert sie beim Commit noch nicht, ist das kein Blocker.
3. Nachweis: `python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/release.yml'))"`
   (PyYAML vorhanden? sonst `pip show pyyaml` melden), `pip install -r
   requirements-dev.txt --dry-run` ohne Konflikt.
Beruehrt: `.github/workflows/release.yml`, `requirements-dev.txt`. Ein
Commit `ci(release): SEC-048 Token nicht persistieren, Testpakete gepinnt,
A-037 body_path`.

## T-284b — technical-writer

1. **`docs/release/RELEASE_BODY.md` (neu, Englisch):** wird von
   `release.yml` woertlich als Release-Beschreibung veroeffentlicht. Aufbau:
   (a) an erster Stelle **Variante A wortgenau** aus
   `docs/release/RELEASE_TEXT.md` in der Fassung von `docs/legal/C-006.md`
   Z. 36-77 (Not affiliated / What this program does / contains no game
   data) — kein Wort aendern; (b) "How to install": EXE herunterladen,
   SmartScreen-Hinweis, Erststart zeigt auf die eigene Spielinstallation,
   Pruefsumme `.sha256`; (c) "What's new since 1.7.1" — kurz, aus
   `CHANGELOG.md` (1.10.0 bis 1.13.0) plus 1.13.1: Erklaerungszeile im
   Effektfilter-Fenster (AK-313), Testsuite im Release-Lauf; Versionen vor
   1.10.0 sind nicht rekonstruiert (CHANGELOG-Kopf) — so sagen. Kein
   Datum eintragen, das nicht stimmt; kein Versionsvergleich, den du nicht
   am `CHANGELOG.md` belegen kannst.
2. **`README.md`:** Abschnitt zum Effektfilter-Fenster um die
   Erklaerungszeile (Wortlaut `UI_SPEC.md` AK-313) ergaenzen, falls der
   Abschnitt Favourite/Avoid erklaert; Versionsnennungen auf 1.13.1
   pruefen. SEC-019 (offen): der Satz "makes no network connections at all"
   in README:546 bleibt, wie er ist — nicht Teil dieses Auftrags, nur
   melden, wenn du ihn siehst.
3. Jeden Schritt der Installationsanleitung, den du nicht ausfuehren kannst
   (Download von GitHub), als ungeprueft markieren.
Beruehrt: `docs/release/RELEASE_BODY.md`, `README.md`,
`docs/anleitung/` nur bei Bedarf. Kein Commit — der Director committet.

## Umfang

**Parallel:** ja (a und b, disjunkte Dateien) · **Scratchpad:**
`<Scratchpad>/T-284/<rolle>/`

## Scope-Grenzen

Kein Anwendungscode. `upx=False` in `NightreignHelper.spec` (C-006-
Nebenfund) ist zurueckgestellt. Kein CHANGELOG (release-manager `notes`).

## Praemissen — jede Bestandsbehauptung mit Quelle

- `release.yml:39` checkout ohne `with:`, `:153` action-gh-release, `:156` `files:` · grep · 16.09.
- `requirements-dev.txt` = `-r requirements.txt`, pytest==9.1.1, pytest-xdist==3.8.0 · cat · 16.09.
- `docs/release/RELEASE_TEXT.md` 181 Zeilen, Variante A/B · head · 16.09.
- `CHANGELOG.md` beginnt bei 1.10.0 · head · 16.09.

## Rueckgabe

Kontraktblock plus hoechstens dreissig Zeilen.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
