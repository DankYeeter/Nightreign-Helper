# T-227 — QA-248: Startbreite aus Beraterleiste und Effekttabelle ableiten (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen
(Testbefehl, Datenverzeichnisse, verbotene Zugriffe, Zielsystem) stehen in
der `CLAUDE.md` des Projekts; Abweichungen fuer diesen Auftrag: Schritt 0
unten (Worktree-Stand).

**Ziel:** Das Fenster startet so breit, dass auch der Vorschlagszustand der
Beraterleiste (Apply all / Why / Clear sichtbar) nichts abschneidet und die
Statuszeile > 0 px behaelt; `test_at_the_opening_width_no_action_button_is_cut`
wird auf HEAD gruen, die Suite ist gruen.

**Stufe/Modus:** developer, klein

**Abnahmekriterium (GOAL.md, woertlich):** A6 — Oberflaeche blockiert
nicht; A7 — die Oberflaeche nennt die Datenlage (abgeschnittene Zielwahl
sagt nichts).

**Stand (docs/state.md, 13.09.2026, woertlich):** "Suite auf HEAD 1 failed /
1517 passed / 9 skipped — der rote Test ist QA-248 (Vorschlagszustand 683 px
> 498 px). Nutzerentscheidungen 13.09.: Startbreite aus Leiste und Tabelle
ableiten".

**Nutzerentscheidung 13.09.2026, woertlich:** "Startbreite aus Leiste UND
Tabelle ableiten — Das Fenster startet so breit, dass auch der
Vorschlagszustand der Leiste passt (ca. 1540 px statt 1350). Eine Zeile Code
in der Startbreiten-Ableitung, bleibt bei A14 ('abgeleitet'), Leiste bleibt
einzeilig."

## Kontext

`qa/findings.md` letzte QA-248-Zeile; `docs/berichte/T-225-developer.md`
Befund 1 (Bedarf 57+183+87+80+80+80+80+6·6 = 683 px bei 498 px Zeile,
Fenster 1350) und Befund 4 (`GOAL_BOX_WIDTH` ist Maximum). Startbreite:
`nrplanner/app.py:2580` `_opening_width` = `max(minimumSizeHint, min(
_width_around_the_effect_table(), room))`. Waechter im Kindprozess:
`tests/advisor_row_at_the_window.py` (T-225), Aufrufer in
`tests/test_advisor_apply.py` und `tests/test_advisor_bar.py`.

## Vorgaben

- Ein zweiter Term in `_opening_width`: `max(_width_around_the_effect_table(),
  <Breite, die die Leiste im Vorschlagszustand braucht>)`, weiter durch
  `room` gedeckelt. Die Leistenbreite **aus der Leiste selbst** holen
  (`sizeHint`/`minimumSizeHint` der Zeile mit sichtbaren Aktionsknoepfen,
  gemessen, nicht 683 hartkodiert) — ein Helfer auf der `AdvisorBar`, wenn
  es sonst nicht geht, sonst nichts Neues. Docstring von `_opening_width`
  um den Term ergaenzen (A14 bleibt "abgeleitet").
- Der Vorschlagszustand zum Messen: die Knoepfe muessen fuer die Messung
  sichtbar sein oder ihre Breite muss unabhaengig von der Sichtbarkeit
  zaehlen — belege, welchen Weg du nimmst und dass die Zahl beim Start (ohne
  Vorschlag) dieselbe ist.
- Rote Phase: der Test ist auf HEAD rot (`ffc0a4c`); nach dem Fix gruen,
  und mit dem alten Term allein (Term per `monkeypatch` entfernt) wieder
  rot. Fensterbreite am laufenden Fenster im Bericht (vorher 1350, nachher
  gemessen).
- Kein Umbau an der Leiste, keine zweite Zeile, keine Aenderung an
  `GOAL_BOX_WIDTH` oder Texten.

## Testumgebung

Windows 11, `pytest tests/test_advisor_apply.py tests/test_advisor_bar.py`
gezielt, dann `pytest -n auto`. Datenumlenkung nach `CLAUDE.md`,
`NIGHTREIGN_SETTINGS_ORG=DankYeeterT-227`, Testabzug **kopieren**.

## Umfang

**Beruehrt Dateien:** `nrplanner/app.py`, `nrplanner/advisorbar.py` (nur
Helfer, falls noetig), `tests/test_advisor_apply.py`; `scripts/differential/mutate.py`
fuer die Mutation. · **Parallel:** ja — `ui-ux-designer` nur `UI_SPEC.md`. ·
**Scratchpad:** `<scratchpad>/T-227/`

**Schritt 0 (Worktree):** `git branch --contains 5d0c1c2` muss den
ausgecheckten Branch nennen. Sonst `STATUS: blockiert`, nichts aendern.

Ein Commit, kein Push.

## Scope-Grenzen

- QA-248 nur ueber die Startbreite; nichts an Leiste, Tabelle, Spec.
- Kurzform `Worst`/`Best` ist gestrichen (Nutzer 13.09.) — nicht bauen.

## Praemissen — jede Bestandsbehauptung mit Quelle

- `_opening_width` `app.py:2580-2600`, `_width_around_the_effect_table`
  `:2602` · `grep -n` · 13.09.2026
- Bedarf 683 px, Zeile 498 px, Fenster 1350 px · T-225 Befund 1, am laufenden
  Fenster · 13.09.2026
- Suite `ffc0a4c`: 1 failed / 1517 passed / 9 skipped · Director `pytest -n
  auto` · 13.09.2026

Widerspricht der Bestand einer Aussage dieses Auftrags, ist das ein Befund:
melden und auf dem Bestand arbeiten. Eine tragende Praemisse ohne Quelle darf
die Rolle zurueckweisen.

## Rueckgabe

Antwort: Kontraktblock plus hoechstens zwanzig Zeilen: Term woertlich,
Fensterbreite vorher/nachher, Statuszeilenbreite im Vorschlagszustand,
Rot/Gruen, Mutation, Suitezahlen, Commit-Sha.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
