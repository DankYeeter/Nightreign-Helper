# T-303 — A24 Schritt 1: Extraktor Stufe 1 (AD-040.1-7) plus QA-286, EXTRACT_VERSION 13 (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen stehen
in der `CLAUDE.md` des Projekts (Umlenkung Pflicht:
`NIGHTREIGN_SETTINGS_ORG=DankYeeterT-303`, eigenes `LOCALAPPDATA`/`APPDATA`;
kein Fensterlauf noetig). Stand `c03fd9d` plus ungecommittet A24, AD-040/041,
QA-286, T-301-Messung (`docs/perf/baselines.md` S13).

**Ziel:** Die Zeile "1 — Stufe 1, Extraktor" aus `ARCHITECTURE.md` Z. 7228
woertlich, mit dem Entwurf AD-040 (Themenbereich L ab Z. 6913) und der
Liste "Was der developer ausdruecklich nicht tut" (Z. 7254-7273).

**Zwei Abweichungen vom Entwurf, entschieden vom Nutzer 19.09. 13:25
(OF-46):** Kategorie 160 und die 16 Karten `m20_00..m21_50` werden **nicht**
gelesen und nicht in `subbosses` aufgenommen; Stufe 1 umfasst nur die 29
Ein-Boss-Karten der Kategorie 120 (Feldbosse). Das Wort "Evergaol" kommt
nirgends vor. Pruefpunkt (b) gilt entsprechend fuer 29 Karten.

**Messbefund T-301 (bindend):** die in `build()`/`_load_text` bereits
offenen Archive wiederverwenden, kein zweiter `dvdbnd.open_all` (sonst
+9,3 s statt +21 ms).

**Stufe/Modus:** developer: mittel, ein Lauf, Commit am Ende (Deutsch,
`feat(extract): …`), Testabzug nach `CLAUDE.md` ersetzen und in
`docs/plan-restarbeiten.md` vermerken.

## Umfang

**Beruehrt:** `nrdata/bossdata.py`, `nrdata/extract.py`,
`tests/test_extraction.py`, `docs/plan-restarbeiten.md`, Testabzug
(ausserhalb des Repos) · **Parallel:** ja (T-302 schreibt nur `UI_SPEC*.md`)
· **Scratchpad:** `<Scratchpad>/T-303/`

## Scope-Grenzen

Kein Tab, keine Oberflaechenwoerter (kommen aus T-302), kein
`depthstab.py` (Schritt 3). Keine Spieldaten ins Repo; `NightreignHelper.spec`
zaehlt weiter zwei Quellen. `pytest tests/test_extraction.py` ohne `-n`,
volle Suite `pytest -n auto` vor dem Commit.

## Praemissen — jede Bestandsbehauptung mit Quelle

- Leseroute · `docs/berichte/T-299-developer.md` Abschn. 7 · geprueft.
- 29 Ein-Boss-Karten Kat 120 sind `single` · T-299 Abschn. 3 / AD-040
  Pruefpunkt (b) · gemessen.
- Zusatzkosten 21 ms bei Archiv-Wiederverwendung · `docs/perf/baselines.md`
  S13 · gemessen n=2.
- Snapshot heute 8 522 218 B · T-269a · Pruefpunkt (d): ueber +10 % faellt
  `parts` weg.

## Rueckgabe

Kontraktblock plus hoechstens fuenfzehn Zeilen: Zaehlung
single/group/ambiguous/unresolved, Snapshot-Groesse vorher/nachher,
`build`-Dauer, Testlauf, Commit-Hash.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
