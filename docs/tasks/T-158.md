# T-158 - Suite gruen, und die letzten elf Registereintraege (developer)

**Ziel:** Ein roter Anker, ein wertlos gewordener Kampagnenlauf, elf fehlende
Eintraege. Danach ist der Stand einfrierbar.

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`.
Es gelten die Projektzeilen aus `CLAUDE.md`. Abweichung: **keine**.

**Scratchpad:** `T-158/` fuer Eigenes. **Lies zuerst `T-156/`** — dort liegen
Katalog und Texte fuer T-151 fertig.

## Umfang

**Stufe:** klein.
**Beruehrt Dateien:** `scripts/differential/mutate.py`. **Sonst nichts.**
*(`Beruehrt Dateien` bindet als Whitelist — Director-Entscheidung 09.09.2026.
Brauchst du eine Datei ausserhalb: **melden und anhalten**.)*

## 1. Der rote Anker

**Von zwei Laeufen unabhaengig gemeldet** (T-156 und T-157), gleiche
Diagnose: der Eintrag `the-limit-is-under-a-real-save` ankert auf
`LARGEST_SAVE_TO_READ` in `nrplanner/app.py` — die Konstante ist nach
`nrplanner/inventory.py` umgezogen (T-157 hat nachgezaehlt: **dort genau
einmal, in `app.py` null mal**).

**Das ist eine Pfadreparatur, kein "traegt nicht mehr":** gleicher Name,
gleicher Wert, gleicher Zweck. **Pruef das nach, bevor du es glaubst** — wenn
die Konstante inhaltlich etwas anderes tut als vorher, ist es ein Befund und
kein Umbiegen.

**Suite steht damit auf `1 failed, 1673 passed, 9 skipped`** (T-157, gemessen).

## 2. Ein Kampagnenlauf, der wiederholt werden muss

**T-157 hat es selbst gemeldet, und es ist der Grund fuer diesen Punkt:**

> Beim Stoppen war mein Prozessfilter zu breit und hat **zwei
> pytest-Prozesse von T-156 erschlagen** (ca. 04:20, gegen
> `test_save_path_memory.py`); dessen Kampagnenergebnis aus dem Zeitraum ist
> wertlos und muss wiederholt werden.

**Betroffen ist der T-150-Block** (18 Eintraege, Commit `aeaab8c`). Die
**Anker** sind davon unberuehrt — sie werden statisch geprueft. Wertlos ist
der **Toetungsnachweis**: ob jede der 18 Mutationen wirklich einen Fall rot
macht, ist in diesem Fenster nicht verlaesslich gemessen worden.

**Fahr die Kampagne fuer den T-150-Block erneut.** Ergibt sie ein anderes
Bild als eingetragen — eine Mutation ueberlebt —, ist das ein **Befund**:
melden, den Eintrag mit der Begruendung versehen, **nicht** den Test passend
machen.

## 3. Die letzten elf

**T-151s elf Eintraege sind nicht begonnen.** T-156 dazu: *"Katalog und
`survival_means`-Text liegen bereits fertig in `…/scratchpad/T-156/means.py`,
nur der Kampagnenlauf und das Eintragen fehlen."*

**Zwei Fehler in der Infrastruktur sind bekannt und in T-156s Kopie
korrigiert** — ein fehlendes Backslash-Escaping in `write_registry.py` (vom
Anker-Waechter selbst gefangen) und eine tote `RENAME`-Zuordnung. **Benutz
die korrigierte Fassung**, nicht die aus `T-155/`.

**Und pruef selbst**, ob die Git-Archiv-Baeume aus den Vorlaeufen fuer T-151
noch taugen — T-156 hat das ausdruecklich offengelassen: *"fuer T-151 muss der
naechste Lauf das selbst pruefen."*

## Vorgaben

- **Committe nach jedem der drei Punkte.** Zwei Laeufe in Folge sind an der
  Zugschwelle geendet; beim zweiten war nichts verloren, weil blockweise
  committet wurde.
- **Kein Hintergrundlauf, auf dessen Ende du wartest**
  (`~/.claude/agents/_rahmen.md:49`).
- **Beim Beenden eigener Prozesse: filter eng.** T-157 hat mit einem zu
  breiten Filter fremde Testlaeufe erschlagen — deshalb gibt es Punkt 2. Nimm
  die Prozess-Id, nicht ein Namensmuster.
- **Traegt eine Mutation nicht mehr: Befund.** Ueberlebende werden **mit
  Begruendung eingetragen**, nicht weggelassen.
- **Am Ende muss der Anker-Waechter gruen sein und die Suite `0 failed`
  melden.** Das ist das eigentliche Abnahmekriterium dieses Auftrags.

## Abnahmekriterium aus GOAL.md (woertlich)

> **A1** Ein schriftlicher Audit-Bericht liegt vor, der Korrektheit,
> Sicherheit, Architektur und Bedienbarkeit abdeckt, mit priorisierten
> Befunden. Jeder Befund hat Status (offen / behoben / zurueckgestellt).

**Stand laut docs/state.md** (woertlicher Auszug, autonomer Lauf):

> 1. Zyklus 18 schliessen — U8 (AD-029 Stufe B), die zwei fehlenden
>    U7-Messungen, **Pruefphase**.

*Die Pruefphase wartet auf diesen Auftrag: sie braucht einen eingefrorenen,
gruenen Stand.*

## Testumgebung

Windows 10/11 x64, Branch `docs/audit-and-advisor-design`. Testbefehl nach
`CLAUDE.md`. **Suitezahl gegen 1 failed, 1673 passed, 9 skipped** (T-157).

Alle drei Datenverzeichnisse umlenken und nachweisen, sonst abbrechen und nur
das melden. **Achtung, aus T-157 gelernt:** eine Mutation an einer
Groessenschranke kann die Tests eine riesige Datei anlegen lassen — dort
wurden 140 GB belegt. **Sieh dir an, was eine Mutation ausloest, bevor du sie
faehrst.**

## Scope-Grenzen

**Nicht anfassen:** `nrplanner/`, `nrdata/`, `tests/`, `scripts/` ausser
`differential/mutate.py`, `UI_SPEC.md`, `ARCHITECTURE.md`, `GOAL.md`,
`docs/state.md`, `qa/findings.md`, `docs/tasks/`.

**Parallel laeuft nichts, und der Director committet waehrend deines Laufs
nur `docs/` und nur mit Pfad.**

## Rueckgabeformat

Uebergabe-Kontrakt-Block plus Abschlussbericht. **Der Auftrag gilt erst als
erledigt, wenn `docs/berichte/T-158-developer.md` auf der Platte liegt.**
Commit mit Pfad hinter `--`.
