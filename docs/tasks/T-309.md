# T-309 — A24 Nachtrag: Balkenname ueber EMEVD-Ereignis 90015000 (architect)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen in der
`CLAUDE.md`. Stand `b59e85e`; T-308 (Schritt 4) laeuft parallel in
`nrdata/extract.py` und `nrplanner/bosstab.py`.

**Befund T-307 (`docs/berichte/T-307-developer.md`):** Der Name am
Lebensbalken haengt am Platz, nicht an der Figur: Ereignis 90015000 in der
Karten-EMEVD reicht je Entity-Id eine NpcName-Id herein. Damit sind c3252 =
Royal Carian Knight (`m46_54`) und c4021 = Royal Revenant (`m46_88`,
`m46_59`) belegt. `nameId` ist in Nightreign bei allen NpcParam-Zeilen 0;
der heutige `nameId`-Weg im Extraktor traegt nirgends. Von 116 Orten sind
26 namenlos, genau diese zwei gewinnen. Bindung nur ueber die Entity-Id,
nie ueber die Karte (`m50_10` ruft fuenf fremde Entities).

**Ziel:** AD-040 (Verbotsliste "kein EMEVD-Weg fuer die Unterbosse") um die
Ausnahme fuer den Balkennamen ergaenzen oder begruendet ablehnen: Ist die
Route (Entity-Id der Part-Zeile → 90015000-Aufruf → NpcName) als Namensquelle
**vor** dem `90<chr><variant>`-Weg oder nur als Rueckfall zu nehmen? Was
passiert, wenn beide Wege verschiedene Namen liefern (Zaehlung T-307: 90
gleich, 40 bewusst fremd)? Gilt sie auch fuer die 35 Nachtkarten (T-308)?
Schrittzeile fuer den developer (Datei, Test mit den zwei Karten, Mutation:
Bindung ueber Karte statt Entity muss rot werden), `EXTRACT_VERSION`-Folge
(T-308 hebt auf 14; wenn dieser Schritt danach kommt, 15).

**Stufe/Modus:** architect, kein Anwendungscode.

## Umfang

**Beruehrt:** `ARCHITECTURE.md`, `ARCHITECTURE_REGISTER.md` · **Parallel:**
ja (T-308 in Code) · **Scratchpad:** `<Scratchpad>/T-309/`

## Praemissen — jede Bestandsbehauptung mit Quelle

- Alles oben · T-307-Bericht · gemessen an der Installation (3400 Parts,
  sechs Karten; 116 Orte).
- `_flag_entities`/EMEVD-Leser vorhanden · `nrdata/bossdata.py` · geprueft
  (T-299).

## Rueckgabe

Kontraktblock plus hoechstens acht Zeilen, darunter die Schrittzeile.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
