# T-222 — A16: Entwurf fuer die zwei Lesarten des Beraters (architect)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen stehen
in der `CLAUDE.md` des Projekts; Abweichungen fuer diesen Auftrag: keine.
Kein Anwendungscode.

**Ziel:** Eine AD-Entscheidung, wie "Worst case" und "Best case" die
Bedingungsfelder des Beraters vorbelegen, klein genug fuer **einen**
`developer`-Auftrag (hoechstens fuenf Dateien).

**Stufe/Modus:** architect, Entscheidungstiefe klein — eine AD, keine
Optionen-Studie ueber zwei Absaetze hinaus.

**Abnahmekriterium (GOAL.md, woertlich, A16):** "Der Spieler kann im Berater
zwischen beiden Lesarten wechseln, jede Zahl sagt, welche gerade gilt, und
die sieben bedingten Flueche seines Spielstands bewegen im schlechtesten Fall
die Rangfolge nachweislich." Und: "Beide Lesarten sind **Voreinstellungen fuer
die Bedingungen**, keine zweite Rechnung und keine erfundenen Gewichte: das
Feld, mit dem der Spieler eine Bedingung heute von Hand erklaert, wird dafuer
gesetzt."

**Stand (docs/state.md, 13.09.2026, woertlich):** "A16 (Spec AK-182 liegt
vor)"; Tabelle: "A16 | best/worst case | **nicht gebaut**, keine Entscheidung
getroffen".

## Kontext

`GOAL.md:247-300` (A16 ganz, mit der Director-Korrektur zu den Zahlen: 112
Fluchrollen, 27 konditional auf 23 Kopien; im schlechtesten Fall aendern 8
von 309 Kopien `max_damage`). `UI_SPEC.md:3114` ff. AK-182 (zweite
`QComboBox`, `Worst case`/`Best case`, `Worst case` voreingestellt), AK-183
(Umschalten verwirft den Vorschlag, rechnet nicht selbst), AK-184 (nie zwei
Zahlen). Code: `nrplanner/advisor/types.py:283,436-437` (`declared: effect id
-> how many times its condition is declared met`), `advisorbar.py:401-428`
(`planner.declared` wird als Tupel in die Anfrage gegeben),
`advisor/evaluate.py:121`, `advisor/goals.py:234` (Kommentar zu A16),
`model.py:197` (Flueche mit Vorzeichen). ARCHITECTURE.md fuehrt **keine**
AD zu A16 (`grep -in "worst case"` leer, 13.09.2026).

## Vorgaben

- Zu entscheiden: (1) wo die Lesart lebt (Fensterzustand wie AD-017, oder
  Anfrageparameter); (2) wie sie `declared` vorbelegt — Vorrang von
  Handeingabe vs. Lesart, und was "Bedingung eines Fluchs" im Datenmodell
  ist (welches Feld sagt, dass ein Effekt ein Fluch ist); (3) der Zaehlwert
  fuer "gilt als aktiv" bei `how many times`; (4) wie "jede Zahl sagt,
  welche gerade gilt" ohne zweite Rechnung erfuellt wird — Verweis auf die
  AK, nicht neue UI.
- Ponytail: kein neuer Typ, wenn ein Tupel/Flag reicht; keine Registry.
- Ablage: Abschnitt in `ARCHITECTURE.md` (Themenbereich G oder eigener),
  Registerzeile AD-035, Dateiliste fuer den `developer`, Test, der die
  GOAL-Abnahme "bewegen die Rangfolge nachweislich" am Testabzug misst
  (benennen, nicht schreiben). Mutationsanker: die Registry ist nach T-223
  leer — keine Anker zu nennen.

## Testumgebung

Lesend. Kein Programmstart noetig.

## Umfang

**Beruehrt Dateien:** `ARCHITECTURE.md`, `ARCHITECTURE_REGISTER.md`. ·
**Parallel:** ja — T-221 (`ui-ux-designer`, `UI_SPEC.md`), T-223
(`developer`, Worktree). · **Scratchpad:** `<scratchpad>/T-222/`

Ein Schritt. Kein Commit — der Director committet.

## Scope-Grenzen

Keine Aenderung an AD-033/AD-034; `UI_SPEC.md` nicht anfassen — braucht
die AD ein AK, nenne es fuer den `ui-ux-designer`.

## Praemissen — jede Bestandsbehauptung mit Quelle

- `declared` Tupel `(effect id, count)` · `nrplanner/advisor/types.py:283,436-437`
  · 13.09.2026
- Keine A16-AD in `ARCHITECTURE.md` · `grep -in "worst case" ARCHITECTURE.md`
  leer · 13.09.2026
- AK-182 bis AK-184 · `UI_SPEC.md:3114-3140` · 13.09.2026
- 24 Fluch-Ids, 7 mit Bedingung (Ids, nicht Vorkommen) · `GOAL.md:253-262`,
  Director-Korrektur T-092 · 07.09.2026 — am Datensatz nachzaehlen, wenn es
  die Entscheidung traegt

Widerspricht der Bestand einer Aussage dieses Auftrags, ist das ein Befund:
melden und auf dem Bestand arbeiten. Eine tragende Praemisse ohne Quelle darf
die Rolle zurueckweisen.

## Rueckgabe

Antwort: Kontraktblock plus hoechstens dreissig Zeilen: Entscheidung (1)-(4)
je ein Satz, Dateiliste, benannter Test, AD-Nummer. Datei
`docs/berichte/T-222-architect.md` bei Befunden oder `STATUS:
teilweise/blockiert`.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
