# T-207 — UI-Review auf dem Stand `9cb8bae` (Review-Modus)

**Rolle:** `ui-ux-designer` · **Modus: REVIEW**, nicht Spec · **Modell:** sonnet

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`.
Es gelten die Projektzeilen aus `CLAUDE.md`, Abschnitt
"Projektzeilen fuer jeden Auftrag".

**Scratchpad:** `…/scratchpad/T-207/` und nichts daneben.

## Modus, damit kein Zweifel bleibt

**Review.** Du prueft die **fertige Oberflaeche** aus Endnutzersicht und
schreibst Befunde nach `DESIGN_REVIEW.md`. Du schreibst **keine** neue Vorgabe
und keine neuen Akzeptanzkriterien.

Der Grund fuer den Nachdruck: die letzten drei Laeufe dieser Rolle waren alle
**Spec** (T-178, T-184, T-192 — an ihren Berichtsueberschriften abgelesen,
12.09.2026), und T-163 war ein Sicherheits-/Testauftrag ohne Modusangabe. Wann
der letzte echte Review-Lauf war, **habe ich nicht feststellen koennen** —
`DESIGN_REVIEW.md` fuehrt 18 DR-Eintraege, die naechste freie Nummer ist
**DR-019**.

## Der Stand

**`9cb8bae`.** Es laeuft kein anderer Agent, und ich committe in dieser Zeit
nur unter `docs/tasks/`.

Seit der letzten Pruefung sind gebaut: die **dritte Zielrichtung im Relic
Picker** (`06be06e`, T-194, gemessen in T-199), der A8-Fix an den
Verweigerungen (QA-211, geschlossen), sowie Prosa- und Formaenderungen ohne
Verhaltensanteil (T-204, T-206).

## Dein erster Punkt: QA-239, eine Zahl in deiner eigenen Datei

Wortlaut aus `qa/findings.md`:

> **`UI_SPEC.md:2230` nennt eine Zahl, die die Messung nicht bestaetigt.** Die
> Tabelle fuehrt `wanted_height` fuer `MINIMUM_ROWS` = 3 mit **1136 px (+54)**;
> T-199 hat am echten Fenster **1121 px** gemessen (15 px darunter).
> **AK-196 haelt** -- drei ganz sichtbare Kartenzeilen, keine Bildlaufleiste --
> die Zahl in der Vorgabe ist also nicht falsch in der Wirkung, sondern im
> Wert: 1136 war die Vorabschaetzung aus T-192, 1121 ist die Messung.

**`UI_SPEC.md` ist deine Datei; der Director traegt darin keine Zahl nach.**
Zieh sie nach, kennzeichne sie als Messung und nenne den Lauf. **Miss sie
vorher selbst** — zwei unabhaengige Messungen derselben Zahl sind der Sinn
dieses Punkts, nicht das Abschreiben meiner.

## Was du sonst pruefst

Die **ganze** Oberflaeche, nicht nur das Neue — das ist der Zweck des
Review-Modus. Zwei Schwerpunkte, weil dort gebaut wurde:

1. **Der Relic Picker mit drei Zielrichtungen.** Sechs vorgezogene Karten
   statt drei (Entscheidung des App Designers, 12.09.), der dritte Chip heisst
   `BEST FOR STATS`, drei staendige Wertzeilen je Karte. **Frag als Nutzer, ob
   das noch lesbar ist** — ob sechs vorgezogene Karten vor der Wertordnung
   dem Spieler helfen oder ihn die Ordnung nicht mehr erkennen lassen.
2. **QA-229 ist offen und ausdruecklich nicht behoben:** die beiden
   vorhandenen Chips sind auf favorisierten Karten schon heute abgeschnitten.
   Der dritte Chip kommt dazu. **Sag, ob das jetzt schlimmer ist**, aber
   repariere nichts.

## Was du wissen musst, bevor du ein Fenster startest

**Das ist keine Formalie — hier ist heute ein Lauf daran gescheitert.**

1. **Alle drei Variablen umlenken**, in derselben Kommandozeile wie der Start:
   `NIGHTREIGN_SETTINGS_ORG` (eigener Wert, z. B. `DankYeeterT-207`),
   `LOCALAPPDATA` und `APPDATA` je auf dein eigenes Testverzeichnis.
   **Der Nutzer hat 309 Relikte und rund 110 gespeicherte Builds. Lesen ja,
   schreiben nie.**
   *Ein Hook erzwingt das seit `130f02d` — aber **nicht in dieser Sitzung**
   (QA-241, er wird beim Sitzungsstart geladen). Du bist also auf dich
   gestellt.*
2. **Nenne die gesetzten Werte, und nenne sie keinen Nachweis.** QA-237:
   Schreibvorgaenge unter dem echten `%LOCALAPPDATA%` landen in einer
   Ueberlagerung, die alle Claude-Sitzungen teilen — wer darin sitzt, liest
   seine eigenen Schreibvorgaenge immer erfolgreich zurueck. **Was haelt, ist
   der positive Pfadnachweis:** lies aus dem Prozess zurueck, **welchen** Pfad
   der Code berechnet. Was nicht haelt: "hier wurde nichts geschrieben".
3. **Der feste Testabzug ist da und geprueft** — 841 Dateien, 20 812 293
   Bytes, `EXTRACT_VERSION` 11, von 1.9.0, Nachweis durch eine Probe des
   Nutzers am echten System:

   ```
   C:\Users\Daniel\Desktop\ClaudeCode\NightreignHelper-Testabzug
   ```

   **In dein umgelenktes `LOCALAPPDATA` kopieren**, nicht darauf zeigen
   lassen. Spart rund 110 s je Lauf (gemessen 110,0 s ohne gegen 5,75 s mit).
   *Der Pfad in `CLAUDE.md` ist seit `f110dd4` dieser; der alte unter
   `%LOCALAPPDATA%` ist tot.*
4. **L-009, heute in T-199 wieder aufgelaufen:** `QT_QPA_PLATFORM=offscreen`
   liefert falsche Pixelzahlen — dort **278 px statt der echten 228**. **Miss
   nie am Offscreen-Plugin.**
5. **Bildnachweise ausschliesslich aus dem Programmfenster** (`PrintWindow`).
   **Keine Bildschirmabzuege** — NH-002, das Repository ist oeffentlich.

## Scope-Grenzen

- **Kein Anwendungscode.** Befunde gehen an den `developer`.
- **Keine neuen Akzeptanzkriterien.** Review, nicht Spec. Faellt dir eine
  fehlende Vorgabe auf, ist das ein **Befund** mit dem Vermerk, dass sie
  fehlt — kein AK.
- **QA-229 und QA-230** nicht reparieren, nur einordnen.
- **Du hast kein Commit-Recht.** Lass `DESIGN_REVIEW.md` und `UI_SPEC.md`
  geaendert im Baum liegen; der Director committet sie.

## Beruehrt Dateien

`DESIGN_REVIEW.md`, `UI_SPEC.md` (nur die QA-239-Zahl),
`docs/berichte/T-207-ui-ux-designer.md`.

## Rueckgabeformat

Uebergabe-Kontrakt-Block, dann Abschlussbericht gemaess Rollendefinition, mit
priorisierten Befunden ab **DR-019** und **vierwertigem Urteil**. Je
Fensterzahl das Kommando.

**Widersprich, wenn der Auftrag nicht stimmt.**
