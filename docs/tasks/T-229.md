# T-229 — Pruefphase Zyklus 21 auf dem eingefrorenen Stand `05c6787`

Drei Rollen, eine Nachricht, ein Stand. Es gilt der Rahmen aus
`~/.claude/agents/_rahmen.md`; Projektzeilen (Testbefehl, Datenumlenkung,
verbotene Zugriffe) in der `CLAUDE.md` des Projekts. **Niemand aendert
Anwendungscode oder Tests.** Berichte per `Write` nach
`docs/berichte/T-229-<rolle>.md`; die einzige Codeaenderung, die erlaubt ist,
steht in T-229a Punkt 4 (Loeschen nachgefahrener `MUTATIONS`-Eintraege, nur
der `qa-engineer`, nur `scripts/differential/mutate.py`, kein Commit — der
Director committet).

**Stand, gemessen (Director, 13.09.2026):** HEAD `05c6787`, Code `719c46d`.
Seit der letzten Pruefphase (`b33461d`, T-201/T-202): 38 Dateien, +541/-5672
in `nrplanner/`, `nrdata/`, `scripts/`; 17 Code-Commits. Suite `pytest -n
auto` 1519 passed / 9 skipped (dreimal vom Director auf Merges gemessen).
Registry `scripts/differential/mutate.py`: **10** lebende Eintraege
(T-224: 4, T-225: 5, T-227: 1).

**Abnahmekriterien (GOAL.md, woertlich):** A2 "kritisch/hoch behoben" · A6
"Oberflaeche blockiert nicht" · A7 Datenlage nennen · A8 "Englisch, ohne
Ausnahme" · A15 "Der Erststart fuehrt jeden Nutzer bis zu lesbaren Daten,
ohne fremde Hilfe [...] der gewaehlte Pfad wird geprueft, behalten" · A16
"Der Spieler kann im Berater zwischen beiden Lesarten wechseln, jede Zahl
sagt, welche gerade gilt, und die sieben bedingten Flueche seines Spielstands
bewegen im schlechtesten Fall die Rangfolge nachweislich" · A17 Ranking ohne
Bezugswaffe.

**Quittungen:** `docs/berichte/T-217-developer.md` (Herkunftspruefung),
T-218 (Flaechenbefunde, QA-242-Messung), T-224 (A16, vier Befunde), T-225
(fuenf Abschnitte, Befund 3 Slots leer nach Rescan), T-228 (Loeschungen,
Fund 16, sporadischer Test). Spec-Nachtraege A30-A32 in `UI_SPEC.md`,
AD-033 bis AD-035 in `ARCHITECTURE.md` (Themenbereiche G, H).

**Scratchpad:** `<scratchpad>/T-229/<rolle>/`. Datenumlenkung
`NIGHTREIGN_SETTINGS_ORG=DankYeeterT-229`, Testabzug **kopieren**. Spielstand
read-only.

---

## T-229a — `qa-engineer` (opus)

**Ziel, in dieser Reihenfolge:**

1. **Bestaetigen oder zurueckweisen**, was als "behoben -- Retest in der
   Pruefphase" steht (Register `qa/findings.md`, `security/findings.md`,
   Zeilen vom 13.09.): QA-210, QA-232, QA-242, QA-247, QA-248; SEC-026,
   SEC-036, SEC-037, SEC-038, SEC-039 (die SEC-Zeilen prueft der
   `security-reviewer` auf Angriffspfad, du auf Verhalten und Test).
2. **Sagen, ob die Tests die Kriterien pruefen** — oder nur, dass der Code
   tut, was er tut. Ausdruecklich fuer A16: misst
   `test_the_worst_case_moves_the_ranking_where_a_conditional_curse_sits` die
   GOAL-Abnahme (Rangfolge bewegt sich, jede bewegte Kopie traegt einen
   Fluch) am Spielstand des Nutzers (312 Kopien)? Fuer A15: prueft ein Test,
   dass der gemerkte Pfad beim naechsten Start die Herkunftspruefung
   durchlaeuft?
3. **Adversarial** gegen A6/A7/A8/A15/A16: Rescan ohne Fund bei offenem
   Picker (T-225 Befund 3: Slots leer — Absicht oder A7-Bruch?), Lesart
   umschalten waehrend eines Laufs (AK-183), Fremdordner ohne Steam in der
   Registry (T-217 Befund 2: jeder Ordner abgelehnt), Fensterbreite 1608 px
   auf einem 1366-px-Bildschirm (`_opening_width(room=1366)`), englische
   Verweigerungen des Extraktionspfades erreichen die Flaeche woertlich.
4. **Mutationen nachfahren** (AD-033 Punkt 5): die 10 lebenden Eintraege
   gegen `git archive HEAD` fahren, Ergebnis je Name; danach die
   nachgefahrenen Eintraege aus `MUTATIONS` loeschen (Datei bleibt sonst
   wortgleich), Anzahl und Sha in den Bericht. Kein Commit.
5. **QA-249** reproduzieren: `pytest -n auto` zweimal, dann
   `tests/test_save_read_in_the_background.py` unter Last; Fehlertext
   erfassen.
6. **Nachzaehlen** auf 312 Kopien: die Zahlen in `UI_SPEC.md` AK-188
   (Z. 3388) und AK-169 (Z. 3096) — 67/42/426/323 — mit Befehl; nur Zahlen,
   keine Spec-Aenderung (die macht der `ui-ux-designer` danach).

**Rueckgabe:** Urteil PASS / CONCERNS / FAIL fuer den Stand, je Befund
Prioritaet, Schwere, Adressat, Beleg; Kontraktblock. Bericht
`docs/berichte/T-229-qa-engineer.md` immer.

## T-229b — `security-reviewer` (opus)

**Ziel:** Retest SEC-026, SEC-036, SEC-037, SEC-038, SEC-039 auf `719c46d`
am Angriffspfad, und die neue Flaeche: `gamefiles.in_a_steam_library`
(`resolve()` beider Seiten, Junction aus einer Bibliothek hinaus, Bibliothek
selbst als Junction, UNC-Pfad in `libraryfolders.vdf`, Registry-Wert mit
Umgehungszeichen), `dcx.py` Deckel (ZSTD vor Allokation, DFLT
`unconsumed_tail`, KRAK unveraendert?), `tpf.py` Grenzpruefung,
`shortcut.py` `stderr` auf Konsole (was steht jetzt auf der Konsole, und wer
liest sie?), `NotWhatItClaims`-Saetze auf Dateiinhalt (SEC-041-Klasse: kein
Byte der Datei im Satz — `grep -n "NotWhatItClaims(f" nrdata/*.py` und jede
Formatierung pruefen). Dazu: haelt die Freigabe von SEC-016/017/018 nach der
Herkunftshaertung wieder (T-202 sagte: ueber Herkunft ja)? SEC-042
`STEAM_COMMON` — Einstufung nach dem Fix.

**Rueckgabe:** Urteil PASS / CONCERNS / FAIL, Befunde mit Angriffspfad und
Behebungsrichtung, Statusvorschlag je SEC-Zeile; Kontraktblock. Bericht
`docs/berichte/T-229-security-reviewer.md` immer.

## T-229c — `ui-ux-designer` (Review, sonnet)

**Ziel:** Review der Oberflaeche nach A16 und den Nachtraegen A30-A32 gegen
`UI_SPEC.md`, am laufenden Programm (Datenumlenkung, Testabzug kopiert,
Bildnachweise per `PrintWindow`): Beraterleiste mit Lesart-Box (AK-182 bis
AK-188, AK-268; Startbreite 1608 px, AK-05/AK-194 an der abgeleiteten
Startbreite), E1 im Herkunftsfall (AK-264: Headline und dritte Zeile),
Picker ohne Spielstand (AK-265, AK-267 — Slots leer nach Rescan ohne Fund,
T-225 Befund 3: entspricht das AK-212?), Picker-Hoehe (AK-266, AK-51,
AK-196), Statuszeile im Vorschlagszustand (67 px — lesbar?). Dazu **AK-160**:
letzter "widerspruechlicher" Fall — Bezugsbreite folgt AK-05 (Director
13.09.); die AK-189-Haelfte pruefen und einen Vorschlag zur Aufloesung
machen, nicht aendern. `UI_SPEC.md` in diesem Lauf **nicht** aendern; Befunde
nach `DESIGN_REVIEW.md` (DR ab **DR-022**, Register nachzaehlen) und Bericht.

**Rueckgabe:** Befunde mit Prioritaet und AK-Bezug, Bildnachweise im
Scratchpad; Kontraktblock. Bericht `docs/berichte/T-229-ui-ux-designer.md`
immer.

---

## Scope-Grenzen (alle drei)

Kein Fix, kein Commit, keine Aenderung an Spec/Architektur/Code ausser
T-229a Punkt 4. Befunde gehen gebuendelt in **einen** Fixauftrag, danach
**ein** Retest.

## Praemissen — jede Bestandsbehauptung mit Quelle

- HEAD `05c6787`, Code `719c46d`, Suite 1519/9 · Director `pytest -n auto`
  13.09.2026 — jede Rolle misst selbst
- 10 lebende Mutationen · `grep -c "Mutation(" scripts/differential/mutate.py`
  · 13.09.2026
- 312 Kopien im Spielstand · T-222 B-1, T-224 · 13.09.2026
- Diff seit `b33461d`: 38 Dateien +541/-5672, 17 Commits · `git diff
  --shortstat`, `git log --oneline b33461d..HEAD -- nrplanner nrdata | wc -l`
  · 13.09.2026

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
