# T-257 — Bauwelle 2 aus der Register-Triage (developer x3, Worktrees)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`; Projektzeilen in der
`CLAUDE.md` des Projekts. Autonomer Lauf; Annahmen in `docs/state.md`.
Quelle: `docs/berichte/T-256-qa-engineer.md` (Triage, Kategorie b) und
Register `qa/findings.md` (juengste Zeilen). Stand `5d649e5`, Suite 1655/9
(+1 Flacker). Jeder Auftrag: Worktree auf `5d649e5` oder juenger, ein Commit
je Befund erlaubt, Waechter + Mutation je Fix, `pytest -n auto`,
Kontraktblock + max. 15 Zeilen.

## T-257a — Daten und Beschriftung (`effectstab.py`, `model.py`, `advisor/explain.py`, `arsenaltab.py`)

- **QA-172:** `EffectsTab.refresh` behaelt je Identitaet den ersten
  Roheintrag samt `curse` (`effectstab.py:913-920`); 12 von 1064
  Identitaeten widersprechen sich. Fix: `curse` ueber die Gruppe bilden
  (alle always → always, gemischt → sometimes, keine → never).
- **QA-186:** `model.GATE_FIELDS` beschriftet `wepTypeTriggerCount` als
  "needs several of that weapon equipped" (`model.py:1207-1210`); 51 Effekte
  mit Wert != 3 tragen alle `startGoodsId` (Startwaffen-Bedingung), 31 mit 3
  keines. Fix: Beschriftung nach `startGoodsId` unterscheiden
  ("needs the starting armament equipped" o. ae., A7/A8; Wortlaut in
  `effecttext.py` ablegen, `explain.py` liest ihn).
- **QA-099a (P4):** drei Doppelnamen im Arsenal (`Finger Seal` 2x,
  `Scholar's Thrusting Sword` 4x, `Recluse's Staff` 2x, zahlengleich):
  zahlengleiche Namensdubletten in der Arsenal-Liste zusammenfassen
  (eine Kachel, Tooltip nennt die Anzahl). Nur wenn < 30 Zeilen; sonst melden.

## T-257b — Fenster und Erststart (`app.py`, `firstrun.py`, `iconpack.py`, `nrdata/iconbuild.py`)

- **QA-175:** `showEvent` klemmt nur die Breite (`app.py:941-958, 996-1021`);
  Position nach dem Resize gegen `availableGeometry` klemmen (Fenster
  darf nicht ausserhalb des Bildschirms landen).
- **QA-036:** `firstrun.what_is_needed` prueft nur Manifest + `icon_version`,
  kein Abgleich Manifest ↔ Dateibestand; `iconbuild.py` schreibt ohne
  temp+rename. Fix: Manifest gegen Dateien pruefen (fehlende Datei →
  Neubau), Manifest atomar schreiben (temp + `os.replace`).
- **AK-296 (QA-173):** Tooltip der Gefaessliste nach `UI_SPEC.md` §4.4
  (Wortlaut woertlich, Farben aus `model.COLOUR_NAMES`), am bestehenden
  `item.setToolTip` in `reload_chalices`; Trennzeilen-Tooltips.

## T-257c — Flacker-Klasse QA-249/QA-238 (`tests/`, ggf. `savereader.py`)

Vier Tests fallen sporadisch unter `-n auto`, einzeln gruen:
`test_save_read_in_the_background::test_the_waiting_state_is_a_state_and_not_a_nothing`
(Register QA-249: `activeModalWidget()` liefert einen `RelicPicker` eines
Nachbartests), `test_save_path_memory::test_a_window_whose_picked_file_is_
gone_says_nothing_about_it`, `test_advisor_worker::test_cancel_is_visible_
at_once_however_long_the_worker_takes`, `test_pointer_mark_follows_the_
layout` ("Right contains one more item"). Ursache je Test finden
(Testisolation: offene Dialoge/Fenster des Vorgaengers, geteilter Zustand,
Zeitannahme) und beheben — am Test, wenn der Test die Annahme bricht; am
Code, wenn der Code einen Zustand leckt. Nachweis: 3 Volllaeufe `-n auto`
gruen (Hook-Grenze 2 → dritter Lauf als `-n auto -p no:randomly` auf die
vier Dateien + 10 Nachbarn). Nicht anfassen: Dateien von a und b.

**Parallel:** a, b, c disjunkt (c nur `tests/`, `savereader.py`).
**Scratchpad:** `<scratchpad>/T-257/<a|b|c>/`.
