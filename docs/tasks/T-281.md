# T-281 — [x] erledigt 16.09. (8e1fc9d, c148585) — A21-Beschriftung (AK-313) und A-008 pytest im Release-Lauf (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen
(Testbefehl, Datenverzeichnisse, verbotene Zugriffe, Zielsystem) stehen in
der `CLAUDE.md` des Projekts; Abweichungen fuer diesen Auftrag: keine

**Ziel:** Das Effektfilter-Fenster erklaert in einer Zeile, was `Favourite`
und `Avoid` dort bewirken (AK-313); der Release-Workflow fuehrt die Testsuite
aus, bevor er baut (Auflage A-008).

**Stufe/Modus:** developer: klein

**Abnahmekriterium (GOAL.md, woertlich):** A21 — "Effektfilter als eigenes
Fenster" · A8 — "Alle Texte in der Oberflaeche sind Englisch".

**Stand (docs/state.md, woertlich, 16.09.2026):** "Artefakt 1.13.0 …
Commit ffb6b9f … Heute gebaut: A21 Effektfilter-Fenster Favourite/Avoid
(T-277, AK-300..311)".

## Kontext

Der Nutzer hat am 16.09. die Woerter `Favourite`/`Avoid` (AK-300) und den
`Filters`-Textknopf (AK-302) angenommen mit der Auflage: es muss klar sein,
dass `Favourite` hier die Filterfunktion meint, nicht den Stern je Relikt.
Spec dazu: `UI_SPEC.md` AK-313 (heute angehaengt). A-008 steht seit
`docs/legal/AUFLAGEN.md:47` offen: `release.yml` prueft nur `nrplanner/data/`.

## T-281a — AK-313 Erklaerungszeile

- `nrplanner/effectfilterdialog.py`: Konstante mit dem AK-313-Wortlaut,
  `QLabel` unter dem Suchfeld, `setFocusPolicy(Qt.NoFocus)`, `wordWrap`,
  gedaempft wie `COPIES_DEFINITION`. `OPENING_SIZE` nur nachziehen, wenn die
  Zeile die Tabelle sichtbar verkuerzt — dann Messwert im Bericht.
- `tests/test_effectfilterdialog.py`: ein Test, dass das Label existiert,
  den Wortlaut traegt und `NoFocus` hat.

## T-281b — A-008 pytest vor dem Bau

- `.github/workflows/release.yml`: vor `Build` ein Schritt, der pytest wie in
  `tests.yml` (Z. 40-60: Installation ohne pytest in `requirements.txt`,
  `python -m pytest -rs`) ausfuehrt. Der Kopfkommentar der Datei sagt, dass
  sie in zwei Repos identisch bleibt — nichts einbauen, was dort fehlt
  (`tests/` existiert in beiden? unbekannt → Schritt wie den Lizenzschritt
  auf Vorhandensein von `tests/` guarden).
- Der Runner hat keine Spieldaten; Tests, die den Testabzug brauchen,
  muessen dort `skip`en. Pruefen: `NIGHTREIGN_*`-Umlenkung im Workflow wie in
  `tests.yml` setzen. Nachweis lokal: `act` gibt es nicht — Schritt per
  `python -m pytest -rs` in einem frischen `LOCALAPPDATA` ohne Testabzug
  nachstellen und die Zahl skipped/passed berichten.

## Vorgaben

- AK-313 woertlich (`UI_SPEC.md`, Ende der Datei). Englisch (A8).
- Kein Netzwerk im Anwendungscode, keine Spieldaten ins Repo (CLAUDE.md).
- Keine Versionsaenderung — die macht der `release-manager` (1.13.1).

## Testumgebung

CLAUDE.md: `pytest tests/test_effectfilterdialog.py` gezielt ohne `-n`;
Umlenkung `NIGHTREIGN_SETTINGS_ORG=DankYeeterT-281`, `LOCALAPPDATA`,
`APPDATA` auf Scratchpad.

Klon-Volllauf: im Hintergrund starten, sobald der letzte Code-Commit steht;
Ausgabe `<Scratchpad>/T-281/klon.txt`; der Bericht nennt Zahl und Uhrzeit
oder "laeuft seit hh:mm, Datei …" (L-031).

## Umfang

**Beruehrt Dateien:** `nrplanner/effectfilterdialog.py`,
`tests/test_effectfilterdialog.py`, `.github/workflows/release.yml` ·
**Parallel:** nein · **Scratchpad:** `<Scratchpad>/T-281/`

Zwei Commits (a, b), Conventional Commits, Message deutsch.

## Scope-Grenzen

Keine Aenderung an Spaltenueberschriften, Tooltips, Sortierung oder
`Filters`-Knopf. Keine Aenderung an `tests.yml`. Kein Versionssprung.

## Praemissen — jede Bestandsbehauptung mit Quelle

- `TITLE`, `HEADINGS`, `COPIES_DEFINITION` in `nrplanner/effectfilterdialog.py:30-42` · grep · 16.09.
- `release.yml` hat Schritte `Build` → `Refuse to ship game data` → `Compute SHA-256` (Z. 78-93) · sed · 16.09.
- `tests.yml:40-60` installiert ohne pytest in requirements und laeuft `python -m pytest -rs` · grep · 16.09.
- Suite 15.09.: 1726 passed / 9 skipped (docs/state.md) · ungeprueft, miss selbst.

## Rueckgabe

Antwort: Kontraktblock plus hoechstens dreissig Zeilen. Datei
`docs/berichte/T-281-developer.md` nur bei Befunden oder `STATUS:
teilweise/blockiert`.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
