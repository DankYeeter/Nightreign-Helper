# T-252 — Fixauftrag nach Pruefphase T-251 (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`; Projektzeilen in der
`CLAUDE.md` des Projekts; Abweichungen: keine.

**Ziel:** Die Befunde der Pruefphase auf `528ff78` sind behoben; ein Retest
kann folgen.

**Stufe/Modus:** normal

**Befunde (Register-Zeilen, juengste):** `qa/findings.md` QA-267, QA-266,
QA-268 · `security/findings.md` SEC-045 (= QA-267) · `DESIGN_REVIEW.md`
DR-025, DR-026 · Berichte `docs/berichte/T-251-qa-engineer.md`,
`T-251-ui-ux-designer.md`.

## Vorgaben

1. **QA-267 / SEC-045 (P2):** `effectfilters.EffectFilters.__init__` macht
   die Mengen beim Laden disjunkt (Director: **`required` gewinnt**, die Id
   faellt aus `excluded`) und schreibt zurueck; `mark()` schreibt beide
   Schluessel ueber **ein** `QSettings`-Objekt mit einem `sync()`. Waechter
   mit ueberlappendem Store: kein `ValueError`, Berater rechnet.
2. **DR-025:** `advisorblock.MarkedLine.mark` reagiert auf Enter **und**
   Leertaste (`pressable.PRESS_KEYS` als Muster). Waechter.
3. **DR-026:** Tooltips der drei Zustaende nach dem korrigierten AK-277 in
   `UI_SPEC.md` (Nachtrag T-251c) — Wortlaut von dort. Waechter prueft die
   drei Texte.
4. **QA-266 (Testisolation):** die drei Tests (`test_advisor_bar::test_the_
   row_does_not_scroll_away_with_the_slots`, `test_relic_restore::…no_other_
   slot_was_given`, `test_save_read_in_the_background::…neither_the_window_
   nor_the_focus`) lesen ueber den `Path.home()`-Fallback (`nrdata/
   savefile.py:711`) den **lebenden** Spielstand. Suite darf den nie lesen:
   in `tests/conftest.py` den Fallback fuer alle Tests auf das
   Testverzeichnis lenken (Monkeypatch von `savefile`-Wurzeln oder
   `Path.home`), Tests mit Bedarf an Spielstanddaten nutzen die
   eingefrorene Kopie `tests/data/frozen_inventory.json` o. ae. Waechter:
   Suite gruen, egal welches Gefaess im Spiel gewaehlt ist (Nachweis: mit
   beiden Kopien 19:43/23:01 aus dem QA-Scratchpad `T-251/`, falls noch da,
   sonst Beschreibung).
5. **QA-268:** Markierung oder Build-Aenderung innerhalb der 250-ms-
   Debounce nach Optimize muss als veraltet erkannt werden (AK-289/AK-270-
   Mechanik greift ab Klick, nicht ab `started`). Waechter mit
   Zeitsteuerung ohne Sleep.

Nicht dazu: QA-269 (Feldnamen, vorbestehend), QA-270 (Wortlaut → Spec),
A18-Satz-1-Frage (Bedingung in der Why-Zeile → Spec). Melden, nicht bauen.

## Umfang

**Beruehrt Dateien:** `nrplanner/effectfilters.py`, `nrplanner/advisorblock.py`,
`nrplanner/advisorbar.py`, `tests/conftest.py`, Tests · **Parallel:** nein ·
**Scratchpad:** `<scratchpad>/T-252/`. Ein Commit je Punkt erlaubt, Suite am
Ende `pytest -n auto` **gruen ohne Ausnahme** (QA-266 ist Teil des Auftrags).

## Praemissen

- Suite auf `528ff78` mit Spielstand 23:01: 1592 passed / 10 skipped / 3 failed · qa-engineer T-251a
- Fensterlauf frei (0 Instanzen seit 23:28) · Director · `tasklist`

## Rueckgabe

Kontraktblock + max. 20 Zeilen: Commits, Suitezahl, je Punkt behoben/offen.
