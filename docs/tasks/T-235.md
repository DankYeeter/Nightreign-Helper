# T-235 — AD-034: `Planner` in drei Schritten schneiden (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen
(Testbefehl, Datenverzeichnisse, verbotene Zugriffe, Zielsystem) stehen in
der `CLAUDE.md` des Projekts; Abweichungen fuer diesen Auftrag: keine.

**Ziel:** `class Planner` (`nrplanner/app.py`) verlassen die drei Bloecke aus
AD-034 mit eigener Naht — Relikt-Slots, Spielstand-Lesen, Werteblatt. Reiner
Modulumzug, kein Verhaltens-, Text- oder Layoutwechsel.

**Stufe/Modus:** normal

**Abnahmekriterium (GOAL.md, woertlich):** keines direkt — Auftrag aus P10
(`docs/plan-restarbeiten.md:344`), Prioritaet "kein Technical Debt".

**Stand (docs/state.md, 13.09.2026, woertlich):** "AD-034 Planner-Schnitt in
drei Schritten `relicslots.py` → `savereader.py` → `statsheet.py`;
`ARCHITECTURE.md` ab Z. 5325" · "T-220 B-1: `VariantDialog` (`app.py:1363-1435`,
kein Aufrufer) wird in AD-034 Schritt 1 **gestrichen**, nicht umgezogen".

## Kontext

AD-033 Punkt 2 ist erledigt (`64dd006`, `mutate.py` 158 Zeilen); die
Registry prueft ihre Anker gegen den echten Quelltext
(`tests/test_differential_track.py`). Jeder Schritt bricht benannte Anker —
sie ziehen im selben Commit mit.

## Vorgaben

`ARCHITECTURE.md:5468-5680` (AD-034) ist die Vorgabe, woertlich, je Schritt:

- **T-235a Schritt 1** — `nrplanner/relicslots.py`: `slot_chip`, `_same_copy`,
  `_relic_count`, `_custom_effects`, `class RelicSlot`. **`VariantDialog`
  wird geloescht** (Director-Entscheid T-220 B-1). Anker: 6 genannte.
- **T-235b Schritt 2** — `nrplanner/savereader.py`: `where_saves_usually_are`,
  `_pick_a_save_file`, `_refuse_a_file_no_save_can_be`, `read_the_save`,
  `_SaveReadWorker`, `class SaveReader`; Tests patchen `savereader`. Anker: 6.
- **T-235c Schritt 3** — `nrplanner/statsheet.py`, `class StatSheet(QWidget)`
  nach `ARCHITECTURE.md:5599-5680`; OF-34: Test-Umbenennungen zaehlen nicht
  gegen die Fuenf-Dateien-Grenze.

Reihenfolge strikt a → b → c, jeder Schritt ein eigener Commit, Suite je
Schritt gruen. Kein AK betroffen (kein Text, kein Layout). AD-029/030/031
bleiben Wortlaut, Nachtrag macht der `architect` — nicht du.

## Testumgebung

`CLAUDE.md`: `pytest -n auto` je Schritt; Datenumlenkung Pflicht fuer jeden
Programmlauf; Testabzug kopieren, nicht darauf zeigen.

## Umfang

**Beruehrt Dateien:** `nrplanner/app.py`, `nrplanner/relicslots.py` (neu),
`nrplanner/savereader.py` (neu), `nrplanner/statsheet.py` (neu),
`scripts/differential/mutate.py` (Anker), Tests laut AD-034 ·
**Parallel:** ja — T-236 (`effectstab.py`, `bosstab.py`, `eventstab.py`,
`depthstab.py`, `deeptab.py`) und T-237 (`relicpicker.py`, `arsenaltab.py`,
`advisorbar.py`, `advisorblock.py`) laufen in eigenen Worktrees; **diese
Dateien nicht anfassen.** · **Scratchpad:** `<scratchpad>/T-235/`

**Schritt 0:** Worktree steht auf `origin/HEAD` = `origin/docs/audit-and-advisor-design`
(`git log --oneline -1` muss `49b5a78` oder juenger zeigen); sonst `blockiert`.

## Scope-Grenzen

Kein Verhaltenswechsel, keine Textaenderung, keine Bereinigung "nebenbei".
Was beim Umzug auffaellt (toter Code, falsche Docstrings), melden, nicht
aendern. `HeroTile`, `VesselStrip`, `SituationalRow` bleiben in `app.py`.

## Praemissen — jede Bestandsbehauptung mit Quelle

- `app.py` hat 5145 Zeilen · `wc -l` · 14.09.2026
- `VariantDialog` hat keinen Aufrufer in `nrplanner/`, `tests/` · `grep -rn VariantDialog` (architect 13.09.) · **vor dem Loeschen selbst nachpruefen**
- Suite auf `49b5a78`: 1531 passed / 9 skipped · Director 13.09.2026
- Zeilenangaben in AD-034 stammen vom 13.09. vor T-228/T-230 — Anker per Symbolname suchen, nicht per Zeile

Widerspricht der Bestand einer Aussage dieses Auftrags, ist das ein Befund:
melden und auf dem Bestand arbeiten.

## Rueckgabe

Antwort: Kontraktblock plus hoechstens dreissig Zeilen, je Schritt Commit-Hash
und Suitezahl. Datei `docs/berichte/T-235-developer.md` nur bei Befunden oder
`STATUS: teilweise/blockiert`.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
