# T-201 — Pruefphase, QA: der eingefrorene Stand `b33461d`

**Rolle:** `qa-engineer` · **Modell:** opus (Erst- und Regressionslauf)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`.
Es gelten die Projektzeilen aus `CLAUDE.md`, Abschnitt
"Projektzeilen fuer jeden Auftrag".

**Scratchpad:** `…/scratchpad/T-201/` und nichts daneben.

## Der Stand ist eingefroren

**`b33461d`.** Es aendert in dieser Zeit niemand Code. Der
`security-reviewer` prueft denselben Stand parallel (T-202) und schreibt
nichts; ein `developer` baut parallel einen Testabzug **ausserhalb** des
Repositorys (T-200) und fasst keine Datei darin an. **Ich committe in dieser
Zeit nur unter `docs/tasks/` und `docs/berichte/`.**

## Warum dieser Lauf ueberfaellig ist

Die letzte QA-Pruefung war **T-186**, die letzte Sicherheitspruefung
**T-185** (an `git log` nachgesehen, 12.09.2026 — in `docs/state.md` stand
dazu "seit T-188", das war eine Notiz und keine Messung). Seither, Bereich
**`28dc45c..b33461d`**: 13 Dateien, +685/-136 in `nrplanner/` und `nrdata/`,
ueber 8 Commits.

## Ziel

Zwei Dinge, und das zweite ist das wichtigere:

1. **Die fuenf Befunde bestaetigen oder zurueckweisen**, die als
   "behoben -- QA-Bestaetigung offen" stehen: **QA-211** (A8, eigene
   Verweigerungen erkennbar), **QA-228** (die dritte Zielrichtung ist
   erreichbar), **QA-233** (die zwei Testdateien laufen wieder mit),
   **QA-234** (Mutationsanker), **QA-235** (der Test haengt nicht mehr an der
   Maschine).
2. **Sagen, ob die vorhandenen Tests die Kriterien ueberhaupt pruefen** — oder
   nur, dass der Code tut, was er tut. Das ist die Frage, die ein gruener
   `developer`-Lauf nicht beantwortet, und in diesem Zyklus ist sie **zweimal**
   negativ ausgefallen: QA-233 (zehn Erwartungen auf `ValueError`, das die
   eigene Fehlerklasse durchliess) und QA-235 (ein Test, der die Maschine
   befragte statt die Umgebung zu setzen). **Such die dritte Stelle desselben
   Musters.**

## Die Abnahmekriterien, woertlich aus `GOAL.md`

> **A8** Alle Texte in der Oberflaeche sind Englisch (bestehende
> Projektregel).
>
> **A17** Ranking ohne Bezugswaffe

Stand dazu in `docs/state.md`, woertlich:

> | A7 | sagen, wo die Daten nichts hergeben | erfuellt, **aber QA-210 und
> QA-232 offen** |
> | A8 | alles Englisch | **QA-211 behoben** — Bestaetigung durch QA steht aus |
> | A17 | Ranking ohne Bezugswaffe | **rechnerisch fertig** (AK-191 woertlich
> erfuellt, 75 von 210 unterscheidbar); sichtbar gebaut in `06be06e`,
> **unbestaetigt** |

**A9** (Pruefung gegen ein gebautes Artefakt) ist **nicht** Teil dieses
Auftrags — hier wird der Quellstand geprueft. A9 kommt in der Baurunde.

## Die Zahlen, die ich selbst gemessen habe

Zum Vergleichen, nicht zum Abschreiben. Alle vom 12.09.2026:

| was | Zahl | Befehl |
|---|---|---|
| Suite nach T-199 | `0 failed, 1781 passed, 9 skipped in 135.24s` | `python -m pytest -n auto -q` |
| Suite nach T-197 | `2 failed, 1779 passed, 9 skipped in 138.40s` | dito |
| Kartenzeile | 228 px | T-199, echtes Fenster |
| `wanted_height` | **1121 px**, nicht die 1136 aus `UI_SPEC.md:2230` | T-199 |

**`texture2ddecoder` und `pytest-xdist` sind seit T-197 installiert** — die
Suite laeuft vollstaendig, ohne jedes `--ignore`. **Eine Zahl mit `--ignore`
ist in diesem Auftrag keine Zahl.**

*Die 1257 aus `CLAUDE.md` (Stand 08.09.2026) ist **nicht** die
Vergleichszahl. Warum sie so weit abweicht, ist ungeklaert; ich habe den alten
Stand nicht gemessen.*

## Was du wissen musst, bevor du ein Fenster startest

**QA-237, belegt am 12.09.2026:** Schreibvorgaenge unter dem echten
`%LOCALAPPDATA%` landen in einer Ueberlagerung, die alle Claude-Sitzungen
teilen und die die echte Platte nie erreicht. **Du kannst deine eigenen
Umlenkungen nicht nachweisen** — wer in der Ueberlagerung sitzt, liest seine
Schreibvorgaenge immer erfolgreich zurueck. **Lenk trotzdem alle drei
Variablen um** (Tabelle in `CLAUDE.md`): es kostet nichts, und die Grenze der
Ueberlagerung ist unbekannt — am 07.09.2026 ist eine Verknuepfung im echten
Start-Menue gelandet. **Melde die gesetzten Werte, und nenne sie nicht einen
Nachweis.**

**QA-231 ist offen:** der feste Testabzug ist nicht benutzbar. Ein
Fensterlauf kostet dich rund 110 s Neuaufbau.

**L-009, in T-199 wieder aufgelaufen:** `QT_QPA_PLATFORM=offscreen` liefert
falsche Pixelzahlen — dort 278 px statt der echten 228. **Miss nie am
Offscreen-Plugin.**

## Scope-Grenzen

- **Kein Anwendungscode, kein Fix.** Befunde gehen an den `developer`.
- **QA-231, QA-237, QA-238, QA-239** sind aufgenommen und brauchen von dir
  keine Bestaetigung. QA-239 gehoert dem `ui-ux-designer`.
- **Korb 1 des Ueberbau-Audits** (Prosakuerzung, `StrEnum`, `baseline_for`)
  ist noch nicht gebaut und nicht Teil dieses Stands.
- Deine Befundtabelle liefere im Bericht; **`qa/findings.md` traegt der
  Director nach.**

## Parallel

`security-reviewer` (T-202) auf demselben Stand, `developer` (T-200)
ausserhalb des Repositorys.

## Beruehrt Dateien

`docs/berichte/T-201-qa-engineer.md`. Im Arbeitsbaum aenderst du nichts;
Mutationen und Messskripte baust du im eigenen Klon.

## Rueckgabeformat

Uebergabe-Kontrakt-Block, dann Abschlussbericht gemaess Rollendefinition, mit
**vierwertigem Urteil** (PASS, CONCERNS, FAIL, WAIVED — WAIVED vergibst du
nie) und der Befundtabelle fuer `qa/findings.md`.

**Widersprich, wenn der Auftrag nicht stimmt.**
