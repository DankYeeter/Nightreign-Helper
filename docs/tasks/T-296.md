# T-296 — Aufraeumen nach 1.14.0: README kuerzen, Ponytail-Audit umsetzen, Repo-Hygiene (technical-writer, developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen stehen
in der `CLAUDE.md` des Projekts; Abweichungen: keine. Stand `ffac292`,
v1.14.0 veroeffentlicht (Run 35427762335, drei Assets).

**Ziel (Nutzer 19.09. 08:54):** "gehe alle ponytail skills durch, raeume das
Projekt auf … Die GIT-Seite kuerzen, oft eine wall-of-text; kuerzer, zumindest
in der Introduction, damit Enduser leicht etwas damit anfangen koennen."
Keine neuen Funktionen.

**Abnahmekriterium (GOAL.md, woertlich):** A11 "Ein nicht-technischer
Spieler erreicht … sein Ziel"; Rahmen: README Englisch.

## T-296a — technical-writer: README auf Einstieg kuerzen

`README.md` (769 Zeilen, Guide ab Z. 95) wird zur Einstiegsseite von
hoechstens **~120 Zeilen**: Titel + ein Satz, ein Bild (bestehendes aus
`docs/screenshots/`), "What it does" (5 Zeilen), "Install" (Download-Link
`releases/latest`, SmartScreen, Erststart — die geprueften Schritte aus
`docs/release/RELEASE_BODY.md`), "Requirements", Link auf den Guide,
Disclaimer + Licence + A-024-Transparenzsatz (**woertlich behalten**, C-006;
SEC-019: den Satz "makes no network connections at all" dabei in die
C-006-Fassung "it makes no network connection" ueberfuehren und SEC-019
damit schliessen — melden). Der komplette Guide (Z. 95-735: Tabs, Window,
Searching, Where your data lives, Running from source, How values are
derived, Known limits) zieht **unveraendert im Inhalt** nach
`docs/anleitung/guide.md` (Englisch, mit Inhaltsverzeichnis); interne
Links im Repo (`grep -rn "README.md#" docs nrplanner tests`) nachziehen.
Beruehrt: `README.md`, `docs/anleitung/guide.md` (neu), ggf. Linkstellen.
Kein Commit.

## T-296b — developer (Stufe normal): Ponytail-Audit umsetzen

Befunde mit `delete`, `stdlib`, `native`, `yagni` aus dem Audit-Bericht
(wird als Abschnitt "Audit-Befunde" unten angehaengt, sobald er vorliegt);
`shrink` nicht. Je Befund: Aufrufer-Grep wiederholen, dann streichen; ein
Commit je Befundgruppe. Waechtertests (`tests/`, NH-003, Spec-Datas) muessen
gruen bleiben. Klon-Volllauf (L-031). Parallel zu T-296a: `README.md` und
`docs/anleitung/` nicht anfassen.

## T-296c — Director: Repo-Hygiene (Buchfuehrung)

`docs/handover-2026-09-06.md`, `docs/handover-scaling-2026-09-03.md` →
`docs/archiv/`; `docs/state.md` neu schneiden (Zyklus 27, alles Erledigte
raus); `docs/plan-restarbeiten.md` abgleichen; `.gitignore` gegen
`git status --ignored` (nur lesen).

## Umfang

**Parallel:** a ∥ b (disjunkte Dateien); c danach · **Scratchpad:**
`<Scratchpad>/T-296/<rolle>/`

## Scope-Grenzen

Keine Funktionsaenderung, kein Versionssprung, kein Release in diesem
Auftrag. Guide-Inhalt wird verschoben, nicht umgeschrieben.

## Praemissen — jede Bestandsbehauptung mit Quelle

- README 769 Zeilen, Guide-Kopf Z. 95 · `wc -l`, `grep -n "^## "` · 19.09.
- `docs/anleitung/` existiert nicht · `ls docs` · 19.09.
- SEC-019 offen, Satz README (frueher Z. 546/548) · `security/findings.md` · Zeile heute suchen.

## Rueckgabe

Kontraktblock plus hoechstens fuenfzehn Zeilen.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
