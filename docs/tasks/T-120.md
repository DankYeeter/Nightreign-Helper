# T-120 - sync-out vor dem Neustart (archivist)

**Modus: `sync-out`.** Beide Repos.

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`, dazu die Projektzeilen in
`CLAUDE.md`.

## Lage

**Der Nutzer setzt die Sitzung anschliessend neu auf.** Was jetzt nicht
draussen ist, geht verloren.

- **Projekt** `Nightreign-Helper`, Branch `docs/audit-and-advisor-design`,
  Arbeitsbaum sauber. `main` ist geschuetzt, **PR #16** offen und auf diesen
  Branch gerichtet — Push aktualisiert den PR, **kein Merge**.
- **Agenten-Repo** `%USERPROFILE%\claude-agent-team`, Branch `main`: **ein
  Commit ungepusht** (`486ac4e`) mit den vier vom Nutzer angenommenen
  Massnahmen L-014, L-015, L-017, L-018 — Hook, Pruefung 4, `_rahmen.md`.
  **Der ist der Grund fuer den Neustart; ohne ihn startet die neue Sitzung mit
  den alten Regeln.**
- **Es laeuft kein anderer Agent.** T-118 und T-119 sind abgeschlossen, ihre
  Berichte abgelegt und committet.

**Nicht ins Repo, beides bereits geprueft:** `dist/` (steht in `.gitignore`)
und der Testabzug unter `%LOCALAPPDATA%\NightreignHelper-Testabzug`. Findest du
davon etwas im Index, **committe es nicht, sondern melde es**.

## Erlaubtes Remote

Ausschliesslich `https://github.com/DankYeeter/Nightreign-Helper.git`. Weicht
das tatsaechliche Remote ab, wird **nicht** gepusht, sondern gemeldet.

## Rueckgabeformat

Uebergabe-Kontrakt-Block plus Abschlussbericht. **Nenne je Repo gemessen**, wie
viele Commits nach dem Push noch ungepusht sind
(`git rev-list --count @{upstream}..HEAD`) — beide muessen **0** sein, sonst
`STATUS: teilweise`. Sag ausdruecklich, ob `486ac4e` im Agenten-Repo draussen
ist. Der Director legt deinen Bericht ab.
