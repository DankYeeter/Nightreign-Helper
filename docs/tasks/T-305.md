# T-305 — A24 Schritt 1b: Auswahlregel Ortsroute (AD-042) (developer)

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`. Projektzeilen in der
`CLAUDE.md` (Umlenkung Pflicht: `NIGHTREIGN_SETTINGS_ORG=DankYeeterT-305`,
eigenes `LOCALAPPDATA`/`APPDATA`; kein Fensterlauf). Stand `b27be19` plus
ungecommittet AD-042 in `ARCHITECTURE.md` (T-304, 19.09. 14:15).

**Ziel:** Die Schrittzeile "1b" aus `ARCHITECTURE.md` (AD-042, Themenbereich
L, Abschnitt Umsetzung) woertlich: `_candidates(..., *, min_hp=INFERRED_MIN_HP)`,
`derive_places` mit `min_hp=0`, strikt hoechste HP = `single`, Gleichstand =
`ambiguous`; Randbedingung AD-042 Punkt 4 als Kommentar. `EXTRACT_VERSION`
bleibt 13. Testabzug neu bauen (`scripts/build_snapshot.py`), Byte-Zahl in
`CLAUDE.md` Z. 75 **und** `docs/plan-restarbeiten.md` nachtragen (die
CLAUDE.md-Zeile darfst du diesmal selbst aendern, nur die Zahl). Tests und
zwei Mutationen wie in der Schrittzeile. Erwartung: 29/29 `single`, 4659
nennt c4501, 4671 nennt c4480.

**Stufe/Modus:** developer: klein, ein Lauf, Commit (Deutsch,
`fix(extract): …`) nur der Dateien unter "Beruehrt".

## Umfang

**Beruehrt:** `nrdata/bossdata.py`, `tests/test_extraction.py`,
`scripts/differential/mutate.py`, `docs/plan-restarbeiten.md`, `CLAUDE.md`
(eine Zahl), Testabzug ausserhalb des Repos · **Parallel:** ja (T-306
aendert `nrplanner/`, liest den Testabzug nur als Kopie) · **Scratchpad:**
`<Scratchpad>/T-305/`

## Scope-Grenzen

Kein `extract.py`, kein `nrplanner/`, `INFERRED_MIN_SPREAD` unveraendert
(OF-48, offen). Volle Suite `pytest -n auto` vor dem Commit.

## Praemissen — jede Bestandsbehauptung mit Quelle

- Zaehlung heute 11/1/1/16 · T-303 · gemessen.
- Hoechste HP reproduziert 29/29 · T-304 (Projektion auf T-299-Rohdump) ·
  nicht am geaenderten Code gemessen — das ist dein slow-Test.

## Rueckgabe

Kontraktblock plus hoechstens zehn Zeilen: Zaehlung, Byte-Zahl, Suite,
Mutationen rot, Commit-Hash.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEÄNDERT:
ANNAHMEN:
NÄCHSTER:
BLOCKIERT DURCH:
```
