# T-169 - Die Version sagt 1.8.0 und meint etwas anderes (developer)

**Ziel:** Eine Zeile. Das heute gebaute Artefakt traegt dieselbe Versionsnummer
wie das aus Zyklus 16 — bei anderem Inhalt und anderem Hash.

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`.
Es gelten die Projektzeilen aus `CLAUDE.md`.

**Scratchpad:** eigenes Unterverzeichnis `T-169/`.

## Umfang

**Stufe:** klein. Eine Konstante, ein Waechter.
**Beruehrt Dateien, abschliessend:** `nrplanner/__init__.py` und die
zugehoerigen Dateien unter `tests/`.
*(Whitelist. Brauchst du eine Datei ausserhalb — etwa weil die Version an
mehreren Stellen steht —: **melden und anhalten**, nicht ergaenzen. **Sag
dann, wo ueberall sie steht**; das ist der eigentliche Wert dieses Auftrags.)*

## Der Befund

**Quittung aus T-168 (`release-manager`, Modus `build`):** Er hat das Artefakt
gebaut — `dist/NightreignHelper.exe`, **59 063 047 B**, SHA-256
`357F8D927FB3DFD9E305533AD3A933B7AA89421CD3292D8CE4616955AFDEDEBD`, aus Commit
`f0eb0fd`. **63 von 63 Modulen bytecode-identisch** zum Quellstand, keine
Spieldaten im Bundle, Rauchtest mit allen drei Umlenkungen bestanden.

**Sein Befund:**

> **Version decided but not applied:** proposed **1.9.0** (new feature, not a
> hotfix), with reasoning documented — but did not edit `nrplanner/__init__.py`
> since that's out of this task's scope (source directory, developer's
> territory). Flagged to `developer`/`director` as a one-line fix needed before
> release: today's artifact still reads "1.8.0", **identical in name but not in
> content/hash** to the old Zyklus-16 1.8.0 build.

**Das Zyklus-16-Artefakt:** 59 010 777 B, SHA-256 `42B21AA2…F221`, ebenfalls
1.8.0. **Zwei verschiedene Programme, ein Name.**

**Director-Entscheidung: 1.9.0**, seiner Begruendung folgend. A15 ist eine neue
Funktion — der Erststart fuehrt jetzt ueber einen Auswahldialog, wo er vorher
in einer Fehlermeldung endete —, dazu kommen das Hintergrundlesen des
Spielstands und fuenf behobene Sicherheitsbefunde. Das ist kein Hotfix.

## Warum das vor der clean-room-Pruefung weg muss

Der `clean-room`-Lauf prueft ausdruecklich **die Installation auf einem fremden
System und das Update ueber eine aeltere Version**. Mit zweimal `1.8.0` prueft
er ein Update von 1.8.0 auf 1.8.0 — der Lauf liefe durch und belegte nichts.

## Abnahmekriterium aus GOAL.md (woertlich)

> **A9** Ein `qa-engineer`-Durchlauf bestaetigt A3 bis A8 gegen ein gebautes
> Artefakt, nicht nur gegen den Quellstand.

**Stand laut docs/state.md** (woertlicher Auszug, 09.09.2026):

> `release-manager` (`build`) → (`clean-room`) → `power-user` → (`notes`).

## Vorgaben

- **Kein `CHANGELOG.md`** — das ist der Modus `notes` des `release-manager`
  und kommt nach dem `power-user`-Lauf.
- **Ein Waechter, der die Version an genau einer Stelle festhaelt**, falls es
  ihn noch nicht gibt. Steht die Zahl an mehreren Orten, ist **das** der
  Befund — melde ihn, statt sie ueberall von Hand nachzuziehen.
- **Voller Mutationsbeweis nicht noetig**, wenn ein Bruch sofort sichtbar ist —
  sag, wie du entschieden hast. *(Nutzerentscheidung 08.09.2026.)*
- **Keine Zeitschranke in die Suite.**

## Testumgebung

Windows 10/11 x64, Branch `docs/audit-and-advisor-design`. Testbefehl nach
`CLAUDE.md`. **Suitezahl gegen 1699 passed, 9 skipped, 0 failed** (T-164, von
T-168 unabhaengig bestaetigt).

Alle drei Datenverzeichnisse umlenken und nachweisen, sonst abbrechen und nur
das melden. Der Spielstand ist read-only.

## Scope-Grenzen

- Nicht: bauen (das macht der `release-manager` danach), `CHANGELOG.md`,
  `dist/`, das veraltete `dist/NightreignHelper-notices.zip` (von T-168
  gemeldet, gehoert zum naechsten echten Release).
- **Nicht anfassen:** alles ausser den zwei genannten Orten.

**Parallel laeuft nichts.**

## Rueckgabeformat

Uebergabe-Kontrakt-Block plus Abschlussbericht. **Der Auftrag gilt erst als
erledigt, wenn `docs/berichte/T-169-developer.md` auf der Platte liegt.**
Commit mit Pfad hinter `--`.
