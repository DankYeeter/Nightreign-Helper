# T-186 — Retest QA-215: steht der Fix, den das Register nicht kennt? (qa-engineer)

**Retest.** Ein bereits gemeldeter Fix wird nachgeprueft, kein neuer Durchlauf.

**Ziel:** Beantworten, ob **QA-215** durch den vorhandenen Code geschlossen ist.

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`.
Es gelten die Projektzeilen aus `CLAUDE.md`, Abschnitt "Projektzeilen fuer
jeden Auftrag".

## Kontext

Beim Umbau der Befundregister (T-180 — **dein eigener Auftrag von vorhin**,
Bericht unter `docs/berichte/T-180-qa-engineer.md`) fiel auf, dass QA-215 als
**offen** gefuehrt wird, obwohl ein Test den Fix belegt. Du durftest damals
keinen Befund neu bewerten. Jetzt sollst du.

Quittung aus deinem Bericht: `tests/test_save_read_in_the_background.py:1088`,
Docstring *"Which is where the missing tooth was (QA-215): AK-245 names …"*.

**Das ist ein Zitat aus dem Bericht, keine eigene Messung des Directors.**

**Abnahmekriterium aus GOAL.md:** dieser Auftrag dient keinem Kriterium
unmittelbar. Das naechste offene lautet woertlich: *"**A9** Ein
`qa-engineer`-Durchlauf bestaetigt A3 bis A8 gegen ein gebautes Artefakt, nicht
nur gegen den Quellstand."*

**Stand laut docs/state.md** (09.09.2026, Director): *"2026-09-09, **Zyklus 18
abgeschlossen, Baurunde offen**."*

## Umfang

**Beruehrt Dateien:** `docs/berichte/T-186-qa-engineer.md` (neu). **Sonst
nichts** — insbesondere nicht `qa/findings.md`: die Ausnahme aus T-180 galt nur
fuer T-180. Den Status zieht der Director nach.

**Parallel:** ja. Gleichzeitig laufen T-183, T-184 (`UI_SPEC.md`) und T-185
(`security-reviewer`, vier SEC-Nummern).

## Vorgaben

Ein Urteil aus drei: **behoben**, **teilweise** oder **offen**, mit der Stelle,
die es traegt.

**Die entscheidende Frage ist nicht, ob ein Test existiert, sondern was er
prueft** — bildet er den Fall aus QA-215 ab, oder bestaetigt er nur, dass der
Code tut, was er tut? Sag das ausdruecklich.

Lauf den Test einzeln (`pytest <datei>` **ohne** `-n`, siehe `CLAUDE.md`) und
nenn das Ergebnis. Die volle Suite ist nicht noetig.

## Testumgebung

Quellstand, Branch `docs/audit-and-advisor-design`, Commit `1e6bb98`.
**Startest du das Programm**, lenkst du **alle drei** Datenverzeichnisse aus
`CLAUDE.md` um und weist jede Umlenkung nach — der Nutzer hat 309 Relikte und
rund 110 Builds im echten Profil. Gelingt ein Nachweis nicht, brichst du ab und
meldest nur das.

Scratchpad: **nur** `…/scratchpad/T-186/`.

## Scope-Grenzen

- **Nur QA-215.** SEC-023/031/032/035 laufen parallel beim
  `security-reviewer`.
- Keine neuen Befund-IDs, keine Aenderung am Register, kein Fix. Findest du,
  dass der Befund offen ist, ist die Fixrichtung ein Satz im Bericht — gebaut
  wird sie vom `developer` in einem eigenen Auftrag.

## Widersprich, wenn der Auftrag nicht stimmt — Pflicht

**Widerspricht eine Aussage in diesem Auftrag dem, was du im Code, in den
Dateien oder in deiner Messung vorfindest, ist das ein Befund.** Melde ihn,
auch wenn er nicht zu deiner Aufgabe gehoert, und arbeite auf dem, was du
vorgefunden hast — nicht auf dem, was hier steht.

Das gilt fuer Zahlen, Befundstatus, Dateilisten, Behauptungen ueber andere
Rollen und fuer "existiert nicht". **Der Director ist die einzige Rolle ohne
Pruefer** — seine Auftragstexte sind das, was am seltensten gegengelesen wird.

## Rueckgabeformat

**Der Bericht steht in der Datei, nicht in der Antwort.** Schreib ihn
vollstaendig nach `docs/berichte/T-186-qa-engineer.md` und gib in deiner Antwort
**nur** zurueck:

1. den Uebergabe-Kontrakt-Block (unten, vollstaendig),
2. dein Ergebnis in **hoechstens dreissig Zeilen** — Urteil, Zahlen,
   Befunde, offene Entscheidungen,
3. den Pfad der Berichtsdatei.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEAENDERT:
ANNAHMEN:
NAECHSTER:
BLOCKIERT DURCH:
```
