# T-195 — sync-out vor der Pause (archivist)

**Modus: `sync-out`.**

**Ziel:** Alles Gearbeitete auf das Remote bringen, bevor der Nutzer Claude
neu startet.

Es gilt der Rahmen aus `~/.claude/agents/_rahmen.md`.
Es gelten die Projektzeilen aus `CLAUDE.md`.

## Kontext

Der Nutzer pausiert die Sitzung. **Es laeuft kein anderer Agent** — das ist
eine Zusicherung des Directors: T-194 wurde auf Wunsch gestoppt, sein
Arbeitsbaum ist sauber, seine Aenderung liegt als `06be06e` im Baum.

Gemessen beim Erteilen, 12.09.2026: **32 Commits ungepusht**
(`git rev-list --count @{upstream}..HEAD`), Arbeitsbaum sauber
(`git status --porcelain` leer), Branch `docs/audit-and-audit-advisor-design`
— genauer: `docs/audit-and-advisor-design`.

**Repo-Kategorie:** `CLAUDE.md` nennt als einziges erlaubtes Remote
`https://github.com/DankYeeter/Nightreign-Helper.git`. Pruef `git remote -v`
dagegen. Weicht es ab, **nicht pushen, sondern melden**.

**`main` ist geschuetzt.** Du pushst auf `docs/audit-and-advisor-design`.
PR #16 bleibt offen — **der Merge gehoert dem Nutzer.**

**Das Agenten-Repo liegt nicht unter `%USERPROFILE%`**, sondern unter
`C:\Users\Daniel\Desktop\ClaudeCode\claude-agent-team`, erreichbar ueber die
Junctions in `~/.claude` — das hast du in T-187 selbst herausgefunden und
belegt. Nimm es mit.

**Stand laut docs/state.md** (12.09.2026, Director): *"2026-09-12, **pausiert
auf Wunsch des Nutzers** (Claude-Neustart). Zyklus 20 laeuft, ist **nicht
abgeschlossen**."*

## Umfang

**Beruehrt Dateien:** keine im Projekt. Alles Anstehende ist committet.

**Parallel:** nein.

## Vorgaben

- `fetch`, `pull --ff-only` bzw. `--rebase` auf sauberem Baum, dann `push`.
  Kein `--force`, kein `reset --hard`.
- Faellt der Push auf eine Branch-Protection (GH013), melde `teilweise` mit
  `BLOCKIERT DURCH: Branch-Protection` und uebergib an den Director.
- Fehlende `.gitignore`-Eintraege schlaegst du **vor**. Einer draengt sich
  auf: in dieser Sitzung sind zweimal Klone im Arbeitsbaum gelandet
  (`klon/`, 144 MB, von einem `git clone` ohne `-C`). Beide wurden entfernt,
  keiner committet — aber sag, ob ein Eintrag das kuenftig auffangen sollte
  oder ob er die falsche Stelle legitimieren wuerde.

## Testumgebung

Kein Programmstart, kein Bau. Scratchpad: **nur** `…/scratchpad/T-195/`.

## Scope-Grenzen

- **Kein Merge**, kein Tag, kein Release.
- Kein `checkout`, kein `branch`.
- Konflikte in Anwendungscode loest du nicht; du meldest sie.

## Widersprich, wenn der Auftrag nicht stimmt — Pflicht

**Widerspricht eine Aussage in diesem Auftrag dem, was du im Code, in den
Dateien oder in deiner Messung vorfindest, ist das ein Befund.** Melde ihn,
auch wenn er nicht zu deiner Aufgabe gehoert, und arbeite auf dem, was du
vorgefunden hast — nicht auf dem, was hier steht.

Das gilt fuer Zahlen, Befundstatus, Dateilisten, Behauptungen ueber andere
Rollen und fuer "existiert nicht". **Der Director ist die einzige Rolle ohne
Pruefer** — seine Auftragstexte sind das, was am seltensten gegengelesen wird.

## Rueckgabeformat

**Der Bericht steht in der Datei, nicht in der Antwort.** Schreib ihn
vollstaendig nach `docs/berichte/T-195-archivist.md` und gib in deiner Antwort
**nur** zurueck:

1. den Uebergabe-Kontrakt-Block (unten, vollstaendig),
2. dein Ergebnis in **hoechstens dreissig Zeilen** — Urteil, Zahlen,
   Befunde, offene Entscheidungen,
3. den Pfad der Berichtsdatei.

```
STATUS: erledigt | teilweise | blockiert | abgebrochen
AUFTRAG:
GELESEN:
GEAENDERT:
ANNAHMEN:
NAECHSTER:
BLOCKIERT DURCH:
```
