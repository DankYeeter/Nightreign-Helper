"""Nightreign Helper GUI."""

# Shown in the window title and the environment check, so that a bug report
# names a build rather than "the latest one". The release workflow checks
# this against the tag it is building and fails if the two disagree.
__version__ = "1.0.0"
